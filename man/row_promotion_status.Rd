% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polity_folds.R
\name{row_promotion_status}
\alias{row_promotion_status}
\title{Report which Rest-of-World members report under their own territory}
\usage{
row_promotion_status(crosswalk = NULL)
}
\arguments{
\item{crosswalk}{Crosswalk to inspect. Defaults to the crosswalk this run
resolves through, so the answer reflects
\code{options(whep.unfold_rest_of_world)}.}
}
\value{
A tibble with one row per Rest-of-World member, ordered by
\code{area_code}:
\itemize{
\item \code{area_code}, \code{area_name}, \code{area_iso3c}: The member.
\item \code{cbs}: Whether \link{regions_full} flags it as a commodity-balance reporter,
which is what \code{"cbs_reporters"} promotes.
\item \code{status}: One of the four above.
\item \code{n_periods}: How many polity periods it resolves through.
\item \code{polity_codes}: Those periods, comma-separated.
}
}
\description{
Promoting a member of the FABIO Rest-of-World bucket has two halves: the
numeric one, which stops its rows being summed into \code{polity_area_code} 999,
and the territorial one, which lets it publish under its own year-scoped
\code{reporting_polity_code} instead of the bucket's \code{ROW-1850-2025}. Only the
first is unconditional. The second needs upstream to name a polity for the
area in \code{faostat_area_polity_map.csv}, and for 30 of the 61 members it does
not (whep#717).

Without this, a member still on \code{ROW-1850-2025} is indistinguishable from the
genuine residual: both publish an aggregate polity on the continent \code{"World"}
with no geometry, and nothing says which is which. This names them, and says
for each what is missing.
}
\section{The statuses}{

\itemize{
\item \code{"own_polity"}: promoted, and publishing under its own polity. 31 members.
\item \code{"polity_unmapped"}: promoted, still on \code{ROW-1850-2025}, but a live
non-aggregate polity carrying the area's ISO3 \strong{does} exist upstream. All
that is missing is a row of the FAOSTAT area map naming it for this area,
so this is the actionable list to send upstream. 6 members: 22 Aruba,
71 French Southern and Antarctic Territories, 94 Holy See, 218 Tokelau,
243 Wallis and Futuna, 271 South Georgia and the South Sandwich Islands.
\item \code{"no_polity"}: promoted, still on \code{ROW-1850-2025}, and upstream has no
polity for the territory at all. 24 members. Three of them are not
territories and must never acquire one -- 252 \code{"Unspecified"},
254 \code{"Others (adjustment)"} and the 999 bucket itself, which is excluded
here because it is the residual rather than a member of it.
\item \code{"folded"}: this run is re-folding the member, so it is summed into bucket
999 and carries the bucket's polity. Only under
\code{options(whep.unfold_rest_of_world = "none")} or \code{"cbs_reporters"}.
}

The ISO3 test behind \code{"polity_unmapped"} decides \strong{nothing}: it reports that
upstream holds a polity this package cannot reach, which is a gap in the map,
not a mapping. Resolving an area to a polity by matching ISO3 downstream is
the defect whep#711 removed, and #717 argues explicitly against re-minting a
territorial identity here.
}

\examples{
status <- row_promotion_status()
table(status$status)
status[status$status == "polity_unmapped", c("area_code", "area_name")]
}
\seealso{
\code{\link[=folded_reporting_areas]{folded_reporting_areas()}} for the areas whose rows are summed into
another area's code, and \code{\link[=polity_mapping_provenance]{polity_mapping_provenance()}} for which authority
a resolved row rests on.
}
