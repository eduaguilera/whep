% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polycell_layers.R
\name{glwd_water_fraction}
\alias{glwd_water_fraction}
\title{Derive the lake-and-river fraction of each 0.5-degree cell from GLWD}
\usage{
glwd_water_fraction(glwd_dir, cells = NULL)
}
\arguments{
\item{glwd_dir}{Directory holding the GLWD rasters, as
\code{inst/scripts/download/download_hydrology.R} lays them out: \verb{GLWD_v2/} for
v2, or \code{glwd_3/hdr.adf} / \code{glwd_3.tif} for v1.}

\item{cells}{Optional \code{tibble} of \code{lon}/\code{lat} cell centres to sample at.
\code{NULL} (default) returns every cell of the 0.5-degree grid the rasters
cover.}
}
\value{
A \code{tibble} with \code{lon}, \code{lat} and \code{water_frac}, carrying a
\code{"glwd_version"} attribute of \code{"v1"} or \code{"v2"}.
}
\description{
Aggregates the Global Lakes and Wetlands Database rasters to the 0.5-degree
grid as a fraction of the whole cell. This is the one implementation of that
derivation in WHEP: \code{\link[=read_glwd_water]{read_glwd_water()}} calls it for
\code{\link[=build_polycell_support]{build_polycell_support()}}, and \code{inst/scripts/prepare_spatialize_all.R}
calls it to write LPJmL's \code{lakes_rivers} input. It used to live only in that
script, so the polycell producer read a hand-made \code{.clm} artefact of an
LPJmL run instead and the two answers were free to diverge.
}
\section{Which classes count as inland water}{

GLWD v2 is a \strong{33-class wetland map}, not a water fraction, so a subset has
to be chosen and the choice is a judgement rather than a lookup. Taken here:
lakes as classes 1-3 (freshwater lake, saline lake, reservoir) and rivers as
class 7 (small streams). Everything else -- palustrine and riverine wetland,
peatland, mangrove, saltmarsh, rice paddies -- is \strong{excluded}: those are
land that is wet, not surface water, and \code{build_polycell_support()} books
them under \code{land_area_ha}.

Under GLWD v1 the equivalent classes are 1 (lakes) and 3 (rivers). The two
vintages are not interchangeable and give totals about 20\% apart; see
\code{\link[=read_glwd_water]{read_glwd_water()}}.

Class membership is multiplied by the companion \code{area_pct} raster where one
is present, so a partially covered source pixel contributes its own fraction
rather than counting whole.
}

\examples{
# Requires the GLWD download; not run without it.
if (nzchar(Sys.getenv("WHEP_LPJML_INPUT_DIR"))) {
  glwd_water_fraction(
    file.path(Sys.getenv("WHEP_LPJML_INPUT_DIR"), "GLWD")
  )
}
}
