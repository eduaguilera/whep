% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_sjos_nitrogen.R
\name{build_sjos_nitrogen}
\alias{build_sjos_nitrogen}
\title{Assemble the end-to-end SJOS-N output tables.}
\usage{
build_sjos_nitrogen(
  data = list(),
  surplus_method = "harvest_removal",
  boundary_land_use = "all",
  nh3_source = "soil",
  footprint_category = "exceedance",
  example = FALSE
)
}
\arguments{
\item{data}{Named list of injected module inputs. When \code{example = FALSE} it
must carry a \code{balance} (\code{\link[=build_nitrogen_balance]{build_nitrogen_balance()}} output), a \code{critical}
(\code{\link[=read_critical_n]{read_critical_n()}} critical surplus), a \code{critical_loads} list (the three
medium critical loads for the pathway boundary), \code{cbs_food}, \code{population},
\code{n_inputs}, and optionally \code{biomass_coefs} / \code{items_full} for the food
supply and either an \code{io} model or \code{fp_flows} for the footprint. When
neither \code{io} nor \code{fp_flows} is present the footprint is traced through a
domestic-closure fixture built from the extension. Defaults to \code{list()}.}

\item{surplus_method}{Surplus definition passed to \code{\link[=calculate_n_surplus]{calculate_n_surplus()}},
\code{"harvest_removal"} (default) or \code{"full_balance"}.}

\item{boundary_land_use}{Land-use scope stamp passed to
\code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}}, \code{"all"} (default) or \code{"ara"}.}

\item{nh3_source}{Air-pressure scope passed to \code{\link[=build_n_pathway_exceedance]{build_n_pathway_exceedance()}},
\code{"soil"} (default) or \code{"total_agricultural"}.}

\item{footprint_category}{Which per-crop nitrogen mass the footprint traces,
\code{"exceedance"} (default), \code{"within_boundary"} or \code{"production"}.}

\item{example}{If \code{TRUE}, drive the whole chain from the coherent fixture set
instead of \code{data}. Defaults to \code{FALSE}.}
}
\value{
A named list of SJOS-N output tables: \code{surplus} (per-crop gridded
surplus), \code{boundary_surplus} (a list with the \code{grid} and \code{country}
surplus-mode exceedance), \code{boundary_pathway} (the pathway-mode exceedance
with \code{binding_boundary}), \code{nourishment} (per-capita food supply with the
normalized adequacy score and class), \code{scatter} (the per-capita boundary
versus nourishment points), \code{sjos_class} (the 2-way classification) and
\code{footprint} (a list with the \code{fp_all} and \code{fp_food} embodied-nitrogen
footprints).
}
\description{
Composes the Safe and Just Operating Space for nitrogen (SJOS-N) modules into
a named list of analysis-output tables from one coherent set of inputs. The
gridded soil-surface nitrogen surplus (\code{\link[=calculate_n_surplus]{calculate_n_surplus()}}) is compared to
the Schulte-Uebbing critical nitrogen layer for the surplus-mode boundary
(\code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}}, at grid and country resolution) and the same
balance's process-based losses are routed to their medium-specific critical
loads for the pathway boundary (\code{\link[=build_n_pathway_exceedance]{build_n_pathway_exceedance()}}). The
nourishment axis (\code{\link[=build_food_supply]{build_food_supply()}} then \code{\link[=normalize_nourishment]{normalize_nourishment()}}) is
crossed with the country-aggregated exceedance into the 2-way classification
(\code{\link[=classify_sjos_n]{classify_sjos_n()}}) and, via the per-capita anthropogenic reactive nitrogen
(\code{\link[=build_n_percapita]{build_n_percapita()}}), into the boundary-versus-nourishment scatter
(\code{\link[=build_n_boundary_percapita]{build_n_boundary_percapita()}}). The country exceedance finally becomes an
embodied-nitrogen trade footprint (\code{\link[=build_sjos_n_footprint]{build_sjos_n_footprint()}}).

The same nitrogen balance feeds the surplus and the pathway boundaries, the
same nourishment feeds the classification and the scatter, and the one
country exceedance feeds the classification, the footprint extension and the
footprint: consistency is enforced by construction. When \code{example = TRUE}, a
single coherent fixture set drives the whole chain without any real data.
}
\examples{
build_sjos_nitrogen(example = TRUE)
}
