% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_sjos_nitrogen.R
\name{build_sjos_nitrogen}
\alias{build_sjos_nitrogen}
\title{Assemble the end-to-end SJOS-N output tables.}
\usage{
build_sjos_nitrogen(
  data = list(),
  surplus_method = "harvest_removal",
  boundary_land_use = "ara",
  grassland_split = c("image_density", "none"),
  nh3_source = "soil",
  footprint_category = "exceedance",
  nourishment_thresholds = c("composed", "flat"),
  nourishment_band = list(),
  negative_critical = c("keep", "clamp"),
  example = FALSE
)
}
\arguments{
\item{data}{Named list of injected module inputs. When \code{example = FALSE} it
must carry a \code{balance} (\code{\link[=build_nitrogen_balance]{build_nitrogen_balance()}} output), a \code{critical}
(\code{\link[=read_critical_n]{read_critical_n()}} critical surplus), a \code{critical_loads} list (the three
medium critical loads for the pathway boundary), \code{cbs_food} and
\code{n_inputs}, and optionally \code{population} (read with \code{\link[=read_population]{read_population()}}
at its own default composition over the years of \code{cbs_food} and
\code{n_inputs} when absent; inject a table to use any other source),
\code{biomass_coefs} / \code{items_full} for the food supply,
\code{manure_mgmt_nh3_n_t} for the pathway boundary when
\code{nh3_source = "total_agricultural"}, \code{critical_binding} (a
\code{\link[=build_critical_n_binding]{build_critical_n_binding()}} table for the \code{boundary_land_use} scope, whose
\code{binding_threshold} is then carried into the grid boundary; absent, the
column is \code{NA}), and either an \code{io} model or
\code{fp_flows} for the footprint. A real call without either source aborts
rather than fabricating a domestic-only footprint. \code{grassland} is the
\code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}} grassland-split input, only used and
only optional when \code{boundary_land_use = "all"} and
\code{grassland_split = "image_density"} (see \code{grassland_split}); a real call
without it and without \code{critical} to build it from aborts rather than
guessing a var/threshold to match.
Defaults to \code{list()}.}

\item{surplus_method}{Surplus definition passed to \code{\link[=calculate_n_surplus]{calculate_n_surplus()}},
\code{"harvest_removal"} (default) or \code{"full_balance"}.}

\item{boundary_land_use}{Land-use scope stamp passed to
\code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}}, \code{"ara"} (default, the robust historical
comparison) or \code{"all"} (cropland and intensive grassland compared like
for like against the critical allowance, extensive grassland against
IMAGE's 2010 budget; see \code{grassland_split}) (issue #1285).}

\item{grassland_split}{Grassland treatment passed to
\code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}}, used only when
\code{boundary_land_use = "all"}: \code{"image_density"} (default) splits each
cell into a managed and an extensive component, \code{"none"} compares one
cell pressure with the deposited \code{"all"}-scope allowance. Under
\code{"image_density"}, \code{data$grassland} is used when supplied (its four
elements, see \code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}}); otherwise it is built
from \code{\link[=build_grassland_intensity_classes]{build_grassland_intensity_classes()}}, the IMAGE 2010 extensive
budget, and the \code{"ara"}/\code{"igl"} critical layers matched to
\code{data$critical}'s own var and threshold. Ignored (no extra reads) when
\code{boundary_land_use} is not \code{"all"}.}

\item{nh3_source}{Air-pressure scope passed to \code{\link[=build_n_pathway_exceedance]{build_n_pathway_exceedance()}},
\code{"soil"} (default) or \code{"total_agricultural"}.}

\item{footprint_category}{Which per-crop nitrogen mass the footprint traces,
\code{"exceedance"} (default), \code{"within_boundary"} or \code{"production"}.}

\item{nourishment_thresholds}{Which band the "just" axis classifies against:
\code{"composed"} (default) builds it per country and year from
\code{\link[=build_nourishment_band]{build_nourishment_band()}}'s four sourced terms, or \code{"flat"} restores the
retired 62.1 / 85.05 pair. \code{"flat"} survives for continuity and
sensitivity only: of its five underlying numbers only the 46 g/cap/day
floor was ever sourced, and the 1.35 multiplier behind both bounds was a
preliminary presentation figure (whep#753).}

\item{nourishment_band}{Named list of options for the composed band, ignored
when \code{nourishment_thresholds = "flat"}. \code{quality_method} and
\code{quality_variant} select the protein-quality tier and its bracket
(\code{\link[=build_protein_quality]{build_protein_quality()}}); \code{wedge_method} and \code{wedge_coverage} select the
loss wedge (\code{\link[=build_loss_wedge]{build_loss_wedge()}}); \code{shortfall}, \code{ceiling} and
\code{requirement_sd} go to \code{\link[=build_nourishment_band]{build_nourishment_band()}} itself, and \code{ceiling} is
the sensitivity knob the band's own documentation asks callers to sweep.
An option this list does not name \strong{aborts} rather than being ignored, so
a mistyped knob cannot silently run the default and be reported as a
sensitivity. Defaults to \code{list()}, which leaves every builder on its own
default.}

\item{negative_critical}{Treatment of cells whose critical surplus is below
zero, passed to \code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}}: \code{"keep"} (default, as the
source) or \code{"clamp"} (zero allowance, a declared departure from
Schulte-Uebbing et al. 2022). It reaches the grid and country boundary and
through them the classification and the footprint, and is stamped as
\code{negative_critical} in both boundary tables, in \code{sjos_class} and in both
footprint tables.}

\item{example}{If \code{TRUE}, drive the whole chain from the coherent fixture set
instead of \code{data}. Defaults to \code{FALSE}.}
}
\value{
A named list of SJOS-N output tables: \code{surplus} (per-crop gridded
surplus), \code{boundary_surplus} (a list with the \code{grid} and \code{country}
surplus-mode exceedance), \code{boundary_pathway} (the pathway-mode exceedance
with \code{binding_boundary}), \code{nourishment} (per-capita food supply with the
normalized adequacy score and class), \code{scatter} (the per-capita boundary
versus nourishment points; it and \code{nourishment} carry
\code{method_population}, \code{"read_population"} or \code{"supplied"}), \code{sjos_class}
(the 2-way classification) and \code{footprint} (a list with the \code{fp_all} and
\code{fp_food} embodied-nitrogen footprints, both carrying \code{target_nourish},
and \code{target_class_diag}, the per-year count of flows whose consumer
country-year has no nourishment class). The boundary tables,
\code{sjos_class} and both footprint tables carry \code{negative_critical}.
}
\description{
Composes the Safe and Just Operating Space for nitrogen (SJOS-N) modules into
a named list of analysis-output tables from one coherent set of inputs. The
gridded soil-surface nitrogen surplus (\code{\link[=calculate_n_surplus]{calculate_n_surplus()}}) is compared to
the Schulte-Uebbing critical nitrogen layer for the surplus-mode boundary
(\code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}}, at grid and country resolution) and the same
balance's process-based losses are routed to their medium-specific critical
loads for the pathway boundary (\code{\link[=build_n_pathway_exceedance]{build_n_pathway_exceedance()}}). The
nourishment axis (\code{\link[=build_food_supply]{build_food_supply()}} then \code{\link[=normalize_nourishment]{normalize_nourishment()}}) is
crossed with the country-aggregated exceedance into the 2-way classification
(\code{\link[=classify_sjos_n]{classify_sjos_n()}}) and, via the per-capita anthropogenic reactive nitrogen
(\code{\link[=build_n_percapita]{build_n_percapita()}}), into the boundary-versus-nourishment scatter
(\code{\link[=build_n_boundary_percapita]{build_n_boundary_percapita()}}). The country exceedance finally becomes an
embodied-nitrogen trade footprint (\code{\link[=build_sjos_n_footprint]{build_sjos_n_footprint()}}), which carries
the producer's classes (\code{origin_classes}, the per-crop classification) and
the consuming country's nourishment class (\code{target_nourish}, joined on
\code{target_area} and \code{year} from the same nourishment table). The driver joins
no consumer boundary class: that is a country-year class decided after
aggregation, which a caller supplies to \code{\link[=build_sjos_n_footprint]{build_sjos_n_footprint()}} directly.

The same nitrogen balance feeds the surplus and the pathway boundaries, the
same nourishment feeds the classification and the scatter, and the one
country exceedance feeds the classification, the footprint extension and the
footprint: consistency is enforced by construction. When \code{example = TRUE}, a
single coherent fixture set drives the whole chain without any real data.
}
\examples{
build_sjos_nitrogen(example = TRUE)
}
