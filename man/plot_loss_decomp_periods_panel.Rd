% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{plot_loss_decomp_periods_panel}
\alias{plot_loss_decomp_periods_panel}
\title{Plot period-based drivers of territorial N losses, as one combined
panel plot}
\usage{
plot_loss_decomp_periods_panel(decomp = NULL)
}
\arguments{
\item{decomp}{A named list from
\code{\link[=decompose_terr_losses_periods]{decompose_terr_losses_periods()}}. If \code{NULL}, computed
automatically (slow).}
}
\value{
A patchwork ggplot object with two panels ("By compartment",
"By mechanism").
}
\description{
Combines the two views from \code{\link[=plot_loss_decomp_periods]{plot_loss_decomp_periods()}}
(by compartment and by mechanism) side by side into a single
patchwork plot with one shared y-axis label, since both break down
contributions to the same total territorial N losses. Each panel
keeps its own legend (compartment and mechanism are different fill
scales, so the legends aren't collected into one).
}
\examples{
if (
  requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("patchwork", quietly = TRUE)
) {
  decomp <- list(
    by_compartment = tibble::tribble(
      ~period, ~compartment, ~contribution_per_yr_mgn,
      "1865-1925", "cropland", 120,
      "1925-1965", "cropland", 260,
      "1865-1925", "urban", 15,
      "1925-1965", "urban", 35
    ),
    by_mechanism = tibble::tribble(
      ~period, ~mechanism, ~contribution_per_yr_mgn,
      "1865-1925", "Size", 90,
      "1925-1965", "Size", 150,
      "1865-1925", "Intensification", 45,
      "1925-1965", "Intensification", 145
    )
  )
  panel <- plot_loss_decomp_periods_panel(decomp)
}
}
