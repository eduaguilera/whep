% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_production.R
\name{build_primary_production}
\alias{build_primary_production}
\title{Build primary production dataset}
\usage{
build_primary_production(
  start_year = 1850,
  end_year = 2023,
  smooth_carry_forward = FALSE,
  example = FALSE,
  show_duplicates = FALSE,
  historical_data = NULL,
  federation_land = c("none", "successor_union"),
  land_method = c("present_day", "historical_polity"),
  .raw_data = NULL
)
}
\arguments{
\item{start_year}{Integer. First year to include. Default \code{1850}.}

\item{end_year}{Integer. Last year to include. Default \code{2023}.}

\item{smooth_carry_forward}{Logical. If \code{TRUE}, carry-forward tails
are replaced with a linear trend. Default \code{FALSE}.}

\item{example}{Logical. If \code{TRUE}, return a small hardcoded example
tibble instead of reading remote data. Default \code{FALSE}.}

\item{show_duplicates}{Logical. If \code{TRUE}, return only the rows that
have competing sources in wide format (one column per source) for
diagnostic comparison. Default \code{FALSE}.}

\item{historical_data}{Optional harmonized historical production rows to add
before the LUH2 historical extension. May be a data frame or a path to a
parquet/csv file. Required semantic columns are \code{year},
\code{item_prod_code}, \code{unit}, \code{value}, and one of \code{area_code} or
\code{polity_area_code}. Names such as \code{item_prod_name}, \code{item_cbs_name}, and
\code{source} are used when present; WHEP item and area tables fill canonical
names where possible. Observed historical rows are retained, and LUH2 proxy
filling can use them as anchors. Default \code{NULL}.}

\item{federation_land}{Character. How the pre-1962 LUH2 back-cast reaches an
area whose territory is a dissolved federation. LUH2 land use is keyed on
present-day ISO3, so 15 Belgium-Luxembourg, 51 Czechoslovakia, 228 USSR and
248 Yugoslav SFR have no land record of their own.
\itemize{
\item \code{"none"} (default, current published behaviour) leaves them unmatched;
their pre-1962 production is not back-cast at all and the build warns.
\item \code{"successor_union"} rebuilds each federation's land series as the sum of
its successor states' LUH2 land, resolved from the \code{successor} relation
published in \link{polities}. This back-casts 14.3\% more of the 1961-62
production tonnage and therefore moves published pre-1962 values.
}}

\item{land_method}{Character. Which borders the pre-1962 \code{ha} half of
\code{tonnes = ha * t_ha} is measured on. The yield half is historical either
way and is untouched by this argument.
\itemize{
\item \code{"present_day"} (default, current published behaviour) reads the
\code{luh2-areas} pin, which is LUH2 land pre-aggregated to present-day ISO3,
so a row labelled with the 1961 entity is measured on the borders that
entity has today.
\item \code{"historical_polity"} measures it with \code{\link[=build_historical_land_areas]{build_historical_land_areas()}}:
gridded LUH2 summed inside the polygon of the polity \code{area_code}
resolved to in that year. It moves published pre-1962 values, needs
\code{sf} and \code{terra}, and reads gridded LUH2 for every back-cast year, so it
is minutes of extra work.
}}

\item{.raw_data}{Optional tibble with the same structure as the output
of the internal \code{.read_production()} step. When supplied, the
remote-data read is skipped entirely and the pipeline starts from
\code{.fix_production()}. Columns required: \code{year}, \code{area}, \code{area_code},
\code{item_prod}, \code{item_prod_code}, \code{item_cbs}, \code{item_cbs_code},
\code{live_anim}, \code{live_anim_code}, \code{unit}, \code{value}, \code{source}.
Default \code{NULL}.}
}
\value{
A tibble with the same columns as \code{\link[=get_primary_production]{get_primary_production()}}:
\code{year}, legacy numeric \code{area_code}, numeric \code{polity_area_code},
\code{reporting_polity_code}, \code{reporting_polity_name},
\code{reporting_polity_has_geometry}, \code{item_prod_code}, \code{item_cbs_code},
\code{live_anim_code}, \code{unit}, \code{value}, and \code{source}.
Item names can be recovered via \code{\link[=add_item_prod_name]{add_item_prod_name()}} and related helpers.
When \code{show_duplicates = TRUE}, returns a wide tibble with one
column per source showing the competing values.
}
\description{
Construct the full primary production dataset from raw FAOSTAT inputs.
This is a convenience wrapper that chains the pipeline steps:
\enumerate{
\item \code{.read_production()} — read & reformat FAOSTAT data.
\item \code{.fix_production()} — apply Global-ported corrections.
\item \code{.dedup_production()} — keep one value per key across sources.
\item \code{.qc_production()} — flag data-quality anomalies on the
surviving (deduplicated) values.
}
}
\examples{
build_primary_production(example = TRUE)
}
