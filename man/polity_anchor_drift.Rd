% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polity_anchor_drift.R
\name{polity_anchor_drift}
\alias{polity_anchor_drift}
\title{Find back-cast rows whose polity label and growth proxy describe different
territories}
\usage{
polity_anchor_drift(
  table,
  code_column = "area_code",
  year_column = "year",
  backcast_anchor = 1961L,
  reference_year = 2023L
)
}
\arguments{
\item{table}{A data frame carrying an area-code column and a year column.}

\item{code_column}{Name of the column holding numeric area codes. The
column may hold either a FAOSTAT \code{area_code} or the \code{polity_area_code}
bucket that published outputs are keyed by; both resolve through the same
lookup.}

\item{year_column}{Name of the column holding years. Required: the whole
question is year-dependent.}

\item{backcast_anchor}{First year of reported (non-back-cast) FAOSTAT data;
passed to the same resolution \code{\link[=add_polity_code]{add_polity_code()}} documents, and the year
the level is anchored at.}

\item{reference_year}{The year whose territory the growth proxy describes.
Defaults to \code{2023L}, \code{\link[=build_primary_production]{build_primary_production()}}'s default \code{end_year},
i.e. the last year the published artifact covers, which is the vintage the
present-day ISO3 keying of the \code{luh2-areas} pin resolves to. Measured on
the shipped crosswalk the answer is identical for every reference year
from 2014 to 2024, so the default is not load-bearing.}
}
\value{
A tibble with one row per drifting \verb{(area_code, year)}, ordered by
area code and year, carrying \code{area_code}, \code{year}, \code{anchor_polity_code},
\code{anchor_polity_name}, \code{data_year_polity_code}, \code{reference_polity_code},
\code{reference_polity_name}, \code{drift_kind} and \code{n_rows}, the number of rows of
\code{table} that pair carries. Zero rows means every pre-anchor row's label
describes the same territory its growth proxy does.
}
\description{
A pre-1962 WHEP row is a reconstruction with \strong{two territorial references}
and they need not agree (whep#748):
\itemize{
\item its \strong{level} is the area's reported value at the back-cast anchor, walked
backwards by \code{\link[=fill_proxy_growth]{fill_proxy_growth()}}, so it describes the territory that
area had in \code{backcast_anchor}. That is the territory
\code{reporting_polity_code} names, because \code{\link[=add_polity_code]{add_polity_code()}} floors the
polity lookup at the same anchor;
\item its \strong{year-on-year movement} is a ratio of LUH2 land, and under
\code{build_primary_production(land_method = "present_day")} the \code{luh2-areas}
pin is keyed on \strong{present-day ISO3}, so the movement describes the
territory that area has today.
}

Wherever a territory changed after the anchor those are different polygons.
Nothing in a built table says so: \code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} answers the
neighbouring question "was this polity live in the row's year", which is
true of far more rows and says nothing about whether the label's extent is
the extent the value was computed on. This reports the disagreement itself,
for a table that has already been built, so it can be sized without
re-deriving the crosswalk.

No row is an error and none is dropped from any build. \code{drift_kind} says
what kind of disagreement a pair carries:
\itemize{
\item \code{"entity"}: the anchor polity and the reference polity are different
entities, e.g. area 181 is labelled \code{SRH-1953-1964} (Southern Rhodesia)
while its LUH2 movement is Zimbabwe's. The label can then overlap a
sibling area's own published rows.
\item \code{"interval"}: the same entity in two vintages, e.g. area 238 is labelled
\code{ETH-1952-1993}, which includes Eritrea, while its movement is
\code{ETH-1993-2025}, which does not. The entity name matches and the polygon
does not, which is the harder class to notice.
\item \code{"unmapped_reference"}: the area resolves to no polity at
\code{reference_year} at all, so what the movement describes cannot be named.
}

A pair whose anchor polity is itself \code{NA} is \strong{not} reported: that row has
no label to disagree with, and it is \code{\link[=polity_mapping_provenance]{polity_mapping_provenance()}} and
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} that account for it.

\code{data_year_polity_code} is reported alongside, resolved unfloored, because
it is the third reference in play: \code{build_primary_production(land_method = "historical_polity")} measures the \code{ha} half inside that polity's polygon
(see \code{\link[=build_historical_land_areas]{build_historical_land_areas()}}), so under that method the movement
describes \code{data_year_polity_code} rather than \code{reference_polity_code}.
}
\section{Only the back-cast era is reported}{

A row at or after \code{backcast_anchor} carries a value its own year's territory
reported, resolved at its own year, so its label and its level agree by
construction and it is not a disagreement this function has anything to say
about. Such rows are filtered out, which is why a \code{table} holding no
pre-anchor year returns zero rows. A missing year column aborts rather than
returning zero rows for the same reason: an empty answer must mean "no
drift", never "nothing was supplied to look at".
}

\examples{
# Area 238 Ethiopia is the documented case: the 1850 row is labelled
# `ETH-1952-1993`, which includes Eritrea, while its LUH2 growth proxy is
# keyed on present-day `ETH`, which does not.
polity_anchor_drift(
  tibble::tibble(
    area_code = c(238L, 238L, 11L),
    year = c(1850L, 2000L, 1850L),
    value = 1
  )
)
}
\seealso{
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} for rows attributed to a polity not live
in their year, \code{\link[=polity_mapping_provenance]{polity_mapping_provenance()}} for which authority a row's
identity rests on, and \code{\link[=polity_bucket_coverage]{polity_bucket_coverage()}} for buckets that sum
more than one territory.
}
