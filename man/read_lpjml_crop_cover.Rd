% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpjml_crop_calendar.R
\name{read_lpjml_crop_cover}
\alias{read_lpjml_crop_cover}
\title{Read monthly cropland soil cover from an LPJmL run's crop calendar.}
\source{
Schaphoff, S. et al. (2018). LPJmL4 - a dynamic global vegetation
model with managed land - Part 1: Model description. \emph{Geoscientific Model
Development}, 11, 1343-1375. \doi{10.5194/gmd-11-1343-2018}.
}
\usage{
read_lpjml_crop_cover(
  run_dir = NULL,
  years = NULL,
  first_year = NULL,
  example = FALSE
)
}
\arguments{
\item{run_dir}{Path to the LPJmL run output directory. \code{NULL} (default)
uses \code{WHEP_LPJML_RUN_DIR}.}

\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
(default) keeps every year the files cover.}

\item{first_year}{Calendar year of the files' first time step. \code{NULL}
(default) reads it from the file's own \code{time} axis.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading a run.
Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{lon}, \code{lat}, \code{year}, \code{month} and \code{cropland_cover}
(a fraction in 0-1).
}
\description{
Per-cell, per-month share of a cell's cropped area that is under a growing
crop, derived from LPJmL's sowing (\code{sdate.nc}) and harvest (\code{hdate.nc})
dates and area-weighted by \code{cftfrac.nc}. Supply the result as
\code{data$cropland_cover} to \code{\link[=build_carbon_balance]{build_carbon_balance()}} to place the cropland
soil-cover season where the crops actually are.
}
\section{Why the temperature proxy is not good enough}{

Without a calendar, \link{soc_soil_cover_curve} is anchored to each cell-year's
warmest month as a stand-in for peak canopy. Measured against this run at
2010 over 18,548 cropland cells, the real area-weighted crop mid-season
falls in the warmest month in only \strong{5.2\%} of them, within one month in
22.6\%, and \strong{three or more months away in 51.0\%} (median absolute offset
three months). Winter cereals, Mediterranean systems and irrigated
dry-season crops all grow away from the temperature peak, so the proxy puts
modelled full canopy over real fallow and modelled bare soil over the real
crop. It is a timing error rather than a level one: the curve's annual mean
cover is 0.254 against the calendar's 0.343.
}

\section{What the calendar does not cover}{

\code{sdate}/\code{hdate} carry 24 bands - the twelve named crops, rainfed and
irrigated - while \code{cftfrac} carries 32. The \code{others} and managed-grassland
bands have no calendar, which is \strong{1.2\% of cropped area}; cells are
returned with the cover of the area that does have one, and a cell whose
cropped area is entirely \code{others} yields no row rather than a guess.

Band mapping is by NAME, never by index: only 12 of the 24 line up, because
\code{sdate} band 13 is \code{"irrigated temperate cereals"} where \code{cftfrac} band 13
is \code{"rainfed others"}.
}

\examples{
read_lpjml_crop_cover(example = TRUE)
}
