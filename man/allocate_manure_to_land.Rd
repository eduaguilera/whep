% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manure_allocation.R
\name{allocate_manure_to_land}
\alias{allocate_manure_to_land}
\title{Allocate field-available manure to cropland and grassland by crop.}
\usage{
allocate_manure_to_land(applied, gridded = list(), options = list())
}
\arguments{
\item{applied}{A tibble from \code{\link[=apply_management_losses]{apply_management_losses()}} with at least \code{year},
\code{territory}, \code{sub_territory}, \code{stream}, \code{applied_n}, \code{applied_c} and
\code{applied_vs}.}

\item{gridded}{A named list describing the land surface for each polity:
\itemize{
\item \code{crops}: a tibble keyed by \code{year}, \code{territory}, \code{sub_territory}, \code{crop}
with the allocation weight (\code{manure_n_receptivity} for
\code{"area_x_receptivity"}, \code{crop_n_demand} for \code{"crop_n_demand"}) and the cap
basis (\code{crop_n_cap}, t N, for \code{"potential_uptake"}/\code{"realised_removal"};
\code{crop_area_ha} for \code{"fixed_ceiling"}).
\item \code{grass} (optional): a tibble keyed by \code{year}, \code{territory}, \code{sub_territory}
with \code{grass_n_cap} (t N) or \code{grass_area_ha}. The grassland cap is scaled by
\code{f_n_tolerance} on the same footing as the uptake-based crop caps (not for
\code{"fixed_ceiling"}). Absent grassland means no grassland sink (cap zero).
}}

\item{options}{A named list of method options: \code{method}
(\code{"area_x_receptivity"} default, or \code{"crop_n_demand"}), \code{cap_method}
(\code{"potential_uptake"} default, \code{"realised_removal"}, or \code{"fixed_ceiling"}),
\code{f_n_tolerance} (default 1.2, applied to the uptake-based caps),
\code{fixed_ceiling_kg_ha} (default 170, EU Nitrates) and \code{disposal_method}
(\code{"over_apply_local"} default, \code{"unmanaged_disposal"}, or
\code{"retain_unallocated"}).}
}
\value{
A tibble with one row per allocation target: \code{year}, \code{territory},
\code{sub_territory}, \code{land_use} (\code{"Cropland"}/\code{"Grassland"}/\code{"Disposal"}/
\code{"Unallocated"}), \code{crop}, \code{source_stream} (\code{"collected"}/\code{"grazing"}),
\code{manure_type} (\code{"Excreta"}/\code{"Solid"}/\code{"Liquid"}, carried from \code{applied}
when present; each allocated row's N/C/VS is split across the
\code{manure_type}s in proportion to their share of the cell's collected N,
so the capacity-filling math itself stays pooled), \code{applied_n},
\code{applied_c}, \code{applied_vs}, \code{over_cap} and the \code{method_allocation},
\code{method_cap} and \code{disposal_method} provenance columns. The N split by
\code{manure_type} is exact (mass-conserving per type); \code{applied_c} and
\code{applied_vs} are split by the SAME N-proportional share, not by each
\code{manure_type}'s own C:N/VS:N ratio, so a per-\code{manure_type} carbon or
volatile-solids figure does not reflect that type's actual composition
(e.g. solid vs. liquid manure have materially different C:N). Callers
needing manure-type-specific carbon/VS (e.g. CH4 potential by system)
should not rely on the \code{manure_type}-broken-out \code{applied_c}/
\code{applied_vs} columns without accounting for this.
}
\description{
Distributes the manure nitrogen, carbon and volatile solids that survive
management (the output of \code{\link[=apply_management_losses]{apply_management_losses()}}) across cropland crops
and grassland, with a local agronomic cap and an explicit overflow path. The
collected/housed manure fills cropland up to each crop's cap (weighted by
West-2014 receptivity by default), the surplus spills onto grassland up to the
grassland cap, and any residual beyond all reachable sinks follows the
\code{disposal_method}. The in-situ grazing stream is deposited on grassland where
it falls, uncapped. Carbon and volatile solids ride along with each stream at
its post-storage bundle ratio, so mass is conserved by construction.
}
\examples{
applied <- tibble::tribble(
  ~year, ~territory, ~sub_territory, ~stream, ~manure_type,
  ~applied_n, ~applied_c, ~applied_vs,
  2020L, "ESP", NA, "collected", "Solid", 80, 800, 40,
  2020L, "ESP", NA, "grazing", "Excreta", 20, 380, 12
)
crops <- tibble::tribble(
  ~year, ~territory, ~sub_territory, ~crop, ~manure_n_receptivity, ~crop_n_cap,
  2020L, "ESP", NA, "barley", 6, 50,
  2020L, "ESP", NA, "wheat", 4, 40
)
allocate_manure_to_land(applied, list(crops = crops))
}
