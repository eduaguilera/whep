% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polity_provenance.R
\name{polity_mapping_provenance}
\alias{polity_mapping_provenance}
\title{Report which authority a row's territorial identity rests on}
\usage{
polity_mapping_provenance(
  table = NULL,
  code_column = "area_code",
  year_column = "year",
  backcast_anchor = 1961L
)
}
\arguments{
\item{table}{A data frame carrying an area-code column, and a year column if
the resolution is to be year-aware. \code{NULL} (the default) reports over the
crosswalk's own \verb{(area_code, year)} grid instead, one row per pair, which
is the provenance of the mapping rather than of a build.}

\item{code_column}{Name of the column holding numeric area codes.}

\item{year_column}{Name of the column holding years. Set to \code{NULL}, or leave
it absent from \code{table}, to use the current/default mapping.}

\item{backcast_anchor}{First year of reported (non-back-cast) FAOSTAT data;
passed to the same resolution \code{\link[=add_polity_code]{add_polity_code()}} documents. Set to \code{-Inf}
to resolve strictly by data year, which is what makes the manufactured
pre-1961 periods live.}
}
\value{
A tibble with one row per resolved \verb{(area_code, year)}, ordered by
area code and year, carrying \code{area_code}, \code{year}, \code{polity_code},
\code{mapping_source}, \code{authority}, \code{mapping_status} and \code{n_rows}, the number of
rows of \code{table} that pair carries (always 1 when \code{table} is \code{NULL}).
}
\description{
\link{polity_area_crosswalk} is not the upstream FAOSTAT-to-polity map. It is that
map plus rows this package manufactures, and its \code{mapping_source} column says
which. Measured on the shipped snapshot, of 648 crosswalk rows:
\itemize{
\item \code{"upstream_map"} (245 rows): a row of \code{faostat_area_polity_map.csv} in
\code{eduaguilera/whep-polities}. Upstream's statement about the territory.
\item \code{"fabio_row_promoted"} (52): the same file, for the 47 areas the FABIO
Rest-of-World fold used to shadow. Equally upstream's statement, and kept
separate only because \code{.unfold_rest_of_world()} chooses between it and the
fold row (whep#717). The two together consume the map's 297 rows exactly
once.
\item \code{"prefix_outside_map"} (263) and \code{"prefix_fallback"} (26): WHEP's own
ISO3-prefix match, built in \code{data-raw/table_mappings.R}. No upstream
authority. A prefix match can only ever produce an ISO3-family guess, so it
cannot express the statements the pre-1961 era actually needs -- Turkey
before 1913 is the Ottoman Empire, Pakistan before 1947 is British India --
whose target stem differs from the source's (whep#740).
\item \code{"fabio_row_fold"} (62): WHEP's own Rest-of-World bucket. Legitimately
WHEP's to decide, but the territory still has no upstream row of its own.
In the default mode only the 31 members upstream names nowhere resolve
through one; see \code{\link[=row_promotion_status]{row_promotion_status()}}.
}

Counting crosswalk rows overstates the exposure, because most manufactured
rows are never picked. This reports the provenance of the \strong{resolution}: for
each \verb{(area_code, year)}, the class of the crosswalk row that
\code{\link[=add_polity_code]{add_polity_code()}} actually selected, which is what a published value rests
on. Measured over the crosswalk's own 1850-2025 grid, 259 of the 263
\code{"prefix_outside_map"} rows are the resolution of no \verb{(area_code, year)} at
all: the back-cast anchor floors every lookup at \code{backcast_anchor}, so the
pre-1961 era resolves through whatever answers the anchor year rather than
through the historical periods the prefix rule invented for it.

The same measurement is what whep#717 moved: 10,912 of those \verb{(area_code, year)} resolutions used to rest on \code{"fabio_row_fold"}, and 5,456 of them --
every year of the 31 members upstream names -- now rest on the upstream map
instead.
}
\section{Authority}{

\code{authority} collapses \code{mapping_source} to the question "who said so":
\itemize{
\item \code{"upstream"}: \code{"upstream_map"} or \code{"fabio_row_promoted"}.
\item \code{"whep_prefix"}: \code{"prefix_outside_map"} or \code{"prefix_fallback"} -- a WHEP
guess, and the population whep#740 asks to delete rather than replace.
\item \code{"whep_bucket"}: \code{"fabio_row_fold"} -- WHEP's own documented bucket.
\item \code{"unresolved"}: the area resolves to no polity, so nothing was said.
}

An unrecognised \code{mapping_source} aborts rather than being folded into one of
these, so a new class of manufactured row cannot arrive already classified.
}

\examples{
# Area 238 Ethiopia is the one reporting area whose published identity rests
# on a manufactured row: its pre-1993 years resolve to `ETH-1952-1993`, a
# period no upstream map row declares.
polity_mapping_provenance(
  tibble::tibble(area_code = 238L, year = c(1900L, 2000L), value = 1)
)

# The headline is one summarise away.
polity_mapping_provenance(
  tibble::tibble(area_code = c(11L, 238L), year = 1990L)
) |>
  dplyr::summarise(n_rows = sum(n_rows), .by = authority)
}
\seealso{
\code{\link[=add_polity_code]{add_polity_code()}} for the resolution itself,
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} for the rows whose polity is a nearest-period
stand-in, and \code{\link[=polity_bucket_coverage]{polity_bucket_coverage()}} for the buckets that sum more than
one territory.
}
