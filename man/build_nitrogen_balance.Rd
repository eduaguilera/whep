% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_balance.R
\name{build_nitrogen_balance}
\alias{build_nitrogen_balance}
\title{Build the full nitrogen balance: inputs, outputs, losses and NUE.}
\usage{
build_nitrogen_balance(
  methods = list(nh3 = "manner", n2o = "aguilera", leaching = "meisinger_drainage"),
  resolution = c("grid", "polity"),
  data = list(),
  gwp = c("ar6", "ar5", "ar4"),
  example = FALSE
)
}
\arguments{
\item{methods}{A named list of method choices: \code{nh3} (forwarded to
\code{\link[=calculate_nh3]{calculate_nh3()}}, default \code{"manner"}), \code{n2o} (forwarded to
\code{\link[=calculate_soil_n2o]{calculate_soil_n2o()}}, default \code{"aguilera"}) and \code{leaching} (forwarded
to \code{\link[=calculate_n_leaching]{calculate_n_leaching()}}, default \code{"meisinger_drainage"}).}

\item{resolution}{\code{"grid"} (default) or \code{"polity"}, as in
\code{\link[=build_n_inputs]{build_n_inputs()}}; \code{"polity"} sums every term (and re-derives every
indicator) over cells.}

\item{data}{Named list of pre-loaded upstream inputs. \code{n_inputs}
(\code{\link[=build_n_inputs]{build_n_inputs()}}'s output) is used directly when supplied, else
\code{\link[=build_n_inputs]{build_n_inputs()}} is called with this SAME \code{data} list (so its nested
readers, e.g. \code{bnf_input}/\code{npp_n_input}/\code{livestock_intake}, propagate
through). Also required:
\itemize{
\item \code{npp_n_input}: shared with \code{\link[=build_n_inputs]{build_n_inputs()}}'s \code{"recycling"} term
(see \code{\link[=calculate_npp_carbon_nitrogen]{calculate_npp_carbon_nitrogen()}}); used here for \code{prod_n_t}.
\item \code{residue_destiny_input}: \code{\link[=calculate_residue_destinies]{calculate_residue_destinies()}}'s required
input (\code{item_prod_code}, \code{residue_dm_t}, plus whatever the chosen
\code{residue_destiny_method} needs), for \code{used_residue_n_t}/
\code{burnt_residue_n_t}. \code{residue_destiny_method} selects the method
(default \code{"krausmann_regional"}).
\item \code{livestock_intake}: shared with \code{\link[=build_n_inputs]{build_n_inputs()}}'s manure term;
its \code{"grass"} \code{feed_quality} rows drive \code{grazed_weeds_n_t}.
\item \code{carbon_balance}: shared with \code{\link[=build_n_inputs]{build_n_inputs()}}'s \code{"som_ mineralization"} term; its \code{son_change_kgn_ha} drives \verb{som_ sequestration_n_t}.
\item \code{n_balance_drivers}: additional per-\verb{(key, fert_type)} driver
columns \code{\link[=calculate_nh3]{calculate_nh3()}}/\code{\link[=calculate_soil_n2o]{calculate_soil_n2o()}}/
\code{\link[=calculate_indirect_n2o_nh3]{calculate_indirect_n2o_nh3()}} need beyond \code{n_input_t}/\code{fert_type}
(\code{climate}, \code{irrig_type}, plus every MANNER driver column when
\code{methods$nh3 == "manner"}), joined by the balance key AND
\code{fert_type} (the Title-case vocabulary from \code{\link[=calculate_nh3]{calculate_nh3()}}'s own
documentation, e.g. \code{"Synthetic"}, \code{"Solid"}, \code{"Excreta_other"}; see
the file-level "fert_type vocabulary bridge" note for the exact
mapping from \code{\link[=build_n_inputs]{build_n_inputs()}}'s lowercase values). A second set of
balance-key-only driver columns (\code{irrig_cat}, \code{tillage}, \code{som_share},
\code{cn_input}, \code{land_use}) for \code{\link[=calculate_n_leaching]{calculate_n_leaching()}} is read from
\code{n_balance_leaching_drivers}. Missing required drivers abort inside
the called \verb{calculate_*()} function, naming the exact column.
\item \code{drainage_mm}: annual drainage (mm) for \code{\link[=calculate_n_leaching]{calculate_n_leaching()}}, as
a numeric vector aligned to the balance-key rows, or already present
as a \code{drainage_mm} column via \code{n_balance_leaching_drivers}.
}}

\item{gwp}{100-year global warming potential standard for N2O, \code{"ar6"}
(default), \code{"ar5"} or \code{"ar4"}, matching \code{\link[=build_crop_soil_n2o_extension]{build_crop_soil_n2o_extension()}}.}

\item{example}{If \code{TRUE}, return a small fixture instead of assembling
real data. Defaults to \code{FALSE}.}
}
\value{
A tibble keyed by \code{year}/\code{area_code}/\code{item_cbs_code} (plus
\code{lon}/\code{lat} at \code{resolution = "grid"}) with the input aggregates
(\code{n_input_full_t}, \code{n_input_full_nosom_t}, \code{n_input_std_t},
\code{n_input_som_t}, \code{n_input_for_n2o_t}), the output aggregates
(\code{n_output_residues_t}, \code{n_output_som_t}, \code{n_output_useful_t},
\code{n_output_std_t}, \code{n_output_full_t}), the loss terms (\code{nh3_n_t},
\code{n2o_direct_n_t}, \code{no3_n_t}, \code{denitrification_n_t},
\code{n2o_indirect_no3_n_t}, \code{n2o_indirect_nh3_n_t}), the (post-cap)
\code{som_sequestration_n_t}, \code{n_balance_t}, \code{surplus_t}, \code{surplus_share},
the five NUE ratios (\code{nue_std}, \code{nue_residues}, \code{nue_som},
\code{nue_useful}, \code{nue_full}), \code{total_gwp_co2e_kg}, and the \code{method_nh3}/
\code{method_soil_n2o}/\code{method_leaching} provenance columns.
}
\description{
Assembles \code{\link[=build_n_inputs]{build_n_inputs()}}'s long-format nitrogen inputs into the
aggregate \verb{N_input_*} sums, adds the output side (crop production,
residue use/burning, grazed weeds, soil organic-matter sequestration),
runs the nitrogen-loss cascade (\code{\link[=calculate_nh3]{calculate_nh3()}}, \code{\link[=calculate_soil_n2o]{calculate_soil_n2o()}},
\code{\link[=calculate_n_leaching]{calculate_n_leaching()}}, \code{\link[=calculate_indirect_n2o_nh3]{calculate_indirect_n2o_nh3()}}), closes the
balance (\code{N_input_full - N_output_full}), applies the N-limitation cap on
SOM sequestration, and derives nutrient-use-efficiency (NUE) indicators
plus a GWP/CO2e indicator for the nitrous-oxide streams. Ported from
Spain_Hist's \code{Balance_parameters()}/\code{N_Figs.R} equations.
}
\examples{
build_nitrogen_balance(example = TRUE)
}
