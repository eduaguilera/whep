% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_v2.R
\name{build_primary_production_v2}
\alias{build_primary_production_v2}
\title{Build primary production dataset (v2 — with QC flags)}
\usage{
build_primary_production_v2(
  max_year = 2021,
  version = NULL,
  smooth_carry_forward = FALSE,
  anchor_years = 5L,
  spike_ratio = 10,
  spike_min = 1000,
  carry_forward_run = 3L
)
}
\arguments{
\item{max_year}{Integer. Latest year of FAOSTAT data. Default \code{2021}.}

\item{version}{File version for input pins. \code{NULL} (default) uses the
frozen version from \link{whep_inputs}.}

\item{smooth_carry_forward}{Logical.
If \code{TRUE}, carry-forward tails are replaced with a linear trend
extrapolated from the preceding \code{anchor_years} of non-flagged data.
Default \code{FALSE} (flag only, no value change).}

\item{anchor_years}{Integer. Number of preceding years used for the
linear-trend extrapolation when \code{smooth_carry_forward = TRUE}.
Default \code{5}.}

\item{spike_ratio}{Numeric. Year-on-year ratio threshold: rows where
\code{current / previous > spike_ratio} (or \verb{< 1 / spike_ratio}) and
both values exceed \code{spike_min} are flagged.  Default \code{10}.}

\item{spike_min}{Numeric. Minimum absolute value on both sides of a
year-on-year comparison for a spike to be flagged.  Default \code{1000}.}

\item{carry_forward_run}{Integer. Minimum number of identical
consecutive final-year values to qualify as a carry-forward flag.
Default \code{3}.}
}
\value{
A tibble with the same columns as \code{\link[=build_primary_production]{build_primary_production()}}
\strong{plus} a character column \code{qc_flag} that is \code{NA} for clean rows
or contains semicolon-separated labels (\code{"carry_forward"},
\code{"spike"}, \code{"fodder_break"}).

When \code{smooth_carry_forward = TRUE}, the \code{value} column is modified
for flagged carry-forward rows; the original value is preserved in
an attribute \code{"original_carry_forward"} (a tibble of the replaced
rows with their original value).
}
\description{
Runs the v1 \code{\link[=build_primary_production]{build_primary_production()}} pipeline, then applies a
data-quality layer that detects:
\itemize{
\item \strong{carry_forward} — constant-value tails at the end of a series
(typically FAOSTAT carry-forward imputations).
\item \strong{spike} — year-on-year changes exceeding a threshold (e.g. 10×).
\item \strong{fodder_break} — the known 1984 / 1985 discontinuity in several
European countries' fodder / forage reporting.
}

The original v1 output is returned unchanged in all value columns;
the only additions are the \code{qc_flag} column and, when
\code{smooth_carry_forward = TRUE}, a trend-based replacement of the
flagged carry-forward tail.
}
\examples{
\dontrun{
primary_v2 <- build_primary_production_v2()
primary_v2 |> dplyr::filter(!is.na(qc_flag))
}
}
