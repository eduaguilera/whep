% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{decompose_specialization_cov}
\alias{decompose_specialization_cov}
\title{Decompose specialization from diversification via the Olley-Pakes
allocation covariance}
\usage{
decompose_specialization_cov(n_prov_destiny = NULL, raw = NULL)
}
\arguments{
\item{n_prov_destiny}{Nitrogen flows tibble from
\code{\link[=create_n_prov_destiny]{create_n_prov_destiny()}}. If \code{NULL}, loaded automatically.}

\item{raw}{Named list overriding any of the raw inputs (\code{npp_ygpit},
\code{codes_coefs}, \code{intake_ygiac}, \code{n_excretion_ygs}, \code{stock_prod_ygps},
\code{livestock_units}). Missing elements are loaded automatically.}
}
\value{
A named list with tibbles \code{cropland_province},
\code{cropland_destiny}, and \code{livestock_species}, each with columns \code{year}
and \code{covariance}.
}
\description{
\code{\link[=decompose_cropland_surplus]{decompose_cropland_surplus()}}, \code{\link[=decompose_semi_natural_surplus]{decompose_semi_natural_surplus()}}, and
\code{\link[=decompose_manure_losses]{decompose_manure_losses()}} are all simplified to national-only views
with no spatial, destiny, or species-mix factor, so the LMDI
"Specialization" mechanism (in \code{\link[=decompose_terr_losses]{decompose_terr_losses()}}) is
currently empty. This function recovers the provincial and species
allocation signal independently, straight from the underlying panels:
it shows whether the allocation of area or herd across units
(provinces, destinies, species) concentrated into high-surplus units
(genuine specialization) or spread towards low-surplus ones
(diversification) — a distinction the mix alone cannot make. This
function adds that signal, following the Olley-Pakes
allocation identity used in the decomposition proposal
(\code{sum(w_i * s_i) = mean(s) + covariance(w_i, s_i)}): for a set of units
with area/herd share \code{w_i} and per-unit surplus \code{s_i}, the covariance
between the two is positive and growing when the allocation
concentrates into high-surplus units (specialization raising surplus),
and shrinks towards zero or turns negative under diversification.

Unlike the additive LMDI contributions (in Mg N), the covariance is
expressed in per-unit-area or per-unit-herd surplus terms (Mg N per ha,
or Mg N per livestock unit) — it is not directly comparable in
magnitude to the "Specialization" mechanism total from
\code{\link[=decompose_terr_losses]{decompose_terr_losses()}}, only in sign and trend.
}
\examples{
# decompose_specialization_cov()
}
