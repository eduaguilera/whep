% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ag_land_support.R
\name{build_ag_land_support}
\alias{build_ag_land_support}
\title{Build the gridded agricultural land support.}
\usage{
build_ag_land_support(
  years = NULL,
  grassland = c("gridded_pasture", "luh2", "none"),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
(default) keeps every year the cropland surface covers.}

\item{grassland}{Grassland-support source. \code{"gridded_pasture"} (default) is
the prepared per-cell \code{pasture_ha} + \code{rangeland_ha} surface, which shares
the cropland surface's grid and 1851-2023 span. \code{"luh2"} reads the same
LUH2 classes through \code{\link[=read_luh2_landuse]{read_luh2_landuse()}} and agrees with it where they
overlap, but stops at 2015. \code{"none"} returns cropland-only support, an
explicit choice rather than a silent gap.}

\item{data}{Optional named list of pre-loaded inputs to avoid remote reads:
\code{cell_polity} (the \code{\link[=build_cell_polity]{build_cell_polity()}} crosswalk), \code{type_cropland}
(\code{lon}, \code{lat}, \code{year}, \code{luh2_type}, \code{type_ha}), \code{crop_patterns} (\code{lon},
\code{lat}, \code{item_prod_code}, \code{harvest_fraction}), \code{gridded_pasture} (\code{lon},
\code{lat}, \code{year}, \code{pasture_ha}, \code{rangeland_ha}), \code{states}
(\code{\link[=read_luh2_landuse]{read_luh2_landuse()}}'s raw LUH2 states) and \code{grassland_ha} (\code{lon},
\code{lat}, \code{area_code}, \code{year}, \code{area_ha}, bypassing the grassland read
entirely). Each falls back to its reader when absent.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{lon}, \code{lat}, \code{area_code}, \code{item_cbs_code}, \code{year},
\code{land_use} (\code{"cropland"} or \code{"grassland"}) and positive \code{area_ha}, plus the
polity columns below.
}
\description{
Assembles the physical agricultural land support that \code{\link[=build_n_inputs]{build_n_inputs()}}
allocates its non-crop-specific nitrogen terms over: per grid cell, polity,
year and CBS item, the hectares of agricultural land available to receive
nitrogen. Cropland hectares come from the LUH2-derived \code{type_cropland}
surface, split among crops by the static \code{crop_patterns} composition
(normalised within each cell, so the cell's physical cropland area is
apportioned rather than inflated by multicropping). Grassland hectares come
from \code{\link[=read_luh2_landuse]{read_luh2_landuse()}}'s gridded grassland class and are all carried on
CBS 3000, with no intensive/extensive split inferred. Both sides are split
across border polities by the same \code{cell_polity} crosswalk.

Years with cropland but no grassland coverage (a grassland source that runs
short of the cropland surface, as \code{"luh2"} does after 2015) keep their
cropland support and raise a warning naming the affected years; supply
\code{data$grassland_ha} to cover them.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_ag_land_support(example = TRUE)
}
