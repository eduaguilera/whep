% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/food_gini.R
\name{calculate_food_gini}
\alias{calculate_food_gini}
\title{Between-country population-weighted Gini of per-capita food supply.}
\usage{
calculate_food_gini(x, value_col = protein_g_cap_day, pop_col = population)
}
\arguments{
\item{x}{A tibble with a \code{year} column, the per-capita supply column named by
\code{value_col} and the population column named by \code{pop_col} (for example a
\code{\link[=build_food_supply]{build_food_supply()}} output).}

\item{value_col}{The unquoted per-capita supply column whose between-country
inequality is measured. Defaults to \code{protein_g_cap_day}.}

\item{pop_col}{The unquoted population column used as the inequality weight.
Defaults to \code{population}.}
}
\value{
A tibble keyed by \code{year} with the \code{gini} coefficient.
}
\description{
Reduces a per-capita nourishment series to a per-year, between-country,
population-weighted Gini coefficient, a diagnostic of how unequally
per-capita supply is shared across countries (it does not rescale the
per-country nourishment score). Within each \code{year}, countries are sorted in
descending per-capita supply; each country's population fraction \code{pop_frac}
and its share of the total supply mass \code{value_frac} (per-capita supply times
population, over the world total) give a score \code{value_frac * (pop_frac + 2 * richer_frac)}, where \code{richer_frac} is the cumulative population fraction of
the strictly richer countries. The Gini is \code{1 - sum(score)} over the
countries in that year, \code{0} under a perfectly equal distribution and rising
toward \code{1} as supply concentrates. Protein per-capita supply is the SJOS-N
nourishment axis, so it is the default; passing a different \code{value_col} (for
example \code{energy_kcal_cap_day}) takes the Gini of that axis instead. Rows with
a missing supply or population value are dropped before the computation.
}
\examples{
calculate_food_gini(
  tibble::tribble(
    ~year, ~area_code, ~protein_g_cap_day, ~population,
    2000L, 10L, 40, 100,
    2000L, 20L, 40, 100,
    2000L, 30L, 40, 100
  )
)
}
