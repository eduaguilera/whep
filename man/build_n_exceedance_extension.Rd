% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_exceedance_extension.R
\name{build_n_exceedance_extension}
\alias{build_n_exceedance_extension}
\title{Build the embodied-nitrogen footprint extension.}
\usage{
build_n_exceedance_extension(
  exceedance,
  category = c("exceedance", "within_boundary", "production")
)
}
\arguments{
\item{exceedance}{A \code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}} output at
\code{resolution = "country"}, keyed by \code{year}, \code{area_code}, \code{item_cbs_code}
with the mass terms \code{exceedance_n_t}, \code{within_boundary_n_t}, \code{actual_n_t}
and, for \code{category = "production"}, \code{production_n_t}.}

\item{category}{Which nitrogen mass to carry into \code{impact_u}: \code{"exceedance"}
(default), \code{"within_boundary"} or \code{"production"}. Validated with
\code{\link[rlang:arg_match]{rlang::arg_match()}}.}
}
\value{
A tibble with the \code{\link[=build_footprint]{build_footprint()}} extension contract columns
\code{year}, \code{area_code}, \code{item_cbs_code}, \code{impact_u} (tonnes N) and
\code{method_n_exceedance} (the chosen category).
}
\description{
Selects one nitrogen category from a
\code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}} country-resolution output into the
\code{impact_u} column of a \code{\link[=build_footprint]{build_footprint()}} extension: \code{"exceedance"} (the
default) carries \code{exceedance_n_t}, \code{"within_boundary"} carries
\code{within_boundary_n_t}, and \code{"production"} carries \code{production_n_t}
(harvested product plus used residue plus grazed forage). The chosen
category is stamped in \code{method_n_exceedance}.

Signed crop-attributed values are retained. They arise legitimately when a
negative crop-surplus contribution receives its approved signed share of a
positive cell overshoot. \code{\link[=build_sjos_n_footprint]{build_sjos_n_footprint()}} traces positive and
negative parts separately through the non-negative footprint engine and
recombines them linearly.

If the upstream result contains a zero/near-zero-denominator cell residual,
this function raises a typed undefined-attribution error: a mandatory crop
footprint cannot silently discard or invent an allocation for that residual.

The per-crop (\code{item_cbs_code}) granularity is preserved so the footprint can
be traced to origin (locked plan decision 14). Rows with a missing key are
dropped defensively; zero-impact crops are kept because they still consume
trade.
}
\examples{
build_n_exceedance_extension(
  tibble::tribble(
    ~year,
    ~area_code,
    ~item_cbs_code,
    ~exceedance_n_t,
    ~within_boundary_n_t,
    ~actual_n_t,
    2010L, 10L, 2511L, 5, 3, 8,
    2010L, 10L, 2513L, 0, 4, 4
  ),
  category = "exceedance"
)
}
