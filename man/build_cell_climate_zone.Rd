% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/livestock_climate.R
\name{build_cell_climate_zone}
\alias{build_cell_climate_zone}
\title{Build the per-cell IPCC climate zone from CRU mean annual temperature.}
\source{
CRU TS (Climatic Research Unit, University of East Anglia; Harris,
Osborn & Jones 2020, Scientific Data,
\doi{10.1038/s41597-020-0453-3}). Zone cuts: IPCC 2006 Guidelines Vol.4
Ch.3, adopted by the 2019 Refinement Vol.4 Ch.10.
}
\usage{
build_cell_climate_zone(
  years = NULL,
  cru_dir = NULL,
  data = NULL,
  example = FALSE
)
}
\arguments{
\item{years}{Integer vector of calendar years to return. \code{NULL} (default)
returns every year the CRU record covers. Years before the record are
backcast and years after it are forward-held, both from the nearest
30-year climatology.}

\item{cru_dir}{Path to the CRU TS NetCDF directory. Defaults to
\code{Sys.getenv("WHEP_CRU_DIR")}; aborts when neither is set. Ignored when
\code{data} is supplied.}

\item{data}{Optional pre-read tibble of annual means (\code{lon}, \code{lat}, \code{year},
\code{mean_annual_temp_c}) used in place of reading CRU, for testing. The years
it carries are treated as the measured record, so the backcast and
forward-hold logic still applies to \code{years} outside them.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading CRU
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per cell and year:
\itemize{
\item \code{lon}, \code{lat}: Cell centre coordinates (0.5-degree grid).
\item \code{year}: Calendar year.
\item \code{mean_annual_temp_c}: Mean annual near-surface air temperature (degrees
Celsius), the mean of the twelve monthly CRU values.
\item \code{climate_zone}: IPCC zone, \code{"Cool"}, \code{"Temperate"} or \code{"Warm"}.
\item \code{method_climate_zone}: \code{"cru_ts_annual"} for a measured year, or
\code{"climatology_<first>_<last>"} for a held climatology.
}
}
\description{
Reduce CRU TS monthly near-surface temperature to a mean annual temperature
(MAT) per 0.5-degree land cell and year, and classify each cell into the
IPCC manure-management climate zone (\code{"Cool"}, \code{"Temperate"}, \code{"Warm"}) that
\link{climate_mcf} is keyed on. This is the per-cell replacement for the single
hardcoded \code{"Temperate"} zone the manure CH4 model falls back to when no zone
is supplied.

CRU TS covers 1901 onwards, so years outside its span cannot be measured.
They are not dropped and not silently given a measured year's value: the
30-year climatology at the nearest end of the record is held constant across
them and stamped in \code{method_climate_zone} with its own label (for example
\code{"climatology_1901_1930"}), so a reader can tell a backcast zone from a
measured one without reading this code. Holding an early-record climatology
constant backwards is an assumption, not an observation.
}
\examples{
build_cell_climate_zone(example = TRUE)
}
