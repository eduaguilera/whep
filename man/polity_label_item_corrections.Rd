% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_mappings.R
\docType{data}
\name{polity_label_item_corrections}
\alias{polity_label_item_corrections}
\title{Source label corrections scoped to one item}
\format{
A tibble with one row per rule. It has zero rows when the shipped snapshot
was taken from a \code{whep-polities} revision that published no rules. Columns:
\itemize{
\item \code{source}: Source slug the rule applies to.
\item \code{source_label}: The label the source files the rows under.
\item \code{item}: The item, exactly as the source writes it.
\item \code{year_start}, \code{year_end}: Inclusive year range.
\item \code{correct_label}: The label to resolve instead.
\item \code{polity_code}: Where \code{correct_label} resolves in the same upstream
revision.
\item \code{observed_rows}: Source rows the rule relabels upstream.
\item \code{issue}: The \code{whep-polities} issue that decided the rule.
\item \code{evidence}: Why the rows belong to the other territory.
}
}
\source{
\verb{~/whep-polities/data/final/source_label_item_corrections.csv}.
}
\usage{
polity_label_item_corrections
}
\description{
Rows a source files under ANOTHER territory's label for one item, published
by \code{whep-polities}. An alias maps a label to a polity with no item
dimension, so it cannot say that Mitchell's pre-1910 \code{"south africa"} sugar
cane is Natal's while the other items under that label are the Cape's. Each
rule replaces the label before resolution; \code{\link[=resolve_polity_label]{resolve_polity_label()}} applies
them when it is given \code{item}.
}
\details{
A row matches a rule when \code{source}, \code{source_label} (normalised as the alias
map's labels are) and \code{item} all match and the row's year lies in
\verb{[year_start, year_end]}. Rows without a year are never corrected, and rules
do not chain.
}
\keyword{datasets}
