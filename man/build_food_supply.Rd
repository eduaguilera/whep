% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/food_supply.R
\name{build_food_supply}
\alias{build_food_supply}
\title{Build per-capita food supply for the nourishment axis.}
\usage{
build_food_supply(
  method = c("whep_native", "faostat_fbs"),
  data = list(),
  protein_basis = c("edible_portion", "whole_commodity", "product_nitrogen"),
  example = FALSE
)
}
\arguments{
\item{method}{Supply source: \code{"whep_native"} (default, commodity-balance
food tonnes times \code{whep::biomass_coefs} divided by population) or
\code{"faostat_fbs"} (the injected FAOSTAT FBS per-capita supply).}

\item{data}{Named list of injected inputs. For \code{"whep_native"}:
\code{cbs_food} (\code{year}, \code{area_code}, \code{item_cbs_code}, \code{food_t}) and
\code{population} (\code{year}, \code{area_code}, \code{population}) are required, and
\code{biomass_coefs} / \code{items_full} override the packaged
\code{whep::biomass_coefs} / \code{whep::items_full}. For \code{"faostat_fbs"}:
\code{fbs_supply} (\code{year}, \code{area_code}, \code{protein_g_cap_day},
\code{energy_kcal_cap_day}, \code{population}) is required.}

\item{protein_basis}{How the inedible fraction is treated when converting
nitrogen density to protein, for \code{"whep_native"} only:
\code{"edible_portion"} (default) scales the nitrogen density by
\code{Edible_portion}, which is correct when \code{food_t} is commodity mass while
the density applies to the edible part, and agrees best with FAOSTAT FBS;
\code{"whole_commodity"} applies no edible scaling, the behaviour before this
argument existed, kept for continuity and sensitivity analysis;
\code{"product_nitrogen"} uses the agronomic \code{Product_kgN_kgDM} for both the
edible and inedible fractions, scaled by \code{Edible_portion}, ignoring
\code{N_kgN_kgFM}. A missing \code{Edible_portion} counts as 1.}

\item{example}{If \code{TRUE}, return a small fixture instead of computing.
Defaults to \code{FALSE}.}
}
\value{
A tibble keyed by \code{year}, \code{area_code} with \code{protein_g_cap_day},
\code{energy_kcal_cap_day}, \code{population}, \code{method_food_supply} and
\code{method_protein_basis} (\code{NA} for \code{"faostat_fbs"}), plus the polity columns
below.
}
\description{
Assembles per-capita protein and dietary-energy supply, the state variable
for the SJOS-N nourishment ("just") axis. Protein is the SJOS-N nourishment
axis; dietary energy is a secondary cross-check. The default \code{"whep_native"}
method multiplies the WHEP commodity-balance food element (tonnes fresh
matter, per \code{year}, \code{area_code}, \code{item_cbs_code}) by the per-item nutrition
coefficients in \code{whep::biomass_coefs} and divides by national population.
Protein per kilogram fresh matter is nitrogen times 6.25
(nitrogen-to-protein factor), on the basis selected by \code{protein_basis}.
The nitrogen density is \code{N_kgN_kgFM} where available, otherwise
\code{Product_kgN_kgDM * Product_kgDM_kgFM}. \code{Edible_N_kgFM} is not read: it is
empty in every coefficient row, upstream as well as in the packaged data, so
the edible basis is derived from \code{Edible_portion} instead of stored
redundantly. Energy per kilogram
fresh matter follows \code{GE_product_edible_portion_MJ_kgFM}, then
\code{GE_product_MJ_kgFM} (MJ per kg fresh matter), converted to kilocalories via
\code{MJ / 0.004184}. The energy term is GROSS (combustion) energy, not Atwater
metabolisable energy, and so is only a secondary cross-check for SJOS-N;
Atwater factors could refine it (O-B). Food items with no protein
coefficient after the coalesce chain are excluded with a warning naming the
count and a few examples (the residual gap-fill, O-B), never silently
dropped. The \code{"faostat_fbs"} method returns the injected FAOSTAT Food
Balance Sheet per-capita supply unchanged, as a cross-check / sensitivity.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning.
}

\examples{
build_food_supply(example = TRUE)
}
