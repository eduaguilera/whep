% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet_integrity.R
\name{assert_parquet_integrity}
\alias{assert_parquet_integrity}
\title{Assert that a Parquet file is structurally sound.}
\usage{
assert_parquet_integrity(path, deep = FALSE)
}
\arguments{
\item{path}{Path to a Parquet file.}

\item{deep}{Whether to also read every row group and report the
ones that fail to decode. Exact, but reads the entire file.}
}
\value{
Invisibly, \code{path}. Called for its side effect of aborting
on violation.
}
\description{
Build-time gate over a Parquet artifact. Aborts when
\code{\link[=check_parquet_integrity]{check_parquet_integrity()}} reports any anomaly, so a corrupt
multi-gigabyte cube fails loudly at the moment it is written
instead of being read back truncated for months (whep#531).
}
\examples{
path <- tempfile(fileext = ".parquet")
nanoparquet::write_parquet(data.frame(x = 1:10), path)
assert_parquet_integrity(path)
unlink(path)
}
