% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatialize.R
\name{build_gridded_landuse}
\alias{build_gridded_landuse}
\title{Build gridded landuse dataset}
\usage{
build_gridded_landuse(
  country_areas,
  crop_patterns,
  gridded_cropland,
  country_grid,
  config = list()
)
}
\arguments{
\item{country_areas}{A tibble with country-level crop harvested areas.
Expected columns:
\itemize{
\item \code{year}: Integer year.
\item \code{area_code}: Country code (numeric, matching WHEP polities).
\item \code{item_prod_code}: FAOSTAT item code for the crop.
\item \code{harvested_area_ha}: Total harvested area in hectares.
\item \code{irrigated_area_ha}: Irrigated harvested area in hectares
(optional, defaults to 0).
}}

\item{crop_patterns}{A tibble with per-cell spatial crop patterns.
Expected columns:
\itemize{
\item \code{lon}: Longitude of cell centre.
\item \code{lat}: Latitude of cell centre.
\item \code{item_prod_code}: FAOSTAT item code.
\item \code{harvest_fraction}: Cropping intensity (Monfreda area divided
by reference cropland).
}}

\item{gridded_cropland}{A tibble with per-cell cropland extent.
Expected columns:
\itemize{
\item \code{lon}: Longitude of cell centre.
\item \code{lat}: Latitude of cell centre.
\item \code{year}: Integer year.
\item \code{cropland_ha}: Total cropland area in hectares.
\item \code{irrigated_ha}: Irrigated cropland in hectares (optional,
defaults to 0).
}}

\item{country_grid}{A tibble mapping grid cells to countries.
Expected columns:
\itemize{
\item \code{lon}: Longitude of cell centre.
\item \code{lat}: Latitude of cell centre.
\item \code{area_code}: Country code.
\item \code{cell_area_frac} (or \code{polity_frac}, \code{area_frac}, \code{country_frac}):
This polity compartment's share of the physical cell, a partition
summing to 1 over the polities that overlap the cell. Required: a
grid carrying no share is refused, because defaulting it to 1 gives
a border cell wholly to one polity. Pass 1 only where the polity
does own the whole cell. A land fraction (\code{landfrac}) is a
different quantity and is refused rather than reinterpreted.
Optional columns:
\item \code{polycell_id}, \code{cell_id}: Stable compartment/cell identifiers
preserved in outputs when present.
\item \code{year} or validity intervals (\code{valid_from}/\code{valid_to},
\code{start_year}/\code{end_year}, \code{from_year}/\code{to_year}) for historical,
time-varying polity overlays. The start bound is inclusive; the end
bound is \strong{exclusive at a succession} and \strong{inclusive at the open
end}, so 2014 selects \code{"RUS-2014-2025"} and not \code{"RUS-1991-2014"},
while 2025 still selects \code{"RUS-2014-2025"} because no later interval of
that compartment follows it. See \link{polities} for the full rule.
}}

\item{config}{Named list of optional extras. Unknown keys raise
an error. Recognised keys:
\itemize{
\item \code{years}: Integer vector of years to spatialize. If \code{NULL}
(default), all years present in \code{country_areas} are processed.
When supplied, \code{country_areas}, \code{gridded_cropland}, and
\code{type_cropland} are filtered to this set before processing.
\item \code{cft_mapping}: A tibble mapping FAOSTAT items to CFT names
(\code{item_prod_code}, \code{cft_name}). If \code{NULL}, no CFT aggregation
is performed and individual crop results are returned.
\item \code{type_cropland}: A tibble with per-cell, per-year, per-type
cropland (\code{lon}, \code{lat}, \code{year}, \code{luh2_type}, \code{type_ha},
\code{type_irrig_ha}). When provided alongside \code{type_mapping},
each crop is allocated only into cells containing its LUH2
type. If \code{NULL}, falls back to total cropland.
\item \code{type_mapping}: A tibble (\code{item_prod_code}, \code{luh2_type}) that
maps each crop to its LUH2 type. If \code{NULL}, type-aware
allocation is disabled even when \code{type_cropland} is provided.
\item \code{multicropping}: A tibble with per-cell multi-cropping
suitability factors. Required columns: \code{lon}, \code{lat},
\code{mc_rainfed}, \code{mc_irrigated}. An optional \code{year} column
keys factors to year (one row per cell per year); when
present, the table is filtered to the current year before
the capacity constraint is applied. When absent, the table
is treated as a static spatial layer applied to every year.
If \code{NULL} (default), the capacity constraint still runs with
\code{mc_rainfed = mc_irrigated = 1} (harvested area capped at
physical cropland).
\item \code{max_iterations}: Maximum iterations for the redistribution
loop. Default: \code{1000L}.
\item \code{expansion_threshold}: Iteration number after which crops are
allowed to expand into cells without an existing pattern.
Default: \code{100L}.
\item \code{area_key}: Which area code the output is keyed on, \code{"grid"}
(default) or \code{"polity_area"}. See \emph{Which area code the output is
keyed on}.
}}
}
\value{
A tibble with gridded crop (or CFT) harvested areas.
Columns:
\itemize{
\item \code{lon}, \code{lat}: Cell centre coordinates.
\item \code{year}: Integer year.
\item \code{area_code}: WHEP polity code for this cell compartment.
\item \code{polity_area_code}, \code{reporting_polity_code},
\code{reporting_polity_name}, \code{reporting_polity_has_geometry}: Polity
metadata for \code{area_code}.
\item \code{grid_area_code}: Only under \code{area_key = "polity_area"}; the
reporting code the engine allocated on.
\item \code{polycell_id}, \code{cell_id}: Preserved when supplied in
\code{country_grid}.
\item \code{crop_name} or \code{cft_name}: Crop or CFT identifier.
\item \code{rainfed_ha}: Rainfed harvested area in the cell.
\item \code{irrigated_ha}: Irrigated harvested area in the cell.
}
}
\description{
Disaggregate country-level FAOSTAT crop harvested areas to a 0.5-degree
grid. This reproduces the core spatialization workflow of the
\href{https://github.com/PIK-LPJmL/LandInG}{LandInG} toolbox, adapted to
WHEP conventions and tidy data structures.

The algorithm follows three main steps:
\enumerate{
\item Each crop's country total is distributed to grid cells proportionally
to a spatial reference pattern (e.g. Monfreda) weighted by gridded
cropland extent (e.g. LUH2/HYDE).
\item If total allocated harvested area in any cell exceeds its capacity
(cropland times multi-cropping suitability), excess is iteratively
redistributed using a logit-based transformation.
\item Individual crops are aggregated into crop functional types (CFTs).
}
}
\section{Which area code the output is keyed on}{

The chain allocates \emph{from} a national table keyed on \code{area_code} and
\emph{into} a \code{country_grid} keyed the same way, so both sides speak the raw
reporting vocabulary the grid was rasterized in. WHEP's polity-keyed
national tables are aggregated on \code{polity_area_code} instead, a bucket
that a reporting code need not equal: \code{276} Sudan and \code{277} South Sudan
both fall in bucket \code{206}. Every such output row therefore carries two
territorial keys that disagree, and whether a consumer joins on
\code{area_code} or on \code{polity_area_code} decides whether Sudan exists in its
result (whep#582).

\code{area_key} selects which of the two the output carries. It is not a
fallback: \code{"grid"} is the default, reproduces today's codes
bit-for-bit, and warns naming the codes that cannot join;
\code{"polity_area"} resolves each code to its bucket through
\link{polity_area_crosswalk} before the polity columns are attached, so
\code{area_code} and \code{polity_area_code} agree in every row. It respects
\code{options(whep.unfold_rest_of_world)} (see \code{\link[=folded_reporting_areas]{folded_reporting_areas()}}),
so the output and the national tables agree about where a Rest-of-World
member's rows belong.

Under \code{"polity_area"} the raw reporting code is \strong{carried, not
replaced}: the output gains \code{grid_area_code}, joined with \code{+} where two
reporting areas of one bucket meet in a cell and their rows collapse. So
the fold stays recoverable at the join rather than baked into the output,
the shape \code{\link[=build_cell_polity]{build_cell_polity()}} adopted for the same reason (whep#579).
}

\section{Methodology}{

This function reimplements the spatial crop allocation from the LandInG
toolbox (Ostberg et al. 2023, doi:10.5194/gmd-16-3375-2023) with the
following extensions:
\itemize{
\item LUH2 crop-functional-type constraints (\code{type_cropland} +
\code{type_mapping} parameters) restrict each crop to cells
containing its LUH2 type (c3ann, c4ann, c3per, c3nfx). LandInG
allocates to total cropland without type constraints.
\item MIRCA2000 crop-specific irrigated fractions (Portmann et al.
2010) for irrigation distribution, falling back to
LUH2-proportional allocation.
}
}

\section{Data sources}{

\itemize{
\item Country areas: FAOSTAT QCL via \code{\link{build_primary_production}}
\item Crop patterns: EarthStat / Monfreda et al. (2008)
\item Gridded cropland: LUH2 v2h (Hurtt et al. 2020)
\item Irrigation: MIRCA2000 (Portmann et al. 2010) + LUH2
}
}

\examples{
# Minimal example with toy data
country_areas <- tibble::tribble(
  ~year, ~area_code, ~item_prod_code, ~harvested_area_ha,
  2000L, 1L, 15L, 1000
)
crop_patterns <- tibble::tribble(
  ~lon, ~lat, ~item_prod_code, ~harvest_fraction,
  0.25, 50.25, 15L, 0.6,
  0.75, 50.25, 15L, 0.4
)
gridded_cropland <- tibble::tribble(
  ~lon, ~lat, ~year, ~cropland_ha,
  0.25, 50.25, 2000L, 800,
  0.75, 50.25, 2000L, 500
)
country_grid <- tibble::tribble(
  ~lon, ~lat, ~area_code, ~cell_area_frac,
  0.25, 50.25, 1L, 1,
  0.75, 50.25, 1L, 1
)
build_gridded_landuse(
  country_areas, crop_patterns, gridded_cropland, country_grid,
  config = list(years = 2000L)
)
}
