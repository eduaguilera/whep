% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/footprint_uncertainty.R
\name{footprint_sensitivity}
\alias{footprint_sensitivity}
\title{Local sensitivity of a footprint to each extension.}
\usage{
footprint_sensitivity(run_fn, extensions, options = list())
}
\arguments{
\item{run_fn}{Function taking an extension vector and returning a
footprint tibble with a \code{value} column.}

\item{extensions}{Numeric vector of base extensions per sector.}

\item{options}{Named list overriding \code{delta} (relative step,
default 0.05) and \code{which} (sector indices to test; default all
non-zero extensions).}
}
\value{
A tibble with \code{sector} (index into \code{extensions}) and
\code{elasticity}, ordered by descending absolute elasticity.
}
\description{
One-at-a-time sensitivity analysis: nudge each sector's
extension by a small relative step and measure the elasticity of
the total footprint. High-elasticity sectors are where data
quality matters most and where a result is most fragile.
}
\examples{
run_fn <- function(ext) tibble::tibble(value = sum(ext))
footprint_sensitivity(run_fn, extensions = c(60, 40))
}
