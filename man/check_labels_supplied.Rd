% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/absent_input.R
\name{check_labels_supplied}
\alias{check_labels_supplied}
\title{Refuse a value vocabulary that no longer contains the labels a filter needs}
\usage{
check_labels_supplied(
  data,
  column,
  labels,
  action = c("abort", "warn"),
  details = NULL
)
}
\arguments{
\item{data}{A tibble carrying the vocabulary to check.}

\item{column}{Name of the column the labels are drawn from. A column that is
not there at all is reported as the whole vocabulary being absent, even on
a table with no rows.}

\item{labels}{Labels that must each occur at least once. Compared as
character, so a numeric code may be given as a number.}

\item{action}{\code{"abort"} (default) to raise, \code{"warn"} to carry on.}

\item{details}{Extra \code{cli} bullets appended to the message.}
}
\value{
\code{data}, invisibly. Raises or warns with class \code{whep_absent_label}
(and \code{whep_absent_input}) when a label is missing; the condition carries
the missing labels in its \code{absent} field and what was seen instead in its
\code{observed} field.
}
\description{
Assert that each label a downstream \code{filter()} selects on actually occurs in
the column it selects from. A label that has been renamed upstream does not
raise: the filter matches no rows, the sum of no rows is zero, and the zero
is indistinguishable from a measurement.

This is the sibling of \code{\link[=check_inputs_supplied]{check_inputs_supplied()}} for the case where the
column is present and populated but its \strong{vocabulary} has moved. It is the
shape of whep#1016, where three FAOSTAT emission \code{Element} labels were lost
to a pin revision and roughly 108 Tg CH4/yr shipped as literal zero, and it
is invisible to any check written around \code{coalesce()}, \code{replace_na()} or
\code{na.rm} -- none of those appear anywhere near it.

The message names the labels that are \strong{present}, up to six of them, as
well as the ones that are missing. A rename is only obvious once the new
spelling is in front of the reader.

A table with \strong{no rows} carries no vocabulary and is passed, for the same
reason \code{\link[=check_inputs_supplied]{check_inputs_supplied()}} does not judge one: an empty table is an
absent table rather than a moved label, and the caller that produced it is
the one to answer for it. The case this is for -- whep#1016 -- is a pin of
2.5 million rows whose labels moved, not an empty one.
}
\examples{
landuse <- tibble::tibble(Element = c("Area", "Area"), Value = c(1, 2))
check_labels_supplied(landuse, "Element", "Area")

renamed <- tibble::tibble(Element = c("Area under cultivation"), Value = 1)
try(check_labels_supplied(renamed, "Element", "Area"))
}
