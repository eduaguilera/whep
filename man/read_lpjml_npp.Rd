% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpjml_npp.R
\name{read_lpjml_npp}
\alias{read_lpjml_npp}
\title{Read a per-PFT annual LPJmL carbon variable into a tidy tibble.}
\usage{
read_lpjml_npp(
  var = c("npp", "harvestc"),
  years = NULL,
  run_dir = NULL,
  first_year = 1901L,
  data = NULL,
  example = FALSE
)
}
\arguments{
\item{var}{Logical variable name, \code{"npp"} (per-PFT net primary production)
or \code{"harvestc"} (per-PFT harvested carbon).}

\item{years}{Optional integer vector of calendar years to keep. \code{NULL} keeps
every year present in the file.}

\item{run_dir}{Path to the LPJmL run output directory. Defaults to
\code{Sys.getenv("WHEP_LPJML_RUN_DIR")}.}

\item{first_year}{First calendar year of the run's annual time axis.}

\item{data}{Optional pre-read tibble (\code{lon}, \code{lat}, \code{year}, \code{npft},
\code{name_pft}, \code{value}) used in place of reading NetCDF, for testing.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with columns \code{lon}, \code{lat}, \code{year}, \code{npft} (band index),
\code{name_pft} (PFT name) and \code{value} (gC/m2/yr).
}
\description{
Reads one annual per-plant-functional-type (PFT) carbon output of a finished
LPJmL run, either net primary production (\code{"npp"}, from \code{pft_npp.nc}) or
harvested carbon (\code{"harvestc"}, from \code{pft_harvestc.nc}), and returns it in
tidy long form with the PFT name attached. Values are per-PFT-stand carbon
densities in grams of carbon per square metre per year. Requested years are
sliced at read time so the full cube is never materialised. The two files
index their PFT bands differently, so downstream code should join on
\code{name_pft}, never on the band position.
}
\examples{
read_lpjml_npp(example = TRUE)
}
