% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row_evidence.R
\name{row_evidence}
\alias{row_evidence}
\title{Produce row-level evidence for a table.}
\usage{
row_evidence(
  data,
  source_id,
  key,
  fields,
  source_version = NA_character_,
  recorded_at = Sys.time()
)
}
\arguments{
\item{data}{Table the evidence describes. Not modified. May have
zero rows, which yields a zero-row evidence table.}

\item{source_id}{Immutable identifier of the producer, one non-empty
string.}

\item{key}{Character vector of column names of \code{data} that jointly
identify a row. Must be unique-valued and free of \code{NA}, otherwise
a piece of evidence would address more than one row or none.}

\item{fields}{The row-level evidence to record, either a character
vector of column names of \code{data}, or a named list whose elements
are vectors of length 1 or \code{nrow(data)}. A list column cannot be
recorded.}

\item{source_version}{Version or vintage of the source, one string
or \code{NA}.}

\item{recorded_at}{Timestamp of the record, a length-one \code{POSIXct}.
Defaults to the current time; pass a fixed value for reproducible
output.}
}
\value{
A tibble of row evidence, as described above.
}
\description{
Record, as data, what each row of a result rests on: which producer
claimed it, at which version of that producer's source, and the
documented evidence fields the producer carries per row (a FAOSTAT
flag, a \verb{method_*} label, a gap-filling status). This is the
row-level counterpart of \code{\link[=record_provenance]{record_provenance()}}, which records the
code and input versions behind a whole dataset (whep#372).

The record is a \strong{separate table}, not an attribute. Attributes are
silently dropped by \code{dplyr} joins, filters and \code{summarise()}, so
attribute-borne evidence cannot survive the composition it is meant
to document. A keyed sidecar can: \code{\link[=evidence_for]{evidence_for()}} re-aligns it onto
a table after a join, \code{\link[=combine_row_evidence]{combine_row_evidence()}} merges the output of
several producers without letting one overwrite another, and
\code{\link[=evidence_conflicts]{evidence_conflicts()}} reports where two producers disagree.
}
\section{Evidence table}{

Format \code{"whep-row-evidence/1"}. One row per (table row × evidence
field), every column \code{character} so the table round-trips through
Parquet, CSV and YAML unchanged:
\itemize{
\item \code{row_key}: The row's identity, i.e. its key values joined by
\verb{U+001F} (unit separator). Opaque: build and read it with this
family's functions rather than by hand.
\item \code{key_columns}: The key column names, comma-separated, so a
consumer can re-derive \code{row_key} from the data alone.
\item \code{source_id}: The producer's immutable identifier. Callers should
use the package's existing dataset labels (\code{"FAOSTAT_prod"},
\code{"FAOSTAT_FBS_New"}, \code{"LUH2"}), because nothing downstream can
recover an identity that was renamed between builds.
\item \code{source_version}: Version or vintage of that source, \code{NA} when the
producer has none.
\item \code{recorded_at}: When the record was made, as ISO 8601 UTC.
\item \code{field}, \code{value}: The evidence field's name and its formatted
value for that row. \code{value} may be \code{NA}; the field name may not.
}

Rows are ordered by \code{row_key}, \code{field}, \code{source_id}, \code{recorded_at}
and \code{value}, in the C locale, so two runs over the same input give
byte-identical output apart from \code{recorded_at}.

The schema is available as data from \code{\link[=row_evidence_schema]{row_evidence_schema()}} and is
asserted with \code{\link[=assert_table_schema]{assert_table_schema()}}. It declares no key: combining
two runs of the same producer legitimately repeats a claim, so
duplication is not an error. Disagreement is, and that is what
\code{\link[=evidence_conflicts]{evidence_conflicts()}} finds.
}

\examples{
cbs <- tibble::tibble(
  area_code = c(724L, 724L, 76L),
  item_cbs_code = c(2511L, 2513L, 2511L),
  year = 2020L,
  value = c(1.5, 2.5, 3.5),
  fao_flag = c("A", "E", "A")
)

# Evidence the table already carries per row, plus one derived field.
evidence <- row_evidence(
  cbs,
  source_id = "FAOSTAT_FBS_New",
  key = c("area_code", "item_cbs_code", "year"),
  fields = list(
    fao_flag = cbs$fao_flag,
    imputed = cbs$fao_flag == "E"
  ),
  source_version = "2024-03-14",
  recorded_at = as.POSIXct("2026-01-01", tz = "UTC")
)
evidence

# It is a documented schema, so it can be proved rather than trusted.
assert_table_schema(evidence, row_evidence_schema())

# And it round-trips: every column is character.
path <- tempfile(fileext = ".parquet")
write_table_checked(evidence, path)
identical(tibble::as_tibble(nanoparquet::read_parquet(path)), evidence)
unlink(path)
}
\seealso{
\code{\link[=combine_row_evidence]{combine_row_evidence()}} to merge producers,
\code{\link[=evidence_for]{evidence_for()}} to carry evidence through a join,
\code{\link[=evidence_conflicts]{evidence_conflicts()}} to find disagreement, and
\code{\link[=record_provenance]{record_provenance()}} for the dataset-level record.
}
