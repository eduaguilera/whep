% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grassland_land_extension.R
\name{build_grassland_land_extension}
\alias{build_grassland_land_extension}
\title{Build the native grassland land extension.}
\usage{
build_grassland_land_extension(
  source = c("luh2", "faostat_pasture"),
  grassland_metric = c("occupation", "active_grazing"),
  usable_grass_yield_dm_t_ha = 2.06,
  data = list(),
  example = FALSE
)
}
\arguments{
\item{source}{Grassland area source, \code{"luh2"} (default) or
\code{"faostat_pasture"}.}

\item{grassland_metric}{Grassland land metric, \code{"occupation"} (default) or
\code{"active_grazing"}.}

\item{usable_grass_yield_dm_t_ha}{Usable grass yield in dry-matter tonnes per
hectare, used only by \code{"active_grazing"}. Defaults to \code{2.06}.}

\item{data}{Optional named list of pre-loaded inputs to avoid remote reads:
\code{primary_prod} (for \code{source = "luh2"}), \code{landuse} (the \code{faostat-landuse}
pin, for \code{source = "faostat_pasture"}) and \code{feed_intake} (for
\code{grassland_metric = "active_grazing"}). Each falls back to its reader
(\code{\link[=get_primary_production]{get_primary_production()}}, \code{\link[=whep_read_file]{whep_read_file()}}, \code{\link[=get_feed_intake]{get_feed_intake()}}) when
absent.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (grassland area in hectares) and \code{method_grassland} (the chosen
metric), plus the polity columns below.
}
\description{
Produce a grassland land extension keyed by \verb{(year, area_code, item_cbs_code)}, replacing the grassland rows that used to come from the
external \code{land_fp} pin.

Two area sources are available, selected with \code{source}:
\itemize{
\item \code{"luh2"} (default): permanent and temporary grassland area (item_cbs 3000
and 3002, LUH2 pasture and rangeland) taken from
\code{\link[=build_primary_production]{build_primary_production()}}. This shares the gridded LUH2 land-use basis
used by the crop land extensions and by livestock spatialisation.
Rotational fallow (item_cbs 3003) is excluded because the
\code{cropgrids_fallow} crop extension already attributes fallow to crops, so
counting it here too would double count it.
\item \code{"faostat_pasture"}: FAOSTAT "Permanent meadows and pastures" area (Land
Use item 6655), the statistics-based basis comparable to most published
footprint studies.
}

Two metrics are available, selected with \code{grassland_metric}:
\itemize{
\item \code{"occupation"} (default): the full grassland area is charged as occupied
land.
\item \code{"active_grazing"}: grassland is capped at the area implied by actual
grazing intake (the \code{"grass"} feed in \code{\link[=get_feed_intake]{get_feed_intake()}}) divided by a
usable grass yield, so ungrazed or marginal rangeland is not charged.
}
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_grassland_land_extension(example = TRUE)
}
