% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grassland_land_extension.R
\name{build_grassland_land_extension}
\alias{build_grassland_land_extension}
\title{Build the native grassland land extension.}
\usage{
build_grassland_land_extension(
  source = c("luh2", "faostat_pasture"),
  grassland_metric = c("occupation", "active_grazing"),
  usable_grass_yield_dm_t_ha = 2.06,
  data = list(),
  example = FALSE
)
}
\arguments{
\item{source}{Grassland area source, \code{"luh2"} (default) or
\code{"faostat_pasture"}.}

\item{grassland_metric}{Grassland land metric, \code{"occupation"} (default) or
\code{"active_grazing"}.}

\item{usable_grass_yield_dm_t_ha}{Usable grass yield in dry-matter tonnes per
hectare, used only by \code{"active_grazing"}. Defaults to \code{2.06}.}

\item{data}{Optional named list of pre-loaded inputs to avoid remote reads:
\code{primary_prod} (for \code{source = "luh2"}) and \code{feed_intake} (for
\code{grassland_metric = "active_grazing"}). Each falls back to its reader
(\code{\link[=get_primary_production]{get_primary_production()}}, \code{\link[=get_feed_intake]{get_feed_intake()}}) when absent.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (grassland area in hectares) and \code{method_grassland} (the chosen
metric).
}
\description{
Produce a grassland land extension keyed by \verb{(year, area_code, item_cbs_code)}, replacing the grassland rows that used to come from the
external \code{land_fp} pin.

Two area sources are available, selected with \code{source}:
\itemize{
\item \code{"luh2"} (default): permanent grassland area (item_cbs 3000, LUH2 pasture
and rangeland) taken from \code{\link[=build_primary_production]{build_primary_production()}}. This shares the
gridded LUH2 land-use basis used by the crop land extensions and by
livestock spatialisation. Rotational fallow (item_cbs 3003) is excluded
because the \code{cropgrids_fallow} crop extension already attributes fallow to
crops, so counting it here too would double count it.
\item \code{"faostat_pasture"}: FAOSTAT "Permanent meadows and pastures" area (Land
Use item 6655), the statistics-based basis comparable to most published
footprint studies.
}

Two metrics are available, selected with \code{grassland_metric}:
\itemize{
\item \code{"occupation"} (default): the full grassland area is charged as occupied
land.
\item \code{"active_grazing"}: grassland is capped at the area implied by actual
grazing intake (the \code{"grass"} feed in \code{\link[=get_feed_intake]{get_feed_intake()}}) divided by a
usable grass yield, so ungrazed or marginal rangeland is not charged.
}
}
\examples{
build_grassland_land_extension(example = TRUE)
}
