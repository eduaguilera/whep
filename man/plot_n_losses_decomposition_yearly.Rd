% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{plot_n_losses_decomposition_yearly}
\alias{plot_n_losses_decomposition_yearly}
\title{Plot year-on-year (non-cumulative) drivers of the change in
territorial N losses}
\usage{
plot_n_losses_decomposition_yearly(decomp = NULL)
}
\arguments{
\item{decomp}{A named list from \code{\link[=decompose_territorial_n_losses]{decompose_territorial_n_losses()}}.
If \code{NULL}, computed automatically (slow).}
}
\value{
A named list with ggplot objects \code{by_compartment} and
\code{by_mechanism}.
}
\description{
Uses the same data as \code{\link[=plot_n_losses_decomposition]{plot_n_losses_decomposition()}}, but plots each
year's own additive contribution directly, without accumulating it
over time: each bar shows how much a compartment or mechanism
contributed to the change in territorial N losses in that one
year-on-year transition, not the running total since 1860.
}
\examples{
# plot_n_losses_decomposition_yearly()
}
