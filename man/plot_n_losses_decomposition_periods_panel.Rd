% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{plot_n_losses_decomposition_periods_panel}
\alias{plot_n_losses_decomposition_periods_panel}
\title{Plot period-based drivers of territorial N losses, as one combined
panel plot}
\usage{
plot_n_losses_decomposition_periods_panel(decomp = NULL)
}
\arguments{
\item{decomp}{A named list from
\code{\link[=decompose_territorial_n_losses_periods]{decompose_territorial_n_losses_periods()}}. If \code{NULL}, computed
automatically (slow).}
}
\value{
A patchwork ggplot object with two panels ("By compartment",
"By mechanism").
}
\description{
Combines the two views from \code{\link[=plot_n_losses_decomposition_periods]{plot_n_losses_decomposition_periods()}}
(by compartment and by mechanism) side by side into a single
patchwork plot with one shared y-axis label, since both break down
contributions to the same total territorial N losses. Each panel
keeps its own legend (compartment and mechanism are different fill
scales, so the legends aren't collected into one).
}
\examples{
# plot_n_losses_decomposition_periods_panel()
}
