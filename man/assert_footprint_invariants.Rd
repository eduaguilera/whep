% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conservation.R
\name{assert_footprint_invariants}
\alias{assert_footprint_invariants}
\title{Assert that footprint conservation invariants hold.}
\usage{
assert_footprint_invariants(
  footprint,
  extensions,
  labels,
  x_vec,
  max_rel_loss = 0.05
)
}
\arguments{
\item{footprint}{Footprint tibble from \code{\link[=compute_footprint]{compute_footprint()}},
with \code{origin_area}, \code{origin_item} and \code{value} columns.}

\item{extensions}{Numeric vector of environmental extensions per
sector, as passed to \code{\link[=compute_footprint]{compute_footprint()}}.}

\item{labels}{Tibble with \code{area_code} and \code{item_cbs_code}
mapping each sector to its meaning, as passed to
\code{\link[=compute_footprint]{compute_footprint()}}.}

\item{x_vec}{Numeric vector of total output per sector.}

\item{max_rel_loss}{Maximum tolerated global relative
under-tracing (pressure that never reaches final demand). The
engine's negative-zeroing and column capping cause a small
amount, so this is non-zero by default.}
}
\value{
Invisibly, the one-row summary from
\code{\link[=summarise_conservation]{summarise_conservation()}}. Called for its side effect of
aborting on violation.
}
\description{
Build-time gate over a footprint result, intended for use in a
pipeline or regression test. Aborts when the footprint
over-traces any origin sector (embodied pressure exceeds the
source, which should never happen) or when the global share of
untraced pressure exceeds \code{max_rel_loss}. A regression that
silently loses or inflates pressure then fails loudly instead of
shipping.
}
\examples{
z_mat <- matrix(c(0, 5, 10, 0), nrow = 2)
x_vec <- c(100, 200)
y_mat <- matrix(c(85, 195), ncol = 1)
extensions <- c(50, 30)
labels <- tibble::tibble(
  area_code = c(1L, 1L),
  item_cbs_code = c(1L, 2L)
)
fp <- compute_footprint(
  x_vec = x_vec, y_mat = y_mat, extensions = extensions,
  labels = labels, z_mat = z_mat
)
assert_footprint_invariants(fp, extensions, labels, x_vec)
}
