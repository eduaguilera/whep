% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpjml_natural_cover.R
\name{read_lpjml_natural_cover}
\alias{read_lpjml_natural_cover}
\title{Read natural-land soil cover from an LPJmL run.}
\source{
Schaphoff, S. et al. (2018). LPJmL4 - a dynamic global vegetation
model with managed land - Part 1: Model description. \emph{Geoscientific Model
Development}, 11, 1343-1375. \doi{10.5194/gmd-11-1343-2018}.
}
\usage{
read_lpjml_natural_cover(
  run_dir = NULL,
  years = NULL,
  first_year = NULL,
  example = FALSE
)
}
\arguments{
\item{run_dir}{Path to the LPJmL run output directory. \code{NULL} (default)
uses \code{WHEP_LPJML_RUN_DIR}.}

\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
(default) keeps every year the file covers.}

\item{first_year}{Calendar year of the file's first time step. \code{NULL}
(default) reads it from the file's own \code{time} axis.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading a run.
Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{lon}, \code{lat}, \code{year}, \code{natural_stand_frac} and
\code{natural_cover} (both fractions in 0-1).
}
\description{
Per-cell, per-year vegetated cover of the natural stand, from LPJmL's
foliar projective cover (\code{fpc.nc}). Band 1 of that file is the natural
stand FRACTION -- how much of the cell is natural -- and bands 2 to 15 are
the fourteen natural PFTs' projective cover WITHIN that stand. The cover of
natural land is therefore the sum of bands 2 to 15, capped at 1, and never
band 1, which is an area share and a different quantity.

Supply the result as \code{data$natural_cover} to \code{\link[=build_carbon_balance]{build_carbon_balance()}} to
replace \link{soc_soil_cover_curve}'s constant 0.85 for the natural class.
}
\section{Why it matters where it does}{

The constant is close on the global mean -- measured cover runs 0.858 in
1901 to 0.884 in 2023 -- and wrong in the distribution: the median natural
cell is fully covered at 1.000 while the 5th percentile is bare at roughly
zero. The RothC plant-retainment term is \code{0.6 + 0.4 * (1 - cover)}, so a
near-bare cell moves from 0.66 to 1.00, decomposes half again as fast, and
loses about a third of its equilibrium carbon. Those arid cells are exactly
where the model sits furthest from observation. The effect on the global
mean is about 1.8\%; per cell it spans 0.66 to 1.10 times.

Managed grassland has no FPC band -- \code{fpc.nc} covers the natural stand
only -- so it necessarily stays on the curve.
}

\examples{
read_lpjml_natural_cover(example = TRUE)
}
