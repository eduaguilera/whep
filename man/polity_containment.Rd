% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_mappings.R
\docType{data}
\name{polity_containment}
\alias{polity_containment}
\title{Polity containment edges}
\format{
A tibble with one row per containment edge. Columns:
\itemize{
\item \code{member_code}: \code{polity_code} of the contained polity.
\item \code{container_code}: \code{polity_code} of the containing polity.
\item \code{start_year}, \code{end_year}: Validity interval of the edge itself, on the
same convention as \link{polities}: \code{start_year} is inclusive and \code{end_year}
is exclusive at a succession (a later edge for the same member takes
over from \code{end_year}) and inclusive at the open end (an edge nothing
succeeds still covers its own terminal year). The interval belongs to
the edge, not to either endpoint polity: \code{JPN-AICHI-1871-2025} is one
member polity spanning 1871-2025, but four separate edges route it
through \code{JPN-1800-1895}, \code{JPN-1895-1945}, \code{JPN-1945-1952} and
\code{JPN-1952-2025} as Japan's own periodization changes underneath it.
\item \code{basis}: Free-text description of what the containment relationship
represents (a historical territory, a reporting convention, ...).
}
}
\source{
\verb{~/whep-polities/data/final/polity_containment.csv}.
}
\usage{
polity_containment
}
\description{
Published member-container containment edges between periodized WHEP
\link{polities}: which admin/subnational polity sits inside which container
polity, and for what years. This is the piece \link{polity_area_crosswalk} and
\link{polity_label_aliases} do not carry -- both map a \emph{reporting area} or a
\emph{label} to a polity, while this table relates polities to each other. Any
WHEP admin unit (a province, a prefecture, ...) is already a
\code{polity_type == "subnational"} row of \link{polities}; the containment edge is
the missing link between it and the polity it belongs to
(whep-polities#649, closing whep#51).
}
\details{
The map is authored and gated in \code{whep-polities}; this package embeds a
copy rather than deciding containment itself, on the same grounds as
\link{polity_label_aliases} and \link{polity_area_crosswalk}.
}
\keyword{datasets}
