% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_stats_sidra.R
\name{read_admin_stats_sidra}
\alias{read_admin_stats_sidra}
\title{Read Brazilian state agricultural statistics from IBGE SIDRA}
\source{
IBGE SIDRA API (\url{https://apisidra.ibge.gov.br}), tables 5457
(Producao Agricola Municipal), 3939 and 94 (Pesquisa da Pecuaria
Municipal). Unauthenticated; verified 2026-09-02.
}
\usage{
read_admin_stats_sidra(
  table = c("5457", "3939", "94"),
  years = NULL,
  items = NULL,
  years_per_request = NULL,
  example = FALSE
)
}
\arguments{
\item{table}{SIDRA table to read, one of \code{"5457"} (PAM crops), \code{"3939"}
(PPM herds) or \code{"94"} (milked cows).}

\item{years}{Integer vector of calendar years. \code{NULL} (the default)
requests 1974 -- the first year all three tables publish -- through the
current year; years the table has not published are absent from the
result rather than an error.}

\item{items}{Optional character vector of classification category codes to
restrict the request to (\code{c782} codes for table \code{"5457"}, \code{c79} codes for
\code{"3939"}). \code{NULL} requests every category and drops the \code{"0"} ("Total")
category, which is the all-crops total rather than an item. Must be
\code{NULL} for table \code{"94"}, which has no classification.}

\item{years_per_request}{Number of years per API request. \code{NULL} uses the
per-table default that keeps a request under the 50,000-value cap.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading from
the API. Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per unit, item, indicator and year:
\itemize{
\item \code{source}: \code{"IBGE_PAM"} for table 5457, \code{"IBGE_PPM"} for 3939 and 94.
\item \code{source_native_unit_id}, \code{source_native_unit_name}: the IBGE UF code and
name exactly as served.
\item \code{source_native_item_code}, \code{source_native_item_name}: the classification
category exactly as served; the code is \code{NA} for table 94, whose item is
the table itself and whose name is the served variable name.
\item \code{indicator_used}: \code{"area_harvested"}, \code{"area_planted_or_sown"} or
\code{"production"} for crop rows, \code{NA} for head counts.
\item \code{quantity}: \code{"area"}, \code{"production"} or \code{"heads"}.
\item \code{year}: calendar year.
\item \code{value}: the served value in WHEP units, \code{NA} where the source gave a
missing-value code.
\item \code{value_unit}: \code{"ha"}, \code{"tonnes"} or \code{"heads"}.
\item \code{value_flag}: the source's missing-value code verbatim (\code{"..."}, \code{".."}
or \code{"X"}), \code{NA} when the row carries a value. An absolute zero (\code{"-"})
is a value, not a flag.
\item \code{grain}: always \code{"admin1"}; SIDRA level N3 is the state.
\item \code{nuts_version}: always \code{NA}, Brazil having no NUTS geography.
\item \code{source_version}: always \code{NA}; SIDRA exposes no vintage stamp.
\item \code{recorded_at}: ISO 8601 UTC fetch time of the request the row came from.
}
}
\description{
Read one IBGE SIDRA table of Brazilian state-level (\code{admin1}) agricultural
statistics through the unauthenticated SIDRA values API and return it in
the shared admin-statistics reader shape: one row per source-native unit,
item, indicator and year, in WHEP units.

Three tables are served, all annual from 1974:
\itemize{
\item \code{"5457"} -- Producao Agricola Municipal (PAM) crop areas and production.
Harvested area (\code{indicator_used == "area_harvested"}) is the indicator
the allocation binds on; planted-or-sown area
(\code{"area_planted_or_sown"}) is a flagged proxy SIDRA publishes only from
1988, and production (\code{"production"}) is the last-resort indicator.
\item \code{"3939"} -- Pesquisa da Pecuaria Municipal (PPM) herd inventories, in
head. Livestock rows carry \code{quantity == "heads"} and
\code{indicator_used == NA}, head counts not being one of the
area/production/yield indicators \code{\link[=admin_shares_schema]{admin_shares_schema()}} closes over.
\item \code{"94"} -- milked cows, in head, the dairy split PPM table 3939 does not
report.
}

Rows are \strong{source-native}: unit and item identifiers are returned exactly
as SIDRA served them and are never mapped to \code{item_prod_code} or resolved
to a polity here. Values are returned in WHEP units with a conversion
factor of 1 throughout, SIDRA serving hectares, tonnes and head directly.

Requests are paged by year block so no single query exceeds the API's
50,000-value cap; see \code{years_per_request}.
}
\examples{
read_admin_stats_sidra(example = TRUE)
}
