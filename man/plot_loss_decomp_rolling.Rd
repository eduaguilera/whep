% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{plot_loss_decomp_rolling}
\alias{plot_loss_decomp_rolling}
\title{Plot rolling-mean year-on-year drivers of the change in territorial N
losses}
\usage{
plot_loss_decomp_rolling(decomp = NULL, window = 10)
}
\arguments{
\item{decomp}{A named list from \code{\link[=decompose_terr_losses]{decompose_terr_losses()}}.
If \code{NULL}, computed automatically (slow).}

\item{window}{Width of the centered rolling-mean window, in years.
Default \code{10}.}
}
\value{
A named list with ggplot objects \code{by_compartment} and
\code{by_mechanism}.
}
\description{
Same data as \code{\link[=plot_loss_decomp_yearly]{plot_loss_decomp_yearly()}}, but smooths each
year's own additive contribution with a centered rolling mean
(\code{window} years wide, \code{NA}-padded at the edges) before plotting, to
make sustained multi-year trends (e.g. a period of continuously
improving efficiency) easier to see than in the raw, noisy
year-on-year series.
}
\examples{
# `window` must not exceed the number of years available per group.
decomp <- list(
  by_compartment = tibble::tribble(
    ~t0, ~compartment, ~contribution_mgn, ~cumulative_mgn,
    1861, "cropland", 12000, 12000,
    1862, "cropland", 8000, 20000,
    1863, "cropland", 9000, 29000
  ),
  by_mechanism = tibble::tribble(
    ~t0, ~mechanism, ~contribution_mgn, ~cumulative_mgn,
    1861, "Size", 9000, 9000,
    1862, "Size", 5000, 14000,
    1863, "Size", 6000, 20000
  )
)
plots <- plot_loss_decomp_rolling(decomp, window = 3)
}
