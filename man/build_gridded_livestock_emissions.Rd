% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gridded_livestock_emissions.R
\name{build_gridded_livestock_emissions}
\alias{build_gridded_livestock_emissions}
\title{Build per-cell livestock greenhouse-gas emissions.}
\usage{
build_gridded_livestock_emissions(
  gridded_livestock = NULL,
  method_diet = c("per_cell_feed", "national_feed", "uniform_medium"),
  tier = 2,
  data = list(),
  example = FALSE
)
}
\arguments{
\item{gridded_livestock}{A tibble of gridded head counts (required unless
\code{example = TRUE}), for example from
\code{\link[=build_gridded_livestock]{build_gridded_livestock()}}, with columns \code{lon}, \code{lat}, \code{year},
\code{area_code}, \code{heads}, and either \code{species} (an IPCC species label such as
\code{"Cattle, dairy"}) or \code{species_group} (a spatializer group label). Groups
that name more than one IPCC species (\code{"sheep_goats"}, \code{"equines"},
\code{"poultry"}, \code{"other"}) abort rather than being split on an assumption.
Polity columns and cell identifiers are preserved when present.}

\item{method_diet}{How each row's \code{diet_quality} is resolved, in decreasing
rigour:
\itemize{
\item \code{"per_cell_feed"} (default): from the cell's own feed mix, falling back
per row to that country's national mix where a cell has no classifiable
feed. Needs \code{data$feed_intake} at cell grain (a \code{sub_territory} column),
for example from \code{\link[=build_feed_intake_local]{build_feed_intake_local()}}.
\item \code{"national_feed"}: from the country's feed mix. Needs
\code{data$feed_intake}, for example from \code{\link[=get_feed_intake]{get_feed_intake()}}.
\item \code{"uniform_medium"}: every row gets the IPCC \code{"Medium"} diet. This is an
assumption, not a measurement, and is never selected implicitly.
}

Whatever is requested, the value actually used is recorded per row in
\code{method_diet}. A row that no requested method resolves aborts.}

\item{tier}{IPCC tier, \code{2} (default) or \code{1}. Tier 2 is the default here
because the per-cell drivers only enter the Tier 2 energy and
manure-management equations; Tier 1 emission factors carry no climate,
temperature or diet dimension, so a Tier 1 grid differs from a
disaggregated national total only by rounding.}

\item{data}{Optional named list of pre-loaded inputs: \code{cell_climate} (a
\code{\link[=build_cell_climate_zone]{build_cell_climate_zone()}} output) and \code{feed_intake} (a feed-intake
table). \code{cell_climate} falls back to \code{\link[=build_cell_climate_zone]{build_cell_climate_zone()}}, which
reads CRU from \code{WHEP_CRU_DIR}. \code{feed_intake} has no fallback: the readers
that produce it rebuild the whole feed allocation, so it is supplied or
the diet method is \code{"uniform_medium"}.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per \code{year}, \code{area_code}, \code{lon}, \code{lat} and
\code{species}:
\itemize{
\item \code{heads}: Head count in the cell.
\item \code{enteric_ch4_kt}, \code{manure_ch4_kt}, \code{manure_n2o_kt}: Gridded emissions
(kilotonnes), the primary output.
\item \code{enteric_ch4_national_kt}, \code{manure_ch4_national_kt},
\code{manure_n2o_national_kt}: The national-grain estimate rescaled onto the
cell, so summing these over a country reproduces the national-grain run.
\item \code{divergence_enteric_ch4}, \code{divergence_manure_ch4},
\code{divergence_manure_n2o}: Per-country ratio of the gridded total to the
national-grain total. \code{1} means the two grains agree.
\item \code{mean_annual_temp_c}, \code{climate_zone}, \code{diet_quality}: The resolved
per-cell drivers.
\item \code{method_climate_zone}, \code{method_diet}, \code{method_enteric},
\code{method_manure_ch4}, \code{method_manure_n2o}: Method tracking.
}

plus the polity columns below.
}
\description{
Run the IPCC 2019 livestock emission model on a gridded herd, one 0.5-degree
cell at a time, and return enteric CH4, manure CH4 and manure N2O in
kilotonnes per cell, year and species.

Unlike a spatial disaggregation of a national emission total, this resolves
the drivers that actually vary within a country: the manure-management
climate zone and the ambient temperature come from the cell
(\code{\link[=build_cell_climate_zone]{build_cell_climate_zone()}}), and the diet quality comes from the cell's own
feed mix when one is available. A national total spread over cells cannot
show any of that, because every cell of a country then carries the same
implied climate and diet.

\strong{Both grains are reported.} The gridded sum is the primary output. The
same model is also run once per country on the head-weighted national mean
temperature and the national diet, and each cell additionally carries that
national estimate rescaled onto it (\verb{*_national_kt}) plus the per-country
ratio between the two (\verb{divergence_*}). The global difference between the
two grains is small while the per-country difference is not, so the
per-country ratio is emitted per row rather than summarised away.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning. Where that polity is not live in the
row's own year -- 41.5\% of the pre-1961 \verb{(area, year)} cells --
\code{\link[=add_polity_code]{add_polity_code()}} says so as \code{mapping_status == "backcast_anchor"}, and
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports it as \code{gap_kind == "backcast_anchor"}.
These columns do not say so either way.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_gridded_livestock_emissions(example = TRUE)
}
