% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gridded_livestock_emissions.R
\name{build_gridded_livestock_emissions}
\alias{build_gridded_livestock_emissions}
\title{Build per-cell livestock greenhouse-gas emissions.}
\usage{
build_gridded_livestock_emissions(
  gridded_livestock = NULL,
  method_diet = c("per_cell_feed", "national_feed", "uniform_medium"),
  method_species = c("national_head_share", "refuse"),
  tier = 2,
  options = list(),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{gridded_livestock}{A tibble of gridded head counts (required unless
\code{example = TRUE}), for example from
\code{\link[=build_gridded_livestock]{build_gridded_livestock()}}, with columns \code{lon}, \code{lat}, \code{year},
\code{area_code}, \code{heads}, and either \code{species} (an IPCC species label such as
\code{"Cattle, dairy"}) or \code{species_group} (a spatializer group label). Groups
that name more than one IPCC species (\code{"sheep_goats"}, \code{"equines"},
\code{"poultry"}, \code{"other"}) are split as \code{method_species} says. Polity
columns and cell identifiers are preserved when present.}

\item{method_diet}{How each row's \code{diet_quality} is resolved, in decreasing
rigour:
\itemize{
\item \code{"per_cell_feed"} (default): from the cell's own feed mix, falling back
per row to that country's national mix where a cell has no classifiable
feed. Needs \code{data$feed_intake} at cell grain (a \code{sub_territory} column),
for example from \code{\link[=build_feed_intake_local]{build_feed_intake_local()}}.
\item \code{"national_feed"}: from the country's feed mix. Needs
\code{data$feed_intake}, for example from \code{\link[=get_feed_intake]{get_feed_intake()}}.
\item \code{"uniform_medium"}: every row gets the IPCC \code{"Medium"} diet. This is an
assumption, not a measurement, and is never selected implicitly.
}

Whatever is requested, the value actually used is recorded per row in
\code{method_diet}. A row that no requested method resolves aborts.}

\item{method_species}{How a \code{species_group} that names more than one IPCC
species is split into its species:
\itemize{
\item \code{"national_head_share"} (default): each cell's group head count is
divided among the group's members in the proportions the country itself
reports, from the national head counts by live-animal item
(\code{data$species_heads}). The members are read from the same
\code{livestock_mapping.csv} the spatializer summed them with, so the split
inverts that grouping at national level: a country's gridded species
mix equals its reported one, and the group's gridded head total is
conserved exactly. It assumes the species mix is the
same in every cell of a country, which is also what the spatializer
assumed, because it gave all members of a group one spatial proxy.
Countries are matched on \code{area_code}; a cell whose \code{area_code} the head
table does not carry is matched on \code{polity_area_code} instead (for
example Sudan and South Sudan, reported together as polity 206), and
\code{method_species} says which. A cell matched by neither is returned with
\code{NA} emissions and a warning, never split on an invented ratio.
\item \code{"refuse"}: abort on any aggregate group, the behaviour before whep#1126.
}

The rung used is recorded per row in \code{method_species}.}

\item{tier}{IPCC tier, \code{2} (default) or \code{1}. Tier 2 is the default here
because the per-cell drivers only enter the Tier 2 energy and
manure-management equations; Tier 1 emission factors carry no climate,
temperature or diet dimension, so a Tier 1 grid differs from a
disaggregated national total only by rounding.}

\item{options}{A named list of manure-engine options. All but two defaults
reproduce the behaviour in force before whep#949. The exceptions are
\code{mcf_source}, which moved from the shipped table to the 2019 Refinement
in whep#1022 and does move Tier 2 manure CH4, and \code{mms_shares}, which
moved from the unsourced placeholder table to the GLEAM 2.0 ingest in
whep#958 and does move both tiers' manure N2O.

\code{mms_shares} selects which half of \link{regional_mms_distribution} the
split is read from: \code{"gleam_2_0"} (default) is the GLEAM 2.0 Supplement
S1 Tab. 4.2-4.11 ingest, \code{"placeholder"} the unsourced table it replaced
in whep#958. The placeholder stays selectable so the values WHEP
published before that ingest remain reproducible and the sensitivity to
it stays measurable; it is not a defensible alternative estimate.

\code{mms_region} selects how the manure-management split in
\link{regional_mms_distribution} is keyed:
\itemize{
\item \code{"as_available"} (default): a row uses its own region when the frame
already carries a \code{region} column, and the \code{region == "Global"} split
otherwise. Tier 1 resolves a region for the (sourced) per-head
N-excretion table and so takes the region-specific split; Tier 2 carries
no region and so takes the Global one.
\item \code{"resolve"}: the IPCC region is resolved from \code{iso3}, \code{area_code} or
\code{polity_area_code} where it is missing, which makes the table's
region-specific rows live on the Tier 2 path too. Opt-in because it
changes which rows of the table apply, not because the rows are
doubtful: since whep#958 they are the GLEAM 2.0 ingest.
\item \code{"global"}: every row takes the \code{region == "Global"} split, whatever
region column it carries.
}

\code{mcf_source} selects which methane conversion factor table the Tier 2
manure CH4 weighting reads:
\itemize{
\item \code{"ipcc_2019"} (default): the matching \code{edition} rows of
\link{climate_mcf_ipcc}, read off Table 10.17 (Updated) of the 2019
Refinement, which is the current IPCC guidance.
\item \code{"ipcc_2006"}: the matching \code{edition} rows of \link{climate_mcf_ipcc}, read
off Table 10.17 of the 2006 Guidelines.
\item \code{"as_shipped"}: \link{climate_mcf}, whose live rows are predominantly the
2006 Guidelines Table 10.17 but with six cells that match no published
IPCC value (whep#601, whep#1022). Kept selectable so an older run can
be reproduced; it is no longer the default, because values whose
provenance could not be established should not be what ships.
}

Neither edition publishes one number per Cool/Temperate/Warm zone for
every system, so both as-published tables apply a stated collapse rule;
see \link{climate_mcf_ipcc}. Both rules are WHEP's, not the IPCC's, and the
default makes them live. \code{method_manure_ch4} records the table used.

The default carries one known incompleteness: the Refinement pairs its
single 0.47 percent pasture MCF with a mandatory \code{Bo} of 0.19, and this
engine applies one per-species \code{Bo} to every stream, so the pair cannot be
honoured here. See the corresponding section of \link{climate_mcf_ipcc}.

\code{climate_source} selects where the climate zone the methane conversion
factors in the MCF table are read at comes from. A \code{climate_zone} a row
already carries is always used and stamped \code{climate_from_data}; the option
governs only the rows left without one, whether that is a hole in a
supplied column or a wholly absent column.
\itemize{
\item \code{"assumed"} (default): fill with \code{assumed_climate_zone}.
\item \code{"from_data"}: abort instead of assuming.
}

\code{assumed_climate_zone} is the zone \code{"assumed"} fills in: \code{"Cool"},
\code{"Temperate"} (default) or \code{"Warm"}. It is an assumption, not a
measurement; \code{method_manure_ch4} records per row which of the sources
applied, and this argument exists so the sensitivity to the assumption can
be measured (whep#949).}

\item{data}{Optional named list of pre-loaded inputs: \code{cell_climate} (a
\code{\link[=build_cell_climate_zone]{build_cell_climate_zone()}} output), \code{feed_intake} (a feed-intake table)
and \code{species_heads} (national head counts by live-animal item, with
\code{year}, \code{area_code}, \code{item_cbs_code} and \code{value} or \code{heads}, optionally
\code{polity_area_code} and \code{unit}; rows with a \code{unit} other than \code{"heads"}
are ignored). \code{cell_climate} falls back to \code{\link[=build_cell_climate_zone]{build_cell_climate_zone()}},
which reads CRU from \code{WHEP_CRU_DIR}. \code{species_heads} falls back to
\code{\link[=get_primary_production]{get_primary_production()}}, the source the spatializer's country table
is built from, and is read only when an aggregate group is present.
\code{feed_intake} has no fallback: the readers that produce it rebuild the
whole feed allocation, so it is supplied or the diet method is
\code{"uniform_medium"}.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per \code{year}, \code{area_code}, \code{lon}, \code{lat} and
\code{species}:
\itemize{
\item \code{heads}: Head count in the cell.
\item \code{enteric_ch4_kt}, \code{manure_ch4_kt}, \code{manure_n2o_kt}: Gridded emissions
(kilotonnes), the primary output.
\item \code{enteric_ch4_national_kt}, \code{manure_ch4_national_kt},
\code{manure_n2o_national_kt}: The national-grain estimate rescaled onto the
cell, so summing these over a country reproduces the national-grain run.
\item \code{divergence_enteric_ch4}, \code{divergence_manure_ch4},
\code{divergence_manure_n2o}: Per-country ratio of the gridded total to the
national-grain total. \code{1} means the two grains agree.
\item \code{mean_annual_temp_c}, \code{climate_zone}, \code{diet_quality}: The resolved
per-cell drivers.
\item \code{species_group}: The spatializer group, when the input carried one.
\item \code{method_species}: How the row's \code{species} was resolved: \code{"supplied"},
\code{"one_to_one"} (the group is a single species), \code{"national_head_share"},
\code{"polity_bucket_head_share"} or \code{"unsplit_no_national_mix"} (emissions
\code{NA}, see \code{method_species}).
\item \code{method_climate_zone}, \code{method_diet}, \code{method_enteric},
\code{method_manure_ch4}, \code{method_manure_n2o}: Method tracking.
}

plus the polity columns below.
}
\description{
Run the IPCC 2019 livestock emission model on a gridded herd, one 0.5-degree
cell at a time, and return enteric CH4, manure CH4 and manure N2O in
kilotonnes per cell, year and species.

Unlike a spatial disaggregation of a national emission total, this resolves
the drivers that actually vary within a country: the manure-management
climate zone and the ambient temperature come from the cell
(\code{\link[=build_cell_climate_zone]{build_cell_climate_zone()}}), and the diet quality comes from the cell's own
feed mix when one is available. A national total spread over cells cannot
show any of that, because every cell of a country then carries the same
implied climate and diet.

\strong{Both grains are reported.} The gridded sum is the primary output. The
same model is also run once per country on the head-weighted national mean
temperature and the national diet, and each cell additionally carries that
national estimate rescaled onto it (\verb{*_national_kt}) plus the per-country
ratio between the two (\verb{divergence_*}). The global difference between the
two grains is small while the per-country difference is not, so the
per-country ratio is emitted per row rather than summarised away.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning. Where that polity is not live in the
row's own year -- 41.5\% of the pre-1961 \verb{(area, year)} cells --
\code{\link[=add_polity_code]{add_polity_code()}} says so as \code{mapping_status == "backcast_anchor"}, and
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports it as \code{gap_kind == "backcast_anchor"}.
These columns do not say so either way.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_gridded_livestock_emissions(example = TRUE)
}
