% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redistribute_feed.R
\name{redistribute_feed}
\alias{redistribute_feed}
\title{Redistribute available feed supply among livestock demand.}
\usage{
redistribute_feed(feed_demand, feed_avail, options = list())
}
\arguments{
\item{feed_demand}{A tibble of feed demand with columns \code{year},
\code{territory}, \code{sub_territory}, \code{livestock_category}, \code{item_cbs_code},
\code{feed_group}, \code{feed_quality}, \code{demand_dm_t}, and a logical
\code{fixed_demand}.}

\item{feed_avail}{A tibble of feed availability with columns \code{year},
\code{sub_territory}, \code{item_cbs_code}, \code{feed_group}, \code{feed_quality},
\code{avail_dm_t}, and \code{feed_scale}.}

\item{options}{A named list of allocation options. See
\code{.redistribute_feed_options()} for the available entries and their
defaults. Supply \code{grass_availability} (a tibble with \code{year}, \code{territory}
or \code{area_code}, and \code{grass_avail_dm_t}) to bound the otherwise-unlimited
pasture grass at that supply per polity-year. The grass deficit then
cascades: pasture grass is capped at the ceiling, the deficit is
redistributed to leftover non-grass availability in the polity (added as
\verb{7_grass_deficit_substitute} intake, limited by that leftover), and the
residual stays as biologically-feasible underfeeding (\code{scaling_factor < 1}).
Supply \code{maintenance_share} (a scalar fraction or a tibble with
\code{livestock_category} and \code{maintenance_share}) to also diagnose polities
pushed below maintenance; the over-stocked demand rows are attached to the
result as the \code{grass_deficit_diagnosis} attribute. Set
\code{distribute_surplus = FALSE} to suppress the surplus-distribution pass that
pushes leftover CBS availability onto variable-demand livestock (correct for
historical analyses where the CBS feed element is the realised consumption;
keep \code{TRUE}, the default, for unconstrained scenario projections).}
}
\value{
A tibble of realised intake per demand row. When \code{maintenance_share}
is supplied alongside \code{grass_availability}, a \code{grass_deficit_diagnosis}
attribute lists demand rows underfed below maintenance.
}
\description{
Matches livestock feed demand to available feed items through a
hierarchical allocation that follows the remaining-share principle to
avoid exceeding availability. The redistribution path adapts to the
\code{fixed_demand} column in the demand table.
}
\examples{
# Two variable-demand categories in one territory competing for a single
# feed item. Availability (150) falls short of demand (200), so the
# remaining-share principle underfeeds both by the same factor rather than
# letting either exceed what is there.
feed_demand <- tibble::tribble(
  ~year, ~territory, ~sub_territory, ~livestock_category,
  ~item_cbs_code, ~feed_group, ~feed_quality, ~demand_dm_t, ~fixed_demand,
  2000L, "79", "79", "Cattle_milk",
  NA_integer_, NA_character_, "high_quality", 120, FALSE,
  2000L, "79", "79", "Pigs",
  NA_integer_, NA_character_, "high_quality", 80, FALSE
)

feed_avail <- tibble::tribble(
  ~year, ~territory, ~sub_territory, ~item_cbs_code, ~feed_group,
  ~feed_quality, ~avail_dm_t, ~feed_scale,
  2000L, "79", "79", 2514L, "cereals", "high_quality", 150, "national"
)

redistribute_feed(feed_demand, feed_avail)
}
