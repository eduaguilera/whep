% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_schema.R
\name{assert_table_schema}
\alias{assert_table_schema}
\title{Assert that a table conforms to a declarative schema.}
\usage{
assert_table_schema(data, schema, arg = "data")
}
\arguments{
\item{data}{Table to validate. Not modified.}

\item{schema}{Declarative schema, as described above.}

\item{arg}{Name of the validated object, used in messages.}
}
\value{
Invisibly, \code{data}, unchanged. Called for its side effect of
aborting on violation.
}
\description{
Build-time gate over a tabular artifact. Aborts when
\code{\link[=check_table_schema]{check_table_schema()}} reports any \code{"error"} diagnostic and warns when it
reports only \code{"warning"} ones, so a table that has silently lost a
column, changed type or gained duplicate keys fails at the moment it is
produced instead of downstream. Returns its input, so it can sit inside a
pipeline without changing the value that flows through.
}
\examples{
schema <- list(
  columns = list(
    list(name = "year", type = "integer"),
    list(name = "value", type = "double", min = 0)
  ),
  key = "year"
)
data <- tibble::tibble(year = c(2000L, 2001L), value = c(1, 2))
assert_table_schema(data, schema)
}
