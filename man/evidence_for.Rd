% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row_evidence.R
\name{evidence_for}
\alias{evidence_for}
\title{Carry row evidence through a join or a filter.}
\usage{
evidence_for(data, evidence)
}
\arguments{
\item{data}{Table the evidence is being carried onto. Must carry the
evidence's key columns. Duplicated keys are allowed: a fan-out
join does not change which claims apply.}

\item{evidence}{Evidence table, validated against
\code{\link[=row_evidence_schema]{row_evidence_schema()}}.}
}
\value{
A tibble of row evidence, in canonical order, containing the
rows of \code{evidence} that apply to \code{data}.
}
\description{
Re-align an evidence table onto a table that has since been joined,
filtered, reordered or reduced: the evidence rows whose \code{row_key}
still occurs in \code{data} are returned, in canonical order, with their
\code{source_id} and \code{source_version} untouched. Evidence is therefore
narrowed by composition but never re-attributed by it.

The key is taken from the evidence's own \code{key_columns}, not from the
caller, so a table that no longer carries those columns — an
aggregation that dropped \code{year}, say — aborts instead of matching on
whatever is left. Rows of \code{data} that carry no evidence are a
warning, not a silent gap: after a left join they are exactly the
rows whose provenance was lost.
}
\examples{
rows <- tibble::tibble(area_code = c(724L, 76L), year = 2020L)
evidence <- row_evidence(
  rows,
  source_id = "FAOSTAT_prod",
  key = c("area_code", "year"),
  fields = list(fao_flag = c("A", "E")),
  recorded_at = as.POSIXct("2026-01-01", tz = "UTC")
)

# A filter narrows the evidence to the rows that survive.
evidence_for(dplyr::filter(rows, area_code == 724L), evidence)

# A row with no evidence is reported rather than passed over.
extended <- dplyr::bind_rows(rows, tibble::tibble(
  area_code = 231L,
  year = 2020L
))
suppressWarnings(evidence_for(extended, evidence))
}
