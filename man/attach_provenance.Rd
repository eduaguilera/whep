% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{attach_provenance}
\alias{attach_provenance}
\title{Attach a provenance record to a result.}
\usage{
attach_provenance(x, provenance)
}
\arguments{
\item{x}{Any R object, typically a result tibble.}

\item{provenance}{Provenance tibble from \code{\link[=record_provenance]{record_provenance()}}.}
}
\value{
\code{x}, unchanged except for an added \code{whep_provenance}
attribute.
}
\description{
Store a provenance record (from \code{\link[=record_provenance]{record_provenance()}}) on an
object as an attribute, so the result travels together with its
lineage. Retrieve it later with \code{\link[=get_provenance]{get_provenance()}}.
}
\examples{
prov <- record_provenance(
  aliases = "bilateral_trade",
  recorded_at = as.POSIXct("2026-01-01", tz = "UTC")
)
result <- attach_provenance(tibble::tibble(value = 1), prov)
get_provenance(result)
}
