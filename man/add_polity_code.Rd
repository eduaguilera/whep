% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polities.R
\name{add_polity_code}
\alias{add_polity_code}
\title{Add WHEP polity codes to a table}
\usage{
add_polity_code(
  table,
  code_column = "area_code",
  year_column = "year",
  polity_code_column = "polity_code",
  backcast_anchor = 1961L
)
}
\arguments{
\item{table}{A data frame.}

\item{code_column}{Name of the column containing numeric area codes.}

\item{year_column}{Name of the column containing years. Set to \code{NULL} to
force current/default mapping.}

\item{polity_code_column}{Name of the output polity-code column.}

\item{backcast_anchor}{First year of reported (non-back-cast) FAOSTAT data,
default \code{1961}. Years before it are matched to the polity active in the
anchor year, because WHEP's pre-anchor series are back-cast onto the
anchor-year territory rather than reported under their data-year borders.
Set to \code{-Inf} to disable and match strictly by data year.}
}
\value{
A tibble with added polity metadata columns.
}
\description{
Adds periodized \code{polity_code} information from \link{polity_area_crosswalk} to
a table with FAOSTAT/FABIO \code{area_code} values. If a \code{year} column is
present, the mapping is year-aware; otherwise the current/default mapping
is used.

When no mapped period covers a row's year, the nearest period of the same
area is used as a stand-in and \code{mapping_status} reports \code{"out_of_span"}
rather than the crosswalk's \code{"matched"}/\code{"manual"}. Such a row is attributed
to a polity that did not exist in that year, so treat it as a coverage gap:
either the area needs the missing period added to the crosswalk, or the
reporting area outlived (or predates) every polity mapped to it.
}
\seealso{
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}}, which reports the stand-in rows of an
already-built table, whose published columns no longer carry
\code{mapping_status}.
}
