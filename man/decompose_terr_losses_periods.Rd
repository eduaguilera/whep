% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{decompose_terr_losses_periods}
\alias{decompose_terr_losses_periods}
\title{Decompose territorial N losses by reference period (chained)}
\usage{
decompose_terr_losses_periods(
  n_prov_destiny = NULL,
  raw = NULL,
  example = FALSE
)
}
\arguments{
\item{n_prov_destiny}{Nitrogen flows tibble from
\code{\link[=create_n_prov_destiny]{create_n_prov_destiny()}}, shared across all four compartments. If
\code{NULL}, loaded automatically.}

\item{raw}{Named list overriding any of the raw inputs shared across
compartments (\code{npp_ygpit}, \code{codes_coefs}, \code{intake_ygiac},
\code{n_excretion_ygs}, \code{stock_prod_ygps}, \code{livestock_units},
\code{population_yg}). Missing elements are loaded automatically.}

\item{example}{If \code{TRUE}, return a small hardcoded output without
downloading remote data. Default is \code{FALSE}.}
}
\value{
A named list with tibbles \code{detail} (per-compartment LMDI
output, with \code{compartment} and \code{mechanism} columns added),
\code{by_compartment}, and \code{by_mechanism} (each with \code{period} — one of
"1865-1925", "1925-1965", "1965-2015", "Total (1865-2015)", the mean
year of each reference window (1865 = mean of 1860-1870, and so on)
— plus \code{contribution_mgn}, \code{period_years}, and
\code{contribution_per_yr_mgn}, the per-year-normalized value used for
plotting).
}
\description{
Runs the same four compartments as \code{\link[=decompose_terr_losses]{decompose_terr_losses()}},
but comparing each reference period (each averaged across its ten
years) against the immediately preceding one — 1860-1870 ->
1920-1930 -> 1960-1970 -> 2010-2020 — plus one extra transition
spanning the full analysis window, 1860-1870 straight to 2010-2020
(the total change), instead of chaining year on year. This is the
periodised table recommended alongside the main chained figure in the
decomposition proposal (section 12), summarizing the four historical
phases rather than following the full 160-year trajectory.
}
\examples{
decompose_terr_losses_periods(example = TRUE)
}
