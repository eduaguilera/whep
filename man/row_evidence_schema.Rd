% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row_evidence.R
\name{row_evidence_schema}
\alias{row_evidence_schema}
\title{The declarative schema of a row-evidence table.}
\usage{
row_evidence_schema()
}
\value{
A schema list, as documented in \code{\link[=check_table_schema]{check_table_schema()}}.
}
\description{
Return the \code{"whep-row-evidence/1"} contract as a declarative schema,
ready for \code{\link[=check_table_schema]{check_table_schema()}} or \code{\link[=assert_table_schema]{assert_table_schema()}}. It is
plain data, so it can be serialized next to an evidence artifact and
used by a consumer that does not load this package.

The schema is closed (\code{extra_columns = "forbid"}) and ordered
(\code{column_order = "strict"}): an evidence table with an extra column
or a permuted column order is not this format, because a consumer
rebuilding \code{row_key} from \code{key_columns} relies on both.
}
\examples{
row_evidence_schema()

# The contract is data, so it survives serialization unchanged.
identical(
  yaml::yaml.load(yaml::as.yaml(row_evidence_schema())),
  row_evidence_schema()
)
}
