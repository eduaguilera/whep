% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_schema.R
\name{check_table_schema}
\alias{check_table_schema}
\title{Check a table against a declarative schema.}
\usage{
check_table_schema(data, schema)
}
\arguments{
\item{data}{Table to validate. Not modified.}

\item{schema}{Declarative schema, as described above.}
}
\value{
A tibble of diagnostics, empty when \code{data} conforms.
}
\description{
Validate a tibble against a serializable schema and return one row of
diagnostics per violation. The input is never modified, so this is the
read-only counterpart of \code{\link[=ensure_columns]{ensure_columns()}}, which \emph{coerces} a table to a
typed prototype. Use \code{\link[=ensure_columns]{ensure_columns()}} to reach a schema and
\code{check_table_schema()} to prove a table is already there.

The schema is plain data — nested lists of scalars and atomic vectors — so
it round-trips through \code{yaml} or \code{jsonlite} unchanged and can live next to
the artifact it describes. Project vocabularies and scientific bounds stay
in the caller's schema; this function hard-codes none of them.
}
\section{Schema representation}{

A list with these fields, all optional except \code{columns}:
\itemize{
\item \code{columns}: An \emph{ordered} list of column specifications (see below). A
list, not a named list, so the declared order survives any
serialization. May be empty.
\item \code{key}: Character vector of column names that must jointly be unique.
Every name must be declared in \code{columns}.
\item \code{extra_columns}: \code{"allow"} (default) or \code{"forbid"} for columns the
schema does not declare.
\item \code{column_order}: \code{"ignore"} (default) or \code{"strict"}. Under \code{"strict"}
the declared columns that are present must appear in the declared
relative order.
\item \code{allow_empty}: Whether a zero-row table is acceptable. \code{TRUE} by
default; set \code{FALSE} to flag an empty result.
}

Each column specification is a list with:
\itemize{
\item \code{name}: Column name. Required.
\item \code{type}: One of \code{"logical"}, \code{"integer"}, \code{"double"}, \code{"character"},
\code{"Date"}, \code{"list"} or \code{"any"}. Required. Types are compared exactly
(\code{vctrs::vec_is()}): an \code{integer} column does not satisfy \code{"double"}.
\code{"any"} skips the type check.
\item \code{required}: Whether the column must be present. \code{TRUE} by default.
\item \code{allow_missing}: Whether \code{NA} is acceptable. \code{TRUE} by default.
\item \code{min}, \code{max}: Inclusive bounds, for \code{"integer"}, \code{"double"} and
\code{"Date"} only.
\item \code{allowed}: Permitted values, i.e. a caller-owned vocabulary.
\item \code{unique}: Whether values must be unique within the column. \code{FALSE} by
default.
\item \code{severity}: \code{"error"} (default) or \code{"warning"} for every diagnostic
attributed to this column.
}

\code{min}, \code{max}, \code{allowed}, \code{unique} and \code{allow_missing = FALSE} do not apply
to \code{"list"} or \code{"any"} columns. Unknown fields, at either level, abort
rather than being ignored, so a mistyped \code{minimum} cannot silently disable
a bound.
}

\section{Diagnostics}{

One row per violation, ordered deterministically: table-scope rules
first, then each declared column in schema order (by row, then rule),
then undeclared columns in input order, then key duplicates. Columns:
\itemize{
\item \code{row}: Row index in \code{data}, \code{NA} for table- and column-scope rules.
\item \code{column}: Column name, \code{NA} for table-scope rules.
\item \code{rule}: One of \code{"empty_table"}, \code{"column_order"}, \code{"missing_column"},
\code{"unexpected_column"}, \code{"type_mismatch"}, \code{"missing_value"},
\code{"below_min"}, \code{"above_max"}, \code{"not_allowed"}, \code{"duplicate_value"},
\code{"duplicate_key"}.
\item \code{value}: The offending value, formatted, \code{NA} where no single value is
at fault.
\item \code{severity}: \code{"error"} or \code{"warning"}, from the column specification.
Table-scope rules are always \code{"error"}.
\item \code{detail}: Human-readable context.
}

A column whose type does not match reports \code{type_mismatch} and its value
rules are skipped, so a wrongly typed column yields one diagnostic rather
than one per row.
}

\examples{
# A keyed long table with a vocabulary and a scientific bound.
schema <- list(
  columns = list(
    list(name = "year", type = "integer", min = 1961, max = 2023),
    list(name = "area_code", type = "integer", allow_missing = FALSE),
    list(
      name = "source",
      type = "character",
      allowed = c("FAOSTAT_prod", "LUH2")
    ),
    list(name = "value", type = "double", min = 0)
  ),
  key = c("year", "area_code")
)
data <- tibble::tibble(
  year = c(2000L, 2000L, 1900L),
  area_code = c(4L, 4L, 8L),
  source = c("FAOSTAT_prod", "guess", "LUH2"),
  value = c(1, -2, 3)
)
check_table_schema(data, schema)

# The schema is data: it survives a YAML round trip unchanged.
identical(
  check_table_schema(data, yaml::yaml.load(yaml::as.yaml(schema))),
  check_table_schema(data, schema)
)

# A structurally different schema: closed column set, strict order,
# no key, a non-empty requirement and a list column.
manifest_schema <- list(
  columns = list(
    list(name = "built_at", type = "Date"),
    list(name = "inputs", type = "list")
  ),
  extra_columns = "forbid",
  column_order = "strict",
  allow_empty = FALSE
)
check_table_schema(tibble::tibble(inputs = list()), manifest_schema)
}
