% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_reconcile.R
\name{unit_cropland_extent}
\alias{unit_cropland_extent}
\title{The cropland each unit holds, on the basis the capacity ceiling uses}
\usage{
unit_cropland_extent(allocation_layer, gridded_cropland)
}
\arguments{
\item{allocation_layer}{The country grid the allocation ran on, from
\code{\link[=build_allocation_layer]{build_allocation_layer()}}: \code{lon}, \code{lat}, \code{area_code},
\code{cell_area_frac} and, where a depth is granted,
\code{level_polity_code}.}

\item{gridded_cropland}{Per-cell cropland extent, as
\code{\link[=build_gridded_landuse]{build_gridded_landuse()}} takes it: \code{lon}, \code{lat}, \code{year},
\code{cropland_ha}.}
}
\value{
A tibble of \code{year}, \code{area_code}, \code{level_polity_code},
\code{cropland_ha} and \code{n_cells}.
}
\description{
Sum a gridded cropland extent onto the units of an allocation layer,
weighting every cell by \code{cell_area_frac}. This is the denominator
\code{reconcile_admin_allocation()}'s implied cropping intensity divides by,
and it is deliberately the same quantity \code{.capacity_bases()} multiplies
by the multi-cropping factor: an intensity taken against any other
land basis would not be comparable with the ceiling it is flagged
against.

A physical cell shared by two units contributes its cropland to each in
proportion, so the units' extents inside one cell add up to that cell's
own cropland and nothing is double counted. Rows are selected per year
by the layer's own validity convention, so an interval-keyed layer
returns the unit that existed in each cropland year.
}
\examples{
layer <- tibble::tibble(
  lon = c(0.25, 0.75, 0.75), lat = 50.25, area_code = 1L,
  level_polity_code = c("A1", "A1", "A2"), level = 1L,
  cell_area_frac = c(1, 0.25, 0.75)
)
cropland <- tibble::tibble(
  lon = c(0.25, 0.75), lat = 50.25, year = 2000L,
  cropland_ha = c(100, 400)
)
unit_cropland_extent(layer, cropland)
}
\seealso{
\code{\link[=reconcile_admin_allocation]{reconcile_admin_allocation()}}.
}
