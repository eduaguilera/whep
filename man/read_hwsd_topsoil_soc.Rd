% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soil_ph.R
\name{read_hwsd_topsoil_soc}
\alias{read_hwsd_topsoil_soc}
\title{Read observed topsoil organic carbon from HWSD onto WHEP's grid.}
\source{
FAO/IIASA/ISRIC/ISSCAS/JRC (2012). \emph{Harmonized World Soil Database
version 1.2}. FAO, Rome and IIASA, Laxenburg -- topsoil defined as
0-30 cm. Stock equation and the bulk-density caveat: Hiederer, R. &
Koechy, M. (2011). \emph{Global Soil Organic Carbon Estimates and the
Harmonized World Soil Database}. EUR 25225 EN, Publications Office of
the European Union, 79 pp.
}
\usage{
read_hwsd_topsoil_soc(
  hwsd_dir = NULL,
  bulk_density = c("measured", "reference"),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{hwsd_dir}{Path to the directory holding \code{hwsd_data.csv} and
\code{hwsd.bil}. Defaults to \code{Sys.getenv("WHEP_HWSD_DIR")}.}

\item{bulk_density}{Which HWSD bulk density to use. \code{"measured"}
(default) takes \code{t_bulk_density}, falling back to
\code{t_ref_bulk_density} where it is absent; \code{"reference"} takes
\code{t_ref_bulk_density} alone. The default is not cosmetic:
\code{t_ref_bulk_density} is derived from texture and so knows nothing about
organic matter, and over the 1,375 map units with \code{t_oc} above 6\% it
averages 1.33 against a measured 0.37, which would inflate a peat
soil's carbon stock roughly 3.6-fold. Recorded in \code{method_soc_obs}.}

\item{data}{Optional named list of pre-loaded inputs: \code{cell_polity}
(\code{lon}, \code{lat}, at minimum), used both to crop the HWSD raster to the
region of interest before reclassification and as the target grid for
gap-filling. When absent, cropping and gap-filling are both skipped
(documented fallback above).}

\item{example}{If \code{TRUE}, return a small fixture instead of reading data.
Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{lon}, \code{lat}, \code{soc_obs_mgc_ha} (0-30 cm soil organic
carbon, Mg C per ha) and \code{method_soc_obs}.
}
\description{
Read the observed \strong{0-30 cm} soil organic carbon stock per 0.5-degree
cell from the HWSD (Harmonized World Soil Database) map-unit attribute
table and raster. Each map unit's stock is the share-weighted mean over
its soil components of \code{t_oc * bulk_density * 30 * (1 - t_gravel / 100)},
and the map units are then averaged over the native HWSD cells inside
each 0.5-degree block by the same aggregation \code{\link[=read_soil_ph]{read_soil_ph()}} uses.

This exists to be a \strong{benchmark}, not a model input: nothing in the
carbon pipeline consumes it. HWSD version 1.2's topsoil is 0-30 cm,
exactly the layer \code{\link[=build_carbon_balance]{build_carbon_balance()}} reports (see its Soil depth
section), so it is the only observational anchor available at WHEP's own
modelled depth -- and it comes out of the archive the carbon balance
already reads for clay, so it needs no new download. Note that HWSD\strong{2},
which \code{inst/scripts/download/download_hwsd.R} fetches, layers its topsoil
as D1 = 0-20 cm instead; the two are not interchangeable (whep#851).
}
\examples{
read_hwsd_topsoil_soc(example = TRUE)
}
