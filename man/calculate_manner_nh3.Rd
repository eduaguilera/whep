% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manner_model.R
\name{calculate_manner_nh3}
\alias{calculate_manner_nh3}
\title{Estimate ammonia-N volatilisation with the MANNER process-based model.}
\usage{
calculate_manner_nh3(
  n_applied_t = NULL,
  fertiliser = NULL,
  drivers = list(),
  example = FALSE
)
}
\arguments{
\item{n_applied_t}{Numeric, nitrogen applied (t).}

\item{fertiliser}{One of \code{"Urea"}, \code{"AN"}, \code{"CAN"}, \code{"AS"} (synthetic
path) or \code{"cattle_slurry"}, \code{"pig_slurry"}, \code{"FYM"},
\code{"poultry_manure"}, \code{"urban"} (organic path).}

\item{drivers}{A named list of driver values. Synthetic path: \code{soil_ph}
(numeric soil pH), \code{rate_kg_ha} (numeric N application rate, kg N/ha),
\code{rainfall_mm} (numeric period precipitation, mm), \code{irrigated}
(logical), \code{temp_c} (numeric application-period temperature, deg C),
\code{temp_c_annual_mean} (numeric annual mean temperature, deg C; only
used for CAN/AS). Organic path: \code{rainfall_mm}, \code{irrigated},
\code{windspeed_ms} (numeric wind speed, m/s), \code{technique} (one of the six
\link{manner_params} \code{technique} keys), \code{system} (\code{"Arable"} or
\code{"Grassland"}), \code{temp_c}, \code{incorporation_delay_h} (numeric hours
between surface application and soil incorporation, or \code{Inf}/\code{NA} for
no incorporation), \code{species} (required unless \code{fertiliser == "urban"};
one of the eight \link{manure_inorganic_n} species, used only to look up
\code{inorganic_n_fraction}).}

\item{example}{If \code{TRUE}, return a small fixture instead of computing
from drivers. Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{n_applied_t}, \code{ef} (realised emission factor),
\code{nh3_n_t} and \code{method_manner}.
}
\description{
Ports the MANNER (Nicholson et al. 2013) process-based ammonia
volatilisation model. Dispatches on \code{fertiliser} to one of two
independent paths: a synthetic-fertiliser path (\code{"Urea"}, \code{"AN"},
\code{"CAN"}, \code{"AS"}) driven by soil pH, application rate, rainfall and
temperature, and an organic-manure path (\code{"cattle_slurry"},
\code{"pig_slurry"}, \code{"FYM"}, \code{"poultry_manure"}, \code{"urban"}) driven by
rainfall, temperature, wind speed, application technique, system
(arable/grassland) and incorporation delay.
}
\details{
The organic path's \code{inorganic_n_fraction} lookup (\link{manure_inorganic_n})
maps \code{manure_type} to its ammoniacal \code{manure_stream} (\code{"Liquid"} for
\code{"cattle_slurry"}/\code{"pig_slurry"}, \code{"Solid"} for \code{"FYM"}/
\code{"poultry_manure"}) and reads that stream's fraction for the actual
\code{species} driver supplied by the caller. This matches Spain_Hist, which
maps every species' solid stream to the FYM MANNER class yet looks the
inorganic-N fraction up per real species. When \code{species} is omitted it
falls back to the manure type's default species (Cattle for
\code{"cattle_slurry"}/\code{"FYM"}, Pigs for \code{"pig_slurry"}, Poultry for
\code{"poultry_manure"}). \code{"urban"} bypasses this lookup entirely: it fixes
\code{inorganic_n_fraction = 0.5} regardless of species, matching
\code{nh3.r:102-104}. For the AG availability and incorporation factors,
\code{"urban"} maps to the FYM manure class (Spain_Hist Manner_ferts row 43),
including the 0.4 Org_ef correction.
}
\examples{
calculate_manner_nh3(example = TRUE)
}
