% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sjos_n_class.R
\name{classify_sjos_n}
\alias{classify_sjos_n}
\title{Classify crops into the 2-way SJOS-N safe-and-just space.}
\usage{
classify_sjos_n(exceedance, nourishment, level_col = sjos_class)
}
\arguments{
\item{exceedance}{A \code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}} output at
\code{resolution = "country"}, keyed by \code{year}, \code{area_code}, \code{item_cbs_code}
with the mass terms \code{exceedance_n_t}, \code{within_boundary_n_t}, \code{actual_n_t}.}

\item{nourishment}{A \code{\link[=normalize_nourishment]{normalize_nourishment()}} output carrying \code{year},
\code{area_code} and the \code{nourish} class (\code{"Under"} / \code{"Adequate"} / \code{"Over"}),
one row per country-year.}

\item{level_col}{The unquoted name for the classification column. Defaults to
\code{sjos_class}.}
}
\value{
A tibble keyed by \code{year}, \code{area_code}, \code{item_cbs_code} with the mass
terms \code{exceedance_n_t}, \code{within_boundary_n_t}, \code{actual_n_t}, the joined
\code{nourish} class, the \code{boundary_side} and the classification column (a factor
over \code{sjos_levels$level}, named by \code{level_col}).
}
\description{
Crosses the ecological boundary side with the nourishment side into the six
ordered \link{sjos_levels}. Per crop (\code{item_cbs_code}), the boundary side is
\code{"Exceedance"} when the crop-country-year's \code{exceedance_n_t} is positive and
\code{"Within_boundary"} otherwise (the all-zero and missing cases fall to
\code{"Within_boundary"}). The country's nourishment class (\code{nourish}, from
\code{\link[=normalize_nourishment]{normalize_nourishment()}}) is joined by \code{year} and \code{area_code} and broadcast
to each of its crops. The classification \code{paste(boundary_side, nourish)} is
one of \code{"Within_boundary Under"} ... \code{"Exceedance Over"}, returned as a factor
with all six \code{sjos_levels$level} levels. This reproduces Global's 2-way remap
(\code{Global/R/sjos_n.r:363}) at the per-\code{item_cbs} granularity Module 4's
footprint needs.
}
\examples{
classify_sjos_n(
  exceedance = tibble::tribble(
    ~year,
    ~area_code,
    ~item_cbs_code,
    ~exceedance_n_t,
    ~within_boundary_n_t,
    ~actual_n_t,
    2010L, 10L, 2511L, 5, 3, 8,
    2010L, 10L, 2513L, 0, 4, 4
  ),
  nourishment = tibble::tribble(
    ~year, ~area_code, ~nourish,
    2010L, 10L, "Over"
  )
)
}
