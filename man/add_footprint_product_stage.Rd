% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/footprint_paths.R
\name{add_footprint_product_stage}
\alias{add_footprint_product_stage}
\title{Add a final-demand product-area stage to footprints.}
\usage{
add_footprint_product_stage(
  footprints,
  y_mat,
  labels,
  fd_labels,
  max_product_areas = 5,
  other_area_name = "Other",
  min_share = 0
)
}
\arguments{
\item{footprints}{Footprint table from \code{\link[=compute_footprint]{compute_footprint()}} with
\code{target_area}, \code{target_item}, \code{target_fd}, and \code{value}.}

\item{y_mat}{Final demand matrix from \code{\link[=build_io_model]{build_io_model()}}.}

\item{labels}{Tibble mapping Y rows to \code{area_code} and \code{item_cbs_code}.}

\item{fd_labels}{Tibble mapping Y columns to \code{area_code} and \code{fd_col}.}

\item{max_product_areas}{Maximum number of supplier/product areas to keep
separately for each final-demand area, item, and demand category. Smaller
supplier areas are grouped into \code{other_area_name}.}

\item{other_area_name}{Label for grouped supplier/product areas.}

\item{min_share}{Drop split paths smaller than this percentage of the total
input footprint value. Use 0 to keep all split paths.}
}
\value{
\code{footprints} with \code{product_area}, \code{product_area_name},
\code{product_item}, and \code{product_share} columns. \code{value} is replaced by the
split path value.
}
\description{
Split footprint rows by the area that supplied the final-demand product,
using shares from \code{y_mat}. This preserves the standard footprint totals
while adding a FABIO-viewer style phase:
origin product -> product/supplier area -> product -> final-demand area.

This is a compact global-view helper. It does not recompute the full
origin-sector by product-sector Leontief cube. Instead, each existing
footprint row is allocated over the product-area shares observed in final
demand for the same \code{target_area}, \code{target_fd}, and \code{target_item}.
}
\examples{
# One footprint row of 100, split over the two areas that supply item 20 to
# the final demand of area 1 in the shares observed in `y_mat` (80 / 20).
footprints <- tibble::tibble(
  origin_area = 1L,
  origin_item = 10L,
  target_area = 1L,
  target_area_name = "Target",
  target_item = 20L,
  target_fd = "food",
  value = 100
)

add_footprint_product_stage(
  footprints = footprints,
  y_mat = Matrix::Matrix(c(80, 20), nrow = 2, sparse = TRUE),
  labels = tibble::tibble(
    area_code = c(1L, 2L),
    item_cbs_code = c(20L, 20L)
  ),
  fd_labels = tibble::tibble(area_code = 1L, fd_col = "food")
) |>
  dplyr::select(origin_item, product_area, product_share, value)
}
