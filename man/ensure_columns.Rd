% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensure_columns.R
\name{ensure_columns}
\alias{ensure_columns}
\title{Complete columns from a typed prototype.}
\usage{
ensure_columns(data, prototype, defaults = NULL, extra = c("keep", "drop"))
}
\arguments{
\item{data}{Input tibble.}

\item{prototype}{Zero-row tibble defining required column names, types, and
order.}

\item{defaults}{Optional named list of size-one defaults for absent columns.
Every name must occur in \code{prototype}, and each value must be safely
convertible to the corresponding prototype type.}

\item{extra}{Whether columns absent from \code{prototype} are \code{"keep"} or
\code{"drop"}.}
}
\value{
An ungrouped tibble with the same rows as \code{data}, completed and
ordered from \code{prototype}. Grouping metadata is not preserved.
}
\description{
Add absent columns to a tibble and safely cast present columns to the types
declared by a zero-row prototype. Missing columns receive typed missing
values unless a scalar default is supplied. Defaults never replace missing
values inside a column that is already present.

Prototype columns are returned first in prototype order. With
\code{extra = "keep"}, other input columns follow in their original relative
order. With \code{extra = "drop"}, the output has exactly the prototype schema.
}
\examples{
prototype <- tibble::tibble(
  year = integer(),
  value = double(),
  source = character()
)
data <- tibble::tibble(value = 2, year = 2020L, note = "observed")

ensure_columns(
  data,
  prototype,
  defaults = list(source = "unknown")
)
}
