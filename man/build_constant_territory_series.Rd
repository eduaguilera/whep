% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constant_territory.R
\name{build_constant_territory_series}
\alias{build_constant_territory_series}
\title{Build a constant-territory time series for a reference year's boundaries}
\usage{
build_constant_territory_series(
  data,
  ref_year,
  polities = NULL,
  covariate = NULL,
  resolution = 25000,
  donor = c("regional", "none"),
  crs_equal_area = 6933,
  max_cells = 2e+06,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame of reported values with columns:
\itemize{
\item \code{year}: integer data year.
\item \code{polity_code}: the source polity that reported the value (must be active
in \code{year} and carry a polygon).
\item \code{value}: numeric value (summed if a polity appears more than once).
}}

\item{ref_year}{Integer. Target boundaries are the polities active in this
year (\verb{start_year <= ref_year <= end_year}).}

\item{polities}{An \code{sf} of polity polygons with \code{polity_code}, \code{start_year},
\code{end_year} and geometry. Defaults to \code{\link[=get_polity_geometries]{get_polity_geometries()}}.}

\item{covariate}{\code{NULL} (uniform density, i.e. area weighting) or a function
\verb{function(centroids_sf, year) -> numeric} returning a non-negative density
per grid-cell centroid (centroids are supplied in \code{crs_equal_area}).}

\item{resolution}{Grid cell size, in metres of \code{crs_equal_area}. Default
25000 (25 km). Smaller is more accurate but slower.}

\item{donor}{Gap-imputation rule: \code{"regional"} (default) fills uncovered
target cells at the region-wide value-per-covariate intensity of the
sources with data that year; \code{"none"} leaves them at zero (covered-only).}

\item{crs_equal_area}{EPSG code of an equal-area CRS used for gridding and
areas. Default 6933 (NSIDC EASE-Grid 2.0 Global).}

\item{max_cells}{Safety cap on grid cells per year (default 2e6). Aborts if
the source/target extent would exceed it (usually a stray continent-scale
target); restrict \code{polities}, coarsen \code{resolution}, or raise this.}

\item{verbose}{Logical; emit progress/warnings.}
}
\value{
A tibble, one row per (\code{ref_year}-target, data \code{year}):
\itemize{
\item \code{target_polity_code}, \code{year}
\item \code{value}: constant-territory estimate (\code{covered + imputed})
\item \code{covered}: mass from cells overlapping a source with data
\item \code{imputed}: mass added for uncovered cells
\item \code{imputed_share}: covariate fraction imputed (0 = fully observed)
\item \code{n_sources}: number of source polities contributing that year
}
}
\description{
Estimates a time series of a quantity over a \strong{fixed} set of territorial
boundaries — the polities active in \code{ref_year} — from data reported under
the \emph{changing} historical boundaries of each data year.

Country borders change over time, so there is no raw constant-territory
series: a 1900 figure for "Austria-Hungary" is not a figure for present-day
Austria. This function estimates one by spatial reallocation (dasymetric
areal interpolation):
\enumerate{
\item For each data \code{year}, the value reported by each source polity is spread
over \strong{that polity's own extent for that year} across a regular grid,
weighted by a \code{covariate} density (e.g. gridded cropland or population;
uniform = plain areal weighting).
\item The grid is then re-aggregated to the \code{ref_year} target boundaries: a
target's estimate is the sum of grid mass falling inside it.
\item Target territory \strong{not covered} by any source with data in that year is
\emph{imputed} — its grid cells still carry covariate mass, so they are filled
at a donor intensity (value per unit covariate) rather than left at zero.
The fraction of a target's covariate mass that had to be imputed is
reported as \code{imputed_share}, an honest confidence signal.
}

The estimate is only as good as the \code{covariate}: supply the same gridded
surface used elsewhere in WHEP spatialization (cropland for crop output,
population for demographic series, livestock density for animals). With
\code{covariate = NULL} the method reduces to area-weighted areal interpolation.
}
\section{How \code{polity_code} must have been assigned}{

Step 1 spreads each value over \emph{that polity's own extent for that year}, so it
assumes the \code{polity_code} on a row names the territory the value was actually
reported for. That holds for data resolved by \code{\link[=add_polity_code]{add_polity_code()}} with its
default \code{backcast_anchor = 1961}: WHEP's pre-1961 series are back-cast onto
1961 territory, and the anchor assigns them the polity active in 1961, so the
polity's extent and the value's basis agree.

It does \strong{not} hold if the input was resolved with \code{backcast_anchor = -Inf}.
That matches strictly by data year, so a 1900 row of a back-cast series would
carry a 1900-era polity while the value still describes 1961 borders — and
this function would then spread 1961-border figures across a 1900 extent. The
error is silent: the reallocation succeeds and returns plausible numbers.

Strict matching is the right choice for values genuinely reported under their
own year's borders, such as the historical sources resolved through
year-ranged aliases (Mitchell, IIA, FAO 1952). The two cases cannot be told
apart from the data alone, so the caller has to know which it holds.
}

\examples{
# Self-contained toy: two adjacent square polities. Only "P1" reports a
# value in 1900, so when the series is rebuilt onto the boundaries active in
# `ref_year` 2000 (both polities), "P2" is imputed from "P1"'s intensity.
make_square <- function(xmin, ymin, side) {
  sf::st_polygon(list(rbind(
    c(xmin, ymin),
    c(xmin + side, ymin),
    c(xmin + side, ymin + side),
    c(xmin, ymin + side),
    c(xmin, ymin)
  )))
}
polities <- sf::st_sf(
  polity_code = c("P1", "P2"),
  start_year = c(1800L, 1800L),
  end_year = c(2025L, 2025L),
  geometry = sf::st_sfc(
    make_square(0, 0, 2),
    make_square(2, 0, 2),
    crs = 4326
  )
)
reported <- tibble::tibble(
  year = 1900L,
  polity_code = "P1",
  value = 100
)
build_constant_territory_series(
  reported,
  ref_year = 2000,
  polities = polities,
  resolution = 50000,
  verbose = FALSE
)
}
