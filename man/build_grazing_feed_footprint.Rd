% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/footprint_grazing.R
\name{build_grazing_feed_footprint}
\alias{build_grazing_feed_footprint}
\title{Build a grazing-land footprint by forward feed-allocation.}
\usage{
build_grazing_feed_footprint(
  year,
  products = c("all", "meat_milk"),
  intake_basis = c("grazer_forage", "grass"),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{year}{Year to build the footprint for.}

\item{products}{Co-product split passed to
\code{\link[=allocate_grazing_to_products]{allocate_grazing_to_products()}}. \code{"all"} (default) or
\code{"meat_milk"}.}

\item{intake_basis}{Intake used to split land across animals,
\code{"grazer_forage"} (default) or \code{"grass"}.}

\item{data}{Optional named list of pre-built inputs, any of
\code{grass_land}, \code{grazer_intake}, \code{livestock_production} and \code{trade}.
Each falls back to its builder when absent.}

\item{example}{If \code{TRUE}, return a small example output without
downloading remote data. Default is \code{FALSE}.}
}
\value{
A tibble with \code{area_code} (consuming country),
\code{item_cbs_code} (meat or milk item), \code{value} (embodied grazing
land) and \code{method} (\code{"grazing_feed_allocation"}).
}
\description{
End-to-end grazing-land footprint for one year on real WHEP data.
Grazing land (the grassland extension) is pushed forward onto
livestock meat and milk via \code{\link[=allocate_grazing_to_products]{allocate_grazing_to_products()}}, then
routed to consuming countries through the bilateral meat-trade
network with \code{\link[=compute_footprint_balance]{compute_footprint_balance()}}. This attributes grazing
land to the meat and dairy consumption that drives it -- the chain
the Leontief footprint leaks because grass is non-productive and not
traded.

The inputs are assembled from \code{\link[=build_grassland_land_extension]{build_grassland_land_extension()}},
\code{\link[=get_feed_intake]{get_feed_intake()}}, \code{\link[=get_primary_production]{get_primary_production()}} and
\code{\link[=get_bilateral_trade]{get_bilateral_trade()}}. Supply any of them through \code{data} to reuse
cached inputs or to test without remote reads.

\code{intake_basis} selects the intake that splits grazing land across
animals. The default \code{"grazer_forage"} uses every grazing animal's
grass plus roughage-residue intake, because the feed model classes
the forage of major extensive-grazing countries (for example
Australia, the United States and Argentina) as residues rather than
grass; keying on the \code{"grass"} feed type alone would silently strip
that land. \code{"grass"} reproduces the narrower grass-feed basis for
sensitivity analysis. Land in countries with no grazer intake at all
cannot be attributed and is reported via a warning.
}
\examples{
build_grazing_feed_footprint(example = TRUE)
}
