% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arable_permanent_land.R
\name{build_fao_arable_fallow_extension}
\alias{build_fao_arable_fallow_extension}
\title{Build a per-crop physical land extension with FAO fallow-inclusive arable land.}
\usage{
build_fao_arable_fallow_extension(
  harvested = NULL,
  arable_permanent = NULL,
  base_extension = NULL,
  fallow_weights = NULL,
  items_prod_full = whep::items_prod_full
)
}
\arguments{
\item{harvested}{Tibble of harvested area with columns \code{year}, \code{area_code},
\code{item_cbs_code}, \code{harvested_ha}. If \code{NULL}, built from
\code{\link[=get_primary_production]{get_primary_production()}} (\code{unit == "ha"}); passing a cached harvested
table avoids that rebuild.}

\item{arable_permanent}{Tibble of FAO physical land base with columns
\code{area_code}, \code{year}, \code{arable_ha}, \code{permanent_ha}. If \code{NULL},
\code{\link[=get_arable_permanent_land]{get_arable_permanent_land()}} is called for the years present in
\code{base_extension}.}

\item{base_extension}{Tibble of cropped (fallow-excluding) per-crop physical
area with columns \code{year}, \code{area_code}, \code{item_cbs_code}, \code{impact_u}. If
\code{NULL}, built with \code{\link[=build_cropgrids_land_extension]{build_cropgrids_land_extension()}}\code{(source = "cropgrids")}
from \code{harvested}.}

\item{fallow_weights}{Tibble of \code{area_code}, \code{item_cbs_code}, \code{weight} giving
the within-country fallow allocation weight, e.g. from
\code{\link[=gridded_fallow_weights]{gridded_fallow_weights()}} (the recommended agro-climatic, rainfed-gated
weight). If \code{NULL}, fallow is distributed in proportion to each arable
crop's cropped physical area (perennials always excluded).}

\item{items_prod_full}{Crosswalk used to classify \code{item_cbs_code} as arable or
perennial via \code{Herb_Woody}. Defaults to \link{items_prod_full}.}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (fallow-inclusive physical land in hectares), and \code{method_land}
(\code{"fao_arable_fallow"}).
}
\description{
Turn per-crop harvested-derived physical area into a fallow-inclusive physical
land extension whose arable-crop total reconciles to FAO's physical
\strong{Arable land} and whose perennial-crop total reconciles to FAO's physical
\strong{Permanent crops} (\code{\link[=get_arable_permanent_land]{get_arable_permanent_land()}}), per \verb{(area_code, year)}.

This is the FAO-land-base analogue of \code{\link[=build_cropgrids_land_extension]{build_cropgrids_land_extension()}}\code{(source = "cropgrids_fallow")}. The existing method takes the fallow \emph{magnitude} from
FAOSTAT "Temporary fallow" (item 6640, a sparse and, for many rain-fed
economies, absent series) applied to a single CROPGRIDS 2020 snapshot. Here
the fallow magnitude is the physical arable land that carried no harvest in
that specific year, \verb{FAO Arable land - sum(cropped arable physical)}, so a
drought year's resting cropland is charged to the crops whose rotation it
supports and the arable-crop footprint totals match FAO's land survey in every
year (see the Tunisia/Portugal motivation in \code{\link[=get_arable_permanent_land]{get_arable_permanent_land()}}).

Reconciliation, per \verb{(area_code, year)}:
\itemize{
\item \strong{Arable crops} (\code{items_prod_full$Herb_Woody != "Woody"}): rotational
fallow \code{max(0, arable_ha - S)} (with \code{S} the cropped arable physical total)
is distributed with \code{\link[=attribute_fallow_to_crops]{attribute_fallow_to_crops()}} using \code{fallow_weights}, so
the arable total reaches \code{arable_ha}. Where the cropped physical already
exceeds \code{arable_ha} (heavy multi-cropping, or inflated fodder harvested
area) there is no fallow to add and the arable crops are scaled down to
\code{arable_ha} instead, the physical-container correction. Either way the
arable total equals FAO \code{arable_ha} by construction.
\item \strong{Perennial crops} (\code{Herb_Woody == "Woody"}) receive no fallow and are
scaled so their total equals FAO \code{permanent_ha}, preserving the within-group
physical pattern.
}

The default of \code{\link[=build_cropgrids_land_extension]{build_cropgrids_land_extension()}} and the footprint balance
are unchanged; this is an additive method.
}
\examples{
harvested <- tibble::tribble(
  ~year, ~area_code, ~item_cbs_code, ~harvested_ha,
  2020L, 1L, 2511L, 300, # wheat (arable)
  2020L, 1L, 2560L, 100 # coconuts (perennial)
)
base_extension <- tibble::tribble(
  ~year, ~area_code, ~item_cbs_code, ~impact_u,
  2020L, 1L, 2511L, 300,
  2020L, 1L, 2560L, 100
)
arable_permanent <- tibble::tribble(
  ~area_code, ~year, ~arable_ha, ~permanent_ha,
  1L, 2020L, 500, 100
)
items <- tibble::tribble(
  ~item_cbs_code, ~Herb_Woody,
  2511L, "Herbaceous",
  2560L, "Woody"
)
build_fao_arable_fallow_extension(
  harvested, arable_permanent, base_extension,
  items_prod_full = items
)
}
