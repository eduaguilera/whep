% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/footprint_balance.R
\name{compute_footprint_balance}
\alias{compute_footprint_balance}
\title{Compute land footprints by physical trade balance.}
\usage{
compute_footprint_balance(production, trade, extension)
}
\arguments{
\item{production}{Tibble with \code{area_code}, \code{item_cbs_code} and
\code{value} (quantity produced).}

\item{trade}{Tibble with \code{from_code}, \code{to_code},
\code{item_cbs_code} and \code{value} (quantity exported from \code{from_code}
to \code{to_code}).}

\item{extension}{Tibble with \code{area_code}, \code{item_cbs_code} and
\code{value} (direct land use of domestic production).}
}
\value{
A tibble with \code{area_code} (consuming country),
\code{item_cbs_code}, \code{value} (embodied land in consumption) and
\code{method} (\code{"land_balance"}).
}
\description{
Estimate consumption-based land footprints by propagating direct
land use through the bilateral physical trade network, an
approach independent of the Leontief inverse used by
\code{\link[=compute_footprint]{compute_footprint()}}. Each country's supply pool (production
plus imports) carries an embodied-land intensity \code{s}; solving
the balance \eqn{(D - M) s = L} per item gives \code{s}, where \code{D} is
diagonal supply throughput, \code{M} routes import intensities, and
\code{L} is direct land. The footprint of consumption (supply minus
exports) is then \code{consumption * s}.

Unlike the multi-regional input-output method, this tracer
handles only the trade dimension, not inter-product processing
transformations. Comparing the two with
\code{\link[=compare_footprint_methods]{compare_footprint_methods()}} isolates the effect of that
assumption -- an apples-to-apples stress test on shared FAOSTAT
data.
}
\examples{
production <- tibble::tibble(
  area_code = c(1L, 2L),
  item_cbs_code = c(10L, 10L),
  value = c(100, 0)
)
trade <- tibble::tibble(
  from_code = 1L, to_code = 2L, item_cbs_code = 10L, value = 40
)
extension <- tibble::tibble(
  area_code = c(1L, 2L),
  item_cbs_code = c(10L, 10L),
  value = c(50, 0)
)
compute_footprint_balance(production, trade, extension)
}
