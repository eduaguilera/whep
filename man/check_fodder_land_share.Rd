% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arable_permanent_land.R
\name{check_fodder_land_share}
\alias{check_fodder_land_share}
\title{Check how much arable land the fallow split attributes to fodder crops.}
\usage{
check_fodder_land_share(
  extension,
  threshold = 0.5,
  items_prod_full = whep::items_prod_full
)
}
\arguments{
\item{extension}{Tibble of the arable/permanent land extension with columns
\code{year}, \code{area_code}, \code{item_cbs_code} and \code{impact_u}, as returned by
\code{\link[=build_fao_arable_fallow_extension]{build_fao_arable_fallow_extension()}}.}

\item{threshold}{Share of arable land above which a \verb{(year, area_code)} is
flagged (default \code{0.5}). Fodder is real land use, so a moderate share is
expected; half a country's arable land is not.}

\item{items_prod_full}{Crosswalk used to classify \code{item_cbs_code} as
perennial via \code{Herb_Woody}. Defaults to \link{items_prod_full}.}
}
\value{
A tibble with one row per \verb{(year, area_code)} that has at least one
arable row in \code{extension}, ordered by descending \code{fodder_share}:
\itemize{
\item \code{year}, \code{area_code}: the country-year.
\item \code{fodder_ha}: arable land attributed to fodder items.
\item \code{arable_ha}: total arable (non-perennial) land attributed.
\item \code{fodder_share}: \code{fodder_ha / arable_ha} (\code{NA} when \code{arable_ha} is zero).
\item \code{flagged}: \code{TRUE} when \code{fodder_share > threshold}.
}
}
\description{
Report, per \verb{(year, area_code)}, the share of the reconciled arable land
extension that lands on the FAOSTAT fodder items (commodity-balance items of
\code{Cat_1 == "Fodder_green"} that are not grass, i.e. \code{2000}-\code{2003}).

With \code{fallow_weights = NULL} — the default, and the path the land-balance
footprint (\code{\link[=build_land_balance_footprint]{build_land_balance_footprint()}}) takes —
\code{\link[=build_fao_arable_fallow_extension]{build_fao_arable_fallow_extension()}} rescales every arable crop's cropped
physical area proportionally up to FAO Arable land. Each crop's share of the
output is therefore exactly its share of the input harvested area, so an
inflated fodder harvested area is passed straight through into the published
per-crop arable land footprint, taking land away from the ordinary crops
(whep#356). Fodder harvested area is reconstructed rather than surveyed
(dry-matter yield imputation, EU AgriDB splicing, linear filling), so it is
the term most likely to be wrong, and in some country-years it alone exceeds
the country's whole FAO arable land.

This is a diagnostic, not a correction: nothing is rescaled or dropped. It
flags where the attribution is implausible so the fodder reconstruction can
be inspected, or agro-climatic \code{fallow_weights} (see
\code{\link[=gridded_fallow_weights]{gridded_fallow_weights()}}) supplied instead.
}
\examples{
extension <- tibble::tribble(
  ~year, ~area_code, ~item_cbs_code, ~impact_u,
  2000L, 10L, 2003L, 700, # fodder mix
  2000L, 10L, 2511L, 300 # wheat
)
check_fodder_land_share(extension)
}
