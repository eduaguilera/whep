% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_npp.R
\name{calculate_npp_carbon_nitrogen}
\alias{calculate_npp_carbon_nitrogen}
\title{Partition crop and weed NPP into dry matter, carbon and nitrogen.}
\usage{
calculate_npp_carbon_nitrogen(x)
}
\arguments{
\item{x}{A tibble with \code{item_prod_code}, \code{product_dm_t}, \code{residue_dm_t} and
\code{root_dm_t}. Optional \code{weed_ag_dm_t} (above-ground weed dry matter; treated
as 0 when absent), \code{crop_npp_dm_t} (kept when present) and
\code{residue_soil_dm_t} (enables the soil-residue nitrogen and carbon split).}
}
\value{
The input tibble with weed dry matter, and nitrogen (\verb{*_n_t}) and
carbon (\verb{*_c_t}) for product, residue, root, weeds, crop NPP and total NPP.
}
\description{
Converts crop NPP components (product, residue, root) and weed biomass to
nitrogen and carbon using the \code{bio_coefs} per-component coefficients and the
\code{weed_coefs} scalars. Root and weed below-ground nitrogen include
rhizodeposits. The residue-to-soil split is computed only when a
\code{residue_soil_dm_t} column (from \code{\link[=calculate_residue_destinies]{calculate_residue_destinies()}}) is present.
}
\examples{
tibble::tibble(item_prod_code = "15", production_t = 100, area_ha = 40) |>
  calculate_crop_npp() |>
  calculate_npp_carbon_nitrogen()
}
