% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_prov_destiny.R
\name{build_food_protein_destiny}
\alias{build_food_protein_destiny}
\title{Per-capita food protein from the Spain N destiny flows}
\usage{
build_food_protein_destiny(
  destiny_df,
  population,
  protein_basis = c("edible_portion", "whole_commodity")
)
}
\arguments{
\item{destiny_df}{Output of \code{create_n_prov_destiny()} (per province) or
\code{create_n_nat_destiny()} (national, \code{province_name} is \code{"Spain"}).}

\item{population}{A tibble with \code{year}, \code{province_name}, \code{population}
(head count) -- e.g. \code{whep_read_file("population_yg")} renamed from
\code{Year}, \code{Province_name}, \code{Pop_Mpeop_yg} (which is in millions, so needs
\verb{* 1e6}), or \code{read_population()} filtered to Spain for the national
case.}

\item{protein_basis}{\code{"edible_portion"} (default) sums only
\code{population_food}, matching \code{build_food_supply()}'s default and its best
agreement with FAOSTAT. \code{"whole_commodity"} adds
\code{population_food_inedible} back in, reconstructing the pre-split total.}
}
\value{
A tibble keyed by \code{year}, \code{province_name} with
\code{protein_g_cap_day} and \code{method_protein_basis}.
}
\description{
Converts the \code{population_food} nitrogen from \code{create_n_prov_destiny()} /
\code{create_n_nat_destiny()} into per-capita protein supply (nitrogen times
6.25), for comparison against FAOSTAT (\code{build_food_supply()}).

\code{population_food} is already edible-basis: \code{.split_food_inedible_loss()}
(applied inside both builders) moves the inedible fraction of every food
item into its own \code{population_food_inedible} destiny, so the two bases
this function offers are a plain choice of which destinies to sum, not a
second application of \code{Edible_portion}. Applying \code{Edible_portion} again
here would double-discount.
}
\examples{
# The head count is a round illustrative value, not a historical figure;
# the year and province are picked to match the toy destiny fixture.
population <- tibble::tibble(
  year = 1862,
  province_name = "Huesca",
  population = 250000
)
build_food_protein_destiny(
  create_n_prov_destiny(example = TRUE),
  population
)
build_food_protein_destiny(
  create_n_prov_destiny(example = TRUE),
  population,
  protein_basis = "whole_commodity"
)
}
