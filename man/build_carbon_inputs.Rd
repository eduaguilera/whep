% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carbon_inputs.R
\name{build_carbon_inputs}
\alias{build_carbon_inputs}
\title{Assemble the per-land-use-class soil carbon inputs.}
\source{
Cropland inputs from \code{\link[=build_soil_carbon_inputs]{build_soil_carbon_inputs()}}; grassland and
natural inputs from \code{\link[=build_grass_natural_carbon_inputs]{build_grass_natural_carbon_inputs()}}; assembled per
the WHEP historical carbon-balance design.
}
\usage{
build_carbon_inputs(
  resolution = c("grid", "polity"),
  data = list(),
  years = NULL,
  example = FALSE
)
}
\arguments{
\item{resolution}{\code{"grid"} (default, per cell and class) or \code{"polity"}
(aggregated to \code{area_code}, area-weighting the cropland density by the
polity crop area).}

\item{data}{Named list of pre-loaded inputs, each falling back to its builder
when absent: \code{cropland} (the \code{\link[=build_soil_carbon_inputs]{build_soil_carbon_inputs()}} output, per cell,
crop and year, with \code{total_c_input_mgc_ha_yr} and \code{humified_fraction});
\code{crop_area} (per cell, crop and year harvested area with columns \code{lon},
\code{lat}, \code{area_code}, \code{item_prod_code}, \code{year}, \code{crop_area_ha}, used to
area-weight the crop densities); \code{grass_natural} (the
\code{\link[=build_grass_natural_carbon_inputs]{build_grass_natural_carbon_inputs()}} output at the class grain); and
optional \code{land_use} (per-cell class \code{area_ha}, used to area-weight
grassland/natural polity output); and \code{country_grid}, the polycell support
resolved to one row per cell and \code{area_code}, from which \code{crop_area} is
derived when absent. It is the same support \code{\link[=build_soil_carbon_inputs]{build_soil_carbon_inputs()}}
reads, so the weights and the carbon they weight can never come from two
different crosswalks. When
\code{cropland} or \code{grass_natural} are absent the respective builder is called
with the remaining members of \code{data}.}

\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
(default) keeps every year the inputs cover. Threaded into the default
\code{\link[=build_soil_carbon_inputs]{build_soil_carbon_inputs()}} and \code{\link[=build_grass_natural_carbon_inputs]{build_grass_natural_carbon_inputs()}}
builders so their readers slice to the requested years; ignored for inputs
supplied via \code{data}.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble keyed by \verb{(lon, lat, area_code, year, land_use)} at \code{"grid"}
resolution (or \verb{(area_code, year, land_use)} at \code{"polity"}), with
\code{c_input_mgc_ha_yr}, \code{humified_fraction} and \code{method_c_input}, for
\code{land_use} in \code{"cropland"}, \code{"grassland"} and \code{"natural"}, plus the polity
columns below.
}
\description{
Build the carbon-input layer \code{\link[=build_carbon_balance]{build_carbon_balance()}} consumes, keyed by
\verb{(lon, lat, area_code, year, land_use)}. The cropland class aggregates the
per-crop cropland inputs from \code{\link[=build_soil_carbon_inputs]{build_soil_carbon_inputs()}} to the class
grain: the class carbon density is the harvested-area-weighted mean of the
per-crop densities, and the humification fraction is the carbon-mass-weighted
mean of the per-crop fractions (mass = density times area). The grassland and
natural classes come from \code{\link[=build_grass_natural_carbon_inputs]{build_grass_natural_carbon_inputs()}} unchanged.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_carbon_inputs(example = TRUE)
}
