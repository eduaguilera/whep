% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/footprint_balance.R
\name{build_land_balance_footprint}
\alias{build_land_balance_footprint}
\title{Build a consumption land footprint by physical trade balance.}
\usage{
build_land_balance_footprint(
  year,
  production = NULL,
  trade = NULL,
  extension = NULL,
  example = FALSE
)
}
\arguments{
\item{year}{Year to build the footprint for.}

\item{production}{Optional production tibble
(\code{area_code}, \code{item_cbs_code}, \code{value}); built when \code{NULL}.}

\item{trade}{Optional trade tibble (\code{from_code}, \code{to_code},
\code{item_cbs_code}, \code{value}); built when \code{NULL}.}

\item{extension}{Optional direct-land tibble (\code{area_code},
\code{item_cbs_code}, \code{value}); built when \code{NULL}.}

\item{example}{If \code{TRUE}, return a small example output without
downloading remote data. Default is \code{FALSE}.}
}
\value{
A tibble as returned by \code{\link[=compute_footprint_balance]{compute_footprint_balance()}}.
}
\description{
End-to-end land-balance footprint for one year on real WHEP data:
assemble production (primary crop production plus grass dry-matter
derived from grassland area times yield), the bilateral trade
network, and the crop-plus-grassland direct-land extension, then
trace land to consumers with \code{\link[=compute_footprint_balance]{compute_footprint_balance()}}. This
is the independent, non-Leontief estimator for stress-testing the
multi-regional input-output footprint via
\code{\link[=compare_footprint_methods]{compare_footprint_methods()}}.

The crop side is \code{\link[=build_fao_arable_fallow_extension]{build_fao_arable_fallow_extension()}} with
\code{unsupported_target = "unallocated"}, pinned explicitly: FAO land that no
crop can be named for arrives on an \code{item_cbs_code} of \code{NA}, which the
balance cannot route, so it is reported by the orphan-land warning rather
than folded into a crop (whep#1026).

Its temporary-grassland netting basis is pinned explicitly too, to
\code{temp_grassland_basis = "modelled"}: modelled CBS 3002 covers 26 EU polities
over 2001-2019 only, so for any other country, and for every country from
2020, nothing is netted and the arable land keeps FAO's temporary meadows
(whep#937). The balance output does not carry that provenance per row; read
\code{temp_grassland_source} in \code{\link[=build_fao_arable_fallow_extension]{build_fao_arable_fallow_extension()}}'s output,
or \code{\link[=check_arable_composition]{check_arable_composition()}}, to see which country-years were netted.

Grass items (\code{item_cbs_code} 3000 and 3002) are barely traded, so
their land stays with the producing country: the balance, unlike
the input-output model, does not route grass through the
grass-to-livestock chain. Any \code{production}, \code{trade} or \code{extension}
supplied explicitly is used as-is instead of being built, which is
useful for reusing cached inputs or for testing.
}
\examples{
build_land_balance_footprint(example = TRUE)
}
