% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_model.R
\name{build_io_model}
\alias{build_io_model}
\title{Build multi-regional input-output model.}
\usage{
build_io_model(
  supply_use = build_supply_use(),
  bilateral_trade = get_bilateral_trade(),
  cbs = get_wide_cbs(),
  years = NULL
)
}
\arguments{
\item{supply_use}{Tibble from \code{\link[=build_supply_use]{build_supply_use()}}. By default,
this function calls \code{\link[=build_supply_use]{build_supply_use()}} internally. Must have
columns: \code{year}, \code{area_code}, \code{proc_group}, \code{proc_cbs_code},
\code{item_cbs_code}, \code{type}, \code{value}.}

\item{bilateral_trade}{Tibble from \code{\link[=get_bilateral_trade]{get_bilateral_trade()}}. By
default, this function calls \code{\link[=get_bilateral_trade]{get_bilateral_trade()}}
internally. Must have columns: \code{year}, \code{item_cbs_code},
\code{bilateral_trade} (list-column of matrices).}

\item{cbs}{Tibble from \code{\link[=get_wide_cbs]{get_wide_cbs()}}. By default, this
function calls \code{\link[=get_wide_cbs]{get_wide_cbs()}} internally. Must have
columns: \code{year}, \code{area_code}, \code{item_cbs_code}, \code{production},
\code{import}, \code{export}, \code{stock_withdrawal}, \code{stock_addition},
plus final demand columns (\code{food}, \code{other_uses}).}

\item{years}{Numeric vector of years to compute, or NULL.
If NULL, computes all years in the intersection of
available data across inputs. If specified, must be
a subset of available years.}
}
\value{
A tibble with one row per year and list-columns:
\itemize{
\item \code{Z}: Inter-industry flow matrix (product-by-product).
\item \code{Y}: Final demand matrix.
\item \code{X}: Total output vector.
\item \code{labels}: Tibble mapping row/column indices to
\code{area_code} and \code{item_cbs_code}.
\item \code{fd_labels}: Tibble mapping each Y column to its
\code{area_code} (consuming country) and \code{fd_col} (demand
category, e.g. \code{"food"}) . Pass to
\code{\link[=compute_footprint]{compute_footprint()}} as \code{fd_labels} to get a
\code{target_fd} column in the footprint output.
}
}
\description{
Construct a multi-regional input-output (MRIO) model from
supply-use tables, bilateral trade, and commodity balance
sheets. Uses the industry technology assumption to derive
symmetric product-by-product tables.

The resulting matrices follow the FABIO methodology
(Bruckner et al., 2019). Rows and columns of \code{Z} represent
(country, item) pairs. Each entry \code{Z[i,j]} gives the
intermediate flow from sector \code{i} to sector \code{j}.
}
\examples{
su <- build_supply_use(example = TRUE)
btd <- get_bilateral_trade(example = TRUE)
cbs <- get_wide_cbs(example = TRUE)
build_io_model(su, btd, cbs)
}
