% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_shares_resolve.R
\name{resolve_admin_shares}
\alias{resolve_admin_shares}
\title{Resolve one source per container from the admin-shares table}
\usage{
resolve_admin_shares(
  shares,
  overrides = NULL,
  constraint_exclude = NULL,
  not_shipped = character()
)
}
\arguments{
\item{shares}{Admin-shares table, conforming to \code{\link[=admin_shares_schema]{admin_shares_schema()}}
except that \code{source} joins the key: a multi-source union is exactly
what resolution consumes, and the contract's key holds within one
source. Every row must have \code{treatment_year == "observed"}.}

\item{overrides}{Optional tibble of forced choices, with columns
\code{area_code}, \code{source} and optionally \code{item_prod_code} (\code{NA}, or the
column absent, means every item of that container). An item-specific
row beats a container-wide one. A row that never forces a winner
warns.}

\item{constraint_exclude}{Optional named list of years to withhold, keyed by
\code{area_code} as a character name, such as \code{list("840" = 1961:1989)}.
Those container-years leave the constraint set and are returned in
\code{excluded}, never silently dropped. This is the leave-years-out
validation switch.}

\item{not_shipped}{Character vector of \code{source} labels whose family is
withheld from the pin board (plan T23/T24); the coverage report flags
every resolved row whose source is one of them. Empty by default.}
}
\value{
A list of five tibbles:
\itemize{
\item \code{shares}: the winning rows, each exactly as supplied plus
\code{resolved_source}, \code{resolved_tier}, \code{resolved_grain} and
\code{resolution_rule} (one of \code{"override"}, \code{"grain"}, \code{"tier"},
\code{"run_length"}, \code{"source_name"}, \code{"single_candidate"}).
\item \code{seams}: the seam list described above.
\item \code{coverage}: the coverage report described above.
\item \code{excluded}: the rows \code{constraint_exclude} withheld, in the input's shape.
\item \code{dropped}: the losing rows, in the input's shape plus \code{drop_reason}
(\code{"indicator_never_binds"}, \code{"indicator_precedence"}, \code{"override"},
\code{"grain"}, \code{"tier"}, \code{"run_length"}, \code{"source_name"}).
}

Every input row appears in exactly one of \code{shares}, \code{excluded} and
\code{dropped}.
}
\description{
Choose, for every \verb{(area_code, level, item_prod_code, year)} the
admin-shares table covers, the single source whose reported units set
the within-country shape, and record why it won. Operates on \strong{observed}
rows only: gap filling is a separate step that runs after resolution, so
an \code{interpolated} or \code{carried} row here is an error, not an input.

The result is a list of five tibbles rather than one table with
attributes, because every piece is itself evidence a later task reads,
and an attribute does not survive a write to parquet or CSV.
}
\section{Precedence}{

Within one candidate group -- \verb{(area_code, level, item_prod_code, indicator_used, year)} -- each source is one candidate. Candidates are
ranked, best first, by:
\enumerate{
\item \strong{Override}: a source named for this container in \code{overrides} wins
wherever it is a candidate, and every group it reaches is recorded
\code{resolution_rule = "override"} -- including a group where the named
source had no rival, so an override's footprint can be read off the
coverage report. This is the per-country escape hatch the grain rule
requires: an override may keep a coarser source over a finer one.
\item \strong{Grain}, finer first: \verb{"admin1" < "admin2" < "admin3"}, compared
through \code{match()} on that vocabulary, exactly as
\code{\link[=admin_shares_schema]{admin_shares_schema()}} declares it. Grain beats tier, under decision
4 as amended at lock: public first at equal or finer grain, in-house
where no public source of equal grain exists. So Spain keeps its 53
NUTS-3 provinces (tier 2, \code{"admin2"}) over Eurostat's NUTS-2 (tier 1,
\code{"admin1"}). A candidate reporting at several grains at once is
ranked on its \strong{coarsest}, and warns.
\item \strong{Tier}, lower first.
\item \strong{Run length}, longer first: the contiguous run of that source
around that year, defined below.
\item \strong{Source name}, ascending in the C locale, with a warning naming the
tied sources. This is the deterministic last resort, never a
scientific rule.
}

Before any of that, the indicator in force is chosen per \verb{(area_code, level, item_prod_code, year)} by decision 8's order of acceptance --
\code{"area_harvested"}, \code{"area_planted_or_sown"}, \code{"area_main"},
\code{"area_cultivated"}, then \code{"production"} only where no area exists.
Rows of a losing indicator go to \code{dropped} with \code{drop_reason = "indicator_precedence"}. \code{"yield"} is in the table's vocabulary but
never binds an allocation (a share of a ratio is not a share), so a
\code{"yield"} row is dropped as \code{"indicator_never_binds"}.
}

\section{Contiguous run}{

The run length of source \code{s} at year \code{y} is the number of years in the
maximal block of \strong{consecutive} years containing \code{y} in which \code{s}
supplies at least one row for the same \verb{(area_code, level, item_prod_code, indicator_used)} -- the candidate group's columns except
\code{year}. So a source present in 1990:2000 has run length 11 at every one
of those years; if it is absent in 1997, its run length is 7 at 1996 and
3 at 1998. The run is measured on the rows that actually reach ranking,
after \code{constraint_exclude} and after indicator precedence: a long run of production
years says nothing about the continuity of an area series, and a year
withheld for validation is not evidence of coverage.
}

\section{Seams}{

One row per \verb{(area_code, level, item_prod_code)} series, seam year and
\code{seam_kind}:
\itemize{
\item \code{"start"}: the series' first resolved year. \code{value_to} is the source
it starts on.
\item \code{"source_switch"}, \code{"grain_switch"}, \code{"nuts_version_switch"},
\code{"indicator_switch"}: the resolved value differs from the previous
\strong{resolved} year of that series, which is the previous year present,
not necessarily \code{year - 1}.
\item \code{"coverage_change"}: the set of reporting units differs from the
previous resolved year -- a unit entering or leaving reporting.
}

A missing value and a present one count as a change; two missing values
do not.
}

\section{Coverage report}{

One row per \verb{(area_code, level, item_prod_code, year)}, carrying the
resolved source, tier, grain, indicator, NUTS version and rule, the
number of reporting units, the units themselves as a \code{"|"}-joined string
of \code{level_polity_code} sorted in the C locale (a string, not a
list-column, so the table writes to parquet and CSV unchanged;
unresolved units appear as \code{"<NA>"} and count as one member), and
\code{coverage_change}, and \code{not_shipped}, which marks resolved rows whose
source family is withheld from the pin board (T23).
}

\examples{
# Spain 2010: Eurostat NUTS-2 (tier 1, admin1) against the in-house
# NUTS-3 provinces (tier 2, admin2). Grain beats tier, so tier 2 wins.
rows <- tibble::tibble(
  area_code = 724L,
  level_polity_code = c("ES-N2-A", "ES-N2-B", "ES-N3-1", "ES-N3-2"),
  level = 1L,
  item_prod_code = 15L,
  indicator_used = "area_harvested",
  year = 2010L,
  value = c(100, 200, 120, 180),
  share = c(1 / 3, 2 / 3, 0.4, 0.6),
  source = rep(c("Eurostat_apro_cpshr", "ES_provinces"), each = 2),
  tier = rep(c(1L, 2L), each = 2),
  grain = rep(c("admin1", "admin2"), each = 2),
  concept_break = FALSE,
  nuts_version = rep(c("2021", "2016"), each = 2),
  source_native_id = NA_character_,
  source_native_name = NA_character_,
  source_id = rep(c("Eurostat_apro_cpshr", "ES_provinces"), each = 2),
  source_version = NA_character_,
  recorded_at = "2026-01-01T00:00:00Z",
  treatment_year = "observed",
  value_flag = NA_character_
)
resolved <- resolve_admin_shares(rows)
resolved$coverage
resolved$dropped
}
