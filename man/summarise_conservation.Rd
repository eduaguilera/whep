% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conservation.R
\name{summarise_conservation}
\alias{summarise_conservation}
\title{Summarise a footprint conservation report.}
\usage{
summarise_conservation(report)
}
\arguments{
\item{report}{Tibble returned by
\code{\link[=check_footprint_conservation]{check_footprint_conservation()}}.}
}
\value{
A one-row tibble with \code{n_origin}, \code{n_ok}, \code{n_flagged},
\code{n_dropped}, \code{n_under_traced}, \code{n_over_traced},
\code{total_direct}, \code{total_embodied} and
\code{global_rel_discrepancy}.
}
\description{
Roll up the per-origin report from
\code{\link[=check_footprint_conservation]{check_footprint_conservation()}} into a single global verdict.
}
\examples{
z_mat <- matrix(c(0, 5, 10, 0), nrow = 2)
x_vec <- c(100, 200)
y_mat <- matrix(c(85, 195), ncol = 1)
extensions <- c(50, 30)
labels <- tibble::tibble(
  area_code = c(1L, 1L),
  item_cbs_code = c(1L, 2L)
)
compute_footprint(
  x_vec = x_vec, y_mat = y_mat, extensions = extensions,
  labels = labels, z_mat = z_mat
) |>
  check_footprint_conservation(extensions, labels, x_vec) |>
  summarise_conservation()
}
