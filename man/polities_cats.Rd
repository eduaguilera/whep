% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\docType{data}
\name{polities_cats}
\alias{polities_cats}
\title{Polity categories and regional classifications}
\format{
A tibble where each row corresponds to one polity (country or territory).
It contains the following columns:
\itemize{
\item \code{legacy_polity_prefix}: Legacy current polity prefix, usually ISO 3166-1
alpha-3 (e.g., \code{"AFG"}, \code{"ALB"}). \strong{Not a \link{polities} code} and not a join
key to any polity table: it was called \code{polity_code} until whep#687, where
the name promised an identity none of its values holds. Read
\code{reporting_polity_code} for the polity.
\item \code{polity_name}: Current polity, country, or territory name.
\item \code{V1}: Internal row index from the source table.
\item \code{code}: Numeric FAOSTAT country code.
\item \code{polity_area_code}: Numeric WHEP reporting area code used in matrix
workflows.
\item \code{reporting_polity_code}: Current periodized WHEP polity code for \code{code}.
\item \code{reporting_polity_name}: Current WHEP polity name for \code{code}.
\item \code{reporting_polity_has_geometry}: Logical flag indicating whether the
current reporting polity has a polygon.
\item \code{iso3c}: ISO 3166-1 alpha-3 code (character; may duplicate
\code{legacy_polity_prefix} or differ for aggregates).
\item \code{FAOSTAT_name}: Country name as used in FAOSTAT.
\item \code{EU27}: Logical flag; \code{TRUE} if the polity is a member of the EU27.
\item \code{name}: Country name used in other external databases.
\item \code{eia}: Country name or code used by the US Energy Information
Administration (EIA).
\item \code{iea}: Country identifier used by the International Energy Agency (IEA).
\item \code{water_code}: Numeric code used in water statistics datasets.
\item \code{water_area}: Country/area name used in water statistics.
\item \code{baci}: Numeric BACI trade database country code.
\item \code{fish}: Numeric code used in fisheries datasets.
\item \code{region_code}: Numeric regional grouping code.
\item \code{cbs}: Logical flag; \code{TRUE} if the polity is included in the CBS
dataset.
\item \code{fabio_code}: Numeric country code used in the FABIO database.
\item \code{ADB_Region}: Asian Development Bank regional classification.
\item \code{region}: General world region (e.g., \code{"South Asia"},
\code{"Eastern Europe"}).
\item \code{uISO3c}: Numeric Unicode / UN M49 country code.
\item \code{Lassaletta}: Country grouping used in Lassaletta et al. nitrogen flow
studies.
\item \code{region_krausmann}: Regional grouping from Krausmann et al. biomass
flow accounting.
\item \code{region_HANPP}: Regional grouping used in human appropriation of net
primary production (HANPP) studies.
\item \code{region_krausmann2}: Alternative Krausmann regional grouping.
\item \code{region_UN_sub}: UN sub-regional classification (M49 sub-region).
\item \code{region_UN}: UN macro-regional classification (M49 region).
\item \code{region_ILO1}: ILO primary regional grouping.
\item \code{region_ILO2}: ILO secondary regional grouping.
\item \code{region_ILO3}: ILO tertiary regional grouping.
\item \code{region_IEA}: IEA regional grouping.
\item \code{region_IPCC}: IPCC regional grouping used in climate assessments.
\item \code{region_labour}: Labour-focused regional grouping.
\item \code{region_labour_agg}: Aggregated labour-focused regional grouping.
\item \code{region_labour_mech}: Labour mechanisation regional grouping. Angola
(code 7) holds the sub-region name \code{"Middle Africa"} rather than a
mechanisation class, inherited from \link{regions_full}; the other damaged row
there, Northern Mariana Islands (163), is not a polity, so this table has
no row to inherit it into. See \link{regions_full} (whep#855).
}
}
\source{
Compiled from \href{https://www.fao.org/faostat/}{FAOSTAT}, UN M49,
ILO, IEA, and other international statistical sources.
}
\usage{
polities_cats
}
\description{
Reference table for countries and political entities (polities) with
identifiers from multiple data sources and assignments to various regional
groupings used in the literature and international databases.
}
\note{
Derived from \link{regions_full} rather than vendored separately: the
198-code membership is read from \code{harmonization/polities_cats.csv} and every
column value comes from \code{regions_full}, so the two tables cannot disagree
except where this one deliberately folds an area into a rest-of-world
aggregate. Two areas are folded, both because they had no commodity balance
sheet when the table was compiled: Bhutan under \code{RASI} and Comoros under
\code{RAFR}, each with \code{cbs} \code{FALSE} and \code{fabio_code} \code{999}.
}
\section{Which regional groupings WHEP reads}{

The grouping columns are not all inputs to this package. Six have a consumer
in the tree, measured over \verb{R/}, \verb{data-raw/}, \verb{tests/}, \verb{vignettes/} and
\verb{inst/}: \code{region} (carried into \link{polity_area_crosswalk} at build time),
\code{region_krausmann} (the residue recovery rate, and the IPCC excreta regions
of \code{prepare_spatialize_all.R}), \code{region_HANPP} (the modern-variety adoption
share), \code{region_UN_sub} (the residue feed-use fraction, since whep#405),
\code{ADB_Region} (\link{build_primary_production} area keys) and \code{EU27} (the EU
aggregate of the FABIO comparison). \code{region_code} carries no information
\code{region} does not -- the two are a 1:1 relabelling -- so it needs no
consumer of its own.

The rest -- \code{Lassaletta}, \code{region_krausmann2}, \code{region_UN}, \code{region_ILO1},
\code{region_ILO2}, \code{region_ILO3}, \code{region_IEA}, \code{region_IPCC}, \code{region_labour},
\code{region_labour_agg}, \code{region_labour_mech} -- are published third-party
taxonomies shipped for downstream analysis and read by nothing in the
package. They are shipped as reference, and carry no promise of being
re-validated against their upstream taxonomy on release, so a consumer should
check the gap it inherits before keying anything by one (whep#386).

The gap the present-day taxonomies share is dissolved states. Over the 202
\code{cbs} reporters, \code{region_ILO1}, \code{region_ILO2}, \code{region_ILO3}, \code{region_IEA}
and \code{region_IPCC} are each \code{NA} for exactly the four federations WHEP still
books commodity balances for -- Czechoslovakia (51), Serbia and Montenegro
(186), the USSR (228) and the Yugoslav SFR (248) -- and complete everywhere
else; \code{region_UN} labels three of the four and leaves only Czechoslovakia
\code{NA}. \code{ROW} (999) carries an explicit \code{"RoW"} value in all of them rather
than \code{NA}. Grouping by one of these without deciding what to do with the
federations silently drops the pre-succession record. \code{region_UN_sub}, which
shares the gap and does have a consumer, is pinned against it in
\code{test_region_classifications.R}.

A \code{region_test} column with two values (\code{"Europe"}, \code{"Other"}) and no
consumer was dropped in whep#386.
}

\examples{
head(polities_cats)
}
\keyword{datasets}
