% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/footprint_balance.R
\name{melt_bilateral_trade}
\alias{melt_bilateral_trade}
\title{Melt a bilateral trade matrix to long format.}
\usage{
melt_bilateral_trade(bilateral_trade)
}
\arguments{
\item{bilateral_trade}{Tibble from \code{\link[=get_bilateral_trade]{get_bilateral_trade()}}, with
\code{year}, \code{item_cbs_code} and a \code{bilateral_trade} matrix
list-column.}
}
\value{
A tibble with \code{year}, \code{from_code}, \code{to_code},
\code{item_cbs_code} and \code{value}.
}
\description{
Convert the \code{bilateral_trade} list-column of \code{\link[=get_bilateral_trade]{get_bilateral_trade()}}
(one square origin-by-destination matrix per year and item) into
the tidy \code{from_code}/\code{to_code} long form consumed by
\code{\link[=compute_footprint_balance]{compute_footprint_balance()}}. Self-trade (diagonal) entries are
dropped.
}
\examples{
m <- matrix(
  c(0, 40, 0, 0),
  nrow = 2,
  dimnames = list(c("1", "2"), c("1", "2"))
)
bt <- tibble::tibble(
  year = 2010L, item_cbs_code = 10L, bilateral_trade = list(m)
)
melt_bilateral_trade(bt)
}
