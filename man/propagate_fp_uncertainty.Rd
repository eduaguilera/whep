% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/footprint_uncertainty.R
\name{propagate_fp_uncertainty}
\alias{propagate_fp_uncertainty}
\title{Propagate input uncertainty through a footprint.}
\usage{
propagate_fp_uncertainty(run_fn, extensions, cov = 0.1, options = list())
}
\arguments{
\item{run_fn}{Function taking a perturbed extension vector and
returning a footprint tibble with the grouping columns named
in \code{options$by} plus a \code{value} column. Wrap
\code{\link[=compute_footprint]{compute_footprint()}} with the other arguments fixed.}

\item{extensions}{Numeric vector of base extensions per sector.}

\item{cov}{Coefficient of variation of the extensions: one
number, or one per sector. Zero means no uncertainty.}

\item{options}{Named list overriding \code{n} (draws, default 200),
\code{probs} (lower/median/upper quantiles), \code{by} (grouping
columns) and \code{seed} (for reproducible draws).}
}
\value{
A tibble with the \code{by} columns plus \code{mean}, \code{sd}, \code{cv},
\code{q_low}, \code{q_med} and \code{q_high} per output cell.
}
\description{
Monte Carlo propagation of extension uncertainty: perturb the
extension vector with multiplicative lognormal noise (so values
stay non-negative and the expected factor is one), re-run the
footprint for each draw, and summarise the spread of each output
cell. A point estimate with no interval is not a trustworthy
result; this turns one into a distribution.
}
\examples{
run_fn <- function(ext) {
  tibble::tibble(
    target_area = 1L, target_item = 10L, value = sum(ext)
  )
}
propagate_fp_uncertainty(
  run_fn,
  extensions = c(60, 40),
  cov = 0.1,
  options = list(n = 100, seed = 1, by = c("target_area", "target_item"))
)
}
