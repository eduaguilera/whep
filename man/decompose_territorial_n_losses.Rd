% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{decompose_territorial_n_losses}
\alias{decompose_territorial_n_losses}
\title{Decompose total territorial N losses into compartments and mechanisms}
\usage{
decompose_territorial_n_losses(n_prov_destiny = NULL, raw = NULL)
}
\arguments{
\item{n_prov_destiny}{Nitrogen flows tibble from
\code{\link[=create_n_prov_destiny]{create_n_prov_destiny()}}, shared across all four compartments. If
\code{NULL}, loaded automatically.}

\item{raw}{Named list overriding any of the raw inputs shared across
compartments (\code{npp_ygpit}, \code{codes_coefs}, \code{intake_ygiac},
\code{n_excretion_ygs}, \code{stock_prod_ygps}, \code{livestock_units},
\code{population_yg}). Missing elements are loaded automatically.}
}
\value{
A named list with tibbles \code{detail} (per-compartment LMDI
output, with \code{compartment} and \code{mechanism} columns added),
\code{by_compartment}, and \code{by_mechanism} (each with \code{t0},
\code{contribution_mgn}, and \code{cumulative_mgn}).
}
\description{
Runs the cropland (\code{\link[=decompose_cropland_surplus]{decompose_cropland_surplus()}}), semi-natural
(\code{\link[=decompose_semi_natural_surplus]{decompose_semi_natural_surplus()}}), manure (\code{\link[=decompose_manure_losses]{decompose_manure_losses()}}),
and urban (\code{\link[=decompose_urban_losses]{decompose_urban_losses()}}) decompositions, then combines
them into two cumulative, year-on-year contribution series:
\itemize{
\item \code{by_compartment}: change in total territorial N losses attributed to
each of the four compartments.
\item \code{by_mechanism}: the same total change regrouped across compartments
into scale, intensification, and efficiency (population and
per-capita excretion are grouped under scale, since together they
represent total human N throughput). Cropland, semi-natural, and
manure no longer carry a spatial/destiny/species-mix factor (see
their own simplified decompositions), so no factor currently maps to
a "specialization" mechanism — that signal now lives only in
\code{\link[=decompose_specialization_covariance]{decompose_specialization_covariance()}} and
\code{\link[=decompose_crop_livestock_connectivity]{decompose_crop_livestock_connectivity()}}.
}
}
\examples{
# decompose_territorial_n_losses()
}
