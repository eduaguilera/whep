% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_npp.R
\name{calculate_crop_npp}
\alias{calculate_crop_npp}
\title{Estimate total crop net primary production.}
\usage{
calculate_crop_npp(
  x,
  residue_method = "ensemble",
  root_method = "ensemble",
  weights = list(w_ipcc = 0.5, w_ref = 0.5)
)
}
\arguments{
\item{x}{A tibble with \code{item_prod_code}, \code{production_t} and \code{area_ha}, plus
any optional adjustment columns used by the residue and root steps.}

\item{residue_method}{Residue method passed to \code{\link[=calculate_crop_residues]{calculate_crop_residues()}}.}

\item{root_method}{Root method passed to \code{\link[=calculate_crop_roots]{calculate_crop_roots()}}.}

\item{weights}{Named list of ensemble weights; \code{w_ipcc} for residues and
\code{w_ref} for roots (each 0-1, default 0.5).}
}
\value{
The input tibble with \code{product_dm_t}, \code{yield_dm_t_ha}, \code{residue_dm_t},
\code{root_dm_t}, \code{crop_npp_dm_t}, \code{method_residue} and \code{method_root}.
}
\description{
Assembles total crop net primary production (product plus residue plus root
dry matter) by running \code{\link[=calculate_crop_residues]{calculate_crop_residues()}} then
\code{\link[=calculate_crop_roots]{calculate_crop_roots()}}.
}
\examples{
calculate_crop_npp(
  tibble::tibble(item_prod_code = "15", production_t = 100, area_ha = 40)
)
}
