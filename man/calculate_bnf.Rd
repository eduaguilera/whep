% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnf.R
\name{calculate_bnf}
\alias{calculate_bnf}
\title{Estimate total biological nitrogen fixation.}
\usage{
calculate_bnf(
  x,
  symbiotic_params = list(),
  nonsymbiotic_params = list(),
  soil_params = list()
)
}
\arguments{
\item{x}{A tibble carrying the required columns of all three component
functions, optionally with a \code{climate_type} column.}

\item{symbiotic_params, nonsymbiotic_params, soil_params}{Named lists passed to
the component functions (see those functions).}
}
\value{
The input tibble with all component columns plus \code{fert_type} (\code{"BNF"})
and \code{bnf_t} (total BNF).
}
\description{
Sums the three BNF components: symbiotic crop legumes, symbiotic weeds/cover
crops, and non-symbiotic free-living fixation, by running
\code{\link[=calculate_crop_bnf]{calculate_crop_bnf()}}, \code{\link[=calculate_weed_bnf]{calculate_weed_bnf()}} and
\code{\link[=calculate_nonsymbiotic_bnf]{calculate_nonsymbiotic_bnf()}}. When a \code{climate_type} column is present, the
climate-specific parameters from \code{bnf_climate_params} override the relevant
defaults per climate type.
}
\details{
The weed component uses the \code{weed_npp_n_t} already present in \code{x}. In the
standard crop-NPP chain this is non-zero only when
\code{\link[=calculate_crop_npp_components]{calculate_crop_npp_components()}} has been run, or when callers supply weed
NPP directly; \code{\link[=calculate_npp_carbon_nitrogen]{calculate_npp_carbon_nitrogen()}} treats missing weed biomass as
zero.
}
\examples{
calculate_bnf(
  tibble::tibble(
    item_prod_code = "176", crop_npp_n_t = 10, product_n_t = 5,
    weed_npp_n_t = 4, land_use = "Cropland", legumes_seeded = 0,
    seeded_cover_crop_share = 0, area_ha = 40
  )
)
}
