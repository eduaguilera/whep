% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{plot_compart_factor}
\alias{plot_compart_factor}
\title{Plot each compartment's own factor breakdown}
\usage{
plot_compart_factor(
  cropland = NULL,
  semi_natural = NULL,
  manure = NULL,
  urban = NULL
)
}
\arguments{
\item{cropland}{A tibble from \code{\link[=decompose_cropland_surplus]{decompose_cropland_surplus()}}. If
\code{NULL}, computed automatically (slow).}

\item{semi_natural}{A tibble from \code{\link[=decompose_semi_natural_surplus]{decompose_semi_natural_surplus()}}.
If \code{NULL}, computed automatically (slow).}

\item{manure}{A tibble from \code{\link[=decompose_manure_losses]{decompose_manure_losses()}}. If \code{NULL},
computed automatically (slow).}

\item{urban}{A tibble from \code{\link[=decompose_urban_losses]{decompose_urban_losses()}}. If \code{NULL},
computed automatically (slow).}
}
\value{
A named list with ggplot objects \code{cropland}, \code{semi_natural},
\code{manure}, and \code{urban}.
}
\description{
Plots panels 3-6 of the composite decomposition figure described in the
decomposition proposal (section 14): the cumulative, year-on-year
contribution of each factor \emph{within} one compartment's own
decomposition (e.g. Cropland's Size/Intensity/Inefficiency), as
opposed to the AFS-wide panels 1-2 from
\code{\link[=plot_loss_decomp]{plot_loss_decomp()}}, which only show each compartment's
total contribution.
}
\examples{
# Each argument is a calculate_lmdi() table; the same shape is reused for
# all four compartments here.
lmdi <- tibble::tribble(
  ~period, ~factor_label, ~component_type, ~additive,
  "1861-1862", "N surplus", "target", 20000,
  "1861-1862", "Size", "factor", 9000,
  "1861-1862", "Intensity", "factor", 7000,
  "1861-1862", "Inefficiency", "factor", 4000,
  "1862-1863", "N surplus", "target", 10000,
  "1862-1863", "Size", "factor", 5000,
  "1862-1863", "Intensity", "factor", 3000,
  "1862-1863", "Inefficiency", "factor", 2000
)
plots <- plot_compart_factor(lmdi, lmdi, lmdi, lmdi)
}
