% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_footprint.R
\name{align_extension}
\alias{align_extension}
\title{Align an extension table to input-output sector labels.}
\usage{
align_extension(extension, labels, year, value_col = "impact_u")
}
\arguments{
\item{extension}{Long-format extension tibble with \code{year}, \code{area_code},
\code{item_cbs_code} and the column named by \code{value_col}.}

\item{labels}{One year's \code{labels} tibble from \code{\link[=build_io_model]{build_io_model()}}, with
\code{area_code}, \code{item_cbs_code} and \code{index} columns.}

\item{year}{Year to select from \code{extension}.}

\item{value_col}{Name of the extension magnitude column, \code{"impact_u"} by
default.}
}
\value{
A numeric vector with one entry per row of \code{labels}, ordered by the
label \code{index}.
}
\description{
Turn a long-format extension table into the dense per-sector numeric vector
that \code{\link[=compute_footprint]{compute_footprint()}} expects, ordered to match a single year's
\code{labels} from \code{\link[=build_io_model]{build_io_model()}}. Sectors absent from the extension are
filled with zero, and extension rows outside the model are dropped. Rows
sharing an \verb{(area_code, item_cbs_code)} are summed.
}
\examples{
extension <- tibble::tibble(
  year = 2000L,
  area_code = 1L,
  item_cbs_code = 10L,
  impact_u = 5
)
labels <- tibble::tibble(
  index = 1:2,
  area_code = c(1L, 1L),
  item_cbs_code = c(10L, 20L)
)
align_extension(extension, labels, 2000L)
}
