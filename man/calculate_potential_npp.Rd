% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_npp.R
\name{calculate_potential_npp}
\alias{calculate_potential_npp}
\title{Estimate potential net primary production.}
\usage{
calculate_potential_npp(
  x,
  method = c("lpjml", "miami", "nceas", "rosenzweig"),
  lpjml = list()
)
}
\arguments{
\item{x}{A tibble. The climate methods require \code{temp_c} (mean annual
temperature, degrees C), \code{water_input_mm} (precipitation plus irrigation)
and \code{aet_mm} (actual evapotranspiration). The \code{lpjml} method instead needs
a spatial key to join the gridded grass NPP (see \code{lpjml}).}

\item{method}{Potential-NPP method, one of \code{"lpjml"} (default), \code{"miami"},
\code{"nceas"} or \code{"rosenzweig"}.}

\item{lpjml}{A named list of options for the \code{lpjml} method (passed to the
LPJmL grass reader). Ignored by the climate methods.}
}
\value{
The input tibble with \code{npp_potential_dm_t_ha} (potential NPP, t DM per
hectare) and \code{method_npp_potential} (the method used).
}
\description{
Estimates potential net primary production per unit area, used downstream to
scale weed biomass on cropland. Several methods are available; the default
\code{lpjml} reads gross managed-grassland NPP from a finished LPJmL run, while the
climate methods compute potential NPP from temperature, water input and
actual evapotranspiration.
}
\examples{
calculate_potential_npp(
  tibble::tibble(temp_c = 15, water_input_mm = 800, aet_mm = 700),
  method = "miami"
)
}
