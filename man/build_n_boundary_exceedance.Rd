% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_boundary_exceedance.R
\name{build_n_boundary_exceedance}
\alias{build_n_boundary_exceedance}
\title{Build source-exact gridded critical-nitrogen exceedance.}
\usage{
build_n_boundary_exceedance(
  surplus = NULL,
  critical = NULL,
  land_use = c("ara", "all", "igl"),
  resolution = c("grid", "cell", "polity", "country", "image_region"),
  metric = c("surplus", "input", "new_fixation"),
  cell_polity = NULL,
  allocation_scenario = c("yield_gap", "no_increase", "new_fixation"),
  actual_year = NULL,
  critical_reference_year = NULL,
  actual = NULL,
  boundary = NULL,
  indicator = NULL,
  land_class = NULL,
  impact_scope = NULL,
  grassland_split = c("image_density", "none"),
  grassland = list(classes = NULL, extensive_budget = NULL, critical_ara = NULL,
    critical_igl = NULL),
  negative_critical = c("keep", "clamp"),
  binding = NULL,
  example = FALSE
)
}
\arguments{
\item{surplus}{A \code{\link[=calculate_n_surplus]{calculate_n_surplus()}} output with the grid/crop/year key.
Surplus mode uses signed \code{surplus_n_t} when present, otherwise derives it
from \code{surplus_kgn_ha * area_ha / 1000}. Input mode uses
\code{n_input_std_t}.}

\item{critical}{A \code{\link[=read_critical_n]{read_critical_n()}} critical layer. In addition to layer
identity, it must carry deposited \code{source_area_ha} and \code{image_region}.}

\item{land_use}{Source land class: \code{"ara"}, \code{"igl"}, or \code{"all"}.}

\item{resolution}{Output grain: source \code{"cell"}, crop-cell \code{"grid"}, crop
\code{"polity"}/\code{"country"}, or crop \code{"image_region"}.}

\item{metric}{Selected actual pressure: signed \code{"surplus"} or total
agricultural \code{"input"}.}

\item{cell_polity}{Retained for API compatibility. IMAGE membership now
comes from the deposited cell-key crosswalk in \code{critical}; country
attribution comes from the crop rows themselves.}

\item{allocation_scenario}{Only source-exact \code{"yield_gap"} is supported.
\code{"no_increase"} and \code{"new_fixation"} hard-error.}

\item{actual_year}{The actual-pressure year to compare. Must select exactly
one year from \code{surplus} and is always retained in results.}

\item{critical_reference_year}{Must be \code{2010}, matching the deposited fixed
reference surface, and is always retained in results.}

\item{actual}{Alias of \code{surplus} for the selector-oriented interface.}

\item{boundary}{Alias of \code{critical} for the selector-oriented interface.}

\item{indicator}{Selector-oriented pressure name: \code{"surplus"},
\code{"total_input"}, or the unsupported \code{"new_fixation"} mode (which
hard-errors). When supplied, it overrides \code{metric}.}

\item{land_class}{Alias of \code{land_use}.}

\item{impact_scope}{Deposited impact surface: \code{"mi"}, \code{"sw"}, \code{"gw"}, or
\code{"de"}. When supplied, it is validated against the critical layer.}

\item{grassland_split}{Grassland treatment under \code{land_use = "all"}:
\code{"image_density"} (default) splits each cell into a managed and an
extensive component (see the section below); \code{"none"} compares one cell
pressure with the deposited allowance, as before the split existed.
Ignored for \code{"ara"} and \code{"igl"}. Under \code{"image_density"},
\code{metric = "new_fixation"} aborts: the archive has no extensive budget for
it.}

\item{grassland}{Named list of the inputs the split needs, all required
under \code{grassland_split = "image_density"} (their absence aborts; there is
no fallback): \code{classes}, the \code{build_grassland_intensity_classes()} table
(one row per cell and year: \code{cell_id}, \code{lon}, \code{lat}, \code{year},
\code{country_2010}, \code{image_region}, \code{a_crop_ha}, \code{grass_ha_image},
\code{whep_grass_ha}, \code{image_class_2010}, \code{grassland_class},
\code{method_grassland_split}), and \code{extensive_budget}, IMAGE's 2010
extensive-grassland budget per cell (\code{cell_id}, \code{ext_input_kgn_ha},
\code{ext_surplus_kgn_ha}); and \code{critical_ara} and \code{critical_igl}, the
\code{\link[=read_critical_n]{read_critical_n()}} layers with \code{land_use = "ara"} and \code{"igl"} for the
same threshold and metric as \code{critical} (validated). \code{critical} itself
still defines the cell domain and is checked against the class table's
areas. A cell absent from \code{classes} must carry no grassland pressure; it
is compared as cropland only.}

\item{negative_critical}{Treatment of a cell whose critical value is below
zero. \code{"keep"} (default) compares the actual pressure with the deposited
value as it is, as the source does. \code{"clamp"} sets it to zero (a zero
allowance) before the cell comparison. The choice is stamped in every
output row as \code{negative_critical}; see the Negative critical surplus
section.}

\item{binding}{Optional \code{\link[=build_critical_n_binding]{build_critical_n_binding()}} output for the same
land-use scope. When supplied, its per-cell \code{binding_threshold} and
\code{binding_matches_mi} are carried into the cell and grid results; when
\code{NULL} (default) both columns are \code{NA}.}

\item{example}{If \code{TRUE}, return the package fixture.}
}
\value{
A tibble at the requested grain. Cell results retain actual and
critical masses, signed margin, positive overshoot, coverage state,
integer source-grid key, IMAGE context, explicit years, selectors, and
provenance. \code{critical_kgn_ha} is the value compared (after the
\code{negative_critical} treatment) and \code{source_critical_kgn_ha} the deposited
one; the two differ only in clamped cells. Crop results additionally
retain the signed pressure share and crop-attributed quantities, which
reconcile algebraically to the cell. \code{exceedance_n_t} is the crop's share
of the cell overshoot \code{pmax(actual - critical, 0)} and
\code{within_boundary_n_t} is \code{actual_n_t - exceedance_n_t}, so the two always
sum to the actual pressure. Summed over a cell, \code{within_boundary_n_t} is
\code{min(actual, critical)}: under \code{negative_critical = "keep"} it is negative
wherever the critical value is negative, and under \code{"clamp"} it is
negative only where the actual pressure itself is. Under the grassland
split these hold per component (managed, extensive) rather than per cell.
Cell and grid results also carry the split components:
\code{managed_actual_n_t}, \code{managed_critical_n_t},
\code{managed_positive_overshoot_n_t}, \code{extensive_actual_n_t},
\code{extensive_critical_n_t}, \code{extensive_positive_overshoot_n_t}, their areas
(\code{managed_area_ha}, \code{extensive_area_ha}), compared rates
(\code{managed_critical_kgn_ha}, \code{extensive_critical_kgn_ha}, after the
\code{negative_critical} treatment) and the rates before it
(\code{source_managed_critical_kgn_ha}, \code{source_extensive_critical_kgn_ha}),
coverage states, \code{excluded_actual_n_t} (of which
\code{excluded_igl_actual_n_t} is intensive grassland without an \code{"igl"}
rate), \code{grassland_class}, \code{method_allowance_managed},
\code{method_allowance_extensive} and \code{method_grassland_split} (the per-cell
class method, \code{"no_grassland"} for a cell outside the class table,
\code{"none"} without the split). These are \code{NA} when the split is not
applied, so the schema does not depend on it. Under the split,
\code{critical_kgn_ha} and \code{source_critical_kgn_ha} remain the \code{"all"}-scope
surface (it defines the domain and checks the \code{"ara"}/\code{"igl"} layers);
the cell allowance is the sum of the component allowances. Grid rows add
\code{boundary_component} (\code{"managed"}/\code{"extensive"}, \code{NA} without the
split); within the split, \code{pressure_share} is the row's share of its
component, and \code{binding_threshold}/\code{binding_matches_mi} are \code{NA} on
extensive rows (see the Negative critical surplus section). Every row
carries the call-level \code{grassland_split}; aggregated rows list the
per-cell methods they span in \code{method_grassland_split}, separated by \verb{;}.
}
\description{
Compares WHEP crop nitrogen pressure with the deposited Schulte-Uebbing et
al. (2022) 2010 yield-gap critical surface. The calculation first aggregates
every crop and polity contribution in a source cell, compares that one cell
pressure with one critical allowance, and only then attributes the cell
allowance, signed margin and positive overshoot back to crops. Total-input
attribution uses crop input shares; surplus attribution uses signed crop
surplus shares. Signed surplus shares can be negative or greater than one.

The critical layer must carry its deposited \code{source_area_ha} and
\code{image_region}. \code{resolution = "cell"} returns one row per source cell and
year. Other resolutions return the approved crop attribution. Exactly zero
or numerically near-zero pressure denominators keep the complete cell result
but allocate no crop share; an explicit \code{cell_residual} record carries the
unallocated critical allowance, signed margin and positive overshoot. APIs
requiring complete crop attribution hard-error rather than fabricate a
fallback.

Actual-pressure rows naming no crop cannot meet a critical allowance and are
excluded before the cell comparison. The exclusion is reported, never
silent: a message names the rows and the pressure they carried when that
pressure is zero (the only case a gridded \code{\link[=build_nitrogen_balance]{build_nitrogen_balance()}}
produces), and a warning when it is not.
}
\section{Grassland intensity split (\code{land_use = "all"})}{

The deposited \code{all}-scope critical rate is per hectare of cropland plus
IMAGE-intensive grassland, while WHEP's pressure covers all grassland. With
\code{grassland_split = "image_density"} (the default) each cell is compared as
two independent components:
\itemize{
\item \strong{managed} -- crop rows plus grassland rows (CBS 3000, 3002, 3003) of a
cell classed intensive, against the cell's own \code{"ara"} critical rate
times its IMAGE 2010 cropland plus, when the cell is classed intensive,
an \code{"igl"} critical rate times its IMAGE 2010 grassland. The deposited
per-hectare \code{"ara"} and \code{"igl"} layers combine exactly into the \code{"all"}
layer (area-weighted, measured on the archive for every threshold and
both metrics), so with the IMAGE 2010 classes this reproduces the
\code{"all"}-scope allowance;
\item \strong{extensive} -- grassland rows of a cell classed extensive, against
IMAGE's 2010 extensive-grassland input or surplus per hectare
(Schulte-Uebbing et al. 2022, SI Supplementary Table 4) times the cell's
IMAGE 2010 grassland.
}

Cell overshoot is the \strong{sum} of the two component overshoots: headroom on
one never nets against excess on the other. Cell actual, critical and
margin are the sums over the compared components. A component with no
allowance area but non-zero pressure (in practice crop pressure where IMAGE
has no cropland and the cell is extensive) is excluded from its comparison
(\code{managed_coverage_state}/\code{extensive_coverage_state} \code{"zero_land"}); a
component with area but no rate even after transfer is
\code{"missing_critical"}. Excluded pressure is reported per cell in
\code{excluded_actual_n_t} and in a message (classes
\code{whep_nbx_zero_land_component}, \code{whep_nbx_missing_critical_component}),
and never counts as overshoot. Crop attribution shares each component's
allowance, margin and overshoot among that component's rows only, and
reconciles to the component and to the cell. A compared component with
allowance area but no pressure row keeps its allowance in a
\code{cell_residual} record naming the component.

Declared assumptions (constructed methods without published precedent):
the classes are IMAGE's 2010 production-system map moved through time by a
national grazing-density proxy (see \code{grassland$classes}); the extensive
allowance is IMAGE's 2010 budget held constant -- "no more than in 2010",
not an environmental limit, so 2010 extensive exceedance is zero wherever
WHEP's 2010 extensive pressure equals IMAGE's; a rate a cell lacks for its
class is borrowed from the nearest cell (great-circle distance) with one in
the same 2010 country, else the same IMAGE region -- an \code{"igl"} rate for
grassland promoted from extensive to intensive, an extensive budget rate
for grassland classed extensive; cropland keeps its own \code{"ara"} rate and
is never lent one -- stamped in
\code{method_allowance_managed}/\code{method_allowance_extensive}
(\code{"archive"}, \code{"nearest_country"}, \code{"nearest_region"}, \code{"none"}, or
\code{"no_area"} for a component without area; \code{NA} outside the critical
domain). IMAGE 2010 intensive grassland with no published \code{"igl"} value
is not lent one (maintainer decision 2026-09-24): its pressure is left out
of the comparison and reported in \code{excluded_igl_actual_n_t}, the cell's
cropland is still compared at its own \code{"ara"} rate, and the managed method
is \code{"none"}. Allowance areas are IMAGE 2010
areas, fixed, except WHEP grassland in a cell with no IMAGE grassland,
whose extensive allowance uses WHEP's own grassland area of the year
(method suffix \code{"_whep_area"}, maintainer decision 2026-09-24).
\code{grassland_split = "none"} reproduces the unsplit comparison exactly.
}

\section{Negative critical surplus}{

Schulte-Uebbing et al. (2022, Methods) set critical fertilizer and manure
inputs to zero where non-agricultural losses alone exceed a threshold, but
keep biological fixation and deposition in the critical input, so their
deposited critical surplus stays negative in those cells (on the \code{"mi"}
surface: 1,796 of 28,881 cells for \code{"all"}, minimum -396 kg N/ha; 2,075 of
28,573 cells for \code{"ara"}, minimum -317 kg N/ha). \code{negative_critical = "keep"} follows the source, and it is the setting under which the published
2010 decomposition (43 Mt N allowable plus 76 Mt N exceedance, 119 Mt N
current surplus) is reproduced. \code{"clamp"} is a declared departure from the
source: it gives such cells a zero allowance instead of a negative one,
which lowers their overshoot to the actual pressure and keeps the cell
within-boundary mass at or above zero wherever the actual pressure is.

Under \code{grassland_split = "image_density"} the treatment applies to each
component's allowance, the unit a component is compared against: a
negative managed allowance (the cell's \code{"ara"} rate on its cropland plus
its \code{"igl"} rate on its intensive grassland, which can net against each
other as they do inside the \code{"all"} rate) becomes zero, and so would a
negative extensive one (IMAGE's 2010 extensive budget, which is never
negative on the deposited archive). The component rates are not clamped
one by one, so with the 2010 classes the managed comparison under
\code{"clamp"} matches the unsplit \code{"all"}-scope comparison under \code{"clamp"}
(to the 1 \% within which the \code{"ara"} and \code{"igl"} layers combine into
\code{"all"}).
A binding threshold names the impact that sets a critical surplus, so it
describes the managed allowance only; the extensive allowance is a 2010
level, not a threshold, and its grid rows carry no binding label.
}

\examples{
build_n_boundary_exceedance(example = TRUE)
}
