% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/livestock_ghg_extension.R
\name{build_livestock_ghg_extension}
\alias{build_livestock_ghg_extension}
\title{Build the livestock greenhouse-gas emissions extension.}
\usage{
build_livestock_ghg_extension(
  tier = 1,
  gwp = c("ar6", "ar5", "ar4"),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{tier}{IPCC tier, \code{1} (default) or \code{2}.}

\item{gwp}{100-year global warming potential standard, \code{"ar6"} (default),
\code{"ar5"} or \code{"ar4"}.}

\item{data}{Optional named list of pre-loaded inputs to avoid remote reads:
\code{primary_prod} (the \code{\link[=get_primary_production]{get_primary_production()}} output). It falls back to
its reader when absent.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (livestock emissions in kilograms CO2e) and \code{method_ghg} (the
chosen tier and GWP standard, e.g. \code{"IPCC_2019_Tier1_AR6"}).
}
\description{
Aggregate per-animal IPCC livestock emissions into a footprint extension
keyed by \verb{(year, area_code, item_cbs_code)}, expressed in kilograms of
carbon-dioxide equivalent (CO2e). This bridges the cohort-level emissions
pipeline (\code{\link[=calculate_livestock_emissions]{calculate_livestock_emissions()}}) to the input-output grain used
by \code{\link[=build_io_model]{build_io_model()}} and \code{\link[=compute_footprint]{compute_footprint()}}, exactly like
\code{\link[=build_grassland_land_extension]{build_grassland_land_extension()}} does for land.

Live-animal head counts come from \code{\link[=get_primary_production]{get_primary_production()}}, are bridged to
IPCC species with \code{\link[=prepare_livestock_emissions]{prepare_livestock_emissions()}}, and the resulting enteric
and manure emissions are converted to CO2e and summed back to the
live-animal commodity sector (\code{item_cbs_code}, e.g. 961 for non-dairy
cattle), which is itself a sector in \code{\link[=build_io_model]{build_io_model()}}.

Two IPCC tiers are available, selected with \code{tier}:
\itemize{
\item \code{1} (default): Tier 1 regional emission factors (IPCC 2019). It needs only
species, country and head counts, so it is complete for every country in
\code{\link[=get_primary_production]{get_primary_production()}}. It covers enteric and manure \strong{methane} and
manure \strong{N2O} (direct and indirect, from default per-head nitrogen
excretion rates).
\item \code{2}: Tier 2 cohort energy balance (IPCC 2019). It derives enteric CH4 and
manure N2O from a per-animal energy and nitrogen balance, for finer
resolution, but requires cohort weight and diet inputs. Animals whose
emissions cannot be resolved (missing diet or energy data) are dropped with
a warning rather than entering the footprint as \code{NA}. \strong{Tier 2 is not yet
calibrated} -- its enteric CH4 currently runs well above Tier 1 -- so
Tier 1 is the recommended default until the Tier 2 energy model is
validated.
}

The CO2e conversion uses 100-year global warming potentials selected with
\code{gwp}:
\itemize{
\item \code{"ar6"} (default): IPCC AR6 (2021) Table 7.15, biogenic CH4 = 27,
N2O = 273.
\item \code{"ar5"}: IPCC AR5 (2013), CH4 = 28, N2O = 265 (no climate-carbon
feedback).
\item \code{"ar4"}: IPCC AR4 (2007), CH4 = 25, N2O = 298.
}
}
\examples{
build_livestock_ghg_extension(example = TRUE)
}
