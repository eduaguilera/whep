% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_land_extension.R
\name{build_crop_land_extension}
\alias{build_crop_land_extension}
\title{Build per-crop physical cropland extension.}
\usage{
build_crop_land_extension(
  gridded_crops,
  gridded_cropland,
  items_prod_full = whep::items_prod_full,
  method = c("cropland_apportion", "intensity_divide"),
  multicropping = NULL
)
}
\arguments{
\item{gridded_crops}{Tibble of gridded crop harvested area, the crop-level
output of \code{\link[=build_gridded_landuse]{build_gridded_landuse()}} (built without CFT aggregation). Must
have columns \code{lon}, \code{lat}, \code{year}, \code{area_code}, \code{item_prod_code},
\code{rainfed_ha}, \code{irrigated_ha}.}

\item{gridded_cropland}{Tibble of physical cropland per cell. Must have
columns \code{lon}, \code{lat}, \code{year}, \code{cropland_ha}.}

\item{items_prod_full}{Crosswalk from production items to commodity-balance
items. Defaults to \link{items_prod_full}. Must have columns \code{item_prod_code}
and \code{item_cbs_code}.}

\item{method}{Physical-area conversion method. One of \code{"cropland_apportion"}
(default) or \code{"intensity_divide"}.}

\item{multicropping}{Tibble of per-cell multi-cropping intensity, required for
\code{method = "intensity_divide"}. Must have columns \code{lon}, \code{lat},
\code{mc_rainfed}, \code{mc_irrigated} (and optionally \code{year}).}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (physical land area in hectares), and \code{method_land} (the chosen
method), plus the polity columns below.
}
\description{
Convert gridded crop \emph{harvested} area into per-crop \emph{physical} land area and
aggregate it to commodity-balance items, producing a land extension keyed by
\verb{(year, area_code, item_cbs_code)} for the FABIO footprint model.

The gridded land-use pipeline (\code{\link[=build_gridded_landuse]{build_gridded_landuse()}}) distributes FAOSTAT
harvested area across grid cells; per-crop totals therefore conserve to
harvested area, which over-counts multi-cropped land and under-counts fallow.
This function turns that harvested area into physical occupied land:
\itemize{
\item \code{"cropland_apportion"} (default): within each cell, the cell's physical
cropland (\code{cropland_ha}, from LUH2) is split across crops in proportion to
their share of the cell's harvested area. Per-crop physical area then
conserves to physical cropland rather than to harvested area, capturing both
double-cropping (scaled down) and fallow (resting land charged to the crops
the rotation supports) at the resolution of the grid.
\item \code{"intensity_divide"}: each crop's harvested area is divided by the cell
multi-cropping intensity (\code{mc_rainfed}, \code{mc_irrigated}). Requires
\code{multicropping}.
}

Unlike a single country-level cropping-intensity factor applied uniformly to
every crop, both methods distribute physical cropland by the actual spatial
pattern of each crop.

Coverage note: with \code{"cropland_apportion"} the per-country crop total is
bounded by the LUH2 \code{cropland} layer, which can under-represent perennial or
plantation crops (e.g. oil palm, rubber) classified outside cropland; such
crops may receive less land than a harvested-area baseline implies.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
gridded_crops <- tibble::tribble(
  ~lon, ~lat, ~year, ~area_code, ~item_prod_code, ~rainfed_ha, ~irrigated_ha,
  0.25, 50.25, 2000L, 1L, 15L, 600, 0,
  0.25, 50.25, 2000L, 1L, 27L, 200, 0,
  0.75, 50.25, 2000L, 1L, 15L, 400, 0
)
gridded_cropland <- tibble::tribble(
  ~lon, ~lat, ~year, ~cropland_ha,
  0.25, 50.25, 2000L, 1000,
  0.75, 50.25, 2000L, 500
)
items <- tibble::tribble(
  ~item_prod_code, ~item_cbs_code,
  15L, 2511L,
  27L, 2805L
)
build_crop_land_extension(gridded_crops, gridded_cropland, items_prod_full = items)
}
