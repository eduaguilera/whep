% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_production.R
\name{qc_production}
\alias{qc_production}
\title{Step 3: Flag production data-quality anomalies}
\usage{
qc_production(
  df,
  smooth = FALSE,
  anchor_years = 5L,
  spike_ratio = 10,
  spike_min = 1000,
  min_run = 3L
)
}
\arguments{
\item{df}{A tibble from \code{\link[=fix_production]{fix_production()}} (or \code{\link[=read_production]{read_production()}}).}

\item{smooth}{Logical. Replace carry-forward tails with a linear
trend? Default \code{FALSE}.}

\item{anchor_years}{Integer. Years for trend anchor. Default \code{5}.}

\item{spike_ratio}{Numeric. Threshold. Default \code{10}.}

\item{spike_min}{Numeric. Minimum value. Default \code{1000}.}

\item{min_run}{Integer. Minimum carry-forward run. Default \code{3}.}
}
\value{
The input tibble plus a \code{qc_flag} column.
}
\description{
Detects and optionally smooths data-quality issues in the production
dataset. Adds a \code{qc_flag} character column.

\strong{Flags applied:}
\itemize{
\item \code{carry_forward} — constant-value tail (≥ \code{min_run} identical final
years). Likely FAOSTAT carry-forward imputation.
\item \code{spike} — year-on-year change exceeding \code{spike_ratio}.
\item \code{fodder_break} — the 1984/1985 EU fodder reporting discontinuity.
}
}
\examples{
\dontrun{
read_production() |> fix_production() |> qc_production()
}
}
