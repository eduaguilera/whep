% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnf.R
\name{calculate_crop_bnf}
\alias{calculate_crop_bnf}
\title{Estimate symbiotic biological nitrogen fixation by crop legumes.}
\usage{
calculate_crop_bnf(x, symbiotic_params = list())
}
\arguments{
\item{x}{A tibble with \code{item_prod_code}, \code{crop_npp_n_t} and \code{product_n_t}.
Optional driver columns activate the modifiers: \code{n_synth_kg_ha} /
\code{n_org_kg_ha} (nitrogen inhibition), \code{temp_c} (temperature),
\code{water_input_mm} (or \code{precip_mm} + \code{irrig_mm}) and \code{pet_mm} (water).}

\item{symbiotic_params}{Named list overriding the symbiotic-BNF parameters
\code{k_n_synth}, \code{k_n_org}, \code{t_opt}, \code{t_sigma}, \code{ai_threshold}.}
}
\value{
The input tibble with the environmental factors, \code{ndfa_adj},
\code{crop_bnf_t} (NPP method), \code{crop_bnf_anglade_t} (Anglade method) and the
per-product ratios \code{bnf_product_ratio_npp} / \code{bnf_product_ratio_anglade}.
}
\description{
Estimates symbiotic BNF from leguminous crops via two methods (an NPP-based
estimate and the Anglade product-based estimate). Environmental modifiers
(nitrogen inhibition, temperature, water) are applied only when their driver
columns are present in \code{x}; an absent driver leaves that modifier at 1.
}
\examples{
calculate_crop_bnf(
  tibble::tibble(item_prod_code = "176", crop_npp_n_t = 10, product_n_t = 5)
)
}
