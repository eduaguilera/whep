% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/livestock_bridge.R
\name{prepare_livestock_emissions}
\alias{prepare_livestock_emissions}
\title{Prepare production data for livestock emission calculations.}
\usage{
prepare_livestock_emissions(data, expand_cohorts = FALSE, system_shares = NULL)
}
\arguments{
\item{data}{A tibble from \code{build_primary_production()} with
columns \code{item_cbs_code}, \code{unit}, \code{value}, and optionally
\code{year}, \code{area_code}, \code{live_anim_code}.}

\item{expand_cohorts}{Logical. If \code{TRUE}, distributes herds
across GLEAM cohorts and production systems via
\code{calculate_cohorts_systems()}. Default \code{FALSE}.}

\item{system_shares}{Optional dataframe with custom system
shares. Passed to \code{calculate_cohorts_systems()}.}
}
\value{
A tibble with columns \code{species}, \code{heads}, \code{iso3}
(if \code{area_code} present), and optionally
\code{milk_yield_kg_day}, \code{meat_yield_t_head}, cohort columns,
plus all extra columns from the input.
}
\description{
Bridge between \code{build_primary_production()} output and the
livestock emission functions. Maps species codes to names,
converts area codes to ISO3, extracts milk and meat yields,
and optionally expands herds into GLEAM cohorts.

Any extra columns present in the input (e.g., \code{weight},
\code{diet_quality}, \code{fat_percent}) are preserved and flow
through to the emission functions automatically.
}
\examples{
\dontrun{
  build_primary_production() |>
    prepare_livestock_emissions() |>
    calculate_livestock_emissions(tier = 1)
}
}
