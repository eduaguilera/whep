% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{decompose_urban_losses}
\alias{decompose_urban_losses}
\title{Decompose urban nitrogen losses into population, per-capita, and
recycling drivers}
\usage{
decompose_urban_losses(
  n_prov_destiny = NULL,
  population_yg = NULL,
  by_period = FALSE,
  example = FALSE
)
}
\arguments{
\item{n_prov_destiny}{Nitrogen flows tibble from
\code{\link[=create_n_prov_destiny]{create_n_prov_destiny()}}. If \code{NULL}, loaded automatically.}

\item{population_yg}{Population tibble from
\code{whep_read_file("population_yg")}. If \code{NULL}, loaded automatically.}

\item{by_period}{If \code{TRUE}, compares each reference period (each
averaged across its ten years) against the immediately preceding
one — 1860-1870 -> 1920-1930 -> 1960-1970 -> 2010-2020 — plus one
extra transition spanning the full analysis window, 1860-1870
straight to 2010-2020 (the total change) — instead of chaining year
on year.}

\item{example}{If \code{TRUE}, return a small hardcoded output without
downloading remote data. Default is \code{FALSE}.}
}
\value{
A tibble from \code{\link[=calculate_lmdi]{calculate_lmdi()}} with columns \code{period},
\code{period_years}, \code{factor_label}, \code{component_type}, \code{additive},
\code{multiplicative}, and \code{multiplicative_log}.
}
\description{
Decomposes the year-on-year change in non-recycled human excreta
nitrogen (N) into three multiplicative drivers, computed at the
national level: population, per-capita excreted N (approximated by
per-capita food N consumption, since intake is approximately equal to
excretion), and the non-recycled fraction of excreted N.

Only the \code{population_food} destiny is used as the excretion proxy.
\code{population_other_uses} (non-food industrial use, e.g. cotton/tobacco)
is deliberately excluded even though \code{.create_wastewater_surplus_df()}
in \code{grafs_plot_df.R} includes it: that material is not ingested, so it
cannot be assumed to leave the body as excreta, breaking the
intake-approximates-excretion logic this compartment relies on.

Food waste is not included: the commodity-balance sheets underlying
\code{n_prov_destiny} carry Food/Feed/Seed/Other-uses/Export/Import
destinies but no separate consumer food-waste line, so this loss is
excreta-only. Whether the \code{urban}/\code{People} recycling flows already
folded in some food waste from the original source is unconfirmed.
}
\examples{
decompose_urban_losses(example = TRUE)
}
