% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{decompose_destiny_mix}
\alias{decompose_destiny_mix}
\title{Compute the national cropland destiny mix over time}
\usage{
decompose_destiny_mix(n_nat_destiny = NULL, example = FALSE)
}
\arguments{
\item{n_nat_destiny}{National nitrogen flows tibble from
\code{\link[=create_n_nat_destiny]{create_n_nat_destiny()}}. If \code{NULL}, computed automatically (slow).}

\item{example}{If \code{TRUE}, return a small hardcoded output without
downloading remote data. Default is \code{FALSE}.}
}
\value{
A tibble with columns \code{year}, \code{destiny_grp} (one of
\code{"domestic_food"}, \code{"feed"}, \code{"exported"}, \code{"non_food"}),
\code{output_mg}, and \code{share} (that destiny's share of the year's total
cropland output).
}
\description{
Computes, per year, the share of Spain's cropland output (by mass N)
going to each destiny — domestic food, feed, exported food, non-food —
from \code{\link[=create_n_nat_destiny]{create_n_nat_destiny()}}'s national commodity balance sheets.
Deliberately \emph{not} \code{\link[=create_n_prov_destiny]{create_n_prov_destiny()}}'s provincial data summed
up: the provincial \code{"export"} destiny does not distinguish
inter-provincial trade from true international export (per
\code{\link[=decompose_crop_livestock_conn]{decompose_crop_livestock_conn()}}'s same caveat on \code{"Outside"}
imports), which would overstate the export share here.
\code{\link[=create_n_nat_destiny]{create_n_nat_destiny()}} instead recomputes export/import directly
from the national production-vs-consumption balance per item, so
trade between two Spanish provinces nets out rather than counting as
export. This is the supplementary diagnostic recommended in the
decomposition proposal (section 14, "attach to existing figures"), to
show the local-food -> feed + export transition directly, since
\code{\link[=decompose_cropland_surplus]{decompose_cropland_surplus()}} no longer carries a destiny factor.
}
\examples{
decompose_destiny_mix(example = TRUE)
}
