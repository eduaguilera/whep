% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arable_permanent_land.R
\name{check_arable_composition}
\alias{check_arable_composition}
\title{Check where the arable-land extension changes composition mid-panel.}
\usage{
check_arable_composition(extension, items_prod_full = whep::items_prod_full)
}
\arguments{
\item{extension}{Tibble of the arable/permanent land extension as returned
by \code{\link[=build_fao_arable_fallow_extension]{build_fao_arable_fallow_extension()}}: \code{year}, \code{area_code},
\code{item_cbs_code}, \code{impact_u}, and optionally \code{temp_grassland_netted_ha}.
The \code{temp_grassland_netting} term is reported only when that column is
present.}

\item{items_prod_full}{Crosswalk used to classify \code{item_cbs_code} as
perennial via \code{Herb_Woody}. Defaults to \link{items_prod_full}.}
}
\value{
A tibble with one row per \verb{(term, area_code)}:
\itemize{
\item \code{term}: \code{"fodder"} or \code{"temp_grassland_netting"}.
\item \code{area_code}: the country.
\item \code{panel_first_year}, \code{panel_last_year}: the years that country has arable
rows for.
\item \code{term_first_year}, \code{term_last_year}: the years the term is positive
(\code{NA} when it never is).
\item \code{n_years_absent}: panel years in which the term is absent.
\item \code{break_year}: the first panel year after \code{term_last_year} with no term
(\code{NA} when the term runs to the end of the panel, or never appears).
\item \code{broken}: \code{TRUE} when the term is present in some panel year and absent
in a later one.
\item \code{never_present}: \code{TRUE} when the term is absent for the whole panel.
}
}
\description{
Report, per term and \code{area_code}, whether a term of the fallow-inclusive
arable land extension is present for part of a country's panel and absent
for the rest. Two terms switch off inside the published panel and neither
switch is a land-use change:
\itemize{
\item \strong{\code{fodder}} — the FAOSTAT fodder items (\code{Cat_1 == "Fodder_green"} and not
grass, i.e. CBS \code{2000}-\code{2003}). FAOSTAT's fodder tonnage
(\code{faostat-production-old}, production only) ends in 2013 and EU AgriDB, the
only other source, ends in 2019, so a build reaching 2020 has no fodder at
all from that year on and every ordinary arable crop silently absorbs
fodder's share of the country's arable land (whep#938).
\item \strong{\code{temp_grassland_netting}} — the temporary grassland netted out of the
arable target. Modelled CBS 3002 comes from EU AgriDB alone, so it exists
for 26 EU polities and stops at 2019; from 2020 the netting term is
identically zero while FAO's arable land still contains temporary meadows,
and the reconciliation changes method at the boundary (whep#937).
}

This is a diagnostic, not a correction: it flags the discontinuity so a
series is not read across it. The treatments live behind
\code{\link[=build_fao_arable_fallow_extension]{build_fao_arable_fallow_extension()}}'s \code{fodder_gap} and
\code{temp_grassland_basis} arguments, whose defaults reproduce the published
behaviour.
}
\details{
\code{\link[=check_series_jumps]{check_series_jumps()}} cannot find either break. A term does not fall to a
small value at the boundary, it stops having rows, and its \code{min_value}
guard skips any pair involving a zero, so a scan over the completed series
would not flag it either. Coverage, not a ratio, is what has to be checked.
}
\examples{
extension <- tibble::tribble(
  ~year, ~area_code, ~item_cbs_code, ~impact_u, ~temp_grassland_netted_ha,
  2019L, 10L, 2003L, 100, 50, # fodder mix, netting fires
  2019L, 10L, 2511L, 900, 50,
  2020L, 10L, 2511L, 1000, 0 # fodder gone, netting off
)
check_arable_composition(extension)
}
