% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_production.R
\name{read_production}
\alias{read_production}
\title{Step 1: Read and reformat FAOSTAT production data}
\usage{
read_production(max_year = 2021, version = NULL)
}
\arguments{
\item{max_year}{Integer. Latest year of FAOSTAT data. Default \code{2021}.}

\item{version}{File version for input pins. \code{NULL} (default) uses the
frozen version from \link{whep_inputs}.}
}
\value{
A tibble in long format with columns:
\code{year}, \code{area}, \code{area_code}, \code{item_prod}, \code{item_code_prod},
\code{item_cbs}, \code{item_code_cbs}, \code{Live_anim}, \code{Live_anim_code},
\code{unit}, \code{value}, \code{source}.

The \code{source} column indicates data provenance:
\code{"FAOSTAT"} (original), \code{"EU_AgriDB"} (European AgriDB fodder),
\code{"DM_yield_estimate"} (dry-matter yield imputation),
\code{"fill_linear"} (interpolation), \code{"imputed_yield"} (yield × area),
\code{"imputed_cbs_ratio"} (CBS ratio imputation),
\code{"LUH2_cropland"} / \code{"LUH2_agriland"} (LUH2 proxy),
\code{"fill_linear_historical"} (pre-1962 linear extrapolation),
\code{"LUH2"} (grassland), \code{"Estimated"} (double-product estimation).
}
\description{
Read raw FAOSTAT inputs (crops, livestock, fodder, emission-based
stocks), gap-fill yields, handle double products, extend back to 1850
using LUH2 land-use areas, and add grassland.

No data corrections are applied at this stage. The output preserves
FAOSTAT values as-is (including known issues such as un-corrected tea
tonnages and the absence of game-meat stocks). For the corrected
version, pipe into \code{\link[=fix_production]{fix_production()}}.
}
\examples{
\dontrun{
raw <- read_production()
}
}
