% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet_integrity.R
\name{write_parquet_checked}
\alias{write_parquet_checked}
\title{Write a Parquet file and verify it before returning.}
\usage{
write_parquet_checked(data, path, deep = FALSE, ...)
}
\arguments{
\item{data}{Data frame to write.}

\item{path}{Destination path.}

\item{deep}{Whether to verify by decoding every row group as well
as by checking the layout. Reads the whole file back.}

\item{...}{Passed to \code{nanoparquet::write_parquet()}.}
}
\value{
Invisibly, \code{path}.
}
\description{
Wrapper around \code{nanoparquet::write_parquet()} that reopens the file
it just wrote and runs \code{\link[=assert_parquet_integrity]{assert_parquet_integrity()}} on it. Use it
for pipeline artifacts large enough that a silent write failure
would go unnoticed; the verification is metadata-only, so it costs
milliseconds regardless of file size.
}
\examples{
path <- tempfile(fileext = ".parquet")
write_parquet_checked(data.frame(x = 1:10), path)
unlink(path)
}
