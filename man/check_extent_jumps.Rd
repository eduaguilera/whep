% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_shares_seam.R
\name{check_extent_jumps}
\alias{check_extent_jumps}
\title{Flag implausible year-on-year jumps in a per-unit extent}
\usage{
check_extent_jumps(
  extent,
  ratio_bounds = c(0.55, 1.6),
  min_value = 0,
  verbose = FALSE
)
}
\arguments{
\item{extent}{A per-unit extent table as returned by
\code{\link[=aggregate_unit_extent]{aggregate_unit_extent()}}. Every column that is not \code{year},
\code{extent_ha} or \code{extent_basis} identifies the series.}

\item{ratio_bounds}{Length-2 numeric \code{c(low, high)} plausible band for
the ratio of consecutive years, passed to \code{\link[=check_series_jumps]{check_series_jumps()}},
whose own default it inherits.}

\item{min_value}{Minimum extent both members of a pair must exceed to
be flagged, passed to \code{\link[=check_series_jumps]{check_series_jumps()}}.}

\item{verbose}{Logical, passed to \code{\link[=check_series_jumps]{check_series_jumps()}}. Default
\code{FALSE}, so a guard inside a back-cast stays quiet.}
}
\value{
The \code{\link[=check_series_jumps]{check_series_jumps()}} flags tibble: the series key
columns, \code{year}, \code{prev_value}, \code{value}, \code{ratio} and \code{allowlisted}.
}
\description{
Run \code{\link[=check_series_jumps]{check_series_jumps()}} on \code{extent_ha} per unit series, the check the
plan's seam section asks for before an extent modulates a share. WHEP
repairs LUH2's isolated single-year cropland collapses nationally
(\code{.fix_luh2_crop_collapse()}, \code{R/build_production.R:627-695}); the same
collapse reaches a unit series unrepaired, where it would propagate
into every earlier back-cast year through the growth chain.

The scan covers whatever years \code{extent} carries.
\code{\link[=backcast_admin_shares]{backcast_admin_shares()}} calls it on the back-cast window alone --
\code{t0} and everything before it, the years the modulation actually reads
-- so a jump after the seam is not a reason to refuse a back-cast.
}
\examples{
extent <- tibble::tibble(
  area_code = 900L,
  level_polity_code = "A1",
  level = 1L,
  year = 1900:1903,
  extent_ha = c(1000, 1010, 5, 1020),
  extent_basis = "cropland_ha"
)
check_extent_jumps(extent)
}
