% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_surplus.R
\name{calculate_n_surplus}
\alias{calculate_n_surplus}
\title{Calculate the gridded soil-surface nitrogen surplus.}
\usage{
calculate_n_surplus(
  balance,
  method = c("harvest_removal", "full_balance"),
  example = FALSE
)
}
\arguments{
\item{balance}{A \code{\link[=build_nitrogen_balance]{build_nitrogen_balance()}} output tibble. For
\code{"harvest_removal"} it must carry \code{n_input_std_t}, \code{prod_n_t},
\code{used_residue_n_t} and \code{grazed_weeds_n_t}; for \code{"full_balance"} it must
carry \code{n_balance_t}. When an \code{area_ha} column (each crop's harvested
hectares in the cell) is present, the per-hectare surplus \code{surplus_kgn_ha}
is also emitted.}

\item{method}{Surplus definition: \code{"harvest_removal"} (default,
\code{n_input_std_t - (prod_n_t + used_residue_n_t + grazed_weeds_n_t)}) or
\code{"full_balance"} (the balance's \code{n_balance_t}, a sensitivity).}

\item{example}{If \code{TRUE}, return a small fixture instead of computing from
\code{balance}. Defaults to \code{FALSE}.}
}
\value{
The \code{balance} tibble with \code{surplus_n_t} (tonnes N, may be negative),
\code{method_surplus} and, when \code{area_ha} is present, \code{surplus_kgn_ha}
(kg N per hectare).
}
\description{
Derives the nitrogen surplus from a \code{\link[=build_nitrogen_balance]{build_nitrogen_balance()}} output. The
default \code{"harvest_removal"} method is the standard soil-surface nitrogen
balance: net inputs (\code{n_input_std_t}) minus the nitrogen exported in harvest
(crop product, used or exported residue and grazed forage), matching the
basis of the Schulte-Uebbing et al. (2022) critical nitrogen surplus.
Field-burnt residue nitrogen is not subtracted (it volatilises in place, a
loss inside the surplus, not an export), nor is recycled residue (returned
to the field, internal). The \code{"full_balance"} method instead returns the
balance's post-loss \code{n_balance_t}, a sensitivity alternative. The surplus
may be negative (a nitrogen deficit) and is not clamped here, as clamping is
a boundary or leaching concern. The full grid key (\code{lon}, \code{lat},
\code{area_code}, \code{item_cbs_code}, \code{year}) and every balance column are
preserved.
}
\examples{
calculate_n_surplus(example = TRUE)
}
