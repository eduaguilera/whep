% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polities.R
\name{resolve_polity_label}
\alias{resolve_polity_label}
\title{Resolve a source's country label to a polity}
\usage{
resolve_polity_label(label, source = NULL, year = NULL)
}
\arguments{
\item{label}{Character vector of source labels.}

\item{source}{Optional source slug (e.g. \code{"lassaletta-grassland-share"}).
Length 1, or the same length as \code{label}. On the alias route \code{NULL} matches
unscoped aliases only -- 188 of 1,003 -- so a \code{NULL} source narrows that
route sharply; the identity routes then get their turn, subject to the
guards above.}

\item{year}{Optional integer vector of years. Length 1, or the same length as
\code{label}. On the alias route \code{NULL} matches aliases with no year scope only,
which is the 14 of 1,003 published aliases carrying NEITHER bound. The name
and ISO3 routes can still answer without a year, but only for an identifier
exactly one polity has ever carried, so supplying a year remains much the
stronger question: it is what lets a label resolve to the right \emph{period}
rather than to nothing.}
}
\value{
A character vector of polity codes, \code{NA} where nothing matched.
}
\description{
Maps a country or area \strong{label}, as a source writes it, to a WHEP polity
code. This complements \code{\link[=add_polity_code]{add_polity_code()}}, which resolves numeric
FAOSTAT/FABIO area codes: before this existed there was no supported path
from a label to a polity, so datasets carrying labels went unresolved.
\link{mueller_synthetic_n}'s \code{iso3c} column holds FAO-style legacy codes (\code{"BZE"}
for Belize, \code{"ROM"} for Romania, \code{"ZAR"} for Zaire) and
\link{lassaletta_grassland_share}'s \code{Country} holds name variants (\code{"Cape Verde"},
\code{"Swaziland"}), none of which resolve against \link{polities} directly.
}
\details{
The mapping is \link{polity_label_aliases}, a copy of the map published by
\code{whep-polities}. It is deliberately NOT computed here: a label's meaning is a
fact about the source, upstream already decides it, and a second lookup in
this package would be a second authority for the same question.

Resolution is \strong{source- and year-aware}, and both matter:
\itemize{
\item An alias may be scoped to one \code{source}, because the same label can mean
different things in different sources. A scoped alias never applies to
another source; an unscoped one applies to any.
\item An alias may be scoped to a year range, because a label's referent changes.
\code{"Cape Verde"} in 1970 is the Portuguese colony \code{CPV-1886-1975}; in 1990 it
is \code{CPV-1975-2025}.
}

Where several aliases match, the most specific wins: year-scoped over
unscoped, then source-scoped, then the narrower year range. That ordering
mirrors \code{matchlib.Matcher.match_alias} upstream, so both sides agree.

Where no alias applies, a second route tries the polity's own \code{polity_name}
and then, for a three-letter label, its \code{iso3_code}. That mirrors upstream's
"alias, then ISO/name family + year containment", and both halves are needed.
Without the name half a caller passing the database's own name for a polity
got \code{NA}: \code{resolve_polity_label("Netherlands")} found nothing while \link{polities}
carried a polity named exactly that. Without the ISO3 half the map answers
only for labels a curator had to decide about, which is 380 of
\link{mueller_synthetic_n}'s 5,043 rows -- the 11 legacy codes -- against all
5,043 with it, asked at \code{year = 2000}. Asking without a year resolves only
1,255, because the guard below then refuses every identifier more than one
live polity has ever carried. Two guards bound both halves.
\itemize{
\item An identifier resolves only when \strong{exactly one} polity carries it in the
year asked about, because otherwise row order would decide and \code{NA} is the
honest answer. Sharing an identifier is common in the shipped \link{polities}
snapshot: of its 726 live rows, 110 normalised names and 133 ISO3 codes are
carried by more than one polity. A year separates nearly all of them -- no
two live polities sharing a normalised name cover a common year -- but not
the ISO3 index, where 69 pairs still do, 62 of them naming different
territories rather than successive periods of one. \code{"PAN"} in 1970 is the
case that matters: \code{PAN-1903-1979} and the Canal Zone \code{CZN-1903-1979} both
carry that ISO3 then -- a real territorial overlap no re-sync removes --
so the answer is \code{NA}, while \code{"PAN"} in 2000 resolves to \code{PAN-1979-2025}.
\item An alias covering that year outranks both, whatever its source, and a label
naming an area the crosswalk leaves unmapped is refused outright.
}

Returns \code{NA} when neither route resolves, which is a real answer rather than a
failure. Some labels are aggregates a source keeps reporting after the
territory stopped existing -- \code{"FSU"} runs to 2009 though nothing has held
that territory since 1991 -- and those years are deliberately unmapped rather
than routed to a polity that had ended.

Every resolved code is one \code{\link[=get_polity_geometries]{get_polity_geometries()}} can return a row for,
and that is an invariant rather than a happy accident: \link{polity_label_aliases}
and \link{polities} are regenerated together from a single upstream revision, and
\code{data-raw/table_mappings.R} aborts the build if any alias names a polity the
shipped table does not carry. A dangling resolution therefore cannot ship.
}
\examples{
resolve_polity_label("ZAR", source = "mueller-synthetic-n", year = 2000)
resolve_polity_label(
  c("Cape Verde", "Cape Verde"),
  source = "lassaletta-grassland-share",
  year = c(1970L, 1990L)
)

}
\seealso{
\code{\link[=add_polity_code]{add_polity_code()}} for numeric area codes.
}
