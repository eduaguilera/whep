% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population.R
\name{read_population}
\alias{read_population}
\title{Read national population on WHEP area codes.}
\usage{
read_population(
  years = NULL,
  data = list(),
  population_source = c("pin", "pin_wpp_fallback"),
  example = FALSE
)
}
\arguments{
\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
(default) keeps every year the pin covers.}

\item{data}{Optional named list of pre-loaded inputs to avoid the pin read:
\code{gdp_population} (the raw pin, with \code{Year}, \code{area_code} as ISO3, \code{pop} in
thousands) and \code{wpp_population} (a \code{\link[=read_wpp_population]{read_wpp_population()}} output). Falls
back to \code{\link[=whep_read_file]{whep_read_file()}} when absent.}

\item{population_source}{\code{"pin"} (default, the \code{gdp-population} pin alone) or
\code{"pin_wpp_fallback"}, which additionally fills country-years the pin does
not cover from UN WPP.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{year}, \code{area_code}, \code{population} (persons) and
\code{source_pop}, one row per area code and year, sorted by year then area
code, plus the polity columns below. \code{source_pop} carries the pin's own
vocabulary (\code{"Original"}, \code{"Linear interpolation"}, \code{"First value carried backwards"}), joined with \code{" + "} when a bucket sums ISO3 codes of
differing provenance, or \code{"UN WPP 2024"} for a fallback-filled row. A row is one country in the common case, but \code{area_code} is
an aggregation bucket: rows from 2012 on 206 ("Sudan (former)") are sums
over several territories rather than a single country, as are rows on 999
("Rest of World") when the Rest-of-World fold is restored.
}
\description{
Reads the \code{gdp-population} pin and returns population per country and year on
the numeric \code{area_code} the rest of the package uses. The pin is keyed by
ISO3 (in a column confusingly also called \code{area_code}) and reports population
in thousands; both are converted here, so consumers such as
\code{\link[=build_food_supply]{build_food_supply()}} and \code{\link[=build_n_percapita]{build_n_percapita()}} get the
\code{year}/\code{area_code}/\code{population} contract they document.

Regional residual aggregates in the pin (\code{RAFR}, \code{RASI}, \code{REUR}, \code{RLAM},
\code{ROCE}) are not countries and carry no numeric code. They are dropped and
reported rather than silently discarded, since their omission is what makes
the result a countries-only total rather than a world total.

\code{area_code} here is \code{polity_area_code}, which is a \strong{bucket, not an
identity}: several ISO3 codes can share one code, and this function sums
them, so some rows are aggregates of more than one territory. WHEP models the
reporting members of bucket 999 in their own right, so with the real pin and
the default options the only such row is code 206 "Sudan (former)", carrying
Sudan plus South Sudan from 2012 on; it resolves to \code{SUD-1956-2011}, the
pre-secession territory its two members together cover. Under
\code{options(whep.unfold_rest_of_world = "none")} code 999 folds Syria, North
Macedonia, Palestine, Eswatini, Equatorial Guinea and French Guiana as well.
Every folded row is named in a message.

The pin does not cover every area WHEP models, and that is a bigger gap than
the fold. On the real pin 190 of the 256 area codes the crosswalk resolves
get a population row; the 66 that do not include Bhutan, Comoros, New
Caledonia and the Faroe Islands, all of which the commodity balances do give
food to. \code{\link[=build_food_supply]{build_food_supply()}} and \code{\link[=build_n_percapita]{build_n_percapita()}} inner-join this
table, so those areas are absent from their per-capita output rather than
wrong in it. Both warn and name them instead of dropping them silently;
\code{options(whep.warn_missing_population = FALSE)} silences that warning.

\code{population_source = "pin_wpp_fallback"} fills the country-years the pin
does not reach from \code{\link[=read_wpp_population]{read_wpp_population()}}, and \strong{only} those: the pin wins
wherever both have a value, so turning it on cannot move a denominator that
was already published. On the real inputs it adds 44 areas the pin has no row
for at all — Réunion, Bhutan, Comoros, Western Sahara, New Caledonia, the
French overseas departments and the small island states — and 4,755
country-years inside the pin's own year span. Filled rows are stamped
\code{source_pop = "UN WPP 2024"}.

It is a gap-filler and not a replacement, because the two sources disagree
where they overlap: across 12,309 shared country-years they differ by a
median 0.64\%, a 95th percentile of 4.4\% and a maximum of 81\%. That is why
\code{"pin"} remains the default.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning. Where that polity is not live in the
row's own year -- 41.5\% of the pre-1961 \verb{(area, year)} cells --
\code{\link[=add_polity_code]{add_polity_code()}} says so as \code{mapping_status == "backcast_anchor"}, and
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports it as \code{gap_kind == "backcast_anchor"}.
These columns do not say so either way.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
read_population(example = TRUE)
}
