% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population.R
\name{read_population}
\alias{read_population}
\title{Read national population on WHEP area codes.}
\usage{
read_population(years = NULL, data = list(), example = FALSE)
}
\arguments{
\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
(default) keeps every year the pin covers.}

\item{data}{Optional named list of pre-loaded inputs to avoid the pin read:
\code{gdp_population} (the raw pin, with \code{Year}, \code{area_code} as ISO3, \code{pop} in
thousands). Falls back to \code{\link[=whep_read_file]{whep_read_file()}} when absent.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{year}, \code{area_code} and \code{population} (persons), one row
per country-year, sorted by year then area code.
}
\description{
Reads the \code{gdp-population} pin and returns population per country and year on
the numeric \code{area_code} the rest of the package uses. The pin is keyed by
ISO3 (in a column confusingly also called \code{area_code}) and reports population
in thousands; both are converted here, so consumers such as
\code{\link[=build_food_supply]{build_food_supply()}} and \code{\link[=build_n_percapita]{build_n_percapita()}} get the
\code{year}/\code{area_code}/\code{population} contract they document.

Regional residual aggregates in the pin (\code{RAFR}, \code{RASI}, \code{REUR}, \code{RLAM},
\code{ROCE}) are not countries and carry no numeric code. They are dropped and
reported rather than silently discarded, since their omission is what makes
the result a countries-only total rather than a world total.
}
\examples{
read_population(example = TRUE)
}
