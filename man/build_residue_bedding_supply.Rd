% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residue_bedding.R
\name{build_residue_bedding_supply}
\alias{build_residue_bedding_supply}
\title{Build the bedding straw supply reaching the managed manure chain.}
\usage{
build_residue_bedding_supply(x)
}
\arguments{
\item{x}{A tibble from \code{\link[=calculate_residue_destinies]{calculate_residue_destinies()}} with
\code{item_prod_code}, \code{residue_bedding_dm_t}, \code{year} and \code{territory}, and
optionally \code{sub_territory}.}
}
\value{
A tibble with one row per \verb{year x territory} (x \code{sub_territory}) and
columns \code{bedding_dm_t}, \code{bedding_c_t} and \code{bedding_n_t}.
}
\description{
Converts the bedding destiny of crop residues
(\code{\link[=calculate_residue_destinies]{calculate_residue_destinies()}}'s \code{residue_bedding_dm_t}) into the dry
matter, carbon and nitrogen that \code{\link[=add_manure_bedding]{add_manure_bedding()}} adds to the housed
manure streams. Composition comes from the same \code{bio_coefs} residue
coefficients \code{\link[=calculate_npp_carbon_nitrogen]{calculate_npp_carbon_nitrogen()}} uses for the residue that
stays on the field (\code{residue_c_kgdm}, \code{residue_n_kgdm}, joined on
\code{item_prod_code}), so the straw that goes through the yard and the straw
that does not are described by one table.

The supply is aggregated over crops, because bedding is not traceable to the
crop it came from once it is in the heap: the manure chain keys on
\verb{year x territory} (and \code{sub_territory} when the input carries one).
}
\examples{
tibble::tibble(
  year = 2020L, territory = "203", item_prod_code = "15",
  residue_bedding_dm_t = 1000
) |>
  build_residue_bedding_supply()
}
