% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatialize_levels.R
\name{allocate_level_crops}
\alias{allocate_level_crops}
\title{Allocate national crop areas at a granted containment depth}
\usage{
allocate_level_crops(
  country_areas,
  crop_patterns,
  gridded_cropland,
  allocation_layer,
  admin_shares = NULL,
  config = list()
)
}
\arguments{
\item{country_areas}{National crop areas, as
\code{\link[=build_gridded_landuse]{build_gridded_landuse()}} takes them: \code{year}, \code{area_code},
\code{item_prod_code}, \code{harvested_area_ha} and optionally
\code{irrigated_area_ha}. Keyed on the CONTAINER; the unit split is this
function's job. A negative \code{harvested_area_ha} is refused first, as
\code{whep_alloc_negative_national}: the irrigation check would otherwise
read it as a row with more irrigated than harvested area and say so.}

\item{crop_patterns}{Per-cell crop pattern, as
\code{\link[=build_gridded_landuse]{build_gridded_landuse()}} takes it.}

\item{gridded_cropland}{Per-cell cropland extent, as
\code{\link[=build_gridded_landuse]{build_gridded_landuse()}} takes it.}

\item{allocation_layer}{One country grid choosing a depth per country,
from \code{\link[=build_allocation_layer]{build_allocation_layer()}}: granted countries arrive as their
units, every other country as its level-0 row.}

\item{admin_shares}{Resolved admin shares -- the \code{shares} element of
\code{\link[=resolve_admin_shares]{resolve_admin_shares()}}, optionally completed by
\code{\link[=backcast_admin_shares]{backcast_admin_shares()}}. \code{NULL} allocates every item on
pattern-implied unit shares.}

\item{config}{Named list. Every \code{\link[=build_gridded_landuse]{build_gridded_landuse()}} key is
forwarded (and validated there); \code{pattern_extension} is fixed to
\code{"granted_units"} by decision T31(b). This function's own keys are
\code{tolerance_relative} (0.10), \code{tolerance_absolute} (1000 ha) and
\code{conservation_tolerance} (1e-6, relative).}
}
\value{
A list of seven tibbles:
\itemize{
\item \code{allocation}: the gridded rows, at unit grain, exactly the
\code{\link[=build_gridded_landuse]{build_gridded_landuse()}} schema plus \code{level_polity_code}.
\item \code{targets}: one row per \verb{(year, area_code, level_polity_code, item_prod_code)} with \code{share}, \code{target_ha}, \code{irrigated_target_ha},
\code{rainfed_target_ha}, \code{irrigation_clipped_ha} and the regime,
\code{method_crop_alloc}.
\item \code{coverage}: one row per \verb{(year, area_code, item_prod_code)} with
\code{n_units}, \code{n_units_reporting}, \code{coverage}, \code{admin_sum},
\code{residual_target_ha}, \code{discrepancy_ha} and the denominator \code{basis}.
\item \code{breach}: the capacity excess per \verb{(year, area_code, level_polity_code, item_prod_code, mc_basis)}.
\item \code{straddle}: per unit and item, \code{n_cells}, \code{straddle_sibling},
\code{straddle_foreign} and \code{cell_limited}.
\item \code{conservation}: allocated against target, at container and unit grain.
\item \code{bridges}: per \verb{(area_code, item_prod_code, treatment)}, how many years
of the shares were not observed and the longest contiguous run of them,
so a 60-year LUH2 bridge is visible rather than merely legal (decision
T31(e)).
}
}
\description{
The two-level allocation: a national total is split across the
administrative units of its container by reported harvested-\strong{area}
shares, and each unit's target is then spread across that unit's cells
by the gridded pattern the engine already uses, under one capacity
ceiling, in ONE pass per country. Items with no statistics travel in the
same pass on pattern-implied unit shares, so a constrained country is
allocated at one grain and a cell's capacity is shared by everything in
it.
}
\section{The composite key and where it must be carried}{

A granted-depth compartment is identified by \strong{\verb{(area_code, level_polity_code)}}: the container's integer reporting code, which
every \code{area_code}-typed join, \code{\link[=add_area_name]{add_area_name()}} and the \code{area_key}
switch keep working on, plus the unit's polity code from the containment
edge (\code{NA} at level 0). The pair travels through the whole chain, and
these are the places that had to learn it:

\describe{
\item{\code{.compartment_id_cols()}}{already appends \code{level_polity_code}
(T12), so every capacity, redistribution and CFT grouping keyed on
it followed for free.}
\item{The engine's national-table join and share denominators}{
\code{.spatialize_year()} joined \code{country_areas} to the grid on
\verb{(area_code, item_prod_code)} and formed \code{rf_pot_sum} /
\code{ir_pot_sum} / \code{rainfed_sum} / \code{irrigated_sum} on the same pair.
Both now use \code{.alloc_target_cols()}, the grain the NATIONAL TABLE is
keyed at. That is the load-bearing distinction: the key comes from
the targets, never from the grid, because a unit-keyed grid under a
container-keyed national table is the pattern-implied case and must
stay one national total.}
\item{The LUH2 type-potential fallback}{\code{type_pot} decides per group
whether a crop has any of its LUH2 type to sit in; grouped on the
container it would let one unit's type cropland keep another unit
out of the whole-group fallback.}
\item{\code{.redistribute_country_dt()}}{\code{.crop_group} was \code{item_prod_code}
alone. It is now the target grain inside the country, so the logit
passes and the final rescale conserve each (unit, item). This is
the grouping error the machine criterion pins: with the item alone,
a unit whose cells are too small has its excess pushed across the
border into a sibling, the two units' targets swap, and every
national total still reconciles.}
\item{\code{.warn_unallocated_crops()}}{reports at the target grain, so a
unit that cannot place its target is visible instead of being
averaged into its container's success.}
\item{The capacity ceiling}{\code{.capacity_bases()} keys the unit
multi-cropping factor on \verb{(area_code, level_polity_code)}.}
}

Two places do \strong{not} carry it, for stated reasons.
\code{.redistribute_countries_dt()} still iterates \code{area_code}: it is a
memory bound on the per-country working set, and the compartment key is
inside each subset, so splitting further would only make the chunks
smaller. \code{.compartment_interval_groups()} stays on \verb{(lon, lat, area_code)} because \code{polycell_id} -- and, at depth, the unit code --
changes at a succession, which is exactly what its open-ended-interval
test must see across.
}

\section{What binds, and what is only measured}{

\itemize{
\item The national total binds. Admin statistics set the within-country
shape and nothing else; a yield or production row never binds
(decision T31(i)) and is dropped, counted, by
\code{\link[=build_level_crop_targets]{build_level_crop_targets()}}.
\item The reported share binds inside the country. Where a unit's
target does not fit its cells, the target wins and the ceiling gives
way; the excess is measured per unit, at both multi-cropping bases,
and returned in \code{breach} (decisions T31(c), T31(g)).
\item Partial coverage raises a RESIDUAL pseudo-unit carrying
\code{national - admin_sum}, supported by the non-reporting units' cells
only. With complete coverage there is no residual: the reported units
rescale proportionally and the leftover is a diagnostic, refused only
beyond both tolerances (decisions T31(a), T31(d)).
}
}

\examples{
# Two units of container 1 (Armenia), one cell each, one crop.
layer <- tibble::tribble(
  ~lon,  ~lat, ~area_code, ~level_polity_code, ~level, ~cell_area_frac,
  0.25, 50.25,         1L, "A-A1-1900-2100",       1L,               1,
  0.75, 50.25,         1L, "A-A2-1900-2100",       1L,               1
)
shares <- tibble::tribble(
  ~area_code, ~level_polity_code, ~item_prod_code, ~year, ~value,
          1L, "A-A1-1900-2100",               15L, 2000L,     150,
          1L, "A-A2-1900-2100",               15L, 2000L,     100
) |>
  dplyr::mutate(level = 1L, indicator_used = "area_harvested")
out <- allocate_level_crops(
  country_areas = tibble::tibble(
    year = 2000L, area_code = 1L, item_prod_code = 15L,
    harvested_area_ha = 250
  ),
  crop_patterns = tibble::tibble(
    lon = c(0.25, 0.75), lat = 50.25, item_prod_code = 15L,
    harvest_fraction = c(0.5, 0.5)
  ),
  gridded_cropland = tibble::tibble(
    lon = c(0.25, 0.75), lat = 50.25, year = 2000L,
    cropland_ha = c(1000, 1000)
  ),
  allocation_layer = layer,
  admin_shares = shares
)
out$targets[c("level_polity_code", "target_ha", "method_crop_alloc")]
}
\seealso{
\code{\link[=build_level_crop_targets]{build_level_crop_targets()}}, \code{\link[=build_allocation_layer]{build_allocation_layer()}},
\code{\link[=build_gridded_landuse]{build_gridded_landuse()}}.
}
