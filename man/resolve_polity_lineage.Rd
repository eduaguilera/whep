% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polity_lineage.R
\name{resolve_polity_lineage}
\alias{resolve_polity_lineage}
\title{Resolve a national row onto the polity its grid support carries that year}
\usage{
resolve_polity_lineage(
  national,
  support,
  basis = c("historical_polity", "constant_territory"),
  polities = whep::polities
)
}
\arguments{
\item{national}{A tibble of national rows with at least \code{area_code} and
\code{year}. Any other column is carried through untouched; no row is added,
dropped or reordered.}

\item{support}{A tibble of the polity intervals the spatial support actually
holds cells for, with \code{polity_code}, \code{start_year} and \code{end_year}.
\code{start_year} is inclusive and \code{end_year} exclusive, the \link{polities}
convention. \code{read_polycell_support()} returns a superset of these columns.}

\item{basis}{Which polity vintage binds a row, \code{"historical_polity"}
(default) or \code{"constant_territory"}. See \emph{Which vintage binds is a
choice, not a fact}.}

\item{polities}{The polity table carrying the \code{predecessor} edges the walk
follows; defaults to \link{polities}. Supplied as an argument so a caller can
pin a vintage rather than inherit whichever one the package ships.}
}
\value{
\code{national} with three columns added: \code{lineage_polity_code}, the
polity whose cells the row belongs on, \code{lineage_polity_name}, and
\code{method_polity_lineage}, one of \code{"anchor"} (the reporting polity is
itself carried at that year), \code{"predecessor"} (a predecessor is),
\code{"sibling_interval"} (a different interval of the polity the walk landed
on is -- the support and \code{polities} disagree about the interval),
\code{"constant_territory"}, or \code{"unresolved"}. An unresolved row keeps \code{NA}
rather than being dropped, so the gap stays visible, and is warned about
with condition class \code{whep_lineage_unresolved}.
}
\description{
The national tables and the cell support are keyed on \strong{different polity
vintages}. A 1961 harvested-area row is keyed \code{area_code} 185, whose
reporting polity is \code{RUS-1991-2014}; a year-aware read of the polycell
support holds \code{F228-1945-1991} (the USSR) in 1961 and no Russian polity at
all. The row then matches no cell and
\code{build_gridded_landuse()}'s missing-reporter guard drops its whole national
total.

This is the \strong{national-side lineage step}: for every \verb{(area_code, year)} it
walks the \code{predecessor} edges of \link{polities} from the row's reporting polity
back to the polity the supplied \code{support} actually carries in that year, and
records which rule answered. It moves no value and allocates nothing --
splitting a predecessor's cells between its successors is the consumer's
job, and this function only says \strong{which polity's cells} a row belongs on.

Measured on the live \code{polycell_support} pin (\verb{20260907T111653Z-e654d}) and
the \code{spatialize-country-areas} pin, over the 1961 crop tables: 27 of 194
reporting areas carry 164.2 Mha of harvested area (17.2\% of the world) that
a year-aware support cannot place. Under \code{basis = "historical_polity"} the
lineage places 21 of them, 156.5 Mha, leaving a 7.7 Mha (0.81\%) residue
that is \strong{not} a lineage failure: 0.13 Mha is four Rest-of-region
reporting buckets, which are aggregate polities the support excludes by
construction under either vintage, and 7.6 Mha is Viet Nam and Yemen, whose
reporting areas resolve to aggregate polities (\code{F237-1954-1975},
\code{F249-1918-1990}) whose members the support carries separately -- splitting
those between members needs a share rule this function does not invent.
}
\section{Which vintage binds is a choice, not a fact}{

Two bases are defensible and they differ numerically, so \code{basis} selects
between them and the answer is stamped on every row in
\code{method_polity_lineage}. They are alternatives, never a silent fallback.

\describe{
\item{\code{"historical_polity"}}{The default and the more rigorous. A row is
understood as a share of the entity that reported in that year -- the
FAOSTAT back-series is a modern-territory reconstruction of it -- and is
placed on that entity's cells. Conserves every national total against a
year-aware support; leaves the successor's share of the predecessor's
cells for the consumer to decide.}
\item{\code{"constant_territory"}}{The row keeps its reporting polity, i.e.
modern borders wherever a territory changed. Simpler, and what a
present-day snapshot support does implicitly, but it keeps the
attribution error at every succession: 164.2 Mha of 1961 harvested area
has no cell to land on.}
}
}

\section{The support is an input, and its supply is asserted}{

\code{support} is required and is checked for rows covering every year in
\code{national}, because a lineage resolved against \link{polities} alone can be
confidently wrong. \code{polities} carries two overlapping Yugoslav intervals,
\code{F248-1920-1991} and \code{F248-1947-1991}, and the published support emitted
only the second: a walk that consulted the edges alone would answer
\code{F248-1920-1991}, satisfy every totals check, and still find no cell. An
edgeless \code{polities} is refused for the same reason -- it would return every
anchor unchanged and reconcile perfectly.
}

\examples{
national <- tibble::tribble(
  ~area_code, ~year, ~harvested_area_ha,
         185, 1961L,           85049155,
         185, 2015L,           45000000
)
support <- tibble::tribble(
  ~polity_code, ~start_year, ~end_year,
  "F228-1945-1991",      1945L,     1991L,
  "RUS-2014-2025",       2014L,     2025L
)
resolve_polity_lineage(national, support)
}
