% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_livestock_nutrient_flows.R
\name{build_livestock_nutrient_flows}
\alias{build_livestock_nutrient_flows}
\title{Build livestock nutrient flows from realised feed intake.}
\usage{
build_livestock_nutrient_flows(
  intake,
  resolution = "national",
  methods = list(),
  gridded = NULL
)
}
\arguments{
\item{intake}{A tibble of realised feed intake (the \code{\link[=redistribute_feed]{redistribute_feed()}}
result); see \code{\link[=estimate_n_excretion]{estimate_n_excretion()}} for the required columns. At
\code{"subnational"} resolution \code{sub_territory} is the \code{"lon_lat"} cell id.}

\item{resolution}{One of \code{"global"}, \code{"national"} (default) or
\code{"subnational"}. Transport between cells runs only at \code{"subnational"}.}

\item{methods}{A named list of per-stage option lists, any of \code{excretion},
\code{split}, \code{losses}, \code{allocation} and \code{transport}, each forwarded to the
matching pipeline function's \code{options}.}

\item{gridded}{The land-surface layer (\code{crops} and optional \code{grass} tibbles)
passed to \code{\link[=allocate_manure_to_land]{allocate_manure_to_land()}}; required for the default
\code{"potential_uptake"} cap. \code{NULL} is treated as an empty list.}
}
\value{
A named list with \code{applied} (manure applied per
\verb{land_use x crop (x cell)} with \code{manure_type} (\code{"Excreta"}/\code{"Solid"}/
\code{"Liquid"}) and all \verb{method_*} provenance columns), \code{losses}
(management-loss side-streams per polity) and \code{excretion} (the
per-category excretion totals).
}
\description{
Top-level driver that traces the nitrogen, carbon and volatile solids in
realised feed intake (the \code{\link[=redistribute_feed]{redistribute_feed()}} result) through livestock
excretion, manure management and application to soil by land use and crop,
plus the management-loss side-streams. It chains
\code{\link[=estimate_n_excretion]{estimate_n_excretion()}}, \code{\link[=split_manure_management]{split_manure_management()}},
\code{\link[=apply_management_losses]{apply_management_losses()}} and \code{\link[=allocate_manure_to_land]{allocate_manure_to_land()}}; at the
\code{"subnational"} resolution it additionally spills each cell's un-placeable
surplus to neighbouring cells with \code{\link[=allocate_manure_transport]{allocate_manure_transport()}} before local
disposal. Every method choice is recorded in a \verb{method_*} provenance column and
the nitrogen balance (excreted = applied + management losses) is conserved.
}
\examples{
intake <- tibble::tribble(
  ~year, ~territory, ~sub_territory, ~livestock_category,
  ~item_cbs_code, ~feed_quality, ~intake_dm_t,
  2020L, "ESP", NA, "Cattle_milk", 2513L, "high_quality", 200,
  2020L, "ESP", NA, "Cattle_milk", NA, "grass", 600
)
gridded <- list(
  crops = tibble::tribble(
    ~year, ~territory, ~sub_territory, ~crop, ~manure_n_receptivity, ~crop_n_cap,
    2020L, "ESP", NA, "barley", 6, 200,
    2020L, "ESP", NA, "wheat", 4, 200
  )
)
build_livestock_nutrient_flows(intake, gridded = gridded)
}
