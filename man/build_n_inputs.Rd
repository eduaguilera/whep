% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_balance_inputs.R
\name{build_n_inputs}
\alias{build_n_inputs}
\title{Assemble gridded nitrogen inputs from every WHEP N-input source.}
\usage{
build_n_inputs(
  years = NULL,
  resolution = c("grid", "polity"),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
keeps every year the assembled inputs cover.}

\item{resolution}{\code{"grid"} (default, per cell/crop/year/fert_type) or
\code{"polity"} (summed to \code{area_code}/\code{item_cbs_code}/\code{year}/\code{fert_type}).}

\item{data}{Named list of pre-loaded, caller-supplied upstream inputs.
Each of the following is required for its corresponding \code{fert_type} to
be emitted (a missing one silently skips that source rather than
erroring, since callers may only want a subset):
\itemize{
\item \code{bnf_input}: \code{\link[=calculate_bnf]{calculate_bnf()}}'s required input tibble (\code{lon}, \code{lat},
\code{area_code}, \code{year}, \code{item_prod_code}, \code{crop_npp_n_t}, \code{product_n_t},
\code{weed_npp_n_t}, \code{land_use}, \code{legumes_seeded},
\code{seeded_cover_crop_share}, \code{area_ha}).
\item \code{npp_n_input}: \code{\link[=calculate_npp_carbon_nitrogen]{calculate_npp_carbon_nitrogen()}}'s required input
tibble (\code{lon}, \code{lat}, \code{area_code}, \code{year}, \code{item_prod_code},
\code{item_cbs_code}, \code{product_dm_t}, \code{residue_dm_t}, \code{root_dm_t},
optionally \code{residue_soil_dm_t}).
\item \code{livestock_intake}: \code{\link[=build_livestock_nutrient_flows]{build_livestock_nutrient_flows()}}'s \code{intake}
argument (the \code{\link[=redistribute_feed]{redistribute_feed()}} realised-intake contract), plus
\code{gridded} (its land-surface layer) and \code{resolution}/\code{methods}
(forwarded as-is).
\item \code{nhx}, \code{noy}, \code{cell_polity}: \code{\link[=build_n_deposition]{build_n_deposition()}}'s inputs.
\item \code{urban_population}, \code{cropland_ha}, \code{cell_polity}: \code{\link[=build_urban_n]{build_urban_n()}}'s
inputs.
\item \code{carbon_balance}: \code{\link[=build_carbon_balance]{build_carbon_balance()}}'s \code{"grid"}-resolution
output (\code{lon}, \code{lat}, \code{area_code}, \code{land_use}, \code{year}, \code{area_ha},
\code{son_change_kgn_ha}); this driver requires it supplied directly, it
is never computed here.
\item \code{primary_prod}, \code{fertilizer}, \code{crop_patterns}, \code{type_cropland},
\code{cell_polity}: the synthetic-fertiliser assembly (country total from
\code{fertilizer}, the \code{faostat-fertilizer-nutrients} pin, split to crops
by \code{primary_prod} harvested-area shares, then to cells by
\code{crop_patterns}/\code{type_cropland}).
\item \code{gridded}, \code{resolution} (of the manure engine, default \code{"national"}),
\code{methods}: forwarded to \code{\link[=build_livestock_nutrient_flows]{build_livestock_nutrient_flows()}}.
}}

\item{example}{If \code{TRUE}, return a small fixture instead of assembling
real data. Defaults to \code{FALSE}.}
}
\value{
A tibble. At \code{resolution = "grid"}: \code{lon}, \code{lat}, \code{area_code},
\code{item_cbs_code}, \code{year}, \code{fert_type}, \code{n_input_t}. At
\code{resolution = "polity"}: \code{area_code}, \code{item_cbs_code}, \code{year},
\code{fert_type}, \code{n_input_t} (summed over cells).
}
\description{
Combines biological nitrogen fixation (\code{\link[=calculate_bnf]{calculate_bnf()}}), residue/root N
recycling (\code{\link[=calculate_npp_carbon_nitrogen]{calculate_npp_carbon_nitrogen()}}), livestock manure
(\code{\link[=build_livestock_nutrient_flows]{build_livestock_nutrient_flows()}}), atmospheric deposition
(\code{\link[=build_n_deposition]{build_n_deposition()}}), urban/human-excreta N (\code{\link[=build_urban_n]{build_urban_n()}}), soil
organic-matter mineralization (\code{\link[=build_carbon_balance]{build_carbon_balance()}}'s
\code{son_change_kgn_ha}) and synthetic fertiliser (a country total
spatialized to crops and cells via \code{\link[=spatialize_country_n_to_crops]{spatialize_country_n_to_crops()}})
into one long-format tibble of nitrogen inputs to agricultural land.

\code{fert_type} values: \code{"bnf"}, \code{"recycling"}, \code{"manure_solid"},
\code{"manure_liquid"}, \code{"excreta"}, \code{"deposition"}, \code{"urban"},
\code{"som_mineralization"}, \code{"synthetic"} and \code{"accum_loss"}. The last is a
documented gap (perennial-crop standing-biomass N accumulation from
Spain_Hist's N_balance.R): its source computation was not available for
this task, so it is never emitted, only reserved in the vocabulary.

Terms that are fundamentally per-cell or per-land-use rather than
per-crop (\code{"deposition"}, \code{"urban"}, \code{"som_mineralization"}) carry
\code{item_cbs_code = NA_integer_}, the same "no specific item" sentinel
already used package-wide for non-crop rows (e.g.
\code{\link[=redistribute_feed]{redistribute_feed()}}'s grass-border-grazing rows).
}
\examples{
build_n_inputs(example = TRUE)
}
