% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_balance_inputs.R
\name{build_n_inputs}
\alias{build_n_inputs}
\title{Assemble gridded nitrogen inputs from every WHEP N-input source.}
\usage{
build_n_inputs(
  years = NULL,
  resolution = c("grid", "polity"),
  synthetic_method = NULL,
  data = list(),
  example = FALSE
)
}
\arguments{
\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
keeps every year the assembled inputs cover.}

\item{resolution}{\code{"grid"} (default, per cell/crop/year/fert_type) or
\code{"polity"} (summed to \code{area_code}/\code{item_cbs_code}/\code{year}/\code{fert_type}).}

\item{synthetic_method}{Synthetic-N crop allocation method, \code{"coello"} or
\code{"area_share"}. When \code{NULL} (default), uses
\code{data$synthetic_method \%||\% "coello"} for backwards compatibility.}

\item{data}{Named list of pre-loaded, caller-supplied upstream inputs.
Each of the following is required for its corresponding \code{fert_type} to
be emitted (a missing one silently skips that source rather than
erroring, since callers may only want a subset):
\itemize{
\item \code{bnf_input}: \code{\link[=calculate_bnf]{calculate_bnf()}}'s required input tibble (\code{lon}, \code{lat},
\code{area_code}, \code{year}, \code{item_prod_code}, \code{crop_npp_n_t}, \code{product_n_t},
\code{weed_npp_n_t}, \code{land_use}, \code{legumes_seeded},
\code{seeded_cover_crop_share}, \code{area_ha}).
\item \code{npp_n_input}: \code{\link[=calculate_npp_carbon_nitrogen]{calculate_npp_carbon_nitrogen()}}'s required input
tibble (\code{lon}, \code{lat}, \code{area_code}, \code{year}, \code{item_prod_code},
\code{item_cbs_code}, \code{product_dm_t}, \code{residue_dm_t}, \code{root_dm_t},
optionally \code{residue_soil_dm_t}).
\item \code{livestock_intake}: \code{\link[=build_livestock_nutrient_flows]{build_livestock_nutrient_flows()}}'s \code{intake}
argument (the \code{\link[=redistribute_feed]{redistribute_feed()}} realised-intake contract), plus
\code{gridded} (its land-surface layer) and \code{resolution}/\code{methods}
(forwarded as-is).
\item \code{nhx}, \code{noy}, \code{cell_polity}: \code{\link[=build_n_deposition]{build_n_deposition()}}'s inputs.
\item \code{deposition_scope}: which of a polycell's territory the \code{"deposition"}
term is credited with. \code{"territory"} (default) is land plus inland
water plus ice: nitrogen deposited on a lake or a glacier still drives
indirect N2O and still reaches the eutrophication pathway, so the
impact terms have to account for it. \code{"land"} is the terrestrial
surface alone, for purposes that want it, and it needs a \code{cell_polity}
carrying \code{land_area_ha}, \code{inland_water_ha} and \code{ice_area_ha}; asking
for it without them aborts rather than silently returning the whole
territory. Recorded in \code{method_deposition_scope}.
\item \code{ag_land_support}: agricultural physical land support keyed by \code{lon},
\code{lat}, \code{area_code}, \code{year}, \code{item_cbs_code}, with \code{land_use}
(\code{"cropland"} or \code{"grassland"}) and positive \code{area_ha}. Optional: when
absent it is derived natively by \code{\link[=build_ag_land_support]{build_ag_land_support()}} from
\code{cell_polity}, \code{type_cropland} and \code{crop_patterns} (plus \code{states} or
\code{grassland_ha} for the grassland side). Supply it to override that
derivation with a better land surface. Cropland rows identify crop CBS
items; all pasture/rangeland rows use CBS 3000.
\item \code{grassland_source}, \code{gridded_pasture}, \code{grassland_ha}, \code{states}:
forwarded to \code{\link[=build_ag_land_support]{build_ag_land_support()}} when the support is derived.
\code{grassland_source} selects its \code{grassland} argument
(\code{"gridded_pasture"} default, \code{"luh2"}, or \code{"none"} for cropland-only
support).
\item \code{urban_population}, \code{cropland_ha}, \code{cell_polity}: \code{\link[=build_urban_n]{build_urban_n()}}'s
inputs.
\item \code{carbon_balance}: \code{\link[=build_carbon_balance]{build_carbon_balance()}}'s \code{"grid"}-resolution
output (\code{lon}, \code{lat}, \code{area_code}, \code{land_use}, \code{year}, \code{area_ha},
\code{son_change_kgn_ha}); this driver requires it supplied directly, it
is never computed here.
\item \code{primary_prod}, \code{fertilizer}, \code{crop_patterns}, \code{type_cropland},
\code{cell_polity}: the synthetic-fertiliser assembly (country total from
\code{fertilizer}, the \code{faostat-fertilizer-nutrients} pin, split to crops
by the chosen crop-share method, then to cells by
\code{crop_patterns}/\code{type_cropland}).
\item \code{synthetic_method}: how the synthetic-N country total is split across
crops, \code{"coello"} (default; Coello 2025 rate-weighted, FAOSTAT-
conserving) or \code{"area_share"} (harvested-area shares only).
\item \code{coello_rates}: crop-specific synthetic-N rate table shaped like
\link{coello_synthetic_n} (\code{year}, \code{area_code}, \code{item_cbs_code},
\code{kg_n_ha}); defaults to \code{whep::coello_synthetic_n}. Used only when
\code{synthetic_method = "coello"}.
\item \code{gridded}, \code{resolution}, \code{methods}: forwarded to
\code{\link[=build_livestock_nutrient_flows]{build_livestock_nutrient_flows()}}. \code{resolution} is the manure engine's
own axis, not this function's: it defaults to \code{"subnational"} at
\code{resolution = "grid"} (cell-level nitrogen needs cell-level manure) and
to \code{"national"} otherwise. A value supplied here is always honoured.
}}

\item{example}{If \code{TRUE}, return a small fixture instead of assembling
real data. Defaults to \code{FALSE}.}
}
\value{
A tibble. At \code{resolution = "grid"}: \code{lon}, \code{lat}, \code{area_code},
\code{item_cbs_code}, \code{year}, \code{fert_type}, \code{n_input_t},
\code{method_recycling_n}, \code{method_synthetic}, \code{method_deposition_scope}. At
\code{resolution = "polity"}: \code{area_code}, \code{item_cbs_code}, \code{year},
\code{fert_type}, \code{method_recycling_n}, \code{method_synthetic},
\code{method_deposition_scope}, \code{n_input_t} (summed over cells).
\code{method_recycling_n} records which residue basis the \code{"recycling"} term
used: \code{"residue_soil_returned"} when the upstream NPP input supplied
\code{residue_soil_dm_t} (residue N net of removal for feed/fuel/burning) or
\code{"total_residue"} when only gross residue N was available; it is \code{NA} for
every other \code{fert_type}. \code{method_synthetic} records the synthetic
crop-split basis (\code{"coello"} or \code{"area_share"}) on \code{"synthetic"} rows and
is \code{NA} for every other \code{fert_type}. \code{method_deposition_scope} records
which of the polycell's territory the \code{"deposition"} term was credited
with (\code{"territory"} or \code{"land"}) and is \code{NA} for every other \code{fert_type}.
Both grains also carry the polity columns below.
}
\description{
Combines biological nitrogen fixation (\code{\link[=calculate_bnf]{calculate_bnf()}}), residue/root N
recycling (\code{\link[=calculate_npp_carbon_nitrogen]{calculate_npp_carbon_nitrogen()}}), livestock manure
(\code{\link[=build_livestock_nutrient_flows]{build_livestock_nutrient_flows()}}), atmospheric deposition
(\code{\link[=build_n_deposition]{build_n_deposition()}}), urban/human-excreta N (\code{\link[=build_urban_n]{build_urban_n()}}), soil
organic-matter mineralization (\code{\link[=build_carbon_balance]{build_carbon_balance()}}'s
\code{son_change_kgn_ha}) and synthetic fertiliser (a country total
spatialized to crops and cells via \code{\link[=spatialize_country_n_to_crops]{spatialize_country_n_to_crops()}})
into one long-format tibble of nitrogen inputs to agricultural land.

\code{fert_type} values: \code{"bnf"}, \code{"recycling"}, \code{"manure_solid"},
\code{"manure_liquid"}, \code{"excreta"}, \code{"deposition"}, \code{"urban"},
\code{"som_mineralization"}, \code{"synthetic"} and \code{"accum_loss"}. The last is a
documented gap (perennial-crop standing-biomass N accumulation from
Spain_Hist's N_balance.R): its source computation was not available for
this task, so it is never emitted, only reserved in the vocabulary.

Terms that are fundamentally per-cell or per-land-use rather than per-crop
are allocated over the agricultural land support, either supplied as
\code{data$ag_land_support} or derived by \code{\link[=build_ag_land_support]{build_ag_land_support()}} from the
gridded inputs already present. Deposition uses both cropland and
grassland support. \code{"urban"}, \code{"som_mineralization"}, and manure already
assigned upstream to Cropland but lacking a crop use only local cropland
support, so manure is not reassigned to grassland after the manure engine's
capacity allocation. Forest and natural land are outside that support and
therefore outside the agricultural balance. Grassland is represented by CBS
3000; no intensive/extensive class is inferred.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning. Where that polity is not live in the
row's own year -- 41.5\% of the pre-1961 \verb{(area, year)} cells --
\code{\link[=add_polity_code]{add_polity_code()}} says so as \code{mapping_status == "backcast_anchor"}, and
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports it as \code{gap_kind == "backcast_anchor"}.
These columns do not say so either way.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_n_inputs(example = TRUE)
}
