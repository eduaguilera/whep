% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_faostat.R
\name{.flag_carry_forward}
\alias{.flag_carry_forward}
\title{Flag constant-value streaks at the end of a time series}
\usage{
.flag_carry_forward(
  df,
  by,
  min_run = 3L,
  value_col = "value",
  time_col = "year"
)
}
\arguments{
\item{df}{A tibble in long format with at least \code{year}, \code{value}, and
the id columns listed in \code{by}.}

\item{by}{Character vector of grouping columns (everything except
\code{year} and \code{value}).}

\item{min_run}{Minimum number of identical consecutive final years to
flag (default 3).}

\item{value_col}{Name of the value column (default \code{"value"}).}

\item{time_col}{Name of the time column (default \code{"year"}).}
}
\value{
The input tibble with a logical column \code{qc_carry_forward}
(\code{TRUE} for flagged rows, \code{FALSE} otherwise).
}
\description{
Detects series where the value is identical for \code{min_run} or more
consecutive years ending at \code{max(year)}.
These are almost certainly FAOSTAT carry-forwards (imputed data that
just repeats the last observed value).
}
\keyword{internal}
