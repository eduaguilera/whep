% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manner_model.R
\name{calculate_manner_nh3_default}
\alias{calculate_manner_nh3_default}
\title{Estimate ammonia-N volatilisation with MANNER's gross-default technique
and incorporation-delay blend.}
\usage{
calculate_manner_nh3_default(
  n_applied_t = NULL,
  fertiliser = NULL,
  drivers = list(),
  example = FALSE
)
}
\arguments{
\item{n_applied_t}{Numeric, nitrogen applied (t).}

\item{fertiliser}{One of \code{"cattle_slurry"}, \code{"pig_slurry"}, \code{"FYM"},
\code{"poultry_manure"}, \code{"urban"} (the \code{\link[=calculate_manner_nh3]{calculate_manner_nh3()}}
organic-manure path; the gross default only applies there).}

\item{drivers}{A named list of driver values, as in
\code{\link[=calculate_manner_nh3]{calculate_manner_nh3()}}'s organic path, EXCEPT \code{technique} and
\code{incorporation_delay_h} are neither required nor used (they come from
\link{manner_default_technique_mix} instead).}

\item{example}{If \code{TRUE}, return a small fixture instead of computing
from drivers. Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{n_applied_t}, \code{ef} (share-weighted realised
emission factor), \code{nh3_n_t} and \code{method_manner}.
}
\description{
A thin wrapper around \code{\link[=calculate_manner_nh3]{calculate_manner_nh3()}}'s organic-manure path that
fills in \code{technique} and \code{incorporation_delay_h} from
\link{manner_default_technique_mix} instead of requiring them as caller-
supplied drivers. It calls \code{\link[=calculate_manner_nh3]{calculate_manner_nh3()}} once per row of
\link{manner_default_technique_mix} and combines the results into a single
share-weighted emission factor. Use this only where real per-cell/per-era
application-technique survey data does not exist (see Details).
}
\details{
\link{manner_default_technique_mix} is a deliberate, permanent gross-
assumption default, not a region/era-specific survey. It fixes
\code{technique = "Broadcast"} on every row, matching Spain_Hist's own real
production MANNER run, which itself hardcodes Broadcast application
nationally with no region/era variation
(\code{factor_ap_technique <- application_technique_manure[Technique == "Broadcast", ...]} applied unconditionally to its whole national run).
For incorporation delay, it blends four of
\link{manner_incorporation_factor}'s \code{delay_bin} categories in equal 25\%
shares: 25\% of applied nitrogen assumed never incorporated
(\code{incorporation_delay_h = NA}, matching how \code{\link[=calculate_manner_nh3]{calculate_manner_nh3()}}
treats a missing delay as \code{"No incorporation"}), 25\% incorporated within
2 hours (\code{incorporation_delay_h = 2}), 25\% within 12-24 hours
(\code{incorporation_delay_h = 24}) and 25\% within 1-2 days
(\code{incorporation_delay_h = 48}). Only \code{ef} and \code{nh3_n_t} are
share-weighted across the four blend calls; \code{n_applied_t} is carried
through as-is, not re-weighted, since the full \code{n_applied_t} is assumed
split across the four incorporation-delay scenarios rather than
quadruplicated.
}
\examples{
calculate_manner_nh3_default(example = TRUE)
}
