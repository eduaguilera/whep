% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biodiversity_extension.R
\name{build_biodiversity_extension}
\alias{build_biodiversity_extension}
\title{Build the biodiversity footprint extension.}
\usage{
build_biodiversity_extension(
  method = c("chaudhary_brooks_2018"),
  intensity = c("intense", "light", "minimal"),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{method}{Characterization-factor source. Currently only
\code{"chaudhary_brooks_2018"}, the country-aggregated global land-occupation
CFs of Chaudhary & Brooks (2018).}

\item{intensity}{Land-use intensity assumed for all occupied land, one of
\code{"intense"} (default), \code{"light"} or \code{"minimal"}. Selects the matching CF
and is recorded in \code{method_biodiversity}.}

\item{data}{Optional named list of pre-loaded inputs to avoid remote reads:
\code{land} (a land extension tibble with columns \code{year}, \code{area_code},
\code{item_cbs_code} and \code{impact_u}, the occupied land area in hectares) and
\code{cf} (a CF table overriding \link{biodiversity_cf}). When \code{land} is absent it is
built from \code{\link[=build_cropgrids_land_extension]{build_cropgrids_land_extension()}} and
\code{\link[=build_grassland_land_extension]{build_grassland_land_extension()}}.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (potential species loss in PDF) and \code{method_biodiversity} (the
chosen CF source and intensity).
}
\description{
Layer land-use biodiversity characterization factors (CFs) on a land
extension to produce a biodiversity footprint extension keyed by
\verb{(year, area_code, item_cbs_code)}, ready for \code{\link[=build_footprint]{build_footprint()}}.

Each sector's occupied land area (hectares, taken from a land extension) is
multiplied by the country- and land-use-specific CF in \link{biodiversity_cf}
(Chaudhary & Brooks 2018), giving potential global species loss in
Potentially Disappeared Fraction of species (PDF). Crop sectors take the
\code{"crop"} CF and grassland sectors (group "Grass", item_cbs 3000/3002/3003)
take the \code{"pasture"} CF.

This layers directly on the land footprint, so the land source is the
caller's choice: pass any land extension (e.g. from
\code{\link[=build_cropgrids_land_extension]{build_cropgrids_land_extension()}}, \code{\link[=get_crop_land_extension]{get_crop_land_extension()}} or
\code{\link[=build_grassland_land_extension]{build_grassland_land_extension()}}) through \code{data$land}.

Areas whose \code{area_code} has no ISO3 (region aggregates such as "Rest of
Africa") or whose country has no published CF receive no biodiversity row
and are treated as zero by \code{\link[=build_footprint]{build_footprint()}}.
}
\examples{
build_biodiversity_extension(example = TRUE)
}
