% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nourishment_floor.R
\name{build_nourishment_floor}
\alias{build_nourishment_floor}
\title{Build the SJOS-N nourishment floor.}
\usage{
build_nourishment_floor(
  data = list(),
  shortfall = 0.025,
  quality = "none",
  requirement_sd = 0.12
)
}
\arguments{
\item{data}{Named list of injected inputs. \code{requirement} (\code{year},
\code{area_code}, \code{requirement_g_cap_day}), \code{dispersion} (\code{year}, \code{area_code},
\code{sigma}) and \code{loss_wedge} (\code{year}, \code{area_code}, \code{omega}) are required;
optional \code{supply} (\code{year}, \code{area_code}, \code{protein_g_cap_day}) adds the
prevalence column.}

\item{shortfall}{Tolerated prevalence of inadequacy, in \verb{(0, 1)}. Defaults to
\code{0.025}.}

\item{quality}{Protein-quality adjustment. Only \code{"none"} exists so far; see
the description for what its absence costs.}

\item{requirement_sd}{Log-scale SD of the requirement, \code{S_R}. Defaults to
\code{0.12}, TRS 935 p.38 for adults on a per-kilogram basis. The report notes
this captures only about a fifth of observed between-individual variance,
so it is exposed for sensitivity rather than fixed.}
}
\value{
A tibble keyed by \code{year}, \code{area_code} with \code{floor_g_cap_day},
\code{requirement_g_cap_day}, \code{sigma_intake}, \code{sigma_deficit}, \code{omega},
\code{method_quality} and \code{method_shortfall}, plus \code{prevalence_protein_deficit}
when \code{data$supply} is given, and the polity columns below.
}
\description{
Returns the threshold on \strong{mean per-capita protein supply} below which a
country-year is under-nourished, composed from the terms that replace the
unsourced 1.35 multiplier (whep#753). Per country \code{c} and year \code{t}:

\deqn{S_D = \sqrt{S_I^2 + S_R^2}}
\deqn{F = \bar{R} \cdot
  \exp(z_{1-p} S_D + S_I^2/2) / (1 - \omega)}

\verb{R̄} is the demographically weighted \strong{average} requirement from
\code{\link[=build_protein_requirement]{build_protein_requirement()}}, \code{S_I} the within-country log-scale intake
dispersion from \code{\link[=build_intake_dispersion]{build_intake_dispersion()}}, and \code{omega} the supply-to-intake
wedge from \code{\link[=build_loss_wedge]{build_loss_wedge()}}. \code{S_R} is the log-scale spread of the
requirement itself.

Two terms in that product are easy to drop and both are signed:
\code{exp(z * S_D)} carries the median requirement to the required median intake
at the tolerated shortfall, and \code{exp(S_I^2/2)} carries a required median to a
required \strong{mean}, which is what makes the floor comparable with a per-capita
supply figure at all. Omitting the second is not conservative — it sets the
required mean equal to the required median, about 5\% too low at a typical
dispersion.

\code{shortfall} is the tolerated prevalence of inadequacy, 2.5\% by default, and
that value is not a free choice: TRS 935 Figure 7 is headed "Safe population
intake ie. risk<2.5\%", and FAO separately calls 2.5\% "the lowest feasible
target that can be set for the PoU indicator". Departing from it is the
science decision; adopting it follows the source.

\strong{The protein-quality term is not built, and the floor is a known
understatement without it.} TRS 935 defines its safe level "for proteins
with a PDCAAS value of 1.0" (section 14.2); real national diets score below
1.0, so a quality-adjusted floor is higher, by a level shift the evidence
record puts at +11\% to +36\%. \code{quality = "none"} is therefore stamped in
\code{method_quality} rather than left implicit, and the method name will not
silently change meaning when the term lands.

Supplying \code{data$supply} adds \code{prevalence_protein_deficit}, the internal
quantity the floor inverts:
\eqn{\Phi(-(\log(\mathrm{supply}(1-\omega)) - S_I^2/2 - \log\bar{R})/S_D)}.
At \code{supply = floor} it returns \code{shortfall} exactly, which is the round trip
the tests pin.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning. Where that polity is not live in the
row's own year -- 41.5\% of the pre-1961 \verb{(area, year)} cells --
\code{\link[=add_polity_code]{add_polity_code()}} says so as \code{mapping_status == "backcast_anchor"}, and
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports it as \code{gap_kind == "backcast_anchor"}.
These columns do not say so either way.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_nourishment_floor(
  data = list(
    requirement = tibble::tribble(
      ~year, ~area_code, ~requirement_g_cap_day,
      2010L, 10L,        50
    ),
    dispersion = tibble::tribble(
      ~year, ~area_code, ~sigma,
      2010L, 10L,        0.25
    ),
    loss_wedge = tibble::tribble(
      ~year, ~area_code, ~omega,
      2010L, 10L,        0.025
    )
  )
)
}
