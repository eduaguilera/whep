% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polycell_layers.R
\name{read_luh2_terrestrial}
\alias{read_luh2_terrestrial}
\title{Read the LUH2 terrestrial-area validation layer}
\usage{
read_luh2_terrestrial(vintage = c("GCB2022", "v2h"), dir = NULL)
}
\arguments{
\item{vintage}{Which LUH2 tree to read. \code{"GCB2022"} (default) is the
\code{UofMD-landState-LUH2-GCB2022} release under
\code{Sys.getenv("WHEP_LUH2_DIR")}; \code{"v2h"} is the base release under
\code{Sys.getenv("WHEP_LUH2_V2H_DIR")}.}

\item{dir}{Directory holding \code{staticData_quarterdeg.nc}, overriding the
vintage's environment variable.}
}
\value{
A \code{tibble} with \code{lon}, \code{lat} and \code{terrestrial_ha} on the 0.5-degree
grid, carrying the vintage in its \code{"luh2_vintage"} attribute.
}
\description{
Reads \code{staticData_quarterdeg.nc} and returns \code{(1 - icwtr) * carea} summed to
the 0.5-degree grid: the terrestrial area LUH2 itself implies. This is the
DA-5 validation layer for \code{\link[=build_polycell_support]{build_polycell_support()}} and is never a
production mask, because \code{icwtr} includes the ocean as well as ice and inland
water, and because LUH2 misses small islands its own 0.25-degree mask calls
sea.
}
\examples{
# Requires WHEP_LUH2_DIR to be set; not run without it.
if (nzchar(Sys.getenv("WHEP_LUH2_DIR"))) {
  read_luh2_terrestrial(vintage = "GCB2022")
}
}
