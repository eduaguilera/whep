% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/food_gini.R
\name{disaggregate_ussr}
\alias{disaggregate_ussr}
\title{Split the pre-1992 aggregate USSR supply into successor states.}
\usage{
disaggregate_ussr(x, ussr_shares, cutoff_year = 1992L)
}
\arguments{
\item{x}{A tibble with \code{year}, \code{area_code} and \code{population} columns (plus any
per-capita supply columns to inherit), for example a \code{\link[=build_food_supply]{build_food_supply()}}
output.}

\item{ussr_shares}{A lookup tibble with \code{ussr_area_code} (the aggregate USSR
area code), \code{successor_area_code} (a successor state's area code) and
\code{pop_share} (the successor's 1992 population share, summing to one per
aggregate). Injected as a package-data / integration input.}

\item{cutoff_year}{The first year for which successor states report
separately; the split applies to years strictly before it. Defaults to
\code{1992L}.}
}
\value{
\code{x} with the pre-cutoff aggregate USSR rows replaced by their
successor-state rows.
}
\description{
For years before \code{cutoff_year}, replaces the single aggregate USSR row
(the \code{area_code} listed as \code{ussr_area_code} in \code{ussr_shares}) with one row
per successor state, inheriting the aggregate's per-capita supply values and
scaling its population by the successor's 1992 population share, so the split
conserves the aggregate population and each successor carries the aggregate
per-capita supply. Rows at or after \code{cutoff_year}, and all non-USSR rows,
pass through unchanged. This mirrors the Global SJOS-N pre-1992 USSR
disaggregation, which distributes the Russian Federation Food Balance Sheet
supply across the ex-USSR states by their 1992 population weights.
}
\examples{
disaggregate_ussr(
  tibble::tribble(
    ~year, ~area_code, ~protein_g_cap_day, ~population,
    1990L, 228L, 50, 100,
    1995L, 228L, 55, 120
  ),
  tibble::tribble(
    ~ussr_area_code, ~successor_area_code, ~pop_share,
    228L, 1L, 0.6,
    228L, 2L, 0.4
  )
)
}
