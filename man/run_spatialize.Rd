% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_spatialize.R
\name{run_spatialize}
\alias{run_spatialize}
\title{Run the gridded land-use spatialization pipeline}
\usage{
run_spatialize(
  preset = c("lpjml", "whep"),
  years = NULL,
  components = c("landuse", "livestock"),
  overrides = list(),
  paths = list()
)
}
\arguments{
\item{preset}{One of \code{"lpjml"} or \code{"whep"}. Selects a default
bundle of engine flags and input choices. See \emph{Presets}.}

\item{years}{Integer vector of years to spatialize. If \code{NULL},
the preset default is used: for \code{"lpjml"} a 10-year benchmark
sequence (\code{seq(1850L, 2020L, by = 10L)}), intersected with the
years available in \code{country_areas}; for \code{"whep"} all years
present in \code{country_areas}.}

\item{components}{Character vector selecting which engines to run.
Defaults to \code{c("landuse", "livestock")}. Pass a subset to run
only one (e.g. \code{"landuse"}). Unknown entries raise an error.}

\item{overrides}{Named list of flags that override the preset.
Unknown keys raise an error. Recognised entries:
\itemize{
\item \code{use_type_constraint} (logical): enable/disable LUH2
type-aware allocation.
\item \code{aggregate_to_cft} (logical, default \code{TRUE}): write a
CFT-aggregated parquet alongside the crop-level output.
\item \code{max_iterations}, \code{expansion_threshold}: forwarded to the
landuse engine.
\item \code{cft_target}: one of \code{"whep"} (default for
\code{preset = "whep"}) or \code{"lpjml"} (default for
\code{preset = "lpjml"}). Selects which column of
\link{cft_mapping} drives CFT aggregation: \code{cft_name}
(granular 33-class WHEP taxonomy) or \code{cft_lpjml}
(12 LPJmL crop CFTs + single \code{others} bucket).
\item \code{area_key}: one of \code{"grid"} (default) or \code{"polity_area"},
forwarded to both engines. See \code{\link[=build_gridded_landuse]{build_gridded_landuse()}}'s
\emph{Which area code the output is keyed on}.
\item \code{country_grid}: which cell-to-polity crosswalk the engines
allocate into, \code{"polycell"} (default), \code{"centroid"} or
\code{"fraction"}. See \emph{Which cell-to-polity crosswalk}.
\item \code{grid_vintage}: which vintage of the polycell support the level-0
grid is read at, \code{"snapshot_2015"} (default) or \code{"year_aware"}. See
\code{\link[=read_level_country_grid]{read_level_country_grid()}}'s \emph{Which vintage of the support level 0 is
read at}. The two are different geographies, not two precisions of one:
\code{"snapshot_2015"} allocates every year of a run into the present-day
cell-to-country map, \code{"year_aware"} into the map valid that year. The
snapshot remains the default because the national tables are on a
constant-territory basis the historical support cannot receive: see
\code{.grid_vintages()} and \code{validation/spatialize_grid_vintage.R}.
Recorded in \code{run_metadata.yaml} and per row in \code{method_grid_vintage}.
Not read under \code{country_grid = "centroid"} or \code{"fraction"}, which carry
no validity interval at all and are recorded as \code{"static_crosswalk"}.
\item \code{level} (integer, default \code{0L}): containment depth the grid is
resolved at. \code{0L} is today's cell-to-\code{area_code} grid; \code{1L} and
deeper key the cells on admin units through
\code{\link[=read_level_country_grid]{read_level_country_grid()}}, which only the \code{"polycell"}
crosswalk supports.
\item \code{output_level} (integer, default \code{0L}): grain of the crop output.
\code{0L} sums granted-depth rows back onto the container, so
\verb{(lon, lat, area_code, item_prod_code, year)} stays unique and the
schema equals a level-0 run's; a positive value returns unit-grain
rows carrying \code{level_polity_code}. It may not exceed \code{level}.
\item \code{constraint_exclude} (default \code{NULL}): country x year ranges to hold
out of the admin constraint, as a named list such as
\code{list(USA = 1961:1989)}. Recorded in \code{run_metadata.yaml} and consumed
by the admin-shares resolver; it does not by itself change a run that
has no admin constraint wired.
\item \code{livestock_proxy}: one of \code{"luh2"} (default) or \code{"glw3"}, forwarded
to \code{\link[=build_gridded_livestock]{build_gridded_livestock()}}'s \code{proxy_method}. Under \code{"glw3"} the
density table is read with \code{\link[=read_glw_density]{read_glw_density()}}, which needs a
\code{WHEP_GLW3_DIR} tree and aborts without one; under \code{"luh2"} it is
not read at all.
\item \code{livestock_glw_variant}: which GLW3 product a \code{"glw3"} run
allocates on, \code{"DA"} (default, the dasymetric rasters) or \code{"AW"}
(the areal-weighted ones), forwarded to \code{\link[=read_glw_density]{read_glw_density()}}'s
\code{variant}. They are different within-country geographies, so the
resolved value is recorded twice: in \code{run_metadata.yaml} with the
rest of the config, and per row in the output's
\code{method_livestock_proxy} as \code{"glw3_da"} or \code{"glw3_aw"}. Ignored
under \code{livestock_proxy = "luh2"}, which reads no raster.
}}

\item{paths}{Named list of filesystem paths. Recognised entries:
\itemize{
\item \code{l_files_dir}: path to the \code{L_files} root, for local prepared inputs.
\item \code{input_dir}: directory holding the prepared input parquets. If \code{NULL}
and \code{l_files_dir} is unset, the pinned WHEP spatialization inputs are
used.
\item \code{out_dir}: output directory. If \code{NULL}, defaults to
\verb{<l_files_dir>/whep/spatialize/<preset>} when \code{l_files_dir} is supplied,
otherwise to a session temporary directory (suffixed with \verb{_custom} when
\code{overrides} is non-empty). Created if missing.
}}
}
\value{
Invisibly, a named list with \code{preset}, resolved \code{config},
\code{years}, \code{out_dir}, and \code{output_paths}.
}
\description{
Wrapper around \code{\link[=build_gridded_landuse]{build_gridded_landuse()}} that resolves a named
preset (\code{"lpjml"} or \code{"whep"}) into a consistent bundle of input
files, engine flags, and output paths. Use this to produce two
comparable outputs from the same prepared parquet inputs:
an LPJmL/LandInG-faithful run (for cell-by-cell comparison against
LPJmL inputs) and the full WHEP run (all historical years,
LUH2 type-aware allocation).

Presets can be combined with per-flag \code{overrides} to produce any
intermediate configuration; the resolved configuration is written
next to the outputs as \code{run_metadata.yaml} for traceability.
}
\section{Presets}{

\describe{
\item{\code{lpjml}}{LandInG-faithful configuration: no LUH2
type-aware allocation (\code{use_type_constraint = FALSE}) and a
short default year sample suited to comparison against
LPJmL inputs.}
\item{\code{whep}}{Full WHEP configuration: LUH2 type-aware
allocation (\code{use_type_constraint = TRUE}) and the full
historical year range present in \code{country_areas}.}
}
}

\section{Inputs read from \code{input_dir}}{

Landuse (\code{components} contains \code{"landuse"}):
\itemize{
\item \code{country_areas.parquet}
\item \code{crop_patterns.parquet}
\item \code{gridded_cropland.parquet}
\item \code{country_grid.parquet}
\item \code{type_cropland.parquet} (required when
\code{use_type_constraint = TRUE}).
}
Livestock (\code{components} contains \code{"livestock"}):
\itemize{
\item \code{livestock_country_data.parquet}
\item \code{gridded_pasture.parquet}
\item \code{gridded_cropland.parquet}, \code{country_grid.parquet}
\item \code{manure_pattern.parquet} (optional, enables
manure-intensity weighting if present).
\item \code{livestock_mapping.csv} from the installed package.
\item The GLW3 rasters under \code{WHEP_GLW3_DIR}, read only when
\code{livestock_proxy = "glw3"} (see \code{\link[=read_glw_density]{read_glw_density()}}).
}
}

\section{Which cell-to-polity crosswalk}{

The producer builds two crosswalks from the same polygons.
\code{"centroid"} is the deployed \code{spatialize-country-grid} pin: one
\code{area_code} per 0.5-degree cell, winner-take-all at a border, no share
column, so a whole border cell goes to a single polity. \code{"fraction"} is
\code{cell_polity_fraction.parquet}, which splits each border cell by
fractional coverage; the engines already read its \code{polity_frac} as
\code{cell_area_frac}, so no engine change is involved.

They are alternatives, never a fallback. The fractional parquet used to
carry a different area vocabulary from the centroid grid — it keyed
Ethiopia \code{62} and Sudan \code{206} where today's \code{regions.csv} uses \code{238} and
\code{276}, so substituting it dropped both countries entirely (whep#461).
Regenerating it closed that gap: the two grids now carry the same 178 area
codes, it is published as the \code{spatialize-cell-polity-fraction} pin so no
user has to rebuild it, and \code{\link[=build_cell_polity]{build_cell_polity()}} refuses a copy still
holding a retired code instead of deleting the countries silently
(whep#694). It still cannot
rescue a polity smaller than a cell, because its producer restricts it to
the cells the centroid grid already has, and it drops 4 of those cells,
whose only land is a sliver covering the 0.5-degree cell centre but no
1/12-degree subcell centre. Whichever is selected,
\code{build_gridded_landuse()} and
\code{build_gridded_livestock()} now warn once per call naming every reporting
area the chosen grid has no cell for and the national total at stake.
}

\section{Outputs written to \code{out_dir}}{

Every parquet below carries \code{method_grid_vintage}, the geography the run
allocated into: \code{"year_aware"}, \code{"snapshot_2015"} or \code{"static_crosswalk"}.
\itemize{
\item \code{gridded_landuse_crops.parquet} — crop-level output.
\item \code{gridded_landuse.parquet} — CFT-aggregated output
(when \code{aggregate_to_cft = TRUE}).
\item \code{gridded_livestock_emissions.parquet} — gridded
livestock stocks and emissions (when livestock component
selected).
\item \code{run_metadata.yaml} — resolved preset, components,
flags, years, timestamp, package version, and the resolved
\code{method_grid_vintage}.
\item \code{admin_coverage.csv} — which admin source constrained each
container x item x year, at what tier, grain and depth. Written
only when \code{level > 0}, with its header and no rows where no
coverage is granted. See \code{\link[=admin_coverage_prototype]{admin_coverage_prototype()}}.
}
}

\examples{
# Dispatch to the engine with a filtered year range (offline
# example; normally called against prepared parquet inputs).
country_areas <- tibble::tribble(
  ~year, ~area_code, ~item_prod_code, ~harvested_area_ha,
  1999L,         1L,             15L,                500,
  2000L,         1L,             15L,               1000
)
crop_patterns <- tibble::tribble(
  ~lon,  ~lat, ~item_prod_code, ~harvest_fraction,
   0.25, 50.25,             15L,               0.6,
   0.75, 50.25,             15L,               0.4
)
gridded_cropland <- tibble::tribble(
  ~lon,  ~lat,  ~year, ~cropland_ha,
   0.25, 50.25, 1999L,          800,
   0.75, 50.25, 1999L,          500,
   0.25, 50.25, 2000L,          800,
   0.75, 50.25, 2000L,          500
)
country_grid <- tibble::tribble(
  ~lon,  ~lat, ~area_code, ~cell_area_frac,
   0.25, 50.25,         1L,               1,
   0.75, 50.25,         1L,               1
)
build_gridded_landuse(
  country_areas, crop_patterns, gridded_cropland, country_grid,
  config = list(years = 2000L)
)
}
\seealso{
\code{\link[=build_gridded_landuse]{build_gridded_landuse()}}.
}
