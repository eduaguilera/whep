% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\docType{data}
\name{regions_full}
\alias{regions_full}
\title{Full polity and region reference table}
\format{
A tibble where each row corresponds to one polity or aggregate region. It
contains the following columns (same definitions as \code{polities_cats},
minus the five trailing \verb{0...36}–\verb{0...40} artefact columns):
\itemize{
\item \code{legacy_polity_prefix}: Legacy current polity prefix, kept for
compatibility with older code that expected ISO3-like values. \strong{Not a
\link{polities} code}: it was called \code{polity_code} until whep#687, where the
name promised an identity none of its 271 non-\code{NA} values holds, so a join
to \link{polities} or \link{polity_area_crosswalk} on it returned nothing. Read
\code{reporting_polity_code} for the polity.
\item \code{polity_name}: Current polity, country, territory, or aggregate name.
\item \code{V1}: Internal row index.
\item \code{code}: Numeric FAOSTAT country/region code.
\item \code{polity_area_code}: Numeric WHEP reporting area code used in matrix
workflows.
\item \code{reporting_polity_code}: Current periodized WHEP polity code for \code{code}.
\item \code{reporting_polity_name}: Current WHEP polity name for \code{code}.
\item \code{reporting_polity_has_geometry}: Logical flag indicating whether the
current reporting polity has a polygon.
\item \code{iso3c}: ISO 3166-1 alpha-3 code (\code{NA} for aggregates).
\item \code{FAOSTAT_name}: Name used in FAOSTAT (may be \code{"#N/A"} for aggregates).
\item \code{EU27}: Logical EU27 membership flag.
\item \code{name}: Name used in external databases.
\item \code{eia}: EIA country identifier.
\item \code{iea}: IEA country identifier.
\item \code{water_code}: Water statistics numeric code.
\item \code{water_area}: Name used in water statistics.
\item \code{baci}: BACI trade database country code.
\item \code{fish}: Fisheries dataset numeric code.
\item \code{region_code}: Numeric regional code.
\item \code{cbs}: Logical CBS dataset membership flag; \code{TRUE} if the area has a
commodity balance sheet of its own. 202 areas.
\item \code{fabio_code}: FABIO database numeric code, and the value
\code{polity_area_code} is derived from, so it is the fold instruction as well
as a fact about FABIO. It is the area's own \code{code} for a \code{cbs} reporter and
999 (Rest of World) otherwise, with seven exceptions: 62 -> 238, 276 -> 206
and 277 -> 206 are successor-state folds, and 153, 154, 209 and 212 are
\code{cbs} reporters folded into 999 anyway. Those four are a contradiction
inside this table -- FABIO's own published region list enumerates all four
as regions of their own -- left standing because correcting it would move
published values. See \link{folded_reporting_areas} and issue 556.
\item \code{ADB_Region}: Asian Development Bank region.
\item \code{region}: General world region.
\item \code{uISO3c}: UN M49 numeric code.
\item \code{Lassaletta}: Lassaletta et al. nitrogen study grouping.
\item \code{region_krausmann}: Krausmann regional grouping.
\item \code{region_HANPP}: HANPP study regional grouping.
\item \code{region_krausmann2}: Alternative Krausmann grouping.
\item \code{region_UN_sub}: UN M49 sub-region.
\item \code{region_UN}: UN M49 macro-region.
\item \code{region_ILO1}: ILO primary region.
\item \code{region_ILO2}: ILO secondary region.
\item \code{region_ILO3}: ILO tertiary region.
\item \code{region_IEA}: IEA region.
\item \code{region_IPCC}: IPCC region.
\item \code{region_labour}: Labour-focused region.
\item \code{region_labour_agg}: Aggregated labour region.
\item \code{region_labour_mech}: Labour mechanisation region, \code{"mech"} or
\code{"no_mech"}. Two cells hold a sub-region name instead -- Angola (code 7)
\code{"Middle Africa"} and Northern Mariana Islands (163) \code{"Micronesia"}, each
its own \code{region_labour}-family value -- which looks like a column shift in
the source spreadsheet. Nothing here reads the column, so nothing computes
on them; which class each belongs in is not recoverable from anything
shipped with the package, so they are pinned in
\code{test_region_classifications.R} rather than guessed at.
}
}
\source{
Compiled from \href{https://www.fao.org/faostat/}{FAOSTAT}, UN M49,
ILO, IEA, and other international statistical sources.
}
\usage{
regions_full
}
\description{
Extended reference table covering all polities and aggregate regions,
including countries, territories, and statistical composites that appear
in international databases but may lack standard ISO codes.
}
\section{Which regional groupings WHEP reads}{

The grouping columns are not all inputs to this package. Six have a consumer
in the tree, measured over \verb{R/}, \verb{data-raw/}, \verb{tests/}, \verb{vignettes/} and
\verb{inst/}: \code{region} (carried into \link{polity_area_crosswalk} at build time),
\code{region_krausmann} (the residue recovery rate, and the IPCC excreta regions
of \code{prepare_spatialize_all.R}), \code{region_HANPP} (the modern-variety adoption
share), \code{region_UN_sub} (the residue feed-use fraction, since whep#405),
\code{ADB_Region} (\link{build_primary_production} area keys) and \code{EU27} (the EU
aggregate of the FABIO comparison). \code{region_code} carries no information
\code{region} does not -- the two are a 1:1 relabelling -- so it needs no
consumer of its own.

The rest -- \code{Lassaletta}, \code{region_krausmann2}, \code{region_UN}, \code{region_ILO1},
\code{region_ILO2}, \code{region_ILO3}, \code{region_IEA}, \code{region_IPCC}, \code{region_labour},
\code{region_labour_agg}, \code{region_labour_mech} -- are published third-party
taxonomies shipped for downstream analysis and read by nothing in the
package. They are shipped as reference, and carry no promise of being
re-validated against their upstream taxonomy on release, so a consumer should
check the gap it inherits before keying anything by one (whep#386).

The gap the present-day taxonomies share is dissolved states. Over the 202
\code{cbs} reporters, \code{region_ILO1}, \code{region_ILO2}, \code{region_ILO3}, \code{region_IEA}
and \code{region_IPCC} are each \code{NA} for exactly the four federations WHEP still
books commodity balances for -- Czechoslovakia (51), Serbia and Montenegro
(186), the USSR (228) and the Yugoslav SFR (248) -- and complete everywhere
else; \code{region_UN} labels three of the four and leaves only Czechoslovakia
\code{NA}. \code{ROW} (999) carries an explicit \code{"RoW"} value in all of them rather
than \code{NA}. Grouping by one of these without deciding what to do with the
federations silently drops the pre-succession record. \code{region_UN_sub}, which
shares the gap and does have a consumer, is pinned against it in
\code{test_region_classifications.R}.

A \code{region_test} column with two values (\code{"Europe"}, \code{"Other"}) and no
consumer was dropped in whep#386.
}

\examples{
head(regions_full)
}
\seealso{
\link{polities_cats} for the subset restricted to sovereign countries.
}
\keyword{datasets}
