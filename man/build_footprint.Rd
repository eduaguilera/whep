% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_footprint.R
\name{build_footprint}
\alias{build_footprint}
\title{Compute a footprint end-to-end from an extension table.}
\usage{
build_footprint(
  extension,
  years = NULL,
  io = NULL,
  value_col = "impact_u",
  ...
)
}
\arguments{
\item{extension}{Long-format extension tibble with columns \code{year},
\code{area_code}, \code{item_cbs_code} and the column named by \code{value_col}.}

\item{years}{Years to compute. Defaults to the distinct years present in
\code{extension}. Ignored when \code{io} is supplied.}

\item{io}{Optional pre-built \code{\link[=build_io_model]{build_io_model()}} result (a tibble with one
row per year). Supply it to reuse one model across several extensions
instead of rebuilding it. When \code{NULL} (default), it is built for \code{years}.}

\item{value_col}{Name of the extension magnitude column, \code{"impact_u"} by
default.}

\item{...}{Further arguments passed to \code{\link[=compute_footprint]{compute_footprint()}} (e.g.
\code{conserve_extensions}, \code{report_conservation}).}
}
\value{
A tibble of footprint flows as returned by \code{\link[=compute_footprint]{compute_footprint()}},
with an added \code{year} column.
}
\description{
Trace a long-format environmental extension table through the supply chain
for one or more years and return a tidy footprint. This wraps the three
steps that the footprint driver scripts used to repeat inline: build (or
reuse) the input-output model with \code{\link[=build_io_model]{build_io_model()}}, align the extension
to each year's sector labels with \code{\link[=align_extension]{align_extension()}}, and trace it with
\code{\link[=compute_footprint]{compute_footprint()}}.

The \code{extension} table is the output of any \verb{build_*_extension()} builder,
such as \code{\link[=build_grassland_land_extension]{build_grassland_land_extension()}} or
\code{\link[=build_livestock_ghg_extension]{build_livestock_ghg_extension()}}: rows keyed by \code{year}, \code{area_code} and
\code{item_cbs_code}, with the pressure magnitude in \code{value_col}.
}
\examples{
io <- tibble::tibble(
  year = 2000L,
  Z = list(matrix(c(0, 5, 10, 0), nrow = 2)),
  X = list(c(100, 200)),
  Y = list(matrix(c(85, 195), ncol = 1)),
  labels = list(tibble::tibble(
    index = 1:2,
    area_code = c(1L, 1L),
    item_cbs_code = c(1L, 2L)
  )),
  fd_labels = list(tibble::tibble(area_code = 1L, fd_col = "food"))
)
extension <- tibble::tibble(
  year = 2000L,
  area_code = 1L,
  item_cbs_code = c(1L, 2L),
  impact_u = c(50, 30)
)
build_footprint(extension, io = io)
}
