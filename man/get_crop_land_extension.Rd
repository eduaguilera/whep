% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_land_extension.R
\name{get_crop_land_extension}
\alias{get_crop_land_extension}
\title{Get the per-crop physical cropland extension from spatialization inputs.}
\usage{
get_crop_land_extension(
  input_dir = NULL,
  years = NULL,
  method = c("cropland_apportion", "intensity_divide"),
  use_type_constraint = FALSE,
  fill_missing_patterns = TRUE,
  example = FALSE
)
}
\arguments{
\item{input_dir}{Directory holding the spatialization inputs
(\code{country_areas.parquet}, \code{crop_patterns.parquet},
\code{gridded_cropland.parquet}, \code{country_grid.parquet}, and optionally
\code{multicropping.parquet}). Typically \verb{<l_files_dir>/whep/inputs}. If \code{NULL}
or unset, the pinned WHEP spatialization inputs are used.}

\item{years}{Numeric vector of years to compute, or \code{NULL} for all available.}

\item{method}{Physical-area conversion method passed to
\code{\link[=build_crop_land_extension]{build_crop_land_extension()}}.}

\item{use_type_constraint}{If \code{TRUE}, restrict each crop to cells of its LUH2
type (requires \code{type_cropland.parquet}). Defaults to \code{FALSE}.}

\item{fill_missing_patterns}{If \code{TRUE} (default), crops that have harvested
area but no \code{crop_patterns} rows (e.g. Barley, absent from the Monfreda
layer) are placed with a uniform fallback pattern over each producing
country's cropland, so their land is not silently dropped.}

\item{example}{If \code{TRUE}, return a small example output without reading
remote/large data. Defaults to \code{FALSE}.}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (physical land in hectares), and \code{method_land}, plus the polity
columns below.
}
\description{
Convenience wrapper that loads the gridded land-use inputs, spatializes crop
harvested area with \code{\link[=build_gridded_landuse]{build_gridded_landuse()}} (crop-level, no CFT
aggregation), and converts it to a per-crop physical land extension with
\code{\link[=build_crop_land_extension]{build_crop_land_extension()}}. The result is keyed by
\verb{(year, area_code, item_cbs_code)} and ready to use as \code{extensions} in
\code{\link[=compute_footprint]{compute_footprint()}}.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning.
}

\examples{
get_crop_land_extension(example = TRUE)
}
