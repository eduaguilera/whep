% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{decompose_semi_natural_surplus}
\alias{decompose_semi_natural_surplus}
\title{Decompose semi-natural agroecosystem N surplus into size, intensity,
and inefficiency drivers}
\usage{
decompose_semi_natural_surplus(
  n_prov_destiny = NULL,
  npp_ygpit = NULL,
  by_period = FALSE,
  example = FALSE
)
}
\arguments{
\item{n_prov_destiny}{Nitrogen flows tibble from
\code{\link[=create_n_prov_destiny]{create_n_prov_destiny()}}. If \code{NULL}, loaded automatically.}

\item{npp_ygpit}{Land use and area tibble from
\code{whep_read_file("npp_ygpit")}. If \code{NULL}, loaded automatically.}

\item{by_period}{If \code{TRUE}, compares each reference period (each
averaged across its ten years) against the immediately preceding
one — 1860-1870 -> 1920-1930 -> 1960-1970 -> 2010-2020 — plus one
extra transition spanning the full analysis window, 1860-1870
straight to 2010-2020 (the total change) — instead of chaining year
on year.}

\item{example}{If \code{TRUE}, return a small hardcoded output without
downloading remote data. Default is \code{FALSE}.}
}
\value{
A tibble from \code{\link[=calculate_lmdi]{calculate_lmdi()}} with columns \code{period},
\code{period_years}, \code{factor_label}, \code{component_type}, \code{additive},
\code{multiplicative}, and \code{multiplicative_log}.
}
\description{
Decomposes the year-on-year change in Spain's semi-natural agroecosystem
(grazing land, dehesa, and non-cropland vegetation) nitrogen (N) surplus
into three multiplicative drivers, following an additive LMDI
shift-share decomposition computed at the national level:
\itemize{
\item \strong{Size}: national semi-natural area.
\item \strong{Intensity}: N input per hectare of semi-natural land.
\item \strong{Inefficiency}: surplus fraction of inputs (1 - nitrogen use
efficiency).
}

No destiny factor is used because grazed and cut vegetation is assumed
to be overwhelmingly a single destiny (livestock feed).

The land-use categories included (\code{Dehesa}, \code{Forest_high}, \code{Forest_low},
\code{Other}, \code{Pasture_Shrubland}) are all of \code{npp_ygpit}'s non-cropland
categories, matching the existing \code{semi_natural_agroecosystems} box used
elsewhere in the package. Some of that land (e.g. \code{Forest_high}/
\code{Forest_low}) may not actually be grazed and can produce non-feed output
(firewood), which would call for its own destiny factor (as in
\code{\link[=decompose_cropland_surplus]{decompose_cropland_surplus()}}) rather than the single-destiny
assumption above; that refinement is not implemented here.

This is a simplified, national-only view (no provincial breakdown).
Semi-natural surplus can turn negative (soil N mining) in some years.
LMDI relies on logarithms and cannot handle a series that changes sign
between two compared years; this function warns when that occurs
instead of silently returning \code{NA}, but does not implement the
Shapley/Sun alternative required for those cases.
}
\examples{
decompose_semi_natural_surplus(example = TRUE)
}
