% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soc_crop_groups.R
\name{soc_crop_group}
\alias{soc_crop_group}
\title{Assign each crop to its soil-carbon crop group.}
\usage{
soc_crop_group(
  item_prod_code,
  irrigated = FALSE,
  vocabulary = whep::items_prod_full
)
}
\arguments{
\item{item_prod_code}{Integer vector of FAOSTAT production item codes.}

\item{irrigated}{Logical vector, recycled: is the area irrigated?}

\item{vocabulary}{The crop vocabulary, a tibble with \code{item_prod_code},
\code{Herb_Woody} and \code{Name_biomass}. Defaults to \link{items_prod_full}.}
}
\value{
A character vector of group labels, one per input element. An
item with no \code{Herb_Woody} classification aborts, naming it: an
unclassified crop silently pooled into the wrong group would move
carbon between groups with nothing recording it.
}
\description{
Map \code{item_prod_code} and an irrigation flag to the crop group the soil
carbon balance marches: \code{cropland_rainfed_herbaceous},
\code{cropland_irrigated_herbaceous}, or one group per woody species such as
\code{cropland_rainfed_olive} (the species slug comes from \code{Name_biomass}, the
column Spain_Hist's own category is built from, so it is "Olive" rather
than the \code{names_cats} key "Olives"). Every label keeps the \code{cropland_}
prefix, which
is what lets the balance recognise a group as cropland for its soil-cover
curve, its C:N lookup and its water term without enumerating groups.
}
\examples{
soc_crop_group(c(15L, 260L, 260L), irrigated = c(FALSE, FALSE, TRUE))
}
