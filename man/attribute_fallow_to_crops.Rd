% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_land_extension.R
\name{attribute_fallow_to_crops}
\alias{attribute_fallow_to_crops}
\title{Attribute reported fallow land to crops.}
\usage{
attribute_fallow_to_crops(cropgrids, fallow_total, alloc_weight)
}
\arguments{
\item{cropgrids}{Tibble of national crop areas with columns \code{area_code},
\code{item_cbs_code}, \code{physical_ha} (cropped physical area), \code{harvested_ha}.}

\item{fallow_total}{Tibble of reported fallow area with columns \code{area_code}
and \code{fallow_ha}.}

\item{alloc_weight}{Tibble of \code{area_code}, \code{item_cbs_code}, \code{weight} giving
the within-country allocation weight, e.g. from \code{\link[=gridded_fallow_weights]{gridded_fallow_weights()}}.}
}
\value{
A tibble with \code{area_code}, \code{item_cbs_code}, \code{physical_ha} (cropped
physical area plus attributed fallow), and \code{harvested_ha}.
}
\description{
Distribute each country's FAOSTAT-reported fallow area among its crops using a
precomputed allocation weight, adding the result to each crop's cropped
physical area. The weight is typically \code{\link[=gridded_fallow_weights]{gridded_fallow_weights()}}, which puts
fallow on rainfed dryland cereals and rainfed monsoon rice and keeps it off
irrigated/continuous systems.

The fallow \emph{magnitude} comes from \code{fallow_total} (FAOSTAT "Land with temporary
fallow", item 6640) — reported separately from temporary meadows/pastures, so
it isolates real fallow from fodder.
}
\examples{
cropgrids <- tibble::tribble(
  ~area_code, ~item_cbs_code, ~physical_ha, ~harvested_ha,
  1L, 2511L, 500, 500,
  1L, 2807L, 400, 400
)
fallow_total <- tibble::tribble(~area_code, ~fallow_ha, 1L, 200)
# all weight on wheat -> the 200 ha reported fallow goes to wheat
alloc_weight <- tibble::tribble(
  ~area_code, ~item_cbs_code, ~weight,
  1L, 2511L, 1,
  1L, 2807L, 0
)
attribute_fallow_to_crops(cropgrids, fallow_total, alloc_weight)
}
