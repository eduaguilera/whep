% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prices.R
\name{build_primary_prices}
\alias{build_primary_prices}
\title{Build primary item prices}
\usage{
build_primary_prices(
  primary_prod,
  value_of_production = NULL,
  trade_prices = NULL,
  example = FALSE
)
}
\arguments{
\item{primary_prod}{A tibble of primary production, as returned by
\code{\link[=build_primary_production]{build_primary_production()}} or \code{\link[=get_primary_production]{get_primary_production()}}.}

\item{value_of_production}{A data frame with FAOSTAT Value of
Production data. Must contain columns \code{Item.Code} (or
\code{item_code_prod}), \code{Element}, \code{Unit}, \code{Year}, \code{Value},
\code{Area.Code} (or \code{area_code}). If \code{NULL}, only trade prices are
used.}

\item{trade_prices}{A tibble as returned by \code{\link[=build_trade_prices]{build_trade_prices()}}.
If \code{NULL}, it is computed internally.}

\item{example}{Logical. If \code{TRUE}, return a small example tibble.
Default \code{FALSE}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{year}: Integer year.
\item \code{item_prod_code}: Numeric production item code.
\item \code{price}: Price in KDollars per tonne.
}
}
\description{
Compute prices for primary production items. Export trade prices are
preferred; when unavailable, production value prices (gross
production value divided by quantity) are used as fallback. Gaps are
filled via linear interpolation.
}
\examples{
build_primary_prices(example = TRUE)
}
