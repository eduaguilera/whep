% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_npp.R
\name{calculate_crop_npp_components}
\alias{calculate_crop_npp_components}
\title{Estimate cropland NPP components including weeds.}
\usage{
calculate_crop_npp_components(
  x,
  .by = NULL,
  potential = list(method = "lpjml")
)
}
\arguments{
\item{x}{A tibble with \code{item_prod_code}, \code{area_ha}, \code{year}, \code{product_dm_t},
\code{residue_dm_t} and \code{root_dm_t} (e.g. the output of \code{\link[=calculate_crop_npp]{calculate_crop_npp()}}).
When \code{npp_potential_dm_t_ha} is absent it is computed via
\code{\link[=calculate_potential_npp]{calculate_potential_npp()}} (which, for the default \code{lpjml} method, needs
\code{lon}/\code{lat}).}

\item{.by}{Optional character vector of grouping columns used to fill missing
weed-scaling factors with the group mean.}

\item{potential}{A named list selecting the potential-NPP source: \code{method}
(default \code{"lpjml"}) and \code{lpjml} options.}
}
\value{
The input tibble with weed dry matter, the dry-matter / nitrogen /
carbon partition for crop, weeds and total, and \code{weed_scaling_to_be_revised}.
}
\description{
Assembles full cropland net primary production: scales weed biomass from
potential NPP and the \code{weed_npp_scaling} factors, then partitions crop and
weed biomass into dry matter, carbon and nitrogen via
\code{\link[=calculate_npp_carbon_nitrogen]{calculate_npp_carbon_nitrogen()}}.
}
\details{
The \code{weed_npp_scaling} table is taken from Spain_Hist and is flagged
\code{to_be_revised}: it is Spain-specific and not validated for WHEP's global
scope. A \code{weed_scaling_to_be_revised} column records this on the output and a
one-time warning is emitted.
}
\examples{
tibble::tibble(
  item_prod_code = "15", production_t = 100, area_ha = 40,
  year = 2000, npp_potential_dm_t_ha = 5
) |>
  calculate_crop_npp() |>
  calculate_crop_npp_components()
}
