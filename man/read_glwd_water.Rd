% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polycell_layers.R
\name{read_glwd_water}
\alias{read_glwd_water}
\title{Read the GLWD inland-water fraction on the 0.5-degree grid}
\source{
Ostberg, S., Mueller, C., Heinke, J. and Schaphoff, S. (2023). LandInG 1.0:
a toolbox to derive input datasets for terrestrial ecosystem modelling at
variable resolutions from heterogeneous sources. \emph{Geoscientific Model
Development} 16, 3375-3406. \doi{10.5194/gmd-16-3375-2023}
}
\usage{
read_glwd_water(dir = NULL, file = "glwd_lakes_and_rivers_30arcmin.clm")
}
\arguments{
\item{dir}{Directory holding \code{grid.clm} and the water file. Defaults to
\code{Sys.getenv("WHEP_LPJML_INPUT_DIR")}.}

\item{file}{Name of the water CLM file inside \code{dir}.}
}
\value{
A \code{tibble} with \code{lon}, \code{lat} and \code{water_frac}, one row per cell of
the CLM grid.
}
\description{
Reads the LPJmL \code{glwd_lakes_and_rivers_30arcmin.clm} layer and its companion
\code{grid.clm}, returning the Global Lakes and Wetlands Database surface-water
fraction of each 0.5-degree cell. \code{water_frac} is a fraction of the \strong{whole}
cell (the file's own \code{unit} is \code{"1"}), so the water area of a cell is
\code{water_frac * cell_area_ha}.

This is the inland-water source for \code{\link[=build_polycell_support]{build_polycell_support()}}. It is
preferred over \code{ne_10m_lakes}, which carries roughly half of global inland
water and omits the Caspian Sea entirely.
}
\examples{
# Requires WHEP_LPJML_INPUT_DIR to be set; not run without it.
if (nzchar(Sys.getenv("WHEP_LPJML_INPUT_DIR"))) {
  read_glwd_water()
}
}
