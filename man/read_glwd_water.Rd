% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polycell_layers.R
\name{read_glwd_water}
\alias{read_glwd_water}
\title{Read the GLWD inland-water fraction on the 0.5-degree grid}
\source{
Lehner, B., Anand, M., Fluet-Chouinard, E. et al. (2025). Mapping the
world's inland surface waters: an update to the Global Lakes and Wetlands
Database (GLWD v2). \emph{Earth System Science Data} 17, 2277-2329.
\doi{10.5194/essd-17-2277-2025}
}
\usage{
read_glwd_water(dir = NULL)
}
\arguments{
\item{dir}{Directory holding \verb{GLWD/}, as \code{download_hydrology.R} lays it out.
Defaults to \code{Sys.getenv("WHEP_LPJML_INPUT_DIR")}.}
}
\value{
A \code{tibble} with \code{lon}, \code{lat} and \code{water_frac}, one row per cell the
rasters cover.
}
\description{
Returns the Global Lakes and Wetlands Database surface-water fraction of each
0.5-degree cell. \code{water_frac} is a fraction of the \strong{whole} cell, so the
water area of a cell is \code{water_frac * cell_area_ha}.

This is the inland-water source for \code{\link[=build_polycell_support]{build_polycell_support()}}. It is
preferred over \code{ne_10m_lakes}, which carries roughly half of global inland
water and omits the Caspian Sea entirely.

The fraction is derived from the GLWD rasters
\code{inst/scripts/download/download_hydrology.R} fetches from the published
figshare DOI, through \code{\link[=glwd_water_fraction]{glwd_water_fraction()}} -- the same derivation
\code{inst/scripts/prepare_spatialize_all.R} uses to write LPJmL's own
\code{lakes_rivers} input, so the two cannot drift.

Until WHEP settled on GLWD v2 this read LPJmL's
\code{glwd_lakes_and_rivers_30arcmin.clm} and a companion \code{grid.clm} instead.
That pair is derived from GLWD \strong{v1}, no script in this repository produces
it, and it gives 2.4759 Mkm2 of inland water over the 67,420-cell CRU land
mask against v2's 3.2480 Mkm2. Any figure quoted against the old layer has
to be re-measured rather than carried across.
}
\examples{
# Requires WHEP_LPJML_INPUT_DIR to be set; not run without it.
if (nzchar(Sys.getenv("WHEP_LPJML_INPUT_DIR"))) {
  read_glwd_water()
}
}
