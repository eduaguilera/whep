% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/total_population_grid.R
\name{build_total_population_grid}
\alias{build_total_population_grid}
\title{Build gridded total population from UN WPP and HYDE.}
\usage{
build_total_population_grid(
  years = NULL,
  polity_validity = c("keep", "flag", "drop"),
  hyde_dir = NULL,
  data = list(),
  example = FALSE
)
}
\arguments{
\item{years}{Integer vector of calendar years. Required. A year before the
first HYDE snapshot, or one the WPP table does not carry (WPP 2024 starts
in 1950), aborts with class \code{whep_total_population_uncovered}.}

\item{polity_validity}{What to do with a row whose \verb{(area_code, year)}
resolves to a polity that did not exist in that year (the cell-polity
crosswalk has no year dimension, so an early-20th-century cell is labelled
with its present-day territory). \code{"keep"} (default) keeps every row, which
is the historical behaviour, and warns naming the rows, years and area
codes involved. \code{"flag"} keeps them and adds the per-row logical
\code{reporting_polity_out_of_span}, marking exactly which rows are stand-ins.
\code{"drop"} removes them. All three warn; only \code{"drop"} changes the numbers.
See \code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}}, which reports the same rows for an
already-built table.}

\item{hyde_dir}{Directory holding the HYDE \code{"{year}AD_pop.zip"} archives,
whose file names set the available snapshot years. Defaults to
\code{Sys.getenv("WHEP_HYDE_DIR")}. Ignored when \code{data$hyde} is supplied.}

\item{data}{Named list of inputs: \code{cell_polity} (required; \code{lon}, \code{lat},
\code{area_code}, optional \code{polity_frac}, as \code{\link[=build_cell_polity]{build_cell_polity()}} emits it,
with the numeric WHEP area code), \code{wpp} (optional; \code{year}, \code{area_code},
\code{population}, bypassing \code{\link[=read_wpp_population]{read_wpp_population()}}) and \code{hyde} (optional;
\code{lon}, \code{lat}, \code{year}, \code{popc} on the 0.5-degree grid, bypassing the HYDE
archives; its years are the snapshot years).}

\item{example}{If \code{TRUE}, return a small fixture instead of reading data.
Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{lon}, \code{lat}, \code{area_code}, \code{year} and \code{population}
(persons), one row per populated polycell-year, plus the polity columns
below, and the \code{coverage} attribute described in Details.
}
\description{
Downscales UN World Population Prospects 2024 country totals
(\code{\link[=read_wpp_population]{read_wpp_population()}}, \code{by = "total"}) to WHEP's 0.5-degree polycells by
HYDE's total-population (\code{popc}) pattern. Each polycell's share of its
country's HYDE population is linearly interpolated between the two nearest
HYDE snapshots (decadal before 2001, annual 2001-2017 in the HYDE 3.x
baseline release) and held at the last snapshot for later years, and WPP
sets the level. So every country's cells sum to exactly its WPP total in
every year.

This is the population \code{\link[=build_urban_n]{build_urban_n()}} reads under
\code{population_basis = "total"}, where it is paired with the per-capita rate
\link{urban_kgn_cap_total_reference}, whose denominator is Spain's WPP total
population, so the level and the rate share one basis.
}
\details{
WPP is keyed on the \code{polity_area_code} bucket, so shares are taken within
each bucket (the one \code{build_cell_polity(area_key = "polity_area")} assigns)
and the output keeps the grid \code{area_code} of \code{data$cell_polity}. A bucket
with a WPP total but no HYDE population, and one with HYDE population but no
WPP total, are each reported in a warning and in the \code{coverage} attribute
rather than dropped silently.

The \code{coverage} attribute is a list recording what the numbers are:
\code{population_basis} (\code{"total"}), \code{level_source}, \code{pattern_variable}
(\code{"popc"}), the \code{exact}, \code{interpolated} and \code{held} years with \code{held_at},
the snapshots used, the per-year \code{plan}, and the \code{unplaced},
\code{unmatched_hyde} and \code{share_gaps} tables.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning. Where that polity is not live in the
row's own year -- 41.5\% of the pre-1961 \verb{(area, year)} cells --
\code{\link[=add_polity_code]{add_polity_code()}} says so as \code{mapping_status == "backcast_anchor"}, and
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports it as \code{gap_kind == "backcast_anchor"}.
These columns do not say so either way.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_total_population_grid(example = TRUE)
}
