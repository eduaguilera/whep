% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labour_extension.R
\name{build_labour_extension}
\alias{build_labour_extension}
\title{Build the labour-footprint extension.}
\usage{
build_labour_extension(
  component = c("total", "extreme_poverty", "lmic_poverty", "umic_poverty",
    "not_covered", "child", "forced"),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{component}{Labour tier, one of \code{"total"} (default),
\code{"extreme_poverty"}, \code{"lmic_poverty"}, \code{"umic_poverty"}, \code{"not_covered"},
\code{"child"} or \code{"forced"}.}

\item{data}{Optional named list of pre-loaded inputs to avoid remote reads:
\code{primary_prod} (the \code{\link[=get_primary_production]{get_primary_production()}} output) and \code{labour_crop}
(the \code{gld-labour-crop} pin). Each falls back to its reader when absent.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (labour hours) and \code{method_labour} (the chosen tier, e.g.
\code{"GLD_total"}).
}
\description{
Aggregate Global Labour Database working-hour intensities into a footprint
extension keyed by \verb{(year, area_code, item_cbs_code)}, expressed in labour
hours. This is the (socio-economic) labour analogue of
\code{\link[=build_livestock_ghg_extension]{build_livestock_ghg_extension()}} and \code{\link[=build_crop_soil_n2o_extension]{build_crop_soil_n2o_extension()}}, and
feeds \code{\link[=build_footprint]{build_footprint()}} / \code{\link[=compute_footprint]{compute_footprint()}} the same way.

Coefficient source: the Global Labour Database (Juan Infante-Amate and
colleagues) provides agricultural working hours per crop and country,
disaggregated into a total and several social tiers (extreme-poverty,
lower- and upper-middle-income-poverty, not-covered, child and forced
labour), alongside the harvested area those hours worked. The per-hectare
intensity (hours per harvested hectare) is the ratio of the two.

The footprint engine traces one impact column at a time, so the labour tier
is selected with \code{component} (recorded in \code{method_labour}) rather than
returned as several columns:
\itemize{
\item \code{"total"} (default): all agricultural working hours.
\item \code{"extreme_poverty"}, \code{"lmic_poverty"}, \code{"umic_poverty"}: hours worked by
people living below the extreme-poverty line and the lower- and
upper-middle-income-country poverty lines respectively.
\item \code{"not_covered"}: hours not covered by the poverty estimates.
\item \code{"child"}: hours worked as child labour.
\item \code{"forced"}: hours worked as forced labour.
}

Hour intensities (hours per hectare) are joined to whep's own crop harvested
area (the \code{"ha"} rows of \code{\link[=get_primary_production]{get_primary_production()}}) by FAO crop code and
country-year, so the footprint tracks whep production rather than the
source's, then summed to the commodity-balance item grain. This extension is
crop-only: the Global Labour Database covers crop agriculture, so livestock
and grazing sectors contribute no labour. Crops, countries or years absent
from the coefficient table contribute no hours.
}
\examples{
build_labour_extension(example = TRUE)
}
