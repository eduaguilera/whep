% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cru_climate.R
\name{read_cru_climate}
\alias{read_cru_climate}
\title{Read a CRU TS 4.09 monthly climate variable into a tidy tibble.}
\source{
CRU TS 4.09 (Climatic Research Unit, University of East Anglia;
Harris, Osborn & Jones 2020, Scientific Data,
\doi{10.1038/s41597-020-0453-3}).
}
\usage{
read_cru_climate(
  var = c("tmp", "pet", "pre", "tmn", "tmx", "vap", "cld", "wet", "dtr", "frs"),
  years = NULL,
  cru_dir = NULL,
  data = NULL,
  example = FALSE
)
}
\arguments{
\item{var}{Variable name, one of \code{"tmp"}, \code{"pet"}, \code{"pre"}, \code{"tmn"},
\code{"tmx"}, \code{"vap"}, \code{"cld"}, \code{"wet"}, \code{"dtr"} or \code{"frs"}.}

\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
keeps every year present in the file.}

\item{cru_dir}{Path to the CRU TS NetCDF directory. Defaults to
\code{Sys.getenv("WHEP_CRU_DIR", "C:/XL_files/CRU/CRU_TS_4")}.}

\item{data}{Optional pre-read tibble (\code{lon}, \code{lat}, \code{year}, \code{month},
\code{value}) used in place of reading NetCDF, for testing.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading data.
Defaults to \code{FALSE}.}
}
\value{
A tibble with columns \code{lon}, \code{lat}, \code{year}, \code{month}, \code{value} (native
units) and \code{var} (the requested variable name).
}
\description{
Reads one monthly variable of the CRU TS 4.09 high-resolution gridded
climate dataset (1901-2024, global 0.5 degree) from its NetCDF file and
returns it in tidy long form, one row per land cell-month. Requested years
are sliced at read time so the full grid is never materialised, and ocean
fill cells are dropped. Values are returned in the file's native units (not
converted): \code{tmp}/\code{tmn}/\code{tmx}/\code{dtr} in degrees Celsius, \code{pre} in mm/month,
\code{pet} in mm/day, \code{vap} in hPa, \code{cld} in percent, \code{wet} and \code{frs} in days.
}
\examples{
read_cru_climate(example = TRUE)
}
