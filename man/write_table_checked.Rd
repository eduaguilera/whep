% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_writer.R
\name{write_table_checked}
\alias{write_table_checked}
\title{Write a table to disk safely and verifiably.}
\usage{
write_table_checked(
  data,
  path,
  format = c("auto", "parquet", "csv"),
  overwrite = TRUE,
  sidecars = character(),
  ...
)
}
\arguments{
\item{data}{Table to write. Must be a data frame; a tibble is
returned unchanged by the round trip.}

\item{path}{Destination path. Its parent is created if needed.}

\item{format}{Output format: \code{"parquet"}, \code{"csv"}, or \code{"auto"} to
take it from the file extension.}

\item{overwrite}{Whether an existing target may be replaced.}

\item{sidecars}{Which sidecars to write: any of \code{"schema"} and
\code{"provenance"}. Defaults to none.}

\item{...}{Passed to the underlying writer,
\code{nanoparquet::write_parquet()} or \code{readr::write_csv()}.}
}
\value{
Invisibly, a one-row tibble describing what was written:
\code{path}, \code{format}, \code{n_rows}, \code{n_cols}, \code{bytes}, \code{md5},
\code{schema_path} and \code{provenance_path} (the last two \code{NA} when the
sidecar was not requested).
}
\description{
One writer for WHEP's tabular artifacts, with the four properties an
ad-hoc \code{nanoparquet::write_parquet()} or \code{readr::write_csv()} call
does not have (whep#375):
\itemize{
\item \strong{The parent directory is created}, recursively. This is not a
convenience: \code{nanoparquet::write_parquet()} given a path whose
parent does not exist returns \code{NULL} and writes nothing at all,
without warning, so a mistyped output directory looks like a
successful build until something tries to read the file back.
\item \strong{The write is atomic.} The table goes to a temporary file beside
the target and is renamed onto it only after it has been read back
and verified. An interrupted, failed or corrupt write therefore
leaves the previous artifact exactly as it was, instead of
replacing hours of pipeline output with a truncated file.
\item \strong{The artifact is verified before it is published.} For Parquet
that is \code{\link[=assert_parquet_integrity]{assert_parquet_integrity()}} plus a row and column-name
check; for CSV the file is re-read and its header and row count
compared. \code{write_table_checked()} is the atomic, format-agnostic
layer \emph{on top of} \code{\link[=write_parquet_checked]{write_parquet_checked()}}, which stays the
in-place Parquet primitive it already was.
\item \strong{Overwriting is a decision.} \code{overwrite = FALSE} refuses an
existing target instead of clobbering it.
}

Column order, column types and text encoding are preserved by round
trip, including for zero-row tables: a Parquet round trip returns an
identical tibble, and a CSV is always written as UTF-8. Prefer Parquet
for anything large: CSV cannot carry types, its verification has to
re-read the whole file, and a zero-column table is refused outright
because its CSV is a zero-byte file, indistinguishable from a write
that never happened.
}
\section{Sidecar contract}{

\code{sidecars} optionally writes YAML files beside the artifact, named
after it: \verb{<path>.schema.yaml} and \verb{<path>.provenance.yaml}. They are
written after the data file has landed, so the data file is never
waiting on them.

The schema sidecar is
\verb{\{format: "whep-table-schema/1", n_rows: <int>, columns: [\{name, type\}]\}}
with \code{columns} in the table's own column order. \code{type} is the
column's first class, with \code{"numeric"} reported as \code{"double"} so that
every atomic type name is one \code{vector()} accepts (\code{"integer"},
\code{"double"}, \code{"character"}, \code{"logical"}, \code{"list"}); a classed column
reports its class (\code{"factor"}, \code{"Date"}, \code{"POSIXct"}). A consumer can
therefore rebuild the prototype with no WHEP-specific lookup. This is
deliberately the same shape a declarative schema validator
(whep#373) and a typed empty-table constructor (whep#374) would
consume; neither exists yet, and this function does not implement
either.

The provenance sidecar is \code{{format: "whep-table-provenance/1"}} plus
\code{path}, \code{table_format}, \code{n_rows}, \code{n_cols}, \code{bytes}, \code{md5},
\code{written_at} (UTC, ISO 8601), \code{whep_version}, \code{r_version} and
\code{writer}. Every field except \code{written_at} is a function of the
artifact, so two builds of the same table differ only in that field.
}

\examples{
path <- tempfile(fileext = ".parquet")
table <- tibble::tibble(area_code = 724L, year = 2020L, value = 1.5)
manifest <- write_table_checked(table, path, sidecars = "schema")
manifest$n_rows
nanoparquet::read_parquet(path)
unlink(c(path, manifest$schema_path))
}
