% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polycell_land_uses.R
\name{build_polycell_land_uses}
\alias{build_polycell_land_uses}
\title{Partition each polycell into mutually exclusive land uses.}
\usage{
build_polycell_land_uses(
  years = NULL,
  grassland_level_source = c("faostat_pasture", "luh2"),
  overfull_method = c("spillover", "cap"),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{years}{Integer vector of calendar years to return, or \code{NULL} (default)
for every year present in the support.}

\item{grassland_level_source}{Grassland level basis, \code{"faostat_pasture"}
(default, FAO Land Use item 6655) or \code{"luh2"}. The default matches the
cropland anchor so both agricultural classes rest on one convention;
\code{"luh2"} remains selectable for sensitivity analysis and is recorded in
\code{level_source}. It is never used as a fallback. Note this differs from
\code{\link[=build_grassland_land_extension]{build_grassland_land_extension()}}'s own default, which is \code{"luh2"}: item
6655 excludes temporary meadows and pastures, so the two are a difference
in what \code{grassland} means and not only in provenance. The divergence is
deliberate and tracked in whep#759; this function always passes its choice
explicitly rather than inheriting a default.}

\item{overfull_method}{How to reconcile a polycell whose anchored
agricultural area exceeds its land: \code{"spillover"} (default) or \code{"cap"}.
See Details.}

\item{data}{Named list of pre-loaded inputs bypassing the readers:
\code{polycell_support} (the \code{\link[=read_polycell_support]{read_polycell_support()}} table, interval or year
grain), \code{pattern} (the \code{\link[=read_luh2_landuse]{read_luh2_landuse()}} grid table), \code{natural_split}
(per cell, the non-forested share of natural land), \code{cropland_level} (the
\code{\link[=get_arable_permanent_land]{get_arable_permanent_land()}} table), \code{grassland_level} (the
\code{\link[=build_grassland_land_extension]{build_grassland_land_extension()}} table) and \code{temporary_meadows}.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per polycell-year-class: \code{polycell_id}, \code{lon},
\code{lat}, \code{polity_code}, \code{area_code}, \code{year}, \code{land_use}, \code{area_ha},
\code{area_source} (\code{anchored}, \code{pattern_only}, \code{residual} or \code{unclassified}),
\code{level_source}, \code{pattern_source}, \code{allocation_status},
\code{statistical_pattern_disagreement_ha} (\code{NA} where no statistical level
applies), \code{unplaceable_statistical_ha}, \code{method_overfull},
\code{spillover_max_ring}, \code{coverage_status}, and the polity reporting columns
carried through from the support.
}
\description{
Split every polycell-year's \code{land_area_ha} into \code{cropland}, \code{grassland},
\code{urban}, \code{natural} and \code{unclassified}, so territorial quantities can be
attributed to a land class instead of being assumed agricultural or dropped
(issue #423).

The \strong{level} of each agricultural class comes from the statistical record,
which is authoritative; LUH2 supplies only the \strong{within-country spatial
pattern}. The two provenances are recorded in separate columns
(\code{level_source}, \code{pattern_source}), and their per-polycell difference is
emitted as \code{statistical_pattern_disagreement_ha} rather than being absorbed
into the natural class. That column is a transition instrument: its magnitude
measures how much LUH2 is still doing, and it is the criterion for retiring
LUH2 as a source.

Inland water and ice are not land uses. They stay on the polycell as their
own categories and never enter a class.
}
\details{
Per polycell-year:
\enumerate{
\item Class \strong{shares} come from the LUH2 \code{fraction} column - LUH2's share of
the whole cell, which is identical on every polycell of a border cell and
invariant to the reader's \code{area_basis}. They are applied to the polycell's
own measured \code{land_area_ha}, so the classes tile the polycell's land by
construction rather than to a tolerance.
\item FAO counts temporary meadows and pastures (item 6633) inside arable land,
while LUH2 books that ground as grassland. That component is therefore
spread over the LUH2 \strong{grassland} pattern but still emitted as
\code{cropland}, because FAO's class definition is the one being anchored.
\item Each agricultural class is rescaled so its polycells sum to the
statistical national total for that \verb{(area_code, year)}.
\item Where the anchored area exceeds a polycell's land, it is reconciled by
\code{overfull_method}, never renormalised in silence.
\item \code{natural} takes the remainder of \code{land_area_ha}.
\item A polycell with no pattern coverage is \code{unclassified} in full - never
\code{natural}.
}

Where a statistical level is absent for a class-country-year, the LUH2
pattern level is used and the row is labelled (\code{level_source = "luh2"},
\code{area_source = "pattern_only"}, \code{allocation_status = "level_missing"}). It is
never silently filled. \code{urban} has no statistical source and is always
pattern-only.
}
\section{Pre-1961 levels}{
FAOSTAT land use starts in 1961, so an unextended level would step at that
year. Both agricultural classes are therefore backcast the same way: the FAO
1961 level is carried backwards by LUH2's own national trend, matching FAO
exactly at the splice. \code{\link[=get_arable_permanent_land]{get_arable_permanent_land()}} already does this for
cropland; the pasture backcast here mirrors it, reading the same
\code{luh2-areas} input. A country with no FAO 1961 anchor receives no backcast.

A backcast row is labelled \code{luh2_backcast_cropland} or
\code{luh2_backcast_pasture} in \code{level_source} and is \strong{excluded} from the
statistical-versus-pattern diagnostic, because comparing a backcast with the
pattern compares LUH2 with itself.
}

\section{Reconciling an overfull polycell}{
A national statistical total spread by the LUH2 pattern can give a polycell
more agricultural land than it has, driven by countries where FAO and LUH2
disagree about how much land is permanent pasture. Measured on this function
at 2020: 63.50 Mha, 1.33\% of the anchored agricultural area, of which Saudi
Arabia is 35.10 Mha and Sudan (former) 14.20 Mha. \code{overfull_method} selects
the treatment and is recorded per row in \code{method_overfull}:
\itemize{
\item \code{"spillover"} (default) places the excess on same-country neighbours,
widening the search ring until it is absorbed, taking non-forested natural
land (LUH2 \code{primn}, \code{secdn}) first and forest (\code{primf}, \code{secdf}) only as a
fallback. A neighbour can only receive a class it has a row for, so land
the pattern classified nowhere is never quietly credited. The ring each
hectare reached is reported in \code{spillover_max_ring}; at 2020 it places
63.45 of the 63.50 Mha across 3,878 receiving polycells, at a median ring
of 2 and a maximum of 22, and names the remaining 42,765 ha (two
polycell-classes, in a country whose free land holds neither class) in
\code{unplaceable_statistical_ha}.
\item \code{"cap"} caps the agricultural classes at \code{land_area_ha} pro rata and leaves
the whole 63.50 Mha in \code{unplaceable_statistical_ha}. It is the sensitivity
baseline that quantifies what spillover buys.
}

The methods are alternatives, never fallbacks. If \code{"spillover"} cannot place
a hectare anywhere in the country, it stays in \code{unplaceable_statistical_ha}
with a warning and the row still reads \code{method_overfull = "spillover"}.
}

\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning. Where that polity is not live in the
row's own year -- 41.5\% of the pre-1961 \verb{(area, year)} cells --
\code{\link[=add_polity_code]{add_polity_code()}} says so as \code{mapping_status == "backcast_anchor"}, and
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports it as \code{gap_kind == "backcast_anchor"}.
These columns do not say so either way.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_polycell_land_uses(example = TRUE)
}
