% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_mappings.R
\docType{data}
\name{polity_label_aliases}
\alias{polity_label_aliases}
\title{Source label to polity aliases}
\format{
A tibble with one row per alias. Columns:
\itemize{
\item \code{source_label}: The label exactly as the source writes it.
\item \code{source}: Source slug the alias is scoped to, or \code{NA} when it applies to
any source.
\item \code{year_start}, \code{year_end}: Year range the alias is scoped to. A missing bound
is unbounded on that side; both missing means the alias is not year-scoped.
\item \code{polity_code}: The WHEP polity the label resolves to.
\item \code{common_name}: Human-readable name of that polity.
\item \code{confidence}: Curator's confidence in the alias.
\item \code{observed_rows}: Source rows actually observed for the label, \code{NA} when the
label is merely mappable.
\item \code{disposition}: \code{NA} when the source observed the territory, \code{"back_cast"}
when the years are a reconstruction onto a boundary that did not exist yet.
A \code{"back_cast"} alias may begin before its target polity does, by design:
the polity's own span still starts when the territory did.
\code{\link[=resolve_polity_label]{resolve_polity_label()}} drops these rules when asked \code{back_cast = FALSE}.
All \code{NA} in a snapshot taken before whep-polities introduced the column.
\item \code{indicator}: \code{NA} when the alias applies to every indicator, otherwise the
one indicator it routes (\code{"area"}, \code{"production"}, \code{"yield"},
\code{"livestock_stock"} or \code{"landuse"}, the subnational panel's own values).
whep-polities #703 added it so that a panel unit whose id names different
territories for different indicators can be split; it allows the scope only
on the panel's slugs. \code{\link[=resolve_polity_label]{resolve_polity_label()}} applies a scoped rule only to
rows passing that \code{indicator}. Absent from a snapshot taken before #703,
which is read as all \code{NA}.
}
}
\source{
\verb{~/whep-polities/data/final/label_alias_map.csv}.
}
\usage{
polity_label_aliases
}
\description{
Published map from the country/area \strong{labels} a source writes to periodized
WHEP \code{polity_code} values, consumed by \code{\link[=resolve_polity_label]{resolve_polity_label()}}. It is the
label-keyed counterpart of \link{polity_area_crosswalk}, which is keyed by numeric
reporting area code.
}
\details{
The map is authored and gated in \code{whep-polities}; this package embeds a copy
rather than deciding label identity itself, so that a label's meaning has one
authority.
}
\keyword{datasets}
