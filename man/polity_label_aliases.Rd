% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\docType{data}
\name{polity_label_aliases}
\alias{polity_label_aliases}
\title{Source label to polity aliases}
\format{
A tibble with one row per alias:
\itemize{
\item \code{source_label}: the label as the source writes it, verbatim.
\item \code{source}: source slug the alias is scoped to; empty applies to any source.
\item \code{year_start}, \code{year_end}: inclusive year range; empty applies to any year.
\item \code{polity_code}: the polity the label resolves to.
\item \code{common_name}: human-readable name for that polity.
\item \code{confidence}: the upstream matcher's confidence in the assignment.
\item \code{observed_rows}: how many source rows were actually seen carrying this label,
\code{0} when the label is merely mappable. Published so a consumer can tell
"this label carries data" from "this label could be resolved if it did",
which are different questions. \code{data-raw/table_mappings.R} uses it to decide
which FABIO rest-of-world areas may be folded: an area that reports data must
keep its own polity rather than being summed into an aggregate.
}

Unlike \link{polities} and \link{polity_area_crosswalk}, whose \verb{@format} sections list
\emph{key} columns, this one is exhaustive — every column is above.
}
\usage{
polity_label_aliases
}
\description{
The mapping from a country or area \strong{label}, as a source writes it, to a WHEP
polity. Read by \code{\link[=resolve_polity_label]{resolve_polity_label()}}.
}
\details{
A copy of \code{data/final/label_alias_map.csv} as published by whep-polities, which
is the authority for what a label means. It is embedded rather than resolved at
runtime for the same reason \link{polities} is: a package function cannot depend on a
sibling checkout existing. It is deliberately not \emph{computed} here — a second
lookup in this package would be a second authority for the same question, which
is how the FAOSTAT area mapping came to attribute 118 area-years to polities
that did not exist.

Two scopings carry the meaning, and both are load-bearing:
\itemize{
\item \code{source} — the same label can mean different things in different sources, so
an alias may apply to one source only. Empty means it applies to any.
\item \code{year_start} / \code{year_end} — a label's referent changes. \code{"Cape Verde"} is the
Portuguese colony \code{CPV-1886-1975} before independence and \code{CPV-1975-2025}
after, so it appears twice with different ranges. Empty means any year.
}
}
\examples{
head(polity_label_aliases)
}
\seealso{
\code{\link[=resolve_polity_label]{resolve_polity_label()}}, \link{polity_area_crosswalk} for numeric codes.
}
\keyword{datasets}
