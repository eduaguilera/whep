% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_output_plots.R
\name{plot_input_output_four_panel}
\alias{plot_input_output_four_panel}
\title{Plot all four input-output panels together}
\usage{
plot_input_output_four_panel(example = FALSE)
}
\arguments{
\item{example}{If \code{TRUE}, build all four panels from a small example
dataset without downloading remote data. Default is \code{FALSE}.}
}
\value{
A patchwork ggplot object.
}
\description{
Combines \code{\link[=plot_input_output]{plot_input_output()}} for \code{"Cropland"} and
\code{"semi_natural_agroecosystems"}, \code{\link[=plot_input_output_livestock]{plot_input_output_livestock()}}, and
\code{\link[=plot_input_output_system]{plot_input_output_system()}} into a single four-panel (2x2) figure of
national nitrogen totals (Gg N). There is no \code{per_ha} argument: per
hectare, the four systems do not share one area basis (Cropland and
semi-natural agroecosystems each use their own land-use area; Livestock
and the agro-food system use total agricultural area -- see
\code{\link[=plot_input_output_land_panel]{plot_input_output_land_panel()}} and \code{\link[=plot_input_output_total_panel]{plot_input_output_total_panel()}}),
so folding all four normalized panels into one grid would silently mix
two different denominators. Use those two functions for the per-hectare
view instead, kept as two separate figures for that reason.
}
\examples{
if (
  requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("patchwork", quietly = TRUE)
) {
  plot_input_output_four_panel(example = TRUE)
}
}
