% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_land_extension.R
\name{build_cropgrids_land_extension}
\alias{build_cropgrids_land_extension}
\title{Build a per-crop physical land extension from CROPGRIDS.}
\usage{
build_cropgrids_land_extension(
  harvested = NULL,
  cropgrids = NULL,
  source = c("cropgrids", "cropgrids_fallow")
)
}
\arguments{
\item{harvested}{Tibble of harvested area with columns \code{year}, \code{area_code},
\code{item_cbs_code}, \code{harvested_ha}. If \code{NULL}, built from
\code{\link[=get_primary_production]{get_primary_production()}} (\code{unit == "ha"}).}

\item{cropgrids}{Tibble of national crop areas with columns \code{area_code},
\code{item_cbs_code}, \code{physical_ha}, \code{harvested_ha}. If \code{NULL}, the packaged
table selected by \code{source} is used.}

\item{source}{Which packaged table to use when \code{cropgrids} is \code{NULL}:
\code{"cropgrids"} (physical crop area, excludes fallow) or \code{"cropgrids_fallow"}
(physical area with rotational fallow attributed to crops by
\code{\link[=attribute_fallow_to_crops]{attribute_fallow_to_crops()}}). Also recorded in \code{method_land}.}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (physical land area in hectares), and \code{method_land}.
}
\description{
Convert FAOSTAT harvested area into per-crop \emph{physical} cropland using
CROPGRIDS, then return a land extension keyed by
\verb{(year, area_code, item_cbs_code)} for the FABIO footprint model.

CROPGRIDS (Tang et al. 2024) reports, per crop and country, both \strong{harvested}
area and \strong{crop (physical) area} for 2020. Their ratio is a genuinely
per-crop multi-cropping correction — e.g. rice ~0.81 (heavily double-cropped),
most other crops ~0.95-1.0 (single-cropped) — which a single country-level
cropping-intensity factor, or a cell-level apportionment by harvested share,
cannot reproduce. This function applies that per-(area, item) physical /
harvested ratio to WHEP harvested area in each year:
\eqn{physical = harvested \times (physical_{cg} / harvested_{cg})}.

Note: CROPGRIDS physical area is the land where each crop actually grows; it
excludes fallow land (unlike fallow-inclusive cropland-apportionment), so
totals are typically a few percent below harvested area.
}
\examples{
harvested <- tibble::tribble(
  ~year, ~area_code, ~item_cbs_code, ~harvested_ha,
  2000L, 33L, 2511L, 1000,
  2000L, 33L, 2807L, 500
)
cropgrids <- tibble::tribble(
  ~area_code, ~item_cbs_code, ~physical_ha, ~harvested_ha,
  33L, 2511L, 990, 1000,
  33L, 2807L, 400, 500
)
build_cropgrids_land_extension(harvested, cropgrids)
}
