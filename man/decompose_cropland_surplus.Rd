% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{decompose_cropland_surplus}
\alias{decompose_cropland_surplus}
\title{Decompose cropland N surplus into size, intensity, and inefficiency
drivers}
\usage{
decompose_cropland_surplus(
  n_prov_destiny = NULL,
  npp_ygpit = NULL,
  codes_coefs = NULL,
  by_period = FALSE
)
}
\arguments{
\item{n_prov_destiny}{Nitrogen flows tibble from
\code{\link[=create_n_prov_destiny]{create_n_prov_destiny()}}. If \code{NULL}, loaded automatically.}

\item{npp_ygpit}{Land use and area tibble from
\code{whep_read_file("npp_ygpit")}. If \code{NULL}, loaded automatically.}

\item{codes_coefs}{Item and biomass coefficients tibble from
\code{whep_read_file("codes_coefs")}. If \code{NULL}, loaded automatically.}

\item{by_period}{If \code{TRUE}, compares each reference period (each
averaged across its ten years) against the immediately preceding
one — 1860-1870 -> 1920-1930 -> 1960-1970 -> 2010-2020 — plus one
extra transition spanning the full analysis window, 1860-1870
straight to 2010-2020 (the total change) — instead of chaining year
on year.}
}
\value{
A tibble from \code{\link[=calculate_lmdi]{calculate_lmdi()}} with columns \code{period},
\code{period_years}, \code{factor_label}, \code{component_type}, \code{additive},
\code{multiplicative}, and \code{multiplicative_log}.
}
\description{
Decomposes the year-on-year change in Spain's cropland nitrogen (N)
surplus into three multiplicative drivers, following an additive LMDI
(logarithmic mean Divisia index) shift-share decomposition computed at
the national level:
\itemize{
\item \strong{Size}: national cropland area.
\item \strong{Intensity}: N input per hectare of cropland.
\item \strong{Inefficiency}: surplus fraction of inputs (1 - nitrogen use
efficiency).
}

Contributions are additive and residual-free: they sum exactly to the
observed change in cropland N surplus for every year-on-year transition.

This is a simplified, national-only view (no provincial or destiny
breakdown); \code{\link[=decompose_specialization_covariance]{decompose_specialization_covariance()}} still uses the
full province x destiny detail for its \code{cropland_province} and
\code{cropland_destiny} series.
}
\examples{
# decompose_cropland_surplus()
}
