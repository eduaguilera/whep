% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feed_lpjml.R
\name{build_grass_availability_lpjml}
\alias{build_grass_availability_lpjml}
\title{Build grazable grass availability from an LPJmL run.}
\usage{
build_grass_availability_lpjml(
  run_dir = NULL,
  years = NULL,
  first_year = 1901L,
  shares = grass_access_shares(),
  example = FALSE
)
}
\arguments{
\item{run_dir}{Path to the LPJmL run output directory holding \code{pft_npp.nc}
and \code{cftfrac.nc} (the \verb{scenario_*} output folder).}

\item{years}{Integer vector of calendar years to read.}

\item{first_year}{First calendar year of the run's output time axis.}

\item{shares}{Accessibility and conversion parameters from
\code{\link[=grass_access_shares]{grass_access_shares()}}.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading a run.}
}
\value{
A tibble with \code{lon}, \code{lat}, \code{year}, \code{grass_npp_gc_m2},
\code{grass_avail_dm_t_ha} and \code{grass_avail_dm_t}.
}
\description{
Reads managed-grassland net primary production (the LPJmL grassland CFT) from
a finished run and converts it to grazable above-ground dry-matter
availability, the forage supply ceiling for feed allocation. Availability is
the production flux, not the realised grazing off-take (the off-take is the
intake-validation target, not the supply).
}
\examples{
build_grass_availability_lpjml(example = TRUE)
}
