% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{record_provenance}
\alias{record_provenance}
\title{Record provenance for a reproducible result.}
\usage{
record_provenance(
  aliases = NULL,
  inputs = whep::whep_inputs,
  recorded_at = Sys.time()
)
}
\arguments{
\item{aliases}{Optional character vector of input aliases to
record. When \code{NULL} (default), every registered input is
recorded.}

\item{inputs}{Tibble of registered inputs with \code{alias} and
\code{version} columns. Defaults to \link{whep_inputs}.}

\item{recorded_at}{Timestamp for the record. Defaults to the
current time; pass a fixed value for reproducible output.}
}
\value{
A tibble with one row per recorded input:
\itemize{
\item \code{recorded_at}: When the record was made.
\item \code{whep_version}: Installed package version (the code).
\item \code{r_version}: R version.
\item \code{input_alias}: Input dataset alias.
\item \code{input_version}: Pinned version of that input.
}
}
\description{
Capture the information needed to regenerate a result: the
package version (the code), the R version, the pinned versions
of the input datasets used, and a timestamp. Attach the record
to an output with \code{\link[=attach_provenance]{attach_provenance()}} so any number can be
traced back to the exact inputs and code that produced it.

Unknown aliases are an error rather than a silent omission, so a
provenance record never quietly drops an input it could not
resolve.
}
\examples{
prov <- record_provenance(
  aliases = "bilateral_trade",
  recorded_at = as.POSIXct("2026-01-01", tz = "UTC")
)
prov
}
