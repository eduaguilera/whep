% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nitrogen_extension.R
\name{build_nitrogen_extension}
\alias{build_nitrogen_extension}
\title{Build the nitrogen footprint extension.}
\usage{
build_nitrogen_extension(
  method = c("surplus", "bnf"),
  residue_removed_frac = 0.45,
  data = list(),
  example = FALSE
)
}
\arguments{
\item{method}{Nitrogen metric, \code{"surplus"} (default) or \code{"bnf"}.}

\item{residue_removed_frac}{Fraction of above-ground crop residue removed
from the field and therefore not returned to soil, passed through to the
F_CR input. Defaults to \code{0.45}. Used only for \code{method = "surplus"}.}

\item{data}{Optional named list of pre-loaded inputs to avoid remote reads:
\code{primary_prod} (\code{\link[=get_primary_production]{get_primary_production()}}), \code{fertilizer} (the
\code{faostat-fertilizer-nutrients} pin), \code{manure} (the
\code{faostat-emissions-livestock} pin) and \code{primary_residues}
(\code{\link[=get_primary_residues]{get_primary_residues()}}). Each falls back to its reader when absent; the
\code{"bnf"} method needs only \code{primary_prod}.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading remote
data. Defaults to \code{FALSE}.}
}
\value{
A tibble with columns \code{year}, \code{area_code}, \code{item_cbs_code},
\code{impact_u} (nitrogen in kilograms) and \code{method_nitrogen} (e.g.
\code{"soil_n_surplus"}).
}
\description{
Aggregate the cropland nitrogen balance into a footprint extension keyed by
\verb{(year, area_code, item_cbs_code)}, expressed in kilograms of nitrogen
(kg N). This is the nitrogen analogue of \code{\link[=build_crop_soil_n2o_extension]{build_crop_soil_n2o_extension()}}
and \code{\link[=build_livestock_ghg_extension]{build_livestock_ghg_extension()}} and feeds \code{\link[=build_footprint]{build_footprint()}} /
\code{\link[=compute_footprint]{compute_footprint()}} the same way.

The footprint engine traces one impact column at a time, so the nitrogen
metric is selected with \code{method} (recorded in \code{method_nitrogen}):
\itemize{
\item \code{"surplus"} (default): the soil nitrogen surplus, \code{inputs - removal}, the
canonical nitrogen pressure (the net load available for leaching,
volatilisation and runoff). Inputs reuse the soil-N assembly of
\code{\link[=build_crop_soil_n2o_extension]{build_crop_soil_n2o_extension()}} -- synthetic fertiliser (F_SN), applied
manure (F_ON) and returned crop residue (F_CR), each allocated to crops by
harvested-area share -- plus biological nitrogen fixation (BNF). Removal is
the nitrogen exported in the harvested product. A sector whose removal
exceeds its inputs (net soil-nitrogen mining) is floored to zero rather
than reported as a negative load, because the footprint engine's
conservation step (\code{\link[=compute_footprint]{compute_footprint()}} with \code{conserve_extensions = TRUE})
clamps negative extensions and drops non-positive flows, so only a positive
nitrogen load can be traced. Atmospheric deposition and below-ground
residue nitrogen are further inputs not yet included.
\item \code{"bnf"}: biological nitrogen fixation alone, the nitrogen fixed by
symbiotic crop legumes (\code{\link[=calculate_crop_bnf]{calculate_crop_bnf()}}). It is an input pressure on
its own and a sensitivity reference for the surplus.
}

Biological fixation and product removal come from whep's own crop
net-primary-production chain: \code{\link[=calculate_crop_npp]{calculate_crop_npp()}} and
\code{\link[=calculate_npp_carbon_nitrogen]{calculate_npp_carbon_nitrogen()}} derive per-crop product and residue
nitrogen from production and harvested area, and \code{\link[=calculate_crop_bnf]{calculate_crop_bnf()}}
derives the legume fixation. Non-legumes fix no symbiotic nitrogen, so they
contribute to the surplus only through fertiliser, manure and residues.

All terms are nitrogen masses, so no global-warming-potential or other
characterisation factor is applied; the extension is wholly in-repo and needs
no coefficient pins beyond the FAOSTAT fertiliser and manure inputs already
read by \code{\link[=build_crop_soil_n2o_extension]{build_crop_soil_n2o_extension()}}.
}
\examples{
build_nitrogen_extension(example = TRUE)
}
