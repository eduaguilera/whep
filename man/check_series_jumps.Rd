% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_series_jumps.R
\name{check_series_jumps}
\alias{check_series_jumps}
\title{Flag implausible year-on-year jumps in a time series.}
\usage{
check_series_jumps(
  data,
  value_col,
  time_col = year,
  .by = NULL,
  ratio_bounds = c(0.55, 1.6),
  bands = NULL,
  min_value = 0,
  consecutive_only = TRUE,
  allowlist = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame with one observation per row.}

\item{value_col}{The column holding the series values to scan.}

\item{time_col}{The column holding time values. Default: \code{year}.}

\item{.by}{A character vector of grouping columns identifying each
series (optional). When \code{NULL}, the whole table is one series.}

\item{ratio_bounds}{Length-2 numeric \code{c(low, high)}: the default
plausible band for the ratio of consecutive values. A ratio below
\code{low} or above \code{high} is flagged.}

\item{bands}{Optional data frame of per-group band overrides: the
grouping columns (a subset of \code{.by}) plus \code{lo} and \code{hi}. Where a group
matches, its \code{lo}/\code{hi} replace \code{ratio_bounds} for that group.}

\item{min_value}{Minimum value both members of a pair must exceed to be
flagged. Keeps genuine near-zero technology onsets from firing.
Default: \code{0}.}

\item{consecutive_only}{Logical. If \code{TRUE} (default), only pairs one
time step apart are scanned; larger gaps are skipped.}

\item{allowlist}{Optional data frame of documented break years, matched
on the grouping columns plus \code{time_col}. Matching flags are returned
with \code{allowlisted = TRUE} rather than dropped.}

\item{verbose}{Logical. If \code{TRUE} (default), report flag counts with
\code{cli}.}
}
\value{
A tibble with one row per flagged jump: the grouping columns,
the \code{time_col} of the later observation, \code{prev_value}, \code{value},
\code{ratio} (\code{value / prev_value}) and \code{allowlisted}. When nothing is
flagged, a zero-row tibble of the same shape and column types.
}
\description{
Scan each series for consecutive observations whose ratio falls outside
a plausible band, the level-2 (within-series) detector of the AFE data
and code validation framework. It catches unjustified single-year steps,
spikes and the level shifts that appear where two sources are spliced,
while a break-year allowlist keeps documented regime changes from firing
as false positives.
}
\details{
This is the first landed detector of the reusable \verb{check_*} library
described in the AFE data-validation framework decision
(\code{afse-wiki/wiki/decisions/afse-data-validation-framework.md}, level 2,
within-series). It generalises the energy-hist consecutive-year jump scan
(a world-total ratio scan against a break-year allowlist) into a grouped,
band-parameterised check usable both as an inline pipeline guard and as a
test backend.

Two pieces of the framework's metadata model are exposed as arguments.
\code{bands} supplies per-variable plausible-jump bands (land area is tight,
yield is wide, so a single global band is wrong for a mixed panel), and
\code{allowlist} supplies the historically justified break years that are
reported but not treated as defects. Undocumented jumps stay flagged.

A robust variant (flagging via the median absolute deviation of log
ratios, per the Hampel/MAD anchors in the framework) is a documented
future extension, not implemented here.
}
\examples{
series <- tibble::tibble(
  category = rep(c("area", "yield"), each = 5),
  year = rep(2000:2004, times = 2),
  value = c(100, 101, 102, 180, 181, 3.0, 3.1, 5.2, 3.0, 3.1)
)
# area steps 102 -> 180 (1.76x); yield steps 3.1 -> 5.2 (1.68x)
check_series_jumps(series, value, .by = "category")

# Widen the band for yield only, leaving area on the default:
bands <- tibble::tibble(category = "yield", lo = 0.4, hi = 2.5)
check_series_jumps(series, value, .by = "category", bands = bands)
}
