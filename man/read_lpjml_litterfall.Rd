% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpjml_litterfall.R
\name{read_lpjml_litterfall}
\alias{read_lpjml_litterfall}
\title{Read litterfall by land-use class from an LPJmL run.}
\source{
Schaphoff, S. et al. (2018). LPJmL4 - a dynamic global vegetation
model with managed land - Part 1: Model description. \emph{Geoscientific Model
Development}, 11, 1343-1375. \doi{10.5194/gmd-11-1343-2018}.
}
\usage{
read_lpjml_litterfall(
  class = c("nv", "agr", "mgrass", "luc", "total"),
  run_dir = NULL,
  years = NULL,
  first_year = 1901L,
  example = FALSE
)
}
\arguments{
\item{class}{Which stand's litterfall to read: \code{"nv"} (standing natural
vegetation), \code{"agr"} (crop stands), \code{"mgrass"} (managed grassland),
\code{"luc"} (the land-use conversion pulse) or \code{"total"}.}

\item{run_dir}{Path to the LPJmL run output directory. \code{NULL} (default)
uses \code{WHEP_LPJML_RUN_DIR}.}

\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
(default) keeps every year the file covers.}

\item{first_year}{Calendar year of the file's first time step.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading a run.
Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{lon}, \code{lat}, \code{year}, \code{class} and
\code{litterfall_c_mgc_ha_yr}.
}
\description{
Per-cell, per-year carbon returned to the soil as litter, split by the
stand that shed it. Litterfall is what physically enters the soil;
net primary production is not, because it also contains the increment
that stays in living biomass. On this run natural litterfall is 0.84 times
natural NPP at the median cell and 0.68 in aggregate, the shortfall being
biomass accumulation, fire, and the conversion pulse.
}
\section{Whole-cell densities}{

Every class is a density per square metre of GRIDCELL, not of its own
stand: each accumulation site in LPJmL multiplies by \code{stand->frac}. To get
the density a stand actually experiences, divide by that stand's area
fraction -- for \code{"nv"}, \code{natural_stand_frac} from
\code{\link[=read_lpjml_natural_cover]{read_lpjml_natural_cover()}}. Mixing the two conventions understates a
partly-natural cell's input by exactly its natural fraction.
}

\section{The four classes sum to the total}{

\code{nv + luc + agr + mgrass} recovers \code{total} to float32 rounding, the
remainder being set-aside grass. \code{"luc"} is the pulse released when land is
converted, and it is large -- 9.9\% of all litterfall at 2010 against 1.3\%
for crop stands. It is booked to no destination: at \code{landusechange.c} the
receiving stand is still natural when the pulse is credited, so
apportioning it to the land that gained area is a decision for the caller,
not a quantity LPJmL reports.
}

\examples{
read_lpjml_litterfall(example = TRUE)
}
