% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_shares_pins.R
\name{read_admin_shares}
\alias{read_admin_shares}
\title{Read the assembled subnational admin-shares pin}
\usage{
read_admin_shares(example = FALSE)
}
\arguments{
\item{example}{If \code{TRUE}, return a small fixture instead of reading the
pin. Defaults to \code{FALSE}.}
}
\value{
A named list of three elements:
\itemize{
\item \code{shares}: the pin's rows, in \code{\link[=admin_shares_prototype]{admin_shares_prototype()}}'s shape, or
the zero-row prototype when the pin is not shipped.
\item \code{excluded}: a tibble of \code{source}, \code{reason} and \code{detail}, as above.
\item \code{not_shipped}: \code{"admin-shares"} when the pin's alias is not
registered in \code{\link{whep_inputs}} or holds no rows, \code{character(0)}
otherwise.
}
}
\description{
Read the single \code{admin-shares} pin -- the tier-1, tier-2 and tier-3
administrative statistics assembled onto the one contract
\code{\link[=admin_shares_schema]{admin_shares_schema()}} declares -- together with the report of which
families are \strong{not} in it and why.

Polity resolution is deliberately not done here. The pin is keyed on
source-native identifiers and carries \code{level_polity_code == NA} on
every row; a caller that needs polities passes the rows through
\code{\link[=resolve_admin_units]{resolve_admin_units()}}, which redoes the resolution against the
current \link{polities} snapshot on every load. That is what lets this
artifact age without going stale.

That call needs one rename first. The contract names the identifier
column \code{source_native_id}, where \code{\link[=resolve_admin_units]{resolve_admin_units()}} takes the
readers' \code{source_native_unit_id}, so the composition is
\code{dplyr::rename(shares, source_native_unit_id = source_native_id)} and
then \code{\link[=resolve_admin_units]{resolve_admin_units()}}. Handing the rows over unrenamed aborts
on the missing column.
}
\section{What the excluded report can and cannot say}{

\code{excluded} names every tier-2/3 family of \code{\link[=read_admin_family]{read_admin_family()}} that
contributed no row to this pin, with a reason from a closed vocabulary
and a \code{detail} saying why the family is absent -- not what permission
it ships under, which is the manifest's own job:
\itemize{
\item \code{"no_consent_manifest_row"}: the family is absent from
\code{inst/extdata/admin_stats_pins_manifest.csv}, so its publication
consent is not recorded and the assembly refused to ship it.
\item \code{"no_rows_in_pin"}: its consent is recorded, but none of its rows
survived onto the contract.
\item \code{"pin_not_read"}: no pin was read at all -- the alias is not
registered in \code{\link{whep_inputs}}, or the board returned no rows -- so
nothing about this family was measured. Every family carries this
reason together, and \code{not_shipped} says the same thing.
}

The second reason is expected for \code{"admin-stats-france-livestock"} and
will stay so: that family is head counts, and head counts are outside
\code{\link[=admin_shares_schema]{admin_shares_schema()}} by design -- \code{indicator_used} closes over area,
production and yield (\code{R/admin_stats_sidra.R}). The livestock
constraint travels the reader and family path, not this pin, and that
is what its \code{detail} says.

A read cannot see which rows were dropped or why, so for any other
family \code{detail} points at the assembly's per-source counts. Those
counts are a \strong{second, build-time report}, written by
\code{inst/scripts/prepare_admin_shares_pin.R} and not carried into the pin.
It shares this vocabulary where the two can mean the same thing
(\code{"no_consent_manifest_row"}, \code{"no_rows_in_pin"}) and adds one reason a
read cannot observe: \code{"not_available"}, a consented family that was
neither registered on the board nor staged locally when the pin was
assembled. That is a fact about an assembly this read never saw, so it
stays build-time only. What a read can always tell apart is whether it
read a pin at all, which is what \code{"pin_not_read"} says -- the report
used to claim five families had been read and filtered when the board
had not been touched.
}

\examples{
read_admin_shares(example = TRUE)
}
