% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conservation.R
\name{check_footprint_conservation}
\alias{check_footprint_conservation}
\title{Check footprint conservation against direct extensions.}
\usage{
check_footprint_conservation(
  footprint,
  extensions,
  labels,
  x_vec,
  output_tol = 1e-08,
  tol = 0.01
)
}
\arguments{
\item{footprint}{Footprint tibble from \code{\link[=compute_footprint]{compute_footprint()}},
with \code{origin_area}, \code{origin_item} and \code{value} columns.}

\item{extensions}{Numeric vector of environmental extensions per
sector, as passed to \code{\link[=compute_footprint]{compute_footprint()}}.}

\item{labels}{Tibble with \code{area_code} and \code{item_cbs_code}
mapping each sector to its meaning, as passed to
\code{\link[=compute_footprint]{compute_footprint()}}.}

\item{x_vec}{Numeric vector of total output per sector.}

\item{output_tol}{Minimum output for a sector to be traceable.
Sectors with \code{x_vec <= output_tol} contribute zero direct
pressure, matching \code{\link[=compute_footprint]{compute_footprint()}}.}

\item{tol}{Relative tolerance for the conservation status.
Discrepancies within \code{tol} of the direct pressure are \code{"ok"}.}
}
\value{
A tibble with one row per origin sector:
\itemize{
\item \code{origin_area}: Country where the pressure occurs.
\item \code{origin_item}: Item causing the pressure.
\item \code{direct}: Direct (source) extension for the sector.
\item \code{embodied}: Footprint traced back to the sector.
\item \code{discrepancy}: \code{embodied - direct}.
\item \code{rel_discrepancy}: \code{discrepancy / direct} (\code{NA} when
\code{direct} is zero).
\item \code{status}: One of \code{"ok"}, \code{"under_traced"}, \code{"dropped"}
(embodied is zero while direct is positive) or
\code{"over_traced"}.
Rows are ordered by descending absolute relative discrepancy.
}
}
\description{
Verify the master input-output accounting identity for a
footprint: the environmental pressure embodied across all final
demand and traced back to an origin sector should equal the
direct extension (the source pressure) of that sector.

The footprint engine zeroes negative coefficients, caps column
sums, and drops near-zero-output sectors (the FABIO
conventions), so the identity holds only approximately. This
check quantifies the discrepancy per origin sector instead of
asserting exact equality. Crucially it detects \emph{under-tracing}
(pressure that silently disappears and never reaches final
demand), which \code{\link[=compute_footprint]{compute_footprint()}}'s \code{conserve_extensions}
bounding never reports because it only rescales results
downward.

Only the positive side of the extensions is traced, matching the
engine, which traces \code{pmax(extensions, 0)} and ignores sectors
with output \verb{<= output_tol}.
}
\examples{
z_mat <- matrix(c(0, 5, 10, 0), nrow = 2)
x_vec <- c(100, 200)
y_mat <- matrix(c(85, 195), ncol = 1)
extensions <- c(50, 30)
labels <- tibble::tibble(
  area_code = c(1L, 1L),
  item_cbs_code = c(1L, 2L)
)
fp <- compute_footprint(
  x_vec = x_vec, y_mat = y_mat, extensions = extensions,
  labels = labels, z_mat = z_mat
)
check_footprint_conservation(fp, extensions, labels, x_vec)
}
