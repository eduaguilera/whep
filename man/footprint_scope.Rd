% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scope.R
\name{footprint_scope}
\alias{footprint_scope}
\title{Describe the scope of a footprint result.}
\usage{
footprint_scope(stressor, units, method, details = list())
}
\arguments{
\item{stressor}{What is measured, e.g. \code{"cropland"}.}

\item{units}{Units of the footprint value, e.g. \code{"ha"}.}

\item{method}{Estimation method used, e.g. \code{"FABIO-MRIO"}. This
mirrors the multi-method \verb{method_<quantity>} columns recorded
elsewhere in the package.}

\item{details}{Optional named list overriding any of:
\code{boundary} (system boundary), \code{allocation} (allocation rule),
\code{vintage} (data years) and \code{limitations} (free text).}
}
\value{
A one-row tibble with columns \code{stressor}, \code{units},
\code{method}, \code{boundary}, \code{allocation}, \code{vintage} and
\code{limitations}.
}
\description{
Build a machine-readable scope record for a footprint: what is
measured, in which units, by which method, and under which
system boundary, allocation rule, data vintage and known
limitations. This is the ISO 14044 goal-and-scope made into an
attachable data object rather than prose, so a footprint result
always travels with the assumptions behind it.

Attach it to a result with \code{\link[=attach_scope]{attach_scope()}} and read it back
with \code{\link[=get_scope]{get_scope()}}.
}
\examples{
footprint_scope(
  stressor = "cropland",
  units = "ha",
  method = "FABIO-MRIO",
  details = list(vintage = "1850-2023", allocation = "mass")
)
}
