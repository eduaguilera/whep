% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polities.R
\name{add_reporting_polity_columns}
\alias{add_reporting_polity_columns}
\title{Add reporting polity columns to a table}
\usage{
add_reporting_polity_columns(table, code_column = "area_code")
}
\arguments{
\item{table}{A data frame with a numeric FAOSTAT area-code column.}

\item{code_column}{Name of the column containing numeric area codes.}
}
\value{
A tibble with the reporting polity columns added.
}
\description{
Attach the derived reporting-polity metadata that base WHEP datasets
(e.g. \code{\link[=build_primary_production]{build_primary_production()}}) deliberately omit: the polity
identity is a pure lookup on the area code (and \code{year}, when present), so
it is joined on demand instead of being carried through every table.
Adds \code{polity_area_code} (the numeric WHEP reporting code used in matrix
workflows), \code{reporting_polity_code}, \code{reporting_polity_name}, and
\code{reporting_polity_has_geometry} from \link{polity_area_crosswalk}.
}
\examples{
tibble::tibble(year = 2015L, area_code = 212L, value = 1) |>
  add_reporting_polity_columns()
}
