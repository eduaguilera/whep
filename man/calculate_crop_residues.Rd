% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_npp.R
\name{calculate_crop_residues}
\alias{calculate_crop_residues}
\title{Estimate crop above-ground residue biomass.}
\usage{
calculate_crop_residues(
  x,
  method = c("ensemble", "ipcc", "ratio"),
  weights = list(w_ipcc = 0.5)
)
}
\arguments{
\item{x}{A tibble with \code{item_prod_code}, \code{production_t} (fresh matter) and
\code{area_ha}. Optional \code{water_regime} enables the irrigation adjustment;
optional \code{year} plus \code{region_hanpp} enable the modern-variety adjustment.}

\item{method}{Residue method: \code{"ensemble"} (default, weighted IPCC + ratio),
\code{"ipcc"} (IPCC linear only) or \code{"ratio"} (\code{bio_coefs} ratio only).}

\item{weights}{Named list; \code{w_ipcc} is the IPCC weight in the ensemble (0-1,
default 0.5), used only when \code{method = "ensemble"}.}
}
\value{
The input tibble with \code{product_dm_t}, \code{yield_dm_t_ha}, \code{residue_dm_t}
and \code{method_residue}.
}
\description{
Estimates crop residue dry matter from production and area using an ensemble
of an IPCC 2019 linear model and a \code{bio_coefs} residue:product ratio. The
irrigation and modern-variety adjustments activate only when their driver
columns are present in \code{x}.
}
\examples{
calculate_crop_residues(
  tibble::tibble(item_prod_code = "15", production_t = 100, area_ha = 40)
)
}
