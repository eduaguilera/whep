% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polycell_layers.R
\name{read_glaciated_areas}
\alias{read_glaciated_areas}
\title{Read the Natural Earth glaciated-areas ice layer}
\usage{
read_glaciated_areas(dir = NULL)
}
\arguments{
\item{dir}{Directory holding the shapefile. Defaults to
\code{Sys.getenv("WHEP_NATURALEARTH_DIR")}, under which the layer is expected at
\code{ne_10m_glaciated_areas/ne_10m_glaciated_areas.shp}.}
}
\value{
An \code{sf} table of glaciated polygons in WGS84, carrying a
\code{s2_repaired} logical column. The \code{"unrepaired"} attribute is a \code{tibble} of
the features that stayed \code{s2}-invalid, with their \code{terra::expanse()} area.
}
\description{
Reads \code{ne_10m_glaciated_areas}, the ice source for
\code{\link[=build_polycell_support]{build_polycell_support()}}. A few features are invalid under the spherical
\code{s2} engine and GEOS-level \code{sf::st_make_valid()} does not repair them, so
\code{sf::st_area()} aborts with "Loop 0 is not valid". Those features are
repaired under the planar engine, and any that remain invalid are measured
with \code{terra::expanse()}, which does not go through \code{s2}, and reported rather
than silently dropped.

The layer is a coarse present-day snapshot, so ice area does \strong{not} vary
historically. That is acceptable only while ice is a reporting category
rather than a driver.
}
\examples{
# Requires WHEP_NATURALEARTH_DIR to be set; not run without it.
if (nzchar(Sys.getenv("WHEP_NATURALEARTH_DIR"))) {
  read_glaciated_areas()
}
}
