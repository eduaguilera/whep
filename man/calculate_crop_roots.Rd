% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_npp.R
\name{calculate_crop_roots}
\alias{calculate_crop_roots}
\title{Estimate crop below-ground (root) biomass.}
\usage{
calculate_crop_roots(
  x,
  method = c("ensemble", "root_shoot", "reference"),
  weights = list(w_ref = 0.5)
)
}
\arguments{
\item{x}{A tibble with \code{item_prod_code}, \code{product_dm_t}, \code{residue_dm_t} and
\code{area_ha}. Optional \code{n_input_kg_ha} enables the nitrogen adjustment;
optional \code{water_regime} enables the irrigation adjustment.}

\item{method}{Root method: \code{"ensemble"} (default, weighted root:shoot +
reference), \code{"root_shoot"} (root:shoot ratio only) or \code{"reference"}
(reference below-ground biomass only).}

\item{weights}{Named list; \code{w_ref} is the reference weight in the ensemble
(0-1, default 0.5), used only when \code{method = "ensemble"}.}
}
\value{
The input tibble with \code{root_dm_t} and \code{method_root}.
}
\description{
Estimates root dry matter from above-ground biomass using an ensemble of an
IPCC root:shoot ratio and a reference below-ground biomass per hectare. The
nitrogen-input and irrigation adjustments activate only when their driver
columns are present in \code{x}.
}
\examples{
calculate_crop_roots(
  tibble::tibble(
    item_prod_code = "15",
    product_dm_t = 87.9,
    residue_dm_t = 135.75,
    area_ha = 40
  )
)
}
