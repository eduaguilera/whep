% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nourishment.R
\name{normalize_nourishment}
\alias{normalize_nourishment}
\title{Normalize and classify per-capita nourishment.}
\usage{
normalize_nourishment(x, value_col = protein_g_cap_day, thresholds = NULL)
}
\arguments{
\item{x}{A tibble carrying the per-capita nourishment column named by
\code{value_col} (for example a \code{\link[=build_food_supply]{build_food_supply()}} output).}

\item{value_col}{The unquoted nourishment column to normalize. Defaults to
\code{protein_g_cap_day}.}

\item{thresholds}{Optional named \code{floor} and \code{ceiling} (a named numeric
vector or list). When \code{NULL} (default) the protein floor and ceiling from
\link{nourishment_thresholds} are used.}
}
\value{
\code{x} with \code{value_norm} (numeric score) and \code{nourish} (\code{"Under"},
\code{"Adequate"} or \code{"Over"}) added.
}
\description{
Adds a piecewise-normalized nourishment score \code{value_norm} and its
Under / Adequate / Over class \code{nourish}. The normalization (the Global
\code{Intake_normalization}) is \code{value / floor} below the floor, \code{1 + value / ceiling} above the ceiling and \code{1 + (value - floor) / (ceiling - floor)} in
between, so the score is below 1 under the floor, exactly 1 at the floor,
between 1 and 2 across the adequate band and 2 or above at or past the
ceiling. The class is \code{"Under"} when \code{value_norm < 1}, \code{"Adequate"} when
\code{value_norm < 2} and \code{"Over"} otherwise. Protein is the SJOS-N nourishment
axis, so the defaults are the protein floor and ceiling (62.1 and 85.05
g/cap/day) from \link{nourishment_thresholds}; switching \code{value_col} to
\code{energy_kcal_cap_day} and passing \code{thresholds = c(floor = 2300, ceiling = 2900)} classifies dietary energy instead, a completeness cross-check rather
than the nitrogen classification.
}
\examples{
normalize_nourishment(
  tibble::tribble(
    ~area_code, ~protein_g_cap_day,
    10L, 30,
    20L, 70,
    30L, 100
  )
)
}
