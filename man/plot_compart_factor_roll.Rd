% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{plot_compart_factor_roll}
\alias{plot_compart_factor_roll}
\title{Plot each compartment's own factor breakdown, rolling mean}
\usage{
plot_compart_factor_roll(
  cropland = NULL,
  semi_natural = NULL,
  manure = NULL,
  urban = NULL,
  window = 10
)
}
\arguments{
\item{cropland}{A tibble from \code{\link[=decompose_cropland_surplus]{decompose_cropland_surplus()}}. If
\code{NULL}, computed automatically (slow).}

\item{semi_natural}{A tibble from \code{\link[=decompose_semi_natural_surplus]{decompose_semi_natural_surplus()}}.
If \code{NULL}, computed automatically (slow).}

\item{manure}{A tibble from \code{\link[=decompose_manure_losses]{decompose_manure_losses()}}. If \code{NULL},
computed automatically (slow).}

\item{urban}{A tibble from \code{\link[=decompose_urban_losses]{decompose_urban_losses()}}. If \code{NULL},
computed automatically (slow).}

\item{window}{Width of the centered rolling-mean window, in years.
Default \code{10}.}
}
\value{
A named list with ggplot objects \code{cropland}, \code{semi_natural},
\code{manure}, and \code{urban}.
}
\description{
Same as \code{\link[=plot_compart_factor_yearly]{plot_compart_factor_yearly()}}, but smooths each
year's own additive contribution with a centered rolling mean
(\code{window} years wide, \code{NA}-padded at the edges) before plotting, one
panel per compartment.
}
\examples{
# `window` must not exceed the number of periods available per factor.
lmdi <- tibble::tribble(
  ~period, ~factor_label, ~component_type, ~additive,
  "1861-1862", "N surplus", "target", 20000,
  "1861-1862", "Size", "factor", 9000,
  "1862-1863", "N surplus", "target", 10000,
  "1862-1863", "Size", "factor", 5000,
  "1863-1864", "N surplus", "target", 12000,
  "1863-1864", "Size", "factor", 6000
)
plots <- plot_compart_factor_roll(lmdi, lmdi, lmdi, lmdi, window = 3)
}
