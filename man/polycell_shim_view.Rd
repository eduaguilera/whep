% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polycell_support.R
\name{polycell_shim_view}
\alias{polycell_shim_view}
\title{View the polycell support as today's cell-polity crosswalk}
\usage{
polycell_shim_view(support)
}
\arguments{
\item{support}{A \code{\link[=build_polycell_support]{build_polycell_support()}} table built with
\code{data$crosswalk} supplied.}
}
\value{
A \code{tibble} with \code{lon}, \code{lat}, \code{area_code}, \code{polity_frac} and
\code{cell_area_ha}.
}
\description{
Returns the transitional shim: the \code{lon}, \code{lat}, \code{area_code}, \code{polity_frac}
and \code{cell_area_ha} columns a consumer of \code{\link[=build_cell_polity]{build_cell_polity()}} reads today.
Under the interim geometry this reproduces that crosswalk bit-for-bit, so
consumers migrate to the polycell columns one at a time instead of all on
the commit that reshapes the shared object. The shim is a transition device
with a scheduled removal, not a permanent contract.
}
\examples{
if (requireNamespace("sf", quietly = TRUE)) {
  build_polycell_support(
    years = 2015L,
    geometries = polycell_example_geometries(),
    data = list(
      crosswalk = tibble::tibble(
        lon = c(10.25, 10.75, 11.25),
        lat = 45.25,
        area_code = 11L,
        polity_frac = 1
      )
    )
  ) |>
    polycell_shim_view()
}
}
