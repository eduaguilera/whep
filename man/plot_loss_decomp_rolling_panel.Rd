% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{plot_loss_decomp_rolling_panel}
\alias{plot_loss_decomp_rolling_panel}
\title{Plot rolling-mean year-on-year drivers of territorial N losses, as
one combined panel plot}
\usage{
plot_loss_decomp_rolling_panel(decomp = NULL, window = 10)
}
\arguments{
\item{decomp}{A named list from \code{\link[=decompose_terr_losses]{decompose_terr_losses()}}.
If \code{NULL}, computed automatically (slow).}

\item{window}{Width of the centered rolling-mean window, in years.
Default \code{10}.}
}
\value{
A patchwork ggplot object with two panels ("By compartment",
"By mechanism").
}
\description{
Combines the two views from \code{\link[=plot_loss_decomp_rolling]{plot_loss_decomp_rolling()}}
(by compartment and by mechanism) side by side into a single
patchwork plot, matching \code{\link[=plot_loss_decomp_periods_panel]{plot_loss_decomp_periods_panel()}}'s
style: one shared y-axis label, each panel keeping its own legend
(compartment and mechanism are different fill scales, so the legends
aren't collected into one).
}
\examples{
if (
  requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("patchwork", quietly = TRUE)
) {
  decomp <- list(
    by_compartment = tibble::tribble(
      ~t0, ~compartment, ~contribution_mgn, ~cumulative_mgn,
      1861, "cropland", 12000, 12000,
      1862, "cropland", 8000, 20000,
      1863, "cropland", 9000, 29000
    ),
    by_mechanism = tibble::tribble(
      ~t0, ~mechanism, ~contribution_mgn, ~cumulative_mgn,
      1861, "Size", 9000, 9000,
      1862, "Size", 5000, 14000,
      1863, "Size", 6000, 20000
    )
  )
  panel <- plot_loss_decomp_rolling_panel(decomp, window = 3)
}
}
