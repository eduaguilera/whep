% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_mappings.R
\docType{data}
\name{polity_area_crosswalk}
\alias{polity_area_crosswalk}
\title{FAOSTAT/FABIO area-to-polity crosswalk}
\format{
A tibble with one row per area-code/polity-period mapping. Key columns:
\itemize{
\item \code{area_code}: Numeric FAOSTAT/FABIO reporting area code.
\item \code{area_name}: Reporting area name.
\item \code{area_iso3c}: ISO3-like code of the reporting AREA, where available. Not the
same as \code{iso3_code}, which this table also carries and which belongs to the
resolved POLITY — they differ on 56 of the 273 rows that have both. An area
folded into a rest-of-world polity keeps its own code while the polity's is
\code{ROW} (\code{ASM} against \code{ROW}), and a colonial-era row carries the era's key
while the area keeps the modern one (\code{AGO} against \code{ANG}, \code{BWA} against
\code{BEC}). And some rows record a source's own aggregation: the reporting area is
a parent state while \code{area_iso3c} carries a dependency's code, because the
source reports that dependency under the parent — \code{JEY}, \code{GGY} and \code{IMN}
against \verb{GBR-*}, \code{ALA} against \verb{FIN-*}, \code{SXM} against \verb{NLD-*}. Those rows
have no \code{area_code}, so nothing routes through them; they document how the
source counts, not a territorial claim. Join on whichever column answers your
question, but not on the assumption that they agree.
\item \code{reporting_polity_prefix}: ISO3-shaped family key for the reporting area —
a PREFIX, never a code. Do not join it to \link{polities}; use \code{polity_code}.
It was called \code{reporting_polity_code} until 0.3.0.9000, where it held 609
bare prefixes and no periodized codes, so that join returned nothing.
\item \code{polity_area_code}: Numeric area code retained for WHEP matrix workflows.
\item \code{polity_code}, \code{polity_name}: Matched WHEP polity, or \code{NA} for
statistical composites that are not real polities.
\item \code{polity_start_year}, \code{polity_end_year}: Validity interval for the matched
polity.
\item \code{mapping_status}: \code{"matched"}, \code{"manual"}, \code{"unmapped"}, or
\code{"not_a_reporting_area"}.
\item \code{mapping_note}: Explanation for manual or unmapped rows.
}
}
\source{
Derived from \link{polities} and \code{inst/extdata/harmonization/regions_full.csv}.
}
\usage{
polity_area_crosswalk
}
\description{
Year-aware bridge from numeric reporting \code{area_code} values used by
FAOSTAT/FABIO-derived WHEP data to periodized WHEP \code{polity_code} values.
}
\keyword{datasets}
