% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatialize_levels.R
\name{build_level_crop_targets}
\alias{build_level_crop_targets}
\title{Split a national crop total across the units of its container}
\usage{
build_level_crop_targets(
  country_areas,
  unit_weights,
  admin_shares = NULL,
  tolerance_relative = 0.1,
  tolerance_absolute = 1000
)
}
\arguments{
\item{country_areas}{National crop areas keyed on the container; see
\code{\link[=allocate_level_crops]{allocate_level_crops()}}.}

\item{unit_weights}{Per-unit pattern weights, from
\code{.alloc_unit_weights()}: \code{year}, \code{area_code}, \code{level_polity_code},
\code{item_prod_code}, \code{weight_rainfed}, \code{weight_irrigated},
\code{cropland_rainfed_ha}, \code{cropland_irrigated_ha}, \code{n_cells}.}

\item{admin_shares}{Resolved admin shares, or \code{NULL}.}

\item{tolerance_relative}{Relative discrepancy above which a
complete-coverage group is refused, \code{0.10} by decision T31(d).}

\item{tolerance_absolute}{Absolute discrepancy, in hectares, above which
the same group is refused, \code{1000} by decision T31(d). BOTH must be
breached.}
}
\value{
A list of two tibbles, \code{targets} and \code{coverage}; see
\code{\link[=allocate_level_crops]{allocate_level_crops()}} for their columns.
}
\description{
The first half of \code{\link[=allocate_level_crops]{allocate_level_crops()}}, exposed on its own because
the reconciliation diagnostics read it: it turns national totals plus
resolved admin shares into one target per \verb{(year, container, unit, item)}, and says on every row which regime produced it.
}
\section{How the denominator is chosen}{

Per \verb{(year, area_code, item_prod_code)}, writing \code{D} for the
denominator each unit's reported value is divided by:

\describe{
\item{\code{"pattern"}}{No unit reports. Every unit's share is its own
pattern weight over the group's, \code{method_crop_alloc = "pattern_implied"} -- or \code{"pattern_national"} where the container is
at level 0 and is therefore its own single unit.}
\item{\code{"admin_sum"}}{Every unit of the layer reports a value:
complete coverage, so \code{D = admin_sum} and the reported units rescale
proportionally onto the national total. The leftover is a
diagnostic; it is refused only when it breaks BOTH tolerances
(decision T31(d)).}
\item{\code{"residual"}}{Some units report a value: \code{D = max(admin_sum, national)}, so a reported value binds in absolute hectares while
\code{admin_sum} is below the national total, and rescales down when it
is above. The residual, \code{1 - admin_sum / D} of the total, is split
across the units that reported NO absolute area, and never touches a
unit that did (decision T31(a)). Those units' pattern weights split
it -- unless every one of them declares a share, in which case the
declared shares do, because that is what a declared share states.}
\item{\code{"share_normalised"}}{The rows carry a \code{share} and no \code{value},
which is what \code{\link[=backcast_admin_shares]{backcast_admin_shares()}} produces: the shares already
sum to 1 over the units they cover, so they are used as they stand
and a unit outside them gets zero. \code{method_crop_alloc = "admin_backcast_luh2"}.}
}

\code{method_crop_alloc} therefore takes one of \code{"pattern_national"} (a
container the layer holds at level 0, which is its own single unit),
\code{"pattern_implied"}, \code{"admin_area_shares"}, \code{"admin_backcast_luh2"},
\code{"admin_residual"} (a unit carrying its share of the residual on the
weights that split it -- whether because it reported nothing, or because
it declared a share the split did not run on) and \code{"unallocated"} (a
unit the shares do not cover and no residual reaches, whose target is
0). The column names what placed the hectares, so a unit whose declared
share was displaced by the pattern weights is \code{"admin_residual"} and not
\code{"admin_area_shares"}.

A group whose reported values are all zero while the national total is
positive cannot set a shape. Under complete coverage every share is 0,
the hectares are counted in \code{coverage$dropped_ha}, and the T31(d)
refusal fires on any group where that matters (the discrepancy is then
100\% of the national total). Where that leaves NO positive target at all,
\code{\link[=allocate_level_crops]{allocate_level_crops()}} returns the empty allocation beside this
coverage rather than calling an engine with nothing to place.
}

\section{What counts as a report}{

One predicate. A unit reports when it carries a \code{value}, a \code{share}, or
both, and \code{coverage$n_units_reporting} counts exactly the units that then
bind. \code{reports_value} -- an ABSOLUTE area -- is a narrower question, and
it decides two things only: which denominator the group uses, and which
units the residual is spread over. So a unit declaring a share and no
value is a reporter, and it takes a slice of the residual the valued
units left. What sizes that slice is a property of the candidate SET,
not of the unit: where every residual candidate declares a share, those
shares split the residual (\code{coverage$weight_basis} is then
\code{"declared"}) and the declared share binds; where only some do, the
pattern weights split it, because a declared fraction and a hectare of
potential are not the same quantity. In that second case the unit is
allocated exactly as a silent one is, and \code{method_crop_alloc} says so.

A negative reported value, a negative declared share and a negative
derived target are all refused (classes \code{whep_alloc_negative_value} and
\code{whep_alloc_negative_target}); \code{\link[=allocate_level_crops]{allocate_level_crops()}} refuses a
negative national total ahead of both, as
\code{whep_alloc_negative_national}. A negative target is dropped by the
engine's positivity filter, after which the remaining units divide the
whole national total between them: the container over-allocates and
nothing downstream can see the row that is not there.
}

\section{Irrigation}{

The unit's irrigated target is the national irrigated area split by the
engine's own \code{ir_potential} aggregated per unit, so the two steps
compose; where a group has no irrigated potential the split falls to the
units' irrigated cropland, and where there is none of that either the
irrigated area is unplaceable and is reported as such, because the
engine would drop it too. The rainfed target is the remainder, and the
irrigated target is clipped at the unit's area target so the remainder
is never negative -- the engine computes \code{harvested - irrigated} with no
clipping and lets a negative row through its output filter. Clipped
hectares are reported per unit in \code{irrigation_clipped_ha} and are NOT
redistributed to units with room: that would be an allocation rule
nothing has decided.
}

\examples{
weights <- tibble::tibble(
  year = 2000L,
  area_code = 1L,
  level_polity_code = c("A1", "A2"),
  item_prod_code = 15L,
  weight_rainfed = c(400, 600),
  weight_irrigated = 0,
  cropland_rainfed_ha = c(800, 1200),
  cropland_irrigated_ha = 0,
  n_cells = 1L
)
national <- tibble::tibble(
  year = 2000L, area_code = 1L, item_prod_code = 15L,
  harvested_area_ha = 1000
)
build_level_crop_targets(national, weights)$targets
}
