% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_analysis.R
\name{decompose_manure_losses}
\alias{decompose_manure_losses}
\title{Decompose livestock manure management losses into herd, feed,
excretion, and management-loss drivers}
\usage{
decompose_manure_losses(
  n_prov_destiny = NULL,
  intake_ygiac = NULL,
  n_excretion_ygs = NULL,
  stock_prod_ygps = NULL,
  livestock_units = NULL,
  by_period = FALSE
)
}
\arguments{
\item{n_prov_destiny}{Nitrogen flows tibble from
\code{\link[=create_n_prov_destiny]{create_n_prov_destiny()}}. If \code{NULL}, loaded automatically.}

\item{intake_ygiac}{Feed intake tibble from
\code{whep_read_file("intake_ygiac")}. If \code{NULL}, loaded automatically.}

\item{n_excretion_ygs}{Livestock excretion tibble from
\code{whep_read_file("n_excretion_ygs")}. If \code{NULL}, loaded automatically.}

\item{stock_prod_ygps}{Livestock stock tibble from
\code{whep_read_file("stock_prod_ygps")}. If \code{NULL}, loaded automatically.}

\item{livestock_units}{Livestock unit coefficients tibble from
\code{whep_read_file("livestock_units")}. If \code{NULL}, loaded automatically.}

\item{by_period}{If \code{TRUE}, compares each reference period (each
averaged across its ten years) against the immediately preceding
one — 1860-1870 -> 1920-1930 -> 1960-1970 -> 2010-2020 — plus one
extra transition spanning the full analysis window, 1860-1870
straight to 2010-2020 (the total change) — instead of chaining year
on year.}
}
\value{
A tibble from \code{\link[=calculate_lmdi]{calculate_lmdi()}} with columns \code{period},
\code{period_years}, \code{factor_label}, \code{component_type}, \code{additive},
\code{multiplicative}, and \code{multiplicative_log}.
}
\description{
Decomposes the year-on-year change in nitrogen (N) lost from livestock
housing and manure storage (before any manure reaches land) into four
multiplicative drivers, computed at the national level: herd size,
feed N intake per livestock unit, excreted fraction of feed N
(1 - feed nitrogen use efficiency), and the management-loss fraction of
excreted N.

This is a simplified, national-only view with no species breakdown
(no species-mix factor); \code{\link[=decompose_specialization_cov]{decompose_specialization_cov()}} still
uses the full per-species detail for its \code{livestock_species} series.
Only livestock categories with a livestock-unit (LU) coefficient in
\code{livestock_units} are included in the underlying herd/feed/excretion
totals (currently Cattle_milk, Cattle_meat, Sheep, Goats, Horses,
Donkeys_mules, Pigs, Poultry, Rabbits); categories present in
\code{intake_ygiac}/\code{n_excretion_ygs} but absent from \code{livestock_units}
(e.g. "Fur animals", "Other", "Other_birds") are dropped.

\code{n_prov_destiny} records manure already applied to land without
retaining which species it came from, so the management-loss fraction
has always been computed nationally, not per species.
}
\examples{
# decompose_manure_losses()
}
