% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sjos_n_footprint.R
\name{build_sjos_n_footprint}
\alias{build_sjos_n_footprint}
\title{Build the embodied-nitrogen trade footprint.}
\usage{
build_sjos_n_footprint(
  exceedance = NULL,
  io = NULL,
  category = c("exceedance", "within_boundary", "production"),
  years = NULL,
  data = list(),
  example = FALSE
)
}
\arguments{
\item{exceedance}{A \code{\link[=build_n_boundary_exceedance]{build_n_boundary_exceedance()}} country-resolution output
passed straight to \code{\link[=build_n_exceedance_extension]{build_n_exceedance_extension()}}. Not needed when
\code{example = TRUE}.}

\item{io}{Optional pre-built \code{\link[=build_io_model]{build_io_model()}} result reused across
extensions. When \code{NULL} (default), \code{\link[=build_footprint]{build_footprint()}} builds it for \code{years}
from the package inputs (the real-data path, an integration wiring step).}

\item{category}{Which per-crop nitrogen mass to trace: \code{"exceedance"}
(default), \code{"within_boundary"}, or \code{"production"}. Validated with
\code{\link[rlang:arg_match]{rlang::arg_match()}}.}

\item{years}{Years to trace. Defaults to the years present in the extension;
ignored when \code{io} is supplied.}

\item{data}{Optional named list of injected inputs. \code{data$fp_flows} supplies
pre-traced tidy footprint flows (as from \code{\link[=build_footprint]{build_footprint()}}) directly,
bypassing the model build, for testing the split logic in isolation.
\code{data$origin_classes} may supply producer classifications keyed by \code{year},
\code{area_code}, \code{item_cbs_code} (for example \code{\link[=classify_sjos_n]{classify_sjos_n()}} output).
\code{data$target_classes} may supply consumer classifications, one row per
country-year keyed by \code{year} and \code{area_code}, carrying \code{nourish} (for
example \code{\link[=normalize_nourishment]{normalize_nourishment()}} output) and optionally a country-year
\code{boundary_side} and \code{sjos_class}. They join on \code{target_area} and \code{year} as
\code{target_nourish}, \code{target_boundary_side} and \code{target_sjos_class}. This
function classifies nothing: a country-year boundary class is the caller's
to classify after aggregation, and a table with more than one class per
country-year (a crop-level \code{\link[=classify_sjos_n]{classify_sjos_n()}} output, for instance)
aborts rather than duplicating flows.}

\item{example}{If \code{TRUE}, return a small hardcoded fixture instead of running
the pipeline. Defaults to \code{FALSE}.}
}
\value{
A named list with two tibbles:
\itemize{
\item \code{fp_all}: embodied nitrogen by \code{year}, producer \code{origin_area} /
\code{origin_item}, consumer \code{target_area} / \code{target_item}, \code{target_fd},
\code{origin} (\code{"Domestic consumption"} or \code{"Traded"}), \code{item_cbs_code}
(an alias of \code{target_item}) and \code{impact_u} (tonnes N), stamped with the
traced \code{category}, optional producer classes and, when
\code{data$target_classes} is supplied, the \verb{target_*} consumer classes.
\item \code{fp_food}: \code{fp_all} restricted to food consumption (\code{target_fd == "food"}).
\item \code{target_class_diag}: only when \code{data$target_classes} is supplied. One row
per output table (\code{table}, \code{"fp_all"} or \code{"fp_food"}) and \code{year}: the
flow and consumer-area counts, \code{impact_u} (tonnes N), and how many flows,
consumer areas and tonnes N went to a country-year with no \code{nourish}
class (\code{n_flows_unclassified}, \code{n_target_areas_unclassified},
\code{impact_u_unclassified}). Those flows keep \code{NA} in \code{target_nourish} and
stay in the tables; a warning of class
\code{whep_sjos_fp_unclassified_target} names the consumer areas.
}
}
\description{
Trace a per-crop nitrogen category through the FABIO footprint framework and
return the consumption-side embodied nitrogen split into domestic and traded
flows. The \code{category} selects which nitrogen mass is carried as the footprint
intensity (see \code{\link[=build_n_exceedance_extension]{build_n_exceedance_extension()}}): \code{"exceedance"} (default),
\code{"within_boundary"}, or \code{"production"} (the crop's harvest removal:
harvested product plus used residue plus grazed forage).

The pipeline is \code{\link[=build_n_exceedance_extension]{build_n_exceedance_extension()}} to assemble the extension,
\code{\link[=build_footprint]{build_footprint()}} to trace it through the multi-regional input-output model,
then a domestic-versus-traded relabelling of the tidy flows: a flow is
\code{"Domestic consumption"} when the nitrogen is emitted and consumed in the same
area (\code{origin_area == target_area}) and \code{"Traded"} otherwise. Two outputs are
returned, mirroring Global's \code{FP_all_N} and \code{FP_food_all_N}: \code{fp_all} is the
embodied nitrogen across all final-demand categories, \code{fp_food} is the subset
consumed as food (\code{target_fd == "food"}).
Signed crop attributions are traced as separate positive and negative linear
extensions and recombined. Explicit undefined-attribution residuals are
rejected by \code{\link[=build_n_exceedance_extension]{build_n_exceedance_extension()}} before tracing.
}
\examples{
build_sjos_n_footprint(example = TRUE)
}
