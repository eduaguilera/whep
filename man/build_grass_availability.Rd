% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feed_lpjml.R
\name{build_grass_availability}
\alias{build_grass_availability}
\title{Build grazable grass availability.}
\usage{
build_grass_availability(method = c("lpjml", "coefficient"), ...)
}
\arguments{
\item{method}{Grass-availability method, \code{"lpjml"} or \code{"coefficient"}.}

\item{...}{Passed to the selected method's builder, e.g.
\code{\link[=build_grass_availability_lpjml]{build_grass_availability_lpjml()}}.}
}
\value{
A tibble of grass availability with a \code{method_grass} column
recording the method used.
}
\description{
Multi-method wrapper for the grass forage supply ceiling that feeds
allocation. The default \code{lpjml} method reads pinned LPJmL-derived
managed-grassland net primary production/availability unless custom artifact
data, a custom artifact path, or \code{run_dir} points to local inputs;
\code{coefficient} applies a per-area grass-yield coefficient and is not yet
implemented (it needs a \code{grass_yield_coef} dataset).
}
\examples{
build_grass_availability(method = "lpjml", example = TRUE)
}
