% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_water_use.R
\name{build_crop_water_use}
\alias{build_crop_water_use}
\title{Build per-crop monthly applied irrigation from an LPJmL run.}
\source{
LPJmL run outputs \code{cft_airrig_month.nc} and \code{cftfrac.nc}; see
Schaphoff, S. et al. (2018). LPJmL4 - a dynamic global vegetation model
with managed land - Part 1: Model description. \emph{Geoscientific Model
Development}, 11, 1343-1375. \doi{10.5194/gmd-11-1343-2018}.
}
\usage{
build_crop_water_use(
  resolution = c("grid", "polity"),
  years = NULL,
  run_dir = NULL,
  data = list(),
  example = FALSE
)
}
\arguments{
\item{resolution}{\code{"grid"} (default, per cell) or \code{"polity"} (aggregated
to \code{area_code}, \code{airrig_stand_mm} area-weighted by stand area,
\code{airrig_cell_mm} by cell area).}

\item{years}{Optional integer vector of calendar years to keep. \code{NULL}
(default) keeps every year the run covers.}

\item{run_dir}{Path to the LPJmL run output directory. \code{NULL} (default)
uses \code{WHEP_LPJML_RUN_DIR}.}

\item{data}{Named list of pre-loaded inputs, each falling back to its
reader when absent: \code{airrig_month} (per cell, band and month,
\code{read_lpjml_hydrology("cft_airrig_month")} output), \code{stand_frac} (per
cell, band and year, \code{read_lpjml_hydrology("stand_frac", monthly = FALSE)} output) and \code{country_grid} (the cell-polity support).}

\item{example}{If \code{TRUE}, return a small fixture instead of reading a run.
Defaults to \code{FALSE}.}
}
\value{
A tibble with \code{lon}, \code{lat}, \code{area_code}, \code{cell_area_frac}, \code{year},
\code{month}, \code{band}, \code{band_name}, \code{crop_group}, \code{stand_frac},
\code{airrig_stand_mm} and \code{airrig_cell_mm} at \code{"grid"} resolution
(aggregated over cells at \code{"polity"}), plus the polity columns below.

\code{crop_group} is the soil-carbon crop group the band's water belongs to,
in the vocabulary of \code{\link[=soc_crop_group]{soc_crop_group()}} so the water and carbon ledgers
share it: LPJmL's twelve named crops are all herbaceous, so a band is
\code{cropland_rainfed_herbaceous} or \code{cropland_irrigated_herbaceous} by its
regime. The \code{others} bands pool woody crops with minor herbaceous ones
and cannot be resolved to a species, and the grassland and bioenergy
bands are not cropland; those carry \code{NA} rather than a guess.

A border cell shared by several polities appears ONCE PER POLITY, with
\code{cell_area_frac} carrying that polity's share of the cell. The mm
columns are densities and do not split; the AREA does. Aggregating over
cells therefore weights by \code{cell_area_frac} (times stand or cell area),
and summing rows without it double-counts every border cell.
}
\description{
Applied irrigation per cell, crop and month, from \code{cft_airrig_month.nc}
(first written by the 2026-09-01 v2 run), joined with each crop's stand
area fraction from \code{cftfrac.nc}. This is the disaggregated layer a
per-crop water footprint needs; the cell-level water budget stays with
\code{\link[=build_water_balance]{build_water_balance()}}, which reports whole-cell terms.

Both unit conventions are returned side by side, because both are needed
and confusing them is the characteristic error of per-CFT data:
\code{airrig_stand_mm} is the depth applied per square metre of THE CROP'S OWN
STAND (an irrigation intensity), and \code{airrig_cell_mm} is the same water as
a whole-cell depth (\code{airrig_stand_mm * stand_frac}), which is what sums --
over crops it reproduces the crop-less \code{irrig} cube to 0.999.

Rows are crop stands present in the cell (\code{stand_frac > 0}), rainfed bands
included: LPJmL books paddy water on \emph{rainfed rice} (36\% of the
stand-weighted applied total at July 2010), so dropping rainfed bands
drops real water.
}
\section{Polity columns}{

Every area-keyed output carries the polity its \code{area_code} resolves to in
that row's year:
\itemize{
\item \code{polity_area_code}: The numeric key rows are AGGREGATED on, for the matrix
workflows. It is a bucket, not an identity: use \code{reporting_polity_code} to
say which territory a row belongs to.
\item \code{reporting_polity_code}: The polity itself, e.g. \code{ESP-1846-1914}. It is
year-aware, so the same \code{area_code} resolves to different polities in
different years, which is the point of the crosswalk.
\item \code{reporting_polity_name}: Its name. It can differ from the area's own name
where the area folds into an aggregate.
\item \code{reporting_polity_has_geometry}: Whether the polity has a polygon in the
WHEP polity database, for callers that need to map or intersect it. \code{FALSE}
is a documented gap upstream, not an error.
}

Rows whose \code{area_code} resolves to no polity keep the columns with \code{NA}
rather than being dropped, so a gap is visible instead of silent.

Rows before the back-cast anchor year resolve to the polity live in that
anchor year rather than to the polity live in the row's own year, because
WHEP's pre-anchor series are back-cast onto the anchor-year territory. See
\code{\link[=add_polity_code]{add_polity_code()}} for the reasoning. Where that polity is not live in the
row's own year -- 41.5\% of the pre-1961 \verb{(area, year)} cells --
\code{\link[=add_polity_code]{add_polity_code()}} says so as \code{mapping_status == "backcast_anchor"}, and
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports it as \code{gap_kind == "backcast_anchor"}.
These columns do not say so either way.

A row whose year no mapped period covers is resolved to the NEAREST period of
the same area instead, so \code{reporting_polity_code} can name a polity that did
not exist in that row's year -- FAOSTAT bucket 206 "Sudan (former)" keeps
reporting after \code{SUD-1956-2011} ends, and its post-2011 rows carry that code.
These columns do not say so: \code{\link[=add_polity_code]{add_polity_code()}} reports such a row as
\code{mapping_status == "out_of_span"}, and that column is dropped here so that
adding it does not change the schema of every area-keyed output at once.
\code{\link[=polity_coverage_gaps]{polity_coverage_gaps()}} reports the stand-in rows of a built table, and
\code{options(whep.polity_mapping_status = "flag")} (or \code{"status"}) carries the
signal on the outputs themselves. Both are opt-in; the default is no extra
column.
}

\examples{
build_crop_water_use(example = TRUE)
}
