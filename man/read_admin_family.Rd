% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_shares_pins.R
\name{read_admin_family}
\alias{read_admin_family}
\title{Read the per-family subnational admin-statistics pins}
\usage{
read_admin_family(families = NULL, example = FALSE)
}
\arguments{
\item{families}{Character vector of family aliases to read. \code{NULL}, the
default, reads all five. An alias outside that set aborts.}

\item{example}{If \code{TRUE}, return a small fixture instead of reading any
pin. Defaults to \code{FALSE}.}
}
\value{
A named list with one element per requested family that is
shipped, each a tibble in the shared admin-statistics reader shape
(\code{source}, \code{source_native_unit_id}, \code{source_native_unit_name},
\code{source_native_item_code}, \code{source_native_item_name},
\code{indicator_used}, \code{quantity}, \code{year}, \code{value} or \code{share},
\code{value_unit}, \code{value_flag}, \code{grain}, \code{nuts_version}, \code{source_version},
\code{recorded_at}), plus a \code{not_shipped} element holding the aliases that
were requested but are not on the board. \code{not_shipped} is always
present, and is \code{character(0)} when every requested family loaded.
}
\description{
Read the tier-2 and tier-3 administrative-statistics families that WHEP
compiled for the subnational spatialization, one pin per family, and
return them as a named list of tibbles.

Every family is read by an explicit alias: there is no discovery step
and no wildcard, so a family that is not shipped cannot be silently
substituted by another. A requested family that is absent -- its alias
is not registered in \code{\link{whep_inputs}}, or its pin holds no rows -- is
named in the returned \code{not_shipped} element instead of aborting the
read, because a family whose publication consent has not cleared is a
planned state of this dataset (see the T23 outcome recorded in
\code{inst/extdata/admin_stats_pins_manifest.csv}). Any other failure of
\code{\link[=whep_read_file]{whep_read_file()}} is left to propagate: an unreachable board is not an
embargo.
}
\section{Families}{

The five aliases, their tier and what each ships, as staged on
2026-09-03 from the harmonized subnational panel:
\itemize{
\item \code{"admin-stats-japan"} (tier 2): 46 prefectures, 32,095 observed area
and production rows, 1961-2022.
\item \code{"admin-stats-spain-provinces"} (tier 2): 50 provinces, 323,469
observed-lane area and production rows, 1961-2021. The compilation's
Spanish livestock rows are all flagged \code{admin_level_overlap} and are
therefore not shipped, which is why three of its 53 units are absent.
\item \code{"admin-stats-australia"} (tier 2): 8 states and territories, 6,597
observed area, production and head-count rows, 1961-2022.
\item \code{"admin-stats-france-livestock"} (tier 2): 89 departments, 58,740
observed head counts, 1961-2020.
\item \code{"admin-stats-latam"} (tier 3): 142 first-level units of the
Infante-Amate, Urrego-Mesa, Badia-Miro and Aguilera panel, 875,514
rows, 1961-2023, shipped as \strong{shares only}. Each row carries
\code{share}, its unit's share of the country total for the same item,
indicator and year, and no \code{value}; this reader aborts if a future
version of that pin carries one.
}

\code{"admin-stats-jrc"} is not one of these. It is the public tier-1 JRC
product with its own reader, and passing it here aborts as an unknown
alias.
}

\examples{
read_admin_family(example = TRUE)
}
