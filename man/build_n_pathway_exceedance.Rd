% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_pathway_boundary.R
\name{build_n_pathway_exceedance}
\alias{build_n_pathway_exceedance}
\title{Build the pathway-mode critical-nitrogen boundary exceedance.}
\usage{
build_n_pathway_exceedance(
  balance,
  critical_loads,
  nh3_source = c("soil", "total_agricultural"),
  resolution = c("grid", "polity", "country"),
  data = list(),
  example = FALSE
)
}
\arguments{
\item{balance}{A \code{\link[=build_nitrogen_balance]{build_nitrogen_balance()}} output keyed by \code{lon}, \code{lat},
\code{area_code}, \code{item_cbs_code}, \code{year}, carrying \code{area_ha}, \code{nh3_n_t} and
\code{no3_n_t}.}

\item{critical_loads}{A named list of \code{\link[=read_critical_n]{read_critical_n()}} outputs (each
\code{lon}, \code{lat}, \code{value} in kg N per hectare) with elements
\code{crit_nh3_emission}, \code{crit_leaching_gw} and \code{crit_load_sw}.}

\item{nh3_source}{Air-pressure scope: \code{"soil"} (default, field \code{nh3_n_t}
only, consistent with the surplus boundary) or \code{"total_agricultural"}
(also adds manure housing and storage ammonia from
\code{data$manure_mgmt_nh3_n_t}, keyed to the balance grid; if absent, warns and
falls back to soil ammonia).}

\item{resolution}{Output grain: \code{"grid"} (default, per crop per cell) or
\code{"polity"} / \code{"country"} (per crop per country, summing the mass terms).}

\item{data}{Optional named list of injected inputs. \code{manure_mgmt_nh3_n_t} (a
tibble keyed to the balance grid with a \code{manure_mgmt_nh3_n_t} column)
supplies the housing and storage ammonia for
\code{nh3_source = "total_agricultural"}. Defaults to \code{list()}.}

\item{example}{If \code{TRUE}, return a small fixture instead of computing.
Defaults to \code{FALSE}.}
}
\value{
For \code{resolution = "grid"}, a tibble keyed \code{lon}, \code{lat}, \code{area_code},
\code{item_cbs_code}, \code{year} with \code{area_ha}, the air-medium columns
(\code{critical_air_kgn_ha}, \code{actual_air_kgn_ha}, \code{exceed_share_air},
\code{exceedance_air_kgn_ha}, \code{within_air_kgn_ha}, \code{exceedance_air_n_t},
\code{within_air_n_t}, \code{actual_air_n_t}), the water-medium columns (the same
set with a \code{water} suffix, plus \code{critical_gw_kgn_ha}, \code{critical_sw_kgn_ha}
and \code{binding_water_medium}), \code{binding_boundary}, and the \code{nh3_source} /
\code{method_boundary} stamps. For the aggregate resolutions, the grouping key
with the summed per-medium mass terms and the same stamps.
}
\description{
Compares a \code{\link[=build_nitrogen_balance]{build_nitrogen_balance()}} output's process-based nitrogen losses
against the medium-specific critical loads of Schulte-Uebbing et al. (2022),
per grid cell and per crop (Mode B). The air medium compares field ammonia
emission (\code{nh3_n_t}, converted to a per-hectare rate) to the critical
ammonia emission; the water medium compares nitrate loss (\code{no3_n_t}) to the
tighter (lower) of the critical groundwater leaching and critical
surface-water load, recording which sub-medium binds. Each medium is split
into an \code{exceedance} part (above the load) and a \code{within} part with the
shared exceed-share formula, as a per-hectare intensity and a mass, so
\code{exceedance + within == actual} per medium. \code{binding_boundary} names the
medium (\code{"air"}, \code{"water"}, \code{"both"} on an exact positive tie, or \code{"none"})
with the highest exceedance share. \code{resolution = "grid"} keeps the full
per-crop grid key and per-medium columns; \code{"polity"} and \code{"country"} sum the
mass terms over cells to \code{area_code}, \code{item_cbs_code}, \code{year}. Climate /
nitrous oxide is not a pathway (the archive ships no critical climate-N
load); it stays in the balance's \code{total_gwp_co2e_kg}, and a climate-N
pathway is a documented future hook.
}
\examples{
build_n_pathway_exceedance(example = TRUE)
}
