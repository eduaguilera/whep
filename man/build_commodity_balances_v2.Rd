% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_v2.R
\name{build_commodity_balances_v2}
\alias{build_commodity_balances_v2}
\title{Build commodity balance sheets (v2 — with QC flags)}
\usage{
build_commodity_balances_v2(
  primary_all,
  max_year = 2021,
  version = NULL,
  smooth_carry_forward = FALSE,
  anchor_years = 5L,
  spike_ratio = 10,
  spike_min = 1000,
  carry_forward_run = 3L
)
}
\arguments{
\item{primary_all}{A tibble of primary production, as returned by
\code{\link[=build_primary_production]{build_primary_production()}}.}

\item{max_year}{Integer. Latest year of FAOSTAT data. Default \code{2021}.}

\item{version}{File version for input pins. \code{NULL} (default) uses the
frozen version from \link{whep_inputs}.}

\item{smooth_carry_forward}{Logical. Default \code{FALSE}.}

\item{anchor_years}{Integer. Default \code{5}.}

\item{spike_ratio}{Numeric. Default \code{10}.}

\item{spike_min}{Numeric. Default \code{1000}.}

\item{carry_forward_run}{Integer. Default \code{3}.}
}
\value{
A tibble with the same columns as \code{\link[=build_commodity_balances]{build_commodity_balances()}}
\strong{plus} \code{qc_flag}.
}
\description{
Runs the v1 \code{\link[=build_commodity_balances]{build_commodity_balances()}} pipeline, then applies a
data-quality layer that detects:
\itemize{
\item \strong{carry_forward} — constant-value tails (for Production element).
\item \strong{spike} — extreme year-on-year Production changes.
}

Fodder-break flags are not applied to CBS because the CBS pipeline
already aggregates items differently.
}
\examples{
\dontrun{
primary <- build_primary_production()
cbs_v2 <- build_commodity_balances_v2(primary)
cbs_v2 |> dplyr::filter(!is.na(qc_flag))
}
}
